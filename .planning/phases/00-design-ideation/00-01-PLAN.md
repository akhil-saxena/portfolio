---
phase: 00-design-ideation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .playground/package.json
  - .playground/astro.config.mjs
  - .playground/tsconfig.json
  - .playground/check-bundle.mjs
  - .playground/check-no-js.sh
  - .playground/src/components/ChipIsland.tsx
  - .playground/src/pages/probe/island.astro
  - .playground/src/pages/probe/static.astro
  - .playground/dist
  - .planning/phases/00-design-ideation/00-FINDINGS.md
autonomous: true
requirements: [DSGN-04]

must_haves:
  truths:
    - "The throwaway playground builds against the real @akhil-saxena/design-system tarball, installed as a copy rather than a symlink"
    - "Exactly one copy of React resolves inside the playground, so the duplicate-React invalid-hook-call hazard is proven absent"
    - "The DS-09 tree-shaking verdict exists as a recorded byte count and module list, not as an assertion"
    - "A sketch page carrying no client:* directive emits zero <script> tags"
    - "The playground cannot become the Phase 2 foundation: no adapter, no CI, no api routes, no auth, and it is gitignored"
  artifacts:
    - path: ".playground/package.json"
      provides: "Pinned Astro 7 + React 19 + DS tarball dependency set"
      contains: "akhil-saxena-design-system-1.11.4.tgz"
    - path: ".playground/astro.config.mjs"
      provides: "react() integration and env-driven inlineStylesheets, no adapter"
      contains: "inlineStylesheets"
    - path: ".playground/check-bundle.mjs"
      provides: "Sourcemap sources analyser — the DS-09 measurement"
      contains: "prosemirror"
    - path: ".playground/check-no-js.sh"
      provides: "Zero-JS static render assertion"
    - path: ".planning/phases/00-design-ideation/00-FINDINGS.md"
      provides: "Design-system findings register, seeded from UI-SPEC's 16-row gap table"
      contains: "G-15"
  key_links:
    - from: ".playground/src/pages/probe/island.astro"
      to: ".playground/src/components/ChipIsland.tsx"
      via: "client:load directive"
      pattern: "client:load"
    - from: ".playground/package.json"
      to: "./akhil-saxena-design-system-1.11.4.tgz"
      via: "file: dependency spec"
      pattern: "file:.*\\.tgz|\\./akhil-saxena-design-system"
---

<objective>
Stand up the throwaway Astro playground that is the medium for every Phase 0 sketch, prove
its supply-chain and duplicate-React guards hold, and take the two build-time measurements
that settle DSGN-04's first two claims.

Purpose: D-01 makes every screen in this phase a running design-system sketch, so the
harness is a hard prerequisite for eight downstream plans. Two of the three D-02 claims
(DS-09 tree-shaking, zero-JS static render) are measurable the moment the harness exists,
and their numbers seed the findings register that Phase 1 reads as its scope boundary.

Output: `.playground/` scaffold (gitignored), two measurement scripts, two measurement
fixtures, and `00-FINDINGS.md` seeded with the sixteen-row gap register plus G-15's
measured verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-RESEARCH.md
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-PATTERNS.md
@.planning/phases/00-design-ideation/00-VALIDATION.md

<structural_note>
The working tree has NO `src/` directory and NO root `package.json`. Every path this plan
creates is new, under `.playground/` or `.planning/`. Nothing in this plan edits an
existing application file, because none exists. `../design-system` IS a real sibling
checkout at real paths and is read-only for this phase.

The playground is gitignored by design (UI-SPEC §Review Convention, RESEARCH OQ-2). Only
`.gitignore` and `00-FINDINGS.md` from this plan's `files_modified` are committable; the
rest is intentionally absent from git. Do not fight this by force-adding.
</structural_note>

<interfaces>
Verified 2026-08-17 by direct read of `../design-system/package.json` and `dist/index.d.ts`:

- Package name and version: `@akhil-saxena/design-system` `1.11.4`
- `exports` today has NO `themes` or `fonts` entry. Present entries: `.`, `./hooks`,
  `./icons`, `./tokens.css`, `./primitives.css`, `./utilities.css`, `./css/*`
- `dist/css/` holds 74 per-component sheets including `base.css`, `chip.css`, `card.css`,
  `heading.css`, `text.css`, `appbar.css`, `footer.css`, `timeline.css`
- Hook-free (render correctly as static HTML, no `client:*` needed): `Chip`, `Text`,
  `Heading`, `Card`, `Link`, `Divider`, `Eyebrow`, `Button`, `Badge`, `Timeline`,
  `StatCard`, `AppBar`, `Footer`, `EmptyState`, `StatusPill`, `ProgressBar`
- Local runtime: Node v22.22.3, npm 10.9.8. Astro 7 requires Node >= 22.12.0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the playground and prove the D-02 fence and duplicate-React guard</name>
  <read_first>
    - `.gitignore` (repo root — current contents, to append to rather than overwrite)
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §Standard Stack, specifically the
      verified install sequence and the tarball refresh loop
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §Package Legitimacy Audit
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-02 (the scope fence)
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/package.json`
  </read_first>
  <files>.gitignore, .playground/package.json, .playground/astro.config.mjs, .playground/tsconfig.json</files>
  <action>
Append a `# phase 0 throwaway playground` section to the existing root `.gitignore` with the
single entry `.playground/`. Do not remove or reorder existing entries.

Create `.playground/` and pack the design system into it. Run `npm run build` in
`../design-system` first — `npm pack` does not run `prepublishOnly`, so an unbuilt `dist/`
would be packed silently. Then `npm pack --pack-destination` into `.playground`, producing
`akhil-saxena-design-system-1.11.4.tgz`.

Write `.playground/package.json` by hand — do NOT run `npm create astro`, which is
interactive and scaffolds adapter and CI choices D-02 forbids. Set `"private": true`,
`"type": "module"`, `"engines": { "node": ">=22.12.0" }`, and scripts `build`
(`astro build`) and `dev` (`astro dev`). Declare no `postinstall`. Pin exact versions with
no caret: `astro` `7.2.2`, `@astrojs/react` `6.0.2`, `react` `19.2.8`, `react-dom` `19.2.8`,
`playwright` `1.59.1`, and `@akhil-saxena/design-system` at
`file:./akhil-saxena-design-system-1.11.4.tgz`. These nine packages were cleared by
`slopcheck install -e npm` during research (RESEARCH §Package Legitimacy Audit, all `[OK]`,
zero `[SLOP]`, zero `[SUS]`) — do not re-run that gate as a design task, per UI-SPEC
§Registry Safety.

Write `.playground/astro.config.mjs` with `integrations: [react()]` and nothing else, plus
`build.inlineStylesheets` read from `process.env.INLINE_CSS` defaulting to `'auto'` and
accepting `'never'`. This env switch exists so plan 07's cascade probe can run both Astro
settings without editing this file — a later edit here would be a cross-plan file conflict.
Deliberately absent, and their absence IS the enforcement mechanism: `adapter`, `output`,
`@astrojs/cloudflare`, `wrangler`, `vitest`, any `src/pages/api` directory, any auth
dependency.

Write `.playground/tsconfig.json` extending `astro/tsconfigs/strict` with `jsx: "react-jsx"`.

Install with `npm install` inside `.playground`, then verify the two guards CLAUDE.md names
as hard constraints: the DS must install as a real directory (a symlink carries the
duplicate-React invalid-hook-call hazard) and exactly one `react/package.json` must resolve
at top level.
  </action>
  <verify>
    <automated>cd .playground && npm install && test -d node_modules/@akhil-saxena/design-system && test ! -L node_modules/@akhil-saxena/design-system && [ "$(find node_modules -path '*/react/package.json' -not -path '*/node_modules/*/node_modules/*' | wc -l | tr -d ' ')" = "1" ] && npx astro build && echo GUARDS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore -q .playground` exits 0
    - `test ! -f package.json` at the repo root exits 0 (no root package.json is created this phase, so the CI `file:` dependency-spec gate can never point at the playground's tarball spec)
    - `grep -v '^\s*//' .playground/package.json | grep -cE '@astrojs/cloudflare|wrangler|vitest|@astrojs/node' | grep -qx 0`
    - `grep -v '^\s*//' .playground/astro.config.mjs | grep -cE 'adapter|cloudflare' | grep -qx 0`
    - `test ! -d .playground/src/pages/api` exits 0
    - `grep -q 'inlineStylesheets' .playground/astro.config.mjs`
    - `grep -q 'akhil-saxena-design-system-1.11.4.tgz' .playground/package.json`
    - `test -f .playground/akhil-saxena-design-system-1.11.4.tgz` exits 0
    - `test ! -L .playground/node_modules/@akhil-saxena/design-system` exits 0
    - `find .playground/node_modules -path '*/react/package.json' -not -path '*/node_modules/*/node_modules/*' | wc -l` prints exactly `1`
    - `cd .playground && npx astro build` exits 0
  </acceptance_criteria>
  <done>The playground builds, the DS is installed as a copy with one React, the fence assertions all pass, and `.playground/` is gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Measure DS-09 tree-shaking and the zero-JS static render</name>
  <read_first>
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §Code Examples MEASURE-1 (`check-bundle.mjs`, already written and executed) and MEASURE-2 (`check-no-js.sh`)
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §Measured Baseline Claim 1 and Claim 2 (the expected numbers)
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §Common Pitfalls, Pitfall 1 (stale tarball) and Pitfall 2 (static page proves nothing about tree-shaking)
    - `.playground/astro.config.mjs` (created in task 1)
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/dist/index.d.ts` (confirm the barrel exports `Chip`, `Heading`, `Text`, `Card`, `Timeline`, `StatCard`, `AppBar`, `Footer`)
  </read_first>
  <files>.playground/check-bundle.mjs, .playground/check-no-js.sh, .playground/src/components/ChipIsland.tsx, .playground/src/pages/probe/island.astro, .playground/src/pages/probe/static.astro</files>
  <action>
Create the two measurement fixtures as SEPARATE files with distinct names — Pitfall 2 is
that a zero-JS pass on a static page gets misread as a tree-shaking pass, and merging the
fixtures is how that happens.

`src/components/ChipIsland.tsx` imports only `{ Chip }` from `@akhil-saxena/design-system`
and renders it. `src/pages/probe/island.astro` renders `<ChipIsland client:load />` — the
`client:load` directive is the whole point, since DS-09 is observable only on a hydrated
island. `src/pages/probe/static.astro` imports `Heading`, `Text`, `Card`, `Chip`,
`Timeline`, `StatCard`, `AppBar` and `Footer` from the barrel into `.astro` frontmatter and
renders them with NO `client:*` directive anywhere.

Port `check-bundle.mjs` from RESEARCH §Code Examples MEASURE-1 verbatim — it walks
`dist/_astro/*.js.map`, counts modules in each chunk's `sources` array against the `HEAVY`
regex set (`prosemirror`, `tiptap`, `lowlight`, `highlight.js`, `dnd-kit`, `lucide`),
prints chunk size and module count, and exits 1 when a non-`lucide` heavy family appears in
a client chunk. Add a gzip size line per chunk using `node:zlib` `gzipSync`, because
UI-SPEC §Hydration Budget and VALIDATION.md's open threshold both reason in gzip bytes.
Carry the DS's throw-don't-warn convention (PATTERNS §Shared Patterns 3): the failure
message must name the failure mode, not just the assertion.

Port `check-no-js.sh` from RESEARCH §Code Examples MEASURE-2, adjusted for this phase's
route names: it must scan every emitted HTML file under `dist/` EXCEPT `probe/island`, and
fail on any `<script` occurrence. `probe/island` is excluded because it hydrates on purpose.

Build with a cleared Vite cache — `rm -rf node_modules/.vite dist` before `npx astro build`
— because npm reinstalls a same-named tarball without invalidating Vite's dependency cache
and you would measure the previous copy (Pitfall 1).

`check-bundle.mjs` is EXPECTED TO EXIT 1 at design-system v1.11.4. That non-zero exit is
the finding, not a defect: research measured 570,553 B raw / 176,754 B gzip / 99 modules
for a single `import { Chip }` island, including 10 ProseMirror, 23 TipTap, 4 lowlight, 4
highlight.js and 3 dnd-kit modules. Capture stdout and stderr into
`.playground/bundle-report.txt` rather than treating the exit code as a pass/fail gate.
VALIDATION.md's 50 KB-gzip threshold (assumption A8) is NOT confirmed and gates nothing in
this phase — record that deferral in task 3 rather than adopting the number.
  </action>
  <verify>
    <automated>cd .playground && rm -rf node_modules/.vite dist && npx astro build && { node check-bundle.mjs > bundle-report.txt 2>&1 || true; } && grep -q prosemirror bundle-report.txt && bash check-no-js.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .playground/bundle-report.txt` exits 0
    - `grep -q 'prosemirror' .playground/bundle-report.txt` and `grep -q 'tiptap' .playground/bundle-report.txt` both exit 0 — the heavy families are present, reproducing the measured verdict
    - `.playground/bundle-report.txt` contains a raw byte count and a gzip byte count for the island chunk, both greater than 100000 (assert with `grep -qE '[0-9]{6,} B'`)
    - `bash .playground/check-no-js.sh` exits 0
    - `grep -c '<script' .playground/dist/probe/static/index.html` prints `0`
    - `grep -q 'client:load' .playground/src/pages/probe/island.astro` exits 0
    - `grep -q 'client:' .playground/src/pages/probe/static.astro` exits 1 (no hydration directive on the static fixture)
    - `.playground/src/pages/probe/island.astro` and `.playground/src/pages/probe/static.astro` both exist as distinct files
  </acceptance_criteria>
  <done>Both measurements have run against a freshly built tarball; the tree-shaking failure is captured with byte counts in `bundle-report.txt` and the zero-JS claim passes.</done>
</task>

<task type="auto">
  <name>Task 3: Seed 00-FINDINGS.md with the gap register and G-15's measured verdict</name>
  <read_first>
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Named gaps — the `00-FINDINGS.md` seed" (the sixteen-row register: AAA-1, G-1 … G-15, already written with component, disposition, gap, proposed upstream fix and tiers)
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-04 as revised (the field is `tiers: string[]`, values `blocks-Phase-5`, `should-fix-in-Phase-1`, `backlog`, `blocks-Phase-7`, `blocks-Phase-06.1`)
    - `.planning/phases/00-design-ideation/00-PATTERNS.md` §`00-FINDINGS.md` (this is a transcription-plus-measurement task, not an authoring task)
    - `.playground/bundle-report.txt` (produced by task 2)
    - `.planning/phases/00-design-ideation/00-VALIDATION.md` §"Open Threshold"
  </read_first>
  <files>.planning/phases/00-design-ideation/00-FINDINGS.md</files>
  <action>
Create `00-FINDINGS.md` with YAML frontmatter (`phase: 0`, `slug: design-ideation`,
`status: open`) and these exact H2 headings, in order: `## How to read this register`,
`## Tier vocabulary`, `## Findings`, `## Measured baselines`, `## Deferred thresholds`.

Under `## Tier vocabulary`, state the five legal tier values and the rule that Phase 1's
planner pulls only entries whose `tiers` list contains `blocks-Phase-5` or
`should-fix-in-Phase-1`, so Phase 1's scope boundary stays unambiguous. State explicitly
that `tiers` is a list, not a single value, and that G-1 and G-7 each carry two tiers —
that pairing is the reason the vocabulary was widened and must be preserved verbatim.

Under `## Findings`, transcribe all sixteen rows from UI-SPEC's register — AAA-1, G-1, G-2,
G-3, G-4, G-5, G-6, G-7, G-8, G-9, G-10, G-11, G-12, G-13, G-14, G-15 — preserving each
row's ID, component, disposition, gap statement, proposed upstream fix and tiers. Add an
`Evidence` column, left as `pending` for every row except G-15 and G-12. Do not re-litigate
any row: `StatusPill.stage` is a closed six-literal job-domain union so G-5 stands;
`FormErrorSummaryProps` is `{ errors: string[] }` with no href so G-6 stands; `AppShellProps`
has exactly four slots so G-8 stands.

Fill G-15's Evidence cell from `.playground/bundle-report.txt`: the island chunk's raw and
gzip byte counts, the total module count, and the per-family counts for prosemirror, tiptap,
lowlight, highlight.js and dnd-kit. Record that three configuration fixes were attempted
during research (`sideEffects: false`, removing the `"use client"` directive, marking the
module-scope `createLowlight()` call `@__PURE__`) and each produced byte-identical output,
so the barrel is not shakeable by configuration and DS-09's per-component-JS-subpath
fallback is the live branch.

Fill G-12's Evidence cell with the extensionless-specifier trap: `/css/base.css` resolves to
`dist/css/base.css.css` and fails the Rolldown build while `/css/base` resolves correctly,
and `import.meta.resolve()` under-reports the broken form because it substitutes the
wildcard without stat-ing the target.

Under `## Measured baselines`, record the zero-JS result from task 2 (script-tag count on
`probe/static`) as the counterpart measurement, and note that Astro 7 ships Vite 8
(Rolldown-based), so Rollup-era tree-shaking advice does not transfer.

Under `## Deferred thresholds`, record that VALIDATION.md's 50 KB-gzip pass threshold for a
public island chunk is assumption A8, was derived from the Lighthouse 95+ goal rather than
from a stated budget, gates nothing in Phase 0, and must be confirmed or replaced before
Phase 5's bundle gate uses it. State that no threshold choice changes the v1.11.4 verdict,
which fails by roughly 3.5x on gzip alone.
  </action>
  <verify>
    <automated>test -f .planning/phases/00-design-ideation/00-FINDINGS.md && for h in "## How to read this register" "## Tier vocabulary" "## Findings" "## Measured baselines" "## Deferred thresholds"; do grep -qF "$h" .planning/phases/00-design-ideation/00-FINDINGS.md || { echo "MISSING $h"; exit 1; }; done && for id in AAA-1 G-1 G-2 G-3 G-4 G-5 G-6 G-7 G-8 G-9 G-10 G-11 G-12 G-13 G-14 G-15; do grep -qE "\b$id\b" .planning/phases/00-design-ideation/00-FINDINGS.md || { echo "MISSING $id"; exit 1; }; done && grep -q 'blocks-Phase-7' .planning/phases/00-design-ideation/00-FINDINGS.md && echo FINDINGS_OK</automated>
  </verify>
  <acceptance_criteria>
    - All five H2 headings above are present, spelled exactly
    - All sixteen finding IDs `AAA-1` and `G-1` … `G-15` appear
    - All five tier strings appear: `blocks-Phase-5`, `should-fix-in-Phase-1`, `backlog`, `blocks-Phase-7`, `blocks-Phase-06.1`
    - G-1's and G-7's rows each name two tiers including `blocks-Phase-7`
    - G-15's row contains a six-or-more-digit byte count (`grep -qE 'G-15.*[0-9]{6}'` or the number appears in the same table row)
    - `grep -q '50 KB' .planning/phases/00-design-ideation/00-FINDINGS.md` — the deferred threshold is recorded, not adopted
    - The file contains no `[SLOP]` or `[SUS]` package claim, and does not re-run or contradict RESEARCH §Package Legitimacy Audit
  </acceptance_criteria>
  <done>`00-FINDINGS.md` exists as a committed, Phase-1-readable register with all sixteen gaps, the widened tier vocabulary, G-15's measured evidence and the unconfirmed threshold recorded as deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public npm registry → `.playground/node_modules` | Untrusted third-party code executes at install and build time. This is the only live attack surface Phase 0 has. |
| `../design-system` working copy → packed tarball → playground | First-party code crossing a packaging boundary; the packaging step is where a stale or symlinked dependency becomes a correctness and hook-safety hazard. |
| playground working files → git history | Anything committed is permanent; the fence depends on the playground leaving no recoverable history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | npm installs into `.playground` | mitigate | All nine packages cleared by `slopcheck install -e npm` in RESEARCH §Package Legitimacy Audit — zero `[SLOP]`, zero `[SUS]`, all with real source repos and >= 84K weekly downloads. Versions pinned exactly (no caret) in task 1. No `postinstall` declared by any of the nine. No new package may be added by this plan. |
| T-00-02 | Tampering | DS dependency resolution | mitigate | Tarball-only (`file:*.tgz`), never `file:../design-system` or `npm link`. Task 1 asserts the installed path is a real directory and not a symlink, and that exactly one top-level `react/package.json` resolves — the duplicate-React invalid-hook-call guard CLAUDE.md names as a hard constraint. |
| T-00-03 | Tampering | stale-tarball measurement | mitigate | `rm -rf node_modules/.vite dist` before every measurement build (Pitfall 1). A measurement taken against a cached copy would put a false number into `00-FINDINGS.md`, which Phase 1 consumes as scope. |
| T-00-04 | Information Disclosure | accidental commit of the tarball, `node_modules`, or the playground | mitigate | `.gitignore` gains `.playground/`, asserted by `git check-ignore -q .playground`. The 2.4 MB tarball and the whole harness stay out of history, which is also D-02's fence (a `git rm`-ed playground remains recoverable). No secret is required by any Phase 0 artefact, so none can leak. |
| T-00-05 | Elevation of Privilege | the `file:` dependency spec reaching a shipped build | mitigate | CLAUDE.md's CI gate fails a build whose dependency spec starts with `file:`. Task 1 asserts no root `package.json` exists, so the only `file:` spec in the repo lives inside a gitignored directory that CI never sees and that plan 17 deletes. |
| T-00-06 | Elevation of Privilege | the playground surviving as the Phase 2 foundation | mitigate | No adapter, no CI workflow, no `src/pages/api`, no auth dependency — asserted by grep in task 1. Absence of `@astrojs/cloudflare` is the enforcement mechanism, not a preference. Deletion is a verified task in plan 17 (`test ! -d .playground`). |
| — | Spoofing | — | not applicable | No authentication surface exists in this plan. The 401 re-authentication screen is *designed* in plan 15 and implemented in Phase 2 (AUTH-01…04); nothing here accepts a credential. |
| — | Repudiation | — | not applicable | Single operator, no multi-actor action, no audit surface to forge. |
| — | Denial of Service | — | not applicable | Nothing is served to any client. The playground runs locally and emits static files that are deleted at phase exit. |
| — | Information Disclosure (data) | — | not applicable | No secret, token, binding or credential is read or written. `data/*.json` is already public site content. |
| T-00-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §Package Legitimacy Audit is present and complete for all nine packages with an explicit `[OK]` disposition each. No `[ASSUMED]`, `[SUS]` or `[SLOP]` package exists, so no blocking human legitimacy checkpoint is required. Adding any package not in that table re-triggers the gate. |
</threat_model>

<verification>
- `cd .playground && npx astro build` exits 0
- `bash .playground/check-no-js.sh` exits 0
- `.playground/bundle-report.txt` names prosemirror and tiptap with six-digit byte counts
- `git check-ignore -q .playground` exits 0 and `test ! -f package.json` exits 0
- `00-FINDINGS.md` carries all sixteen gap IDs and the five tier strings
</verification>

<success_criteria>
- The playground builds against `@akhil-saxena/design-system@1.11.4` installed from a packed tarball as a real directory, with exactly one React
- Neither `@astrojs/cloudflare`, `wrangler`, `vitest`, an adapter, an api directory nor an auth dependency appears anywhere in `.playground`
- DSGN-04's tree-shaking claim is answered with a recorded byte count, gzip count and module breakdown rather than an assertion
- DSGN-04's zero-JS claim is answered by a passing script-tag assertion on a page rendering eight design-system components
- `00-FINDINGS.md` exists as a committed Phase-1-readable register with the widened `tiers` vocabulary intact
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-01-SUMMARY.md` when done.

Commit only `.gitignore` and `.planning/phases/00-design-ideation/00-FINDINGS.md` plus the
SUMMARY. The playground is gitignored on purpose — do not force-add it.
</output>
