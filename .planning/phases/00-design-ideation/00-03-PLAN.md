---
phase: 00-design-ideation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/00-design-ideation/00-ADMIN-IA.md
autonomous: true
requirements: [DSGN-01]

must_haves:
  truths:
    - "Every field the legacy admin edited has a named home on one of the seven routes, so nothing is silently dropped in the port"
    - "The seven-route information architecture replaces the legacy three-tab model in writing, with the case-study home D-05 lacked"
    - "The résumé date-shape drift between the admin's split fields and resume.json's single period string is resolved by decision, not left as a discovery"
    - "The lowercase-versus-Title-case category drift has a written cause and a written fix"
  artifacts:
    - path: ".planning/phases/00-design-ideation/00-ADMIN-IA.md"
      provides: "The route-per-entity IA, the recovered field catalog, and the four schema decisions the admin forces"
      contains: "## Field catalog"
      min_lines: 180
  key_links:
    - from: ".planning/phases/00-design-ideation/00-ADMIN-IA.md"
      to: "legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx"
      via: "recovered Selection union and field labels"
      pattern: "PropertiesPanel"
    - from: ".planning/phases/00-design-ideation/00-ADMIN-IA.md"
      to: "data/portfolio_images.json + data/site_config.json"
      via: "the D-25 category drift, cited at its origin"
      pattern: "categoryColumns"
---

<objective>
Write the admin CMS information architecture as a reviewable document: the seven routes,
the entity each owns, the complete field catalog recovered from the legacy admin, the six
screen states and where each genuinely lives, and the four schema decisions the new IA
forces.

Purpose: RESEARCH's Architectural Responsibility Map is explicit that a 39-control,
13-variant field catalog is unreviewable as pictures — the table is the artefact and the
sketches illustrate it. This document is the input plans 12 through 16 sketch against and
the artefact Phase 7 ports from, and it is the half of DSGN-01 that survives the
playground's deletion.

Output: `00-ADMIN-IA.md`, committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-RESEARCH.md
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md

<structural_note>
The legacy admin was purged from `main`. Every legacy file below is reachable ONLY via
`git show legacy/nextjs-portfolio:<path>` — a bare path will not resolve, because the
working tree has no `src/`. Do not attempt to read `src/components/admin/...` directly.

This plan writes one Markdown file into `.planning/`. It builds nothing, installs nothing,
and has no playground dependency, so it runs in wave 1 alongside plans 01 and 02.
</structural_note>

<interfaces>
Verified 2026-08-17 by reading `data/*.json` and `../design-system/dist/index.d.ts`:

Real data the IA must account for:
- `resume.json` — `experience` (Brevo 11 bullets, PharmEasy 3, MAQ 4), `projects` (5),
  `skills` (3 groups), `education` (1)
- `portfolio_images.json` — 39 photos; per-photo keys `id, title, category, tags, date,
  exif, urls, order, dimensions`; seven lowercase categories: `abstract`, `architecture`,
  `nature`, `portraits`, `street`, `wildlife`, `product`
- `home_config.json` — `title, subtitle, intro, peekIds (6), peekPositions (exactly one:
  architecture-hawamahaldaytime = "50% 25%"), socialLinks (3), ctas (2)`
- `site_config.json` — a single key `categoryColumns` with **Title-case** keys
  (`All:3, Abstract:3, Architecture:3, Nature:2, Portraits:2, Street:2, Wildlife:2,
  Product:2`) against `portfolio_images.json`'s lowercase category values — the D-25 drift

Design-system components confirmed exported (names only; API fitness is UI-SPEC's ruling):
`AppShell`, `DataGrid`, `Table`, `Field`, `FieldError`, `TextInput`, `Textarea`, `Select`,
`Checkbox`, `Radio`, `Toggle`, `NumberStepper`, `FileInput`, `RichText`, `Sortable`,
`SortableItem`, `SortableDndContext`, `InlineEdit`, `Sheet`, `Modal`, `ConfirmDialog`,
`TypeToConfirm`, `FormErrorSummary`, `EmptyState`, `Skeleton`, `ProgressBar`, `Badge`,
`StatusPill`, `RelativeTime`, `AlertBanner`, `Chip`, `Wizard`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover the legacy field catalog and write the route-per-entity IA</name>
  <read_first>
    - `git show legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx` — 937 lines, the field catalog; specifically the `Selection` union at lines 92-105 and the per-variant branches (photo 335-418, role/skillGroup/education 421-668, project 499-577, home 669-807, resume 893-937)
    - `git show legacy/nextjs-portfolio:src/types.ts` — the header comment documenting the drift between the admin's local types and the canonical ones
    - `git show legacy/nextjs-portfolio:src/components/admin/DeployButton.tsx` — the JSON-stringify diff and the `baseSha: "latest"` bypass
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §"The field catalog exists — recover it, do not re-derive it" and §"Entity → screen map"
    - `.planning/phases/00-design-ideation/00-PATTERNS.md` §Shared Patterns 6 (the seven dead admin components)
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-05, D-06, D-07, D-20, D-22, D-24, D-25, D-26
    - `data/resume.json`, `data/portfolio_images.json`, `data/home_config.json`, `data/site_config.json`
  </read_first>
  <files>.planning/phases/00-design-ideation/00-ADMIN-IA.md</files>
  <action>
Create `00-ADMIN-IA.md` with YAML frontmatter (`phase: 0`, `slug: design-ideation`,
`consumers: [phase-7]`) and these exact H2 headings in order: `## Routes`,
`## Field catalog`, `## Schema decisions this IA forces`, `## What is deliberately not
ported`.

Under `## Routes`, table the seven routes with entity, source data and one line on what the
screen is for: `/admin` (pending dashboard, D1 draft rows, D-06 — its empty state is a
first-class screen), `/admin/home` (`home_config.json`), `/admin/photos`
(`portfolio_images.json`), `/admin/resume` (`resume.json`), `/admin/projects` (new
`projects.json`, D-24), `/admin/projects/[id]` (one project plus its case study — the
seventh screen, implied by D-24 but absent from D-05's list), `/admin/site`
(`site_config.json`). State that this replaces the legacy three-tab model
(home / photography / dev), which had no home for case studies at all, and cite D-07's
full-width preview toggle as the reason the form gets the whole content column.

Under `## Field catalog`, transcribe the legacy `Selection` union's thirteen variants and
map each onto one of the seven routes, so every variant has a destination and no variant is
orphaned. Then table every recovered field label against its route and its design-system
composition target. The recovered labels, verbatim: Site Title, Tagline, Intro Text,
Subtitle, Introduction, Social Links, Button (Text / Link URL / Style), Photo (Title /
Category / Tags / Order / Position), Camera, Lens, Aperture, Shutter, ISO, Focal Length,
Company, Role, Period, Location, Icon, URL, Bullets, Project (Name / Label / Description /
Tech Stack / Link / Store Links), Skill Group / Skills, School, Degree, CGPA, Leadership,
Resume PDF / Upload New Resume, Add Photo to Gallery, Replace with, Actions. Record the
legacy control counts as the completeness check: 33 `<input>`, 4 `<select>`, 2 `<textarea>`
— 39 form controls — and 34 `<button>`.

Record the EXIF sub-form as a six-field group (Camera, Lens, Aperture, Shutter, ISO, Focal
Length) with the rule that missing fields are OMITTED ENTIRELY, never rendered as an em
dash: `product-peppers` carries no EXIF at all and `architecture-redbuilding` has camera
only.

For each route, state which design-system components compose it, taking the mapping from
UI-SPEC §Component Mapping rather than re-deriving it, and name the gap ID where the
mapping is known-broken (G-1 focal-point picker, G-3/G-4 RichText, G-5 StatusPill,
G-6 FormErrorSummary, G-7 conflict diff, G-8 AppShell banner slot). Do not propose a local
component anywhere — a gap is a findings entry, never a workaround (Core Value).

Under `## What is deliberately not ported`, name the seven dead legacy admin components
verified to have zero import sites — `PhotoGrid.tsx`, `PhotoEditModal.tsx`,
`PreviewPanel.tsx`, `ExperienceEditor.tsx`, `EducationEditor.tsx`, `ProjectEditor.tsx`,
`SkillsEditor.tsx` — and state that no wireframe may be drawn from any of them. Record the
detail worth keeping: the dead `PreviewPanel` was a SPLIT preview, and D-07 independently
chose a full-width toggle, so the legacy code already tried and dropped that design. Name
the five wired components as the only admin analogs: `PropertiesPanel.tsx`,
`DraggableMasonry.tsx`, `PhotoUploadZone.tsx`, `AdminTopBar.tsx`, `DeployButton.tsx`.
  </action>
  <verify>
    <automated>test -f .planning/phases/00-design-ideation/00-ADMIN-IA.md && for h in "## Routes" "## Field catalog" "## Schema decisions this IA forces" "## What is deliberately not ported"; do grep -qF "$h" .planning/phases/00-design-ideation/00-ADMIN-IA.md || { echo "MISSING $h"; exit 1; }; done && for r in "/admin/home" "/admin/photos" "/admin/resume" "/admin/projects/\[id\]" "/admin/site"; do grep -q "$r" .planning/phases/00-design-ideation/00-ADMIN-IA.md || { echo "MISSING ROUTE $r"; exit 1; }; done && echo IA_ROUTES_OK</automated>
  </verify>
  <acceptance_criteria>
    - All four H2 headings present, spelled exactly
    - All seven route strings appear: `/admin`, `/admin/home`, `/admin/photos`, `/admin/resume`, `/admin/projects`, `/admin/projects/[id]`, `/admin/site`
    - All thirteen `Selection` variant names appear: `none`, `photo`, `role`, `project`, `skillGroup`, `education`, `homeTitle`, `homeSubtitle`, `homeIntro`, `homeGallery`, `homeSocial`, `homeCta`, `resume`
    - The literal counts `39` (form controls) and `34` (buttons) appear in the field-catalog section
    - All six EXIF field labels appear: Camera, Lens, Aperture, Shutter, ISO, Focal Length
    - `product-peppers` and `architecture-redbuilding` are both named as the omit-not-em-dash evidence
    - All seven dead component filenames appear under `## What is deliberately not ported`
    - Gap IDs `G-1`, `G-3`, `G-4`, `G-5`, `G-6`, `G-7`, `G-8` each appear at least once
    - The file is at least 180 lines (`wc -l` >= 180)
    - No line proposes building a local replacement component; assert `grep -qiE 'build (our|a) own (component|control)|local component' ` exits 1
  </acceptance_criteria>
  <done>Seven routes, thirteen variants and every recovered field label have a written home, with the design-system composition and known gap named per route.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the four schema decisions and the state-scope reduction</name>
  <read_first>
    - `.planning/phases/00-design-ideation/00-ADMIN-IA.md` (created in task 1 — read current state before appending)
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-03, D-10, D-11, D-12, D-13, D-16, D-17, D-18, D-20, D-22, D-24, D-25
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Review Convention — the artefact classes, canonical IDs, and the 42-cell coverage table contract
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §"The combinatorial risk — with a number" and the state-scope reduction table
    - `git show legacy/nextjs-portfolio:src/types.ts` (the drift comment)
    - `data/resume.json` (the `period` string shape) and `data/site_config.json` (the Title-case keys)
  </read_first>
  <files>.planning/phases/00-design-ideation/00-ADMIN-IA.md</files>
  <action>
Append to the existing `## Schema decisions this IA forces` section, then add two further H2
sections: `## States and where each actually lives` and `## Artefact inventory`.

Under `## Schema decisions this IA forces`, resolve all four in writing, each with a chosen
shape and a one-line reason:

1. **`projects` extracted from `resume.json` into `projects.json` (D-24).** State the fields
   carried across — `id, title, label, description, tech, icon, href, badges` — and that
   `badges` is already an array of objects carrying "Live" on Cairn, which D-45 extends to
   Live / Maintained / Archived.
2. **Résumé bullets become structured segments (D-20).** Arrays of `{text}` and
   `{text, emphasis: true}`, rendered as React elements. State plainly that no HTML string
   exists anywhere in the shape, so the legacy stored-XSS class is designed out rather than
   filtered, and that `<strong>` is the only mark used across `resume.json` today so the
   migration is narrow.
3. **A per-category photo order field (D-22).** Global `order` already exists; the new field
   wins when a category filter is active.
4. **Canonical category records (D-25).** `{ id (lowercase), label (display), columns }`,
   edited on `/admin/site`. Cite the drift at its origin: `portfolio_images.json` stores
   `architecture` while `site_config.categoryColumns` keys on `Architecture`, and the legacy
   `PropertiesPanel` Title-cased at render time with `c.charAt(0).toUpperCase() + c.slice(1)`
   — making display and key different fields is what kills it. State that rename and delete
   need a designed reassignment path ("12 photos use this").

Resolve the **date-shape drift** explicitly as a fifth decision: the legacy admin split
dates into `startMonth / startYear / endMonth / endYear / isPresent` while `resume.json`
stores a single `period` string. Pick one shape, state which, and state the consequence for
the résumé screen's field set. This is a defined port task, not a discovery — leaving it
open is the failure mode.

Under `## States and where each actually lives`, record D-03's six states — empty, loading,
error, dirty, conflict, success — and, for each, the scope it genuinely lives at and the
artefact count that follows: `loading` is a shell plus list treatment (2), `conflict` is one
dedicated screen (1), `success` is a publish outcome (2), `error` is three genuinely
different treatments (inline field warning under lenient draft, publish-block error summary,
network/401), `dirty` is one badge pattern applied everywhere (1), and `empty` is the only
genuinely per-screen state (4-5). State the reduction plainly: the naive product is 7 screens
x 6 states x 2 layouts = 84 plus roughly 24 cross-cutting surfaces, about 108 artefacts, and
D-03's promise that nothing is left for Phase 7 to invent is kept by an explicit, complete
coverage matrix rather than by rendering every cell.

Under `## Artefact inventory`, transcribe UI-SPEC's canonical artefact IDs verbatim — the
seven `S-`, five `E-`, eight `T-`, nine `O-`, four `P-`, two `R-` and five `X-` IDs — with a
one-line note per ID stating what it proves, not what it shows. These IDs are a contract:
plans 09 through 16 may add IDs but may not silently rename one.
  </action>
  <verify>
    <automated>for h in "## States and where each actually lives" "## Artefact inventory"; do grep -qF "$h" .planning/phases/00-design-ideation/00-ADMIN-IA.md || { echo "MISSING $h"; exit 1; }; done && for id in S-dashboard S-home S-photos S-resume S-projects S-project-detail S-site E-dashboard E-photos E-projects E-resume-section E-category-filtered T-loading-shell T-loading-list T-dirty-badge T-ready-badge T-error-inline T-error-publish T-error-network T-success-published O-publish-valid O-publish-invalid O-discard-screen O-discard-all O-conflict-diff O-reauth-401 O-category-reassign O-pipeline-strip O-phone-sidebar P-dashboard P-text-edit P-photo-reorder P-publish R-crop-picker R-case-study-authoring X-work X-photos X-case-long X-case-short X-contact-sheet; do grep -q "$id" .planning/phases/00-design-ideation/00-ADMIN-IA.md || { echo "MISSING ARTEFACT ID $id"; exit 1; }; done && echo IA_INVENTORY_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both new H2 headings present, spelled exactly
    - All forty canonical artefact IDs listed above appear verbatim
    - All five schema decisions are stated with a chosen shape: `projects.json`, segment bullets, per-category order, canonical category records, and the date-shape resolution
    - `grep -q 'startMonth' .planning/phases/00-design-ideation/00-ADMIN-IA.md` exits 0 and the section states which of the two date shapes wins
    - `grep -q 'categoryColumns' .planning/phases/00-design-ideation/00-ADMIN-IA.md` exits 0 and both `architecture` and `Architecture` appear as the drift evidence
    - The segment-bullet decision states that no HTML string exists in the shape; assert `grep -qi 'no HTML string' ` exits 0
    - The numbers `108` and `35` both appear in the states/reduction section
    - All six state names appear: empty, loading, error, dirty, conflict, success
  </acceptance_criteria>
  <done>All five schema decisions are resolved in writing, the six states have a written scope and count, and the forty canonical artefact IDs are recorded as a contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy branch contents → the new IA | Design decisions carried forward from an application with four documented defects; carrying one forward silently is the risk. |
| this document → Phase 7 implementation | An IA that designs an unsafe affordance becomes an unsafe implementation two phases later, when the reasoning is no longer in anyone's context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-09 | Tampering (stored XSS, designed in) | résumé bullet content shape | mitigate | Schema decision 2 fixes bullets as `{text}` / `{text, emphasis}` segments and states in the document that no HTML string exists anywhere in the shape. This designs the legacy stored-XSS class out rather than filtering it. Task 2's acceptance criteria assert that sentence is present, so a later "simplification" back to an HTML string has to argue with a written decision. |
| T-00-10 | Tampering | silent overwrite on publish | mitigate | The document records the legacy `baseSha: "latest"` bypass and `JSON.stringify` diff as the defect being fixed, and states that D-10's server-side draft row stores the blob SHA it was based on at save time, so 409 detection has a real baseline. The IA gives the per-file conflict screen (D-16) a named home rather than leaving it to Phase 7. |
| T-00-11 | Spoofing | 401 re-authentication affordance | mitigate | The IA records D-19's behaviour as deny-and-re-authenticate with on-screen state preserved and the save retried in place. It explicitly forbids depicting a dev-mode bypass or a client-held credential — the legacy `access.ts` cookie-presence fallback is exactly that failure and must not be re-designed in. |
| — | Information Disclosure | — | not applicable | No secret, token or binding is named in this document. All data shapes described are already-public site content committed to the repository. |
| — | Denial of Service | — | not applicable | This plan produces one Markdown file; nothing is served or executed. |
| — | Elevation of Privilege | — | not applicable | No privilege boundary is crossed. Access control for `/admin` is Cloudflare Access plus AUTH-01…04 in Phase 2, out of scope here. |
| — | Repudiation | — | not applicable | Single operator; no multi-actor action to attribute. |
| T-00-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no package and executes no third-party code. |
</threat_model>

<verification>
- All six H2 headings present in `00-ADMIN-IA.md`
- Seven routes, thirteen Selection variants and forty canonical artefact IDs all present
- Five schema decisions resolved, each naming a chosen shape
- The seven dead legacy components named as not-to-port
</verification>

<success_criteria>
- Every field the legacy admin edited maps to exactly one of the seven routes, with the design-system composition and known gap named
- The five schema decisions the new IA forces are resolved in writing, not deferred to discovery
- D-03's exhaustiveness is expressed as a coverage contract with a stated artefact count, so plans 12–16 have a bounded target
- The document stands alone as Phase 7's input after `.playground/` is deleted
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-03-SUMMARY.md` when done.
Commit `00-ADMIN-IA.md` and the SUMMARY.
</output>
