---
phase: 00-design-ideation
plan: 05
type: execute
wave: 2
depends_on: ["00-02"]
files_modified:
  - .planning/phases/00-design-ideation/00-COPY/case-design-system.md
  - .planning/phases/00-design-ideation/00-COPY/case-cairn.md
autonomous: true
requirements: [DSGN-06]

must_haves:
  truths:
    - "The two long-form case studies follow problem then decisions then outcome, and each decision names the option not taken and what it would have cost"
    - "Every factual claim is traceable to a file read this session, and no claim is sourced from the design system's stale planning docs"
    - "Every gap carries a length-realistic placeholder, so the case-study template is laid out against a real measure rather than a stub"
    - "The design-system case study closes by pointing at the page the reader is on"
  artifacts:
    - path: ".planning/phases/00-design-ideation/00-COPY/case-design-system.md"
      provides: "Long-form flagship case study drafted from CHANGELOG and src"
      contains: "## Decisions"
      min_lines: 90
    - path: ".planning/phases/00-design-ideation/00-COPY/case-cairn.md"
      provides: "Long-form case study drafted from Cairn's decisions table and REMOVED.md"
      contains: "## Decisions"
      min_lines: 90
  key_links:
    - from: ".planning/phases/00-design-ideation/00-COPY/case-cairn.md"
      to: "../cairn/.planning/REMOVED.md"
      via: "rejected alternatives quoted with their stated cost"
      pattern: "\\[source:"
    - from: ".planning/phases/00-design-ideation/00-COPY/case-design-system.md"
      to: "../design-system/CHANGELOG.md"
      via: "measured contrast ratios and per-change rationale"
      pattern: "\\[source:"
---

<objective>
Draft the two long-form case studies — the design system and Cairn — at finished length,
from the repositories that actually record their decisions.

Purpose: D-39 tiers case studies by depth, and RESEARCH's derivability matrix independently
validates that tiering: the design system and Cairn are the only two projects with a
`.planning/` directory, a decisions table carrying rationale, and a register that names
options not taken. For those two the drafting task is closer to selection and compression
than to authoring. CASE-01's hardest requirement — a decision section that names the
alternative rejected and its cost — is only satisfiable from these two repos.

Output: two Markdown drafts in `00-COPY/`, both passing the length-realism checker, both
awaiting Akhil's edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-RESEARCH.md
@.planning/phases/00-design-ideation/00-PATTERNS.md
@.planning/phases/00-design-ideation/00-COPY/one-liners.md

<structural_note>
Markdown only. No playground dependency, no build. Runs in wave 2 in parallel with plans 04
and 06 — 06 writes the three short-form studies and touches no file this plan touches.

`../design-system` and `../cairn` are real sibling checkouts at real paths and are read-only.
</structural_note>

<interfaces>
Derivability, measured per repo during research:

**design-system** — derivable: problem, decisions WITH reasons and measured numbers,
rejected approaches, constraints, test strategy. Sources: `CHANGELOG.md` (43 KB, per-change
rationale including measured contrast ratios), `README.md`, `CAIRN-CONSOLIDATION.md`, `src/`,
Playwright + axe + vitest suites. NOT derivable: adoption or outcome beyond "Cairn and this
site use it".

**Cairn** — 873 commits. Derivable: problem, decisions plus rationale, rejected alternatives
verbatim, constraints, refusals-as-identity. Sources: `.planning/PROJECT.md` (12-row Key
Decisions table WITH a rationale column), `.planning/REMOVED.md` (an authoritative list of
features cut, with why), `ARCHITECTURE.md`, `TECH-STACK.md`, `A11Y-AUDIT.md`, `docs/`,
runbook. NOT derivable: usage numbers, whether the wedge worked.

The register these two already write in, quoted from the design system's CHANGELOG as the
target voice: "`--ink-4` … was historically equal to `--ink-3` in light but a much dimmer
grey in dark, which silently dropped ~28 text usages to 2.4:1 in dark mode. Aliasing keeps
both modes honest."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the design-system case study, the flagship</name>
  <read_first>
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/CHANGELOG.md` — the per-change rationale register, the accurate current source
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/README.md` — the current component count and feature claims
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/CAIRN-CONSOLIDATION.md` — Cairn as the second consumer
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/src/tokens.css` — the shipped theming mechanism (`:root.dark, .dark`)
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/src/tokens.test.ts` — the test strategy, and the regression narratives its comments carry
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Copywriting Contract and §"The `[NEEDS AKHIL]` length-realism rule (D-40)"
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §"What 'done well' looks like for an engineering case study" and §Common Pitfalls, Pitfall 7
    - `.planning/phases/00-design-ideation/00-COPY/one-liners.md` (the design-system one-liner, so the case study opens consistently with it)
  </read_first>
  <files>.planning/phases/00-design-ideation/00-COPY/case-design-system.md</files>
  <action>
Create `00-COPY/case-design-system.md` with YAML frontmatter (`project: design-system`,
`tier: long`, `status: first-pass`, `awaiting: akhil-edit`) and these exact H2 headings in
order: `## Problem`, `## Decisions`, `## Outcome`, `## Assets`.

Do NOT source anything from `../design-system/.planning/PROJECT.md`. It is stale: titled
"Project: JobDash Design System", claims 53 sections against the README's current count, and
documents `body.dark` as the theming mechanism when the shipped `src/tokens.css` uses
`:root.dark, .dark`. Putting any of that in front of engineers who will open the repo is the
specific failure Pitfall 7 names. `CHANGELOG.md`, `README.md` and `src/` are the current
source. Any number that was not read out of a file this session is a liability.

Under `## Problem`, state what the library exists to solve in plain language, at paragraph
length. Derivable in full from the README and CHANGELOG.

Under `## Decisions`, write three to five decisions. Each one must name the option NOT taken
and what it would have cost — that is CASE-01's hardest requirement and the one reliable
tell of a case study done well. The CHANGELOG already writes in exactly that register with
measured numbers, so this is selection and compression, not invention. Quote or paraphrase
its measured contrast ratios and per-change rationale, and append a `[source: <file>]`
marker to every factual claim so provenance is auditable and Akhil can check each one.

Under `## Outcome`, be honest: adoption and outcome beyond "Cairn and this site use it" are
NOT derivable from the repo. Open the section with an HTML comment stating what was searched
and why it came up empty — that comment is what makes the final-phase interview efficient.
Then write a `[NEEDS AKHIL]` marker followed by placeholder prose at the length the finished
section needs, at minimum forty words. Every guessed value must be an angle-bracket slot
(`<n>`, `<month>`, `<metric>`), never a bare number: a guess that looks like a fact is worse
than an admitted gap, and the checker enforces it. Never state a motivation or an outcome as
fact — code shows what was decided, rarely why, and never the outcome.

Close the study by pointing at the page the reader is on. It is the only project whose
outcome the reader is looking at while reading it (D-38), and UI-SPEC rule 5 makes that
closing mandatory.

Under `## Assets`, name the hero image and the one or two inline screenshots the study needs
(D-41), each with a one-line note on what decision the screenshot makes easier to see than
to describe. Available sources: the live Storybook and the live site. Record that these
screenshots take the D-42 simple R2 asset path under `assets/` and do NOT go through the
photo pipeline, which composites a watermark and extracts EXIF — neither of which belongs on
a screenshot.
  </action>
  <verify>
    <automated>node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs && for h in "## Problem" "## Decisions" "## Outcome" "## Assets"; do grep -qF "$h" .planning/phases/00-design-ideation/00-COPY/case-design-system.md || { echo "MISSING $h"; exit 1; }; done && echo DS_CASE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs` exits 0
    - All four H2 headings present, spelled exactly
    - `grep -c '\[source:' .planning/phases/00-design-ideation/00-COPY/case-design-system.md` prints at least `5`
    - `grep -c '\[NEEDS AKHIL\]' .planning/phases/00-design-ideation/00-COPY/case-design-system.md` prints at least `1`, and every marker is preceded by an HTML comment stating what was searched (assert `grep -B4 'NEEDS AKHIL' | grep -q '<!--'`)
    - `grep -qi 'JobDash\|53 section\|body\.dark' .planning/phases/00-design-ideation/00-COPY/case-design-system.md` exits 1 — no stale fact carried
    - `grep -q '55 component' .planning/phases/00-design-ideation/00-COPY/case-design-system.md` exits 1
    - The `## Decisions` section contains at least three decisions, each naming a rejected alternative; assert `grep -ciE 'instead of|rejected|not taken|the alternative' ` returns at least `3`
    - The file closes by referencing the page the reader is on; assert the last twenty lines contain one of `this page`, `this site`, `the page you`
    - `wc -l` >= 90
  </acceptance_criteria>
  <done>The flagship case study is drafted at finished length from current sources only, with every claim marked with its source file, every gap length-realistic, and the closing pointer in place.</done>
</task>

<task type="auto">
  <name>Task 2: Draft the Cairn case study</name>
  <read_first>
    - `/Users/akhilsaxena/Documents/Personal/Repositories/cairn/.planning/REMOVED.md` — the authoritative list of features cut, with why; the single best source for CASE-01's rejected-alternatives requirement
    - `/Users/akhilsaxena/Documents/Personal/Repositories/cairn/.planning/PROJECT.md` — the 12-row Key Decisions table with its rationale column
    - `/Users/akhilsaxena/Documents/Personal/Repositories/cairn/ARCHITECTURE.md` and `TECH-STACK.md`
    - `/Users/akhilsaxena/Documents/Personal/Repositories/cairn/A11Y-AUDIT.md`
    - `.planning/phases/00-design-ideation/00-COPY/case-design-system.md` (created in task 1 — match its section shape and its `[source:]` convention exactly, so the two long-form studies are one template)
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Copywriting Contract and the D-40 length-realism rule
    - `.planning/phases/00-design-ideation/00-COPY/one-liners.md` (the cairn one-liner)
  </read_first>
  <files>.planning/phases/00-design-ideation/00-COPY/case-cairn.md</files>
  <action>
Create `00-COPY/case-cairn.md` with the same frontmatter shape (`project: cairn`,
`tier: long`, `status: first-pass`, `awaiting: akhil-edit`) and the same four H2 headings in
the same order as the design-system study. The two long-form studies must be structurally
identical, because plan 10 renders both through ONE template — a structural divergence here
becomes a template that only fits one of them.

Under `## Problem`, state the problem Cairn exists to solve. Fully derivable from
`.planning/PROJECT.md` and `ARCHITECTURE.md`.

Under `## Decisions`, write three to five decisions, each naming the option not taken and its
cost. `REMOVED.md` is an authoritative register of features cut WITH reasons — it is the
strongest raw material for this section in any of the five repos, so quote its rejections
rather than paraphrasing them into vagueness. Cairn's refusals ARE its identity, and the one
liner already carries that; the decisions section should make the refusals concrete. Append
`[source: <file>]` to every factual claim, matching task 1's convention exactly.

Under `## Outcome`, record honestly that usage numbers and whether the wedge worked are NOT
derivable — there are no analytics, no install counts in git, and no retrospective. Open with
an HTML comment stating exactly that, then a `[NEEDS AKHIL]` marker followed by at least
forty words of placeholder prose written at finished length, with every guessed value in
angle brackets. RESEARCH §Code Examples carries a worked example of exactly this section for
Cairn — follow its shape.

Under `## Assets`, name the hero image and one or two inline screenshots (D-41), sourced from
the live site, each with a note on what decision it shows. Record the D-42 R2 `assets/` path
and the exclusion from the photo pipeline.

Do not carry any commit count, star count or usage figure into the prose unless it was read
out of a file this session and is marked with its source.
  </action>
  <verify>
    <automated>node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs && for h in "## Problem" "## Decisions" "## Outcome" "## Assets"; do grep -qF "$h" .planning/phases/00-design-ideation/00-COPY/case-cairn.md || { echo "MISSING $h"; exit 1; }; done && diff <(grep -o '^## .*' .planning/phases/00-design-ideation/00-COPY/case-cairn.md) <(grep -o '^## .*' .planning/phases/00-design-ideation/00-COPY/case-design-system.md) && echo CAIRN_CASE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs` exits 0
    - The H2 heading sequence of `case-cairn.md` is byte-identical to that of `case-design-system.md` (the `diff` in the verify command exits 0)
    - `grep -c '\[source:' .planning/phases/00-design-ideation/00-COPY/case-cairn.md` prints at least `5`
    - `grep -c '\[NEEDS AKHIL\]' .planning/phases/00-design-ideation/00-COPY/case-cairn.md` prints at least `1`, each preceded by an HTML comment
    - At least one `[source:` marker names `REMOVED.md`
    - The `## Decisions` section names at least three rejected alternatives
    - No bare multi-digit number appears inside any `[NEEDS AKHIL]` block (the checker enforces this; confirm it exits 0)
    - `wc -l` >= 90
  </acceptance_criteria>
  <done>Cairn's case study is drafted at finished length with the same structure as the flagship, its rejections quoted from REMOVED.md, and its unknowable outcome marked length-realistically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling repositories → publicly published prose | Facts cross from local checkouts, including `.planning/` directories that were never written for publication, into text engineers will read with the repo open beside it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-07 | Information Disclosure | private planning documents quoted into public copy | mitigate | Only material a reader could already find in a public README, CHANGELOG or live site may be stated as fact. `.planning/` content is used as EVIDENCE OF WHAT WAS DECIDED AND WHY, re-verified against shipped code before any claim is made. The `[source: <file>]` marker on every claim makes provenance auditable at review, so a reviewer can spot anything that should not be public before it ships. No runbook, credential, endpoint or internal URL enters the prose. |
| T-00-08 | Repudiation (reputational) | stale or invented facts in the flagship study | mitigate | `../design-system/.planning/PROJECT.md` is excluded by name and its three known-stale claims (JobDash, 53 sections, `body.dark`) are asserted absent. The handoff's "55 components" is asserted absent. Every number must be read out of a file this session. `[NEEDS AKHIL]` plus angle-bracket slots is the only sanctioned form for anything unknown, and the checker fails a bare number inside a gap block. |
| T-00-14 | Tampering | a motivation or outcome invented as fact | mitigate | D-40 rule 3 is stated in both tasks: never state a motivation or an outcome as fact, because code shows what was decided, rarely why, and never the outcome. In front of engineers an invented decision is worse than an admitted gap. Enforced by requiring an HTML comment recording what was searched above every gap marker. |
| — | Spoofing | — | not applicable | No authentication or identity surface exists in this plan. |
| — | Denial of Service | — | not applicable | Two Markdown files; nothing is served or executed beyond a bounded local file walk by the checker. |
| — | Elevation of Privilege | — | not applicable | No privilege boundary is crossed; no package installed, no platform resource touched. |
| T-00-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no package. |
</threat_model>

<verification>
- `node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs` exits 0
- Both files carry the same four H2 headings in the same order, verified by `diff`
- Every factual claim carries a `[source:` marker; at least five per file
- No stale design-system fact and no "55 components" anywhere
</verification>

<success_criteria>
- Two long-form case studies drafted at finished length, structurally identical so one template renders both
- Each decision section names the option not taken and what it would have cost
- Every gap is a length-realistic placeholder with an HTML comment recording what was searched, so the final-phase interview is efficient
- Every fact is traceable to a file read this session; nothing is sourced from the design system's stale planning docs
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-05-SUMMARY.md` when done.
Commit both `00-COPY/case-*.md` files and the SUMMARY.
</output>
