---
phase: 00-design-ideation
plan: 06
type: execute
wave: 2
depends_on: ["00-02"]
files_modified:
  - .planning/phases/00-design-ideation/00-COPY/case-timeshift.md
  - .planning/phases/00-design-ideation/00-COPY/case-hued.md
  - .planning/phases/00-design-ideation/00-COPY/case-momentum.md
autonomous: true
requirements: [DSGN-06]

must_haves:
  truths:
    - "The three short-form case studies share one structure, so a single short template renders all three"
    - "Each short study carries a real problem statement and one architectural decision visible in the code"
    - "Motivation and outcome are marked as gaps at paragraph length rather than invented"
    - "Momentum's feature-list voice is replaced, not lightly edited"
  artifacts:
    - path: ".planning/phases/00-design-ideation/00-COPY/case-timeshift.md"
      provides: "Short-form case study — the timezone-abbreviation ambiguity problem"
      contains: "## Problem"
    - path: ".planning/phases/00-design-ideation/00-COPY/case-hued.md"
      provides: "Short-form case study — palette-as-theme, design-first"
      contains: "## Problem"
    - path: ".planning/phases/00-design-ideation/00-COPY/case-momentum.md"
      provides: "Short-form case study — the six-noun feature list replaced"
      contains: "## Problem"
  key_links:
    - from: ".planning/phases/00-design-ideation/00-COPY/case-timeshift.md"
      to: ".planning/phases/00-design-ideation/00-COPY/case-hued.md"
      via: "identical H2 heading sequence, so one short template fits all three"
      pattern: "^## "
---

<objective>
Draft the three short-form case studies — TimeShift, hued and Momentum — each with a real
problem statement, one architectural decision that is visible in the code, and an honest
paragraph-length gap where motivation and outcome should go.

Purpose: D-39 tiers case studies by depth, and RESEARCH's derivability matrix shows exactly
why these three get the short form: none has a planning directory, a rationale-carrying
decisions table, or a register of options not taken. The honest structure for them is
narrower, and pretending otherwise would mean inventing decisions in front of the audience
most likely to open the repo. Momentum additionally needs its voice replaced, not edited —
its current copy is a twenty-one-bullet feature list.

Output: three Markdown drafts in `00-COPY/`, structurally identical, all passing the
length-realism checker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-RESEARCH.md
@.planning/phases/00-design-ideation/00-COPY/one-liners.md

<structural_note>
Markdown only. No playground dependency, no build. Runs in wave 2 in parallel with plans 04
and 05; plan 05 writes the two long-form studies and touches no file this plan touches.

`../hued`, `../Momentum` and `../TimeShift` are real sibling checkouts at real paths and are
read-only.
</structural_note>

<interfaces>
Derivability, measured per repo during research:

**TimeShift** — 59 commits. Sources: `README.md` (46 lines), `docs/`, 65 tests,
`PRIVACY_POLICY.md`. Derivable: what it does, supported formats with verbatim examples, the
test count, and the CST/IST/BST disambiguation problem — which RESEARCH calls a genuinely
good problem hook. NOT derivable: why it was built, outcome.

**hued** — only 19 commits, a thin history. Sources: `README.md` (51 lines), `docs/`,
`publishing/`. Derivable: what it does, tech stack, and a stated design philosophy
("design-first app, not an app with design"). Its README already opens idea-first: "Every
photo you take carries color. Over weeks and months, those colors tell a story you've never
seen." NOT derivable: decisions, outcome.

**Momentum** — 396 commits. Sources: `ARCHITECTURE.md`, `README.md` (79 lines, an MVVM layer
diagram), `docs/MIGRATION_PLAN.md`, `store-listing/`. Derivable: what it does,
architecture, the full feature surface, screenshots. NOT derivable: why any of it, outcome.
Its README opens with a twenty-one-bullet feature list and its `resume.json` description is
the six-noun run "Goal tracker with adaptive daily targets, streaks, milestones, badges,
home screen widgets, and cloud sync." — D-43's diagnosis, confirmed against the data.

Structural contract: plan 10 renders all three through ONE short template, so their H2
heading sequences must be byte-identical to each other.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft TimeShift and hued, establishing the short-form structure</name>
  <read_first>
    - `/Users/akhilsaxena/Documents/Personal/Repositories/TimeShift/README.md` and `docs/` — the supported formats with verbatim examples, the test count, and the CST/IST/BST disambiguation problem
    - `/Users/akhilsaxena/Documents/Personal/Repositories/hued/README.md` and `docs/` — the idea-first opening and the stated design philosophy
    - `/Users/akhilsaxena/Documents/Personal/Repositories/hued/publishing/` — the store assets available for D-41
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Copywriting Contract and §"The `[NEEDS AKHIL]` length-realism rule (D-40)"
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §"What 'done well' looks like for an engineering case study" — the narrower honest structure prescribed for the three short-form projects
    - `.planning/phases/00-design-ideation/00-COPY/one-liners.md` (the timeshift and hued entries, so each study opens consistently with its one-liner)
  </read_first>
  <files>.planning/phases/00-design-ideation/00-COPY/case-timeshift.md, .planning/phases/00-design-ideation/00-COPY/case-hued.md</files>
  <action>
Create both files with YAML frontmatter (`project:` set to `timeshift` and `hued`
respectively, `tier: short`, `status: first-pass`, `awaiting: akhil-edit`) and these exact H2
headings in this order, identical in both files: `## Problem`, `## Decision`, `## Outcome`,
`## Assets`.

Note the deliberate difference from the long form, which uses `## Decisions` plural: the
short form has `## Decision` singular, because the honest structure for these three is a real
problem statement plus ONE architectural decision that is visible in the code — not a
decisions register they do not have. Do not pad it to three by inventing two.

TimeShift — lead `## Problem` with the timezone-abbreviation ambiguity. CST alone is
ambiguous across US Central, China Standard and Cuba Standard; IST across India, Israel and
Ireland; BST across British Summer and Bangladesh Standard. That is a genuinely interesting
problem and it is fully derivable from the README and docs. Under `## Decision`, take one
architectural decision that shows in the code and state it with its consequence. The test
count and the verbatim supported-format examples are real hard facts — read the count out of
the repo this session, use it, and mark it `[source: <file>]`.

hued — its README is already written as narrative rather than as a feature list, so
`## Problem` is closer to compression than to authoring. Use its stated design philosophy —
"design-first app, not an app with design" — as the frame, and the palette-as-theme idea as
the concrete mechanism. Under `## Decision`, take the palette extraction approach as the one
decision, naming the technique rather than describing it with an adjective. State honestly
that hued has only 19 commits, so decisions beyond the visible one are not recoverable from
its history.

For BOTH, under `## Outcome`, open with an HTML comment stating exactly what was searched and
why it came up empty, then a `[NEEDS AKHIL]` marker followed by placeholder prose at finished
length — a minimum of forty words for a paragraph-scale gap. Every guessed value must be an
angle-bracket slot such as `<n>`, `<month>` or `<metric>`, never a bare number. Never state a
motivation or an outcome as fact: code shows what was decided, rarely why, and never the
outcome.

Under `## Assets`, name the hero image and at most one inline screenshot per project (D-41),
sourced from the Play Store listing for hued and the Chrome Store listing for TimeShift.
Record the D-42 path: screenshots go straight to R2 under `assets/` on the same custom domain
as photos, with dimensions captured at upload for CLS, and do NOT pass through the photo
pipeline, which composites a watermark and extracts EXIF — neither of which belongs on a
screenshot.

Apply the copywriting rules throughout: no feature lists, the hard fact is a number or a
constraint or a named technique rather than an adjective, no superlatives, and status carries
currency via the D-45 badge rather than via prose or a date.
  </action>
  <verify>
    <automated>node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs && diff <(grep -o '^## .*' .planning/phases/00-design-ideation/00-COPY/case-timeshift.md) <(grep -o '^## .*' .planning/phases/00-design-ideation/00-COPY/case-hued.md)</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs` exits 0
    - Both files carry exactly the four H2 headings `## Problem`, `## Decision`, `## Outcome`, `## Assets` in that order, and a `diff` of their extracted heading sequences exits 0
    - Each file contains at least three `[source:` markers
    - Each file contains at least one `[NEEDS AKHIL]` marker, and each marker is preceded within four lines by an HTML comment opening `<!--`
    - `case-timeshift.md` names at least two of CST, IST, BST and states what each is ambiguous between
    - `case-timeshift.md` contains a test count read from the repository this session, carried with a `[source:` marker
    - `case-hued.md` contains the literal string `design-first`
    - `grep -qiE 'beautiful|seamless|powerful|modern|cutting.edge'` exits 1 for both files
    - Neither file states a status or a date in prose; the D-45 badge carries currency instead
  </acceptance_criteria>
  <done>TimeShift and hued are drafted in the short form with a real problem, one code-visible decision, sourced hard facts and length-realistic gaps, and their heading structure is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Draft Momentum, replacing the feature-list voice</name>
  <read_first>
    - `/Users/akhilsaxena/Documents/Personal/Repositories/Momentum/README.md` — the 79-line file whose opening twenty-one-bullet feature list is the thing being replaced
    - `/Users/akhilsaxena/Documents/Personal/Repositories/Momentum/ARCHITECTURE.md` — the MVVM layer diagram, one candidate for the single decision
    - `/Users/akhilsaxena/Documents/Personal/Repositories/Momentum/docs/MIGRATION_PLAN.md` — a second candidate decision with a real stated consequence
    - `/Users/akhilsaxena/Documents/Personal/Repositories/Momentum/store-listing/` — the assets available for D-41
    - `.planning/phases/00-design-ideation/00-COPY/case-timeshift.md` (created in task 1 — match its heading sequence and its `[source:]` convention exactly)
    - `.planning/phases/00-design-ideation/00-COPY/one-liners.md` (the rewritten momentum one-liner and card, which this study must agree with)
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Copywriting Contract, rules 1 through 5
  </read_first>
  <files>.planning/phases/00-design-ideation/00-COPY/case-momentum.md</files>
  <action>
Create `00-COPY/case-momentum.md` with frontmatter (`project: momentum`, `tier: short`,
`status: first-pass`, `awaiting: akhil-edit`) and the same four H2 headings in the same order
as `case-timeshift.md` — the short template renders all three, so a structural divergence
here breaks it.

Momentum needs the most rewriting of the five. Its README opens with a twenty-one-bullet
feature list and its `resume.json` description is the six-noun run "Goal tracker with
adaptive daily targets, streaks, milestones, badges, home screen widgets, and cloud sync."
Copywriting rule 1 rules that out — a comma-separated run of six nouns is a changelog, not a
sentence. Replace the voice; do not lightly edit it.

Under `## Problem`, write what Momentum is FOR in plain language, at paragraph length. This
must be a problem statement, not a capability list. The feature surface is fully derivable
from the README, but the framing is yours to compress: describe the problem the adaptive
daily target exists to solve rather than recording that the feature exists.

Under `## Decision`, take exactly one architectural decision visible in the code. The MVVM
layer split in `ARCHITECTURE.md` and the migration recorded in `docs/MIGRATION_PLAN.md` are
both real and both carry a stated consequence; pick the one whose consequence is more
concrete and state what it cost. Mark it `[source: <file>]`. Do not manufacture a second or
third decision to match the long form — 396 commits record no rationale, and inventing one in
front of engineers is worse than an admitted gap.

Under `## Outcome`, open with an HTML comment recording that the commit history carries no
rationale, that there is no retrospective and no analytics in the repository, and that
motivation and outcome are therefore unrecoverable from it. Then a `[NEEDS AKHIL]` marker
followed by at least forty words of placeholder prose at finished length, with every guessed
value in angle brackets.

Under `## Assets`, name the hero image and at most one inline screenshot from
`store-listing/`, with the D-42 R2 `assets/` path and the photo-pipeline exclusion recorded,
matching the wording used in task 1.

Finally, cross-check consistency: the `## Problem` opening must agree with the momentum
one-liner and card description written in plan 02. If they disagree, the one-liner is
canonical and this study follows it — the one-liner has a measured character budget and the
Home Act-2 grid is laid out against it.
  </action>
  <verify>
    <automated>node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs && diff <(grep -o '^## .*' .planning/phases/00-design-ideation/00-COPY/case-momentum.md) <(grep -o '^## .*' .planning/phases/00-design-ideation/00-COPY/case-timeshift.md) && diff <(grep -o '^## .*' .planning/phases/00-design-ideation/00-COPY/case-momentum.md) <(grep -o '^## .*' .planning/phases/00-design-ideation/00-COPY/case-hued.md)</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs` exits 0
    - Both `diff` commands in the verify block exit 0 — all three short studies share one heading sequence
    - `grep -q 'streaks, milestones, badges' .planning/phases/00-design-ideation/00-COPY/case-momentum.md` exits 1 — the six-noun run is gone
    - The `## Problem` section contains no Markdown list item at all (`sed -n '/## Problem/,/## Decision/p' | grep -c '^[-*] '` prints `0`)
    - The file contains exactly one `## Decision` heading and at least two `[source:` markers
    - At least one `[NEEDS AKHIL]` marker exists, preceded within four lines by an HTML comment
    - `grep -qiE 'beautiful|seamless|powerful|modern|cutting.edge'` exits 1
    - The `## Problem` opening sentence is consistent with the momentum `- one-liner:` payload in `00-COPY/one-liners.md` — assert at least one content noun of four or more characters is shared between them
  </acceptance_criteria>
  <done>Momentum is drafted as a problem-first narrative with exactly one sourced decision, its feature-list voice removed, and its structure identical to the other two short studies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling repositories → publicly published prose | Facts cross from local checkouts into text engineers will read with the repository open beside it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-07 | Information Disclosure | private repository contents quoted into public copy | mitigate | Only material a reader could already find in a public README, store listing or shipped app may be stated as fact. Nothing from a privacy-policy draft, an unreleased migration plan's internal detail, an endpoint or a credential enters the prose. The `[source: <file>]` marker on every claim makes provenance auditable at review. |
| T-00-08 | Repudiation (reputational) | invented or stale facts in published copy | mitigate | Every number must be read out of a file this session and carried with its source marker. `[NEEDS AKHIL]` plus angle-bracket slots is the only sanctioned form for anything unknown, and `check-copy-length.mjs` fails a bare multi-digit number inside a gap block. |
| T-00-14 | Tampering | motivation or outcome invented as fact | mitigate | These three repositories are precisely the ones that do NOT record why. Both tasks forbid manufacturing decisions to fill the long-form shape, cap the section at one code-visible decision, and require an HTML comment recording what was searched above every gap marker. |
| — | Spoofing | — | not applicable | No authentication or identity surface exists in this plan. |
| — | Denial of Service | — | not applicable | Three Markdown files; the only execution is a bounded local file walk by the checker. |
| — | Elevation of Privilege | — | not applicable | No privilege boundary is crossed; no package installed, no platform resource touched. |
| T-00-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no package. |
</threat_model>

<verification>
- `node .planning/phases/00-design-ideation/scripts/check-copy-length.mjs` exits 0
- All three files share one byte-identical H2 heading sequence, verified by two `diff` calls
- Momentum's six-noun run is absent and its Problem section contains no list items
- Every factual claim carries a `[source:` marker; every gap carries an HTML comment and forty-plus words
</verification>

<success_criteria>
- Three short-form case studies drafted at finished length, structurally identical so one template renders all three
- Each carries a real problem statement and exactly one architectural decision visible in the code, with no invented rationale
- Momentum's feature-list voice is replaced rather than edited, and agrees with its rewritten one-liner
- Every unknowable motivation or outcome is a length-realistic placeholder with an HTML comment recording what was searched
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-06-SUMMARY.md` when done.
Commit the three `00-COPY/case-*.md` files and the SUMMARY.
</output>
