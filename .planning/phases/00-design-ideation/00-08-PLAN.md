---
phase: 00-design-ideation
plan: 08
type: execute
wave: 4
depends_on: ["00-04", "00-07"]
files_modified:
  - .planning/phases/00-design-ideation/00-THEME-API.md
autonomous: true
requirements: [DSGN-05]

must_haves:
  truths:
    - "The charcoal theme's scope selector, its composition with :root.dark, and its font delivery are decided in writing with the arithmetic and the measurements that decided them"
    - "The boundary between what a brand theme owns and what the design system owns is written as an allowlist, so it cannot erode one token at a time"
    - "Phase 1 can implement DS-01 through DS-09 from this document without re-deriving anything"
    - "Every claim in the document is backed by a measurement taken in this phase, not by an assertion"
  artifacts:
    - path: ".planning/phases/00-design-ideation/00-THEME-API.md"
      provides: "The charcoal theme public API, decided in writing — DSGN-05's deliverable and Phase 1's direct input"
      contains: "## Cascade and scoping"
      min_lines: 220
  key_links:
    - from: ".planning/phases/00-design-ideation/00-THEME-API.md"
      to: ".planning/phases/00-design-ideation/00-FINDINGS.md"
      via: "gap IDs cited rather than restated"
      pattern: "G-1[12]|AAA-1"
    - from: ".planning/phases/00-design-ideation/00-THEME-API.md"
      to: "../design-system/src/tokens.test.ts"
      via: "the two invariant assertions handed over as test cases"
      pattern: "tokens.test.ts"
---

<objective>
Write the charcoal theme's public API as a decision document: how it scopes, how it composes
with `:root.dark`, how fonts are delivered, what a brand theme may and may not redefine, how
it is packaged and versioned, and what tests Phase 1 inherits.

Purpose: DSGN-05's wording is "decided in writing", and RESEARCH's Architectural
Responsibility Map is explicit that the written spec is the primary tier for token values and
contrast because numbers must be reviewable as text before Phase 1 implements them. This
document is Phase 1's direct input for DS-01 through DS-09, and it is the artefact that
survives the playground's deletion carrying every measurement plans 04 and 07 took.

Output: `00-THEME-API.md`, committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-RESEARCH.md
@.planning/phases/00-design-ideation/00-PATTERNS.md
@.planning/phases/00-design-ideation/00-FINDINGS.md

<structural_note>
Markdown only, written into `.planning/`. No build, no playground edit. It runs in wave 4 in
parallel with plan 09, which touches only `.playground/` files and `00-PUBLIC-DESIGN-NOTES.md`
— no overlap.

This document must be readable by a Phase 1 executor working in `../design-system` with NO
access to `.playground/`, because the playground is deleted at phase exit. Every number it
needs must be IN this document, not referenced by a path into a deleted directory.
</structural_note>

<interfaces>
Everything below was measured in plans 04 and 07 or verified during research. The document
records these; it does not re-derive them.

Specificity, verified: `:root` (0,1,0) · `.dark` (0,1,0) · `:root.dark` (0,2,0) ·
`:root[data-brand="charcoal"]` (0,2,0) · `:root[data-brand="charcoal"].dark` (0,3,0).
`tokens.css` declares its dark block as the selector list `:root.dark, .dark`, and each
selector in a list carries its own specificity, so charcoal light beats bare `.dark`
unconditionally and ties only with `:root.dark`.

Font packages: `@fontsource-variable/playfair-display@5.3.0` (`wght.css`, 4 subsets),
`@fontsource-variable/dm-sans@5.3.0` (`wght.css`, 2 subsets), `@fontsource/ibm-plex-mono@5.3.0`
(static only, per-subset `latin-400.css` / `latin-500.css`).
`@fontsource-variable/ibm-plex-mono` returns npm 404 and DOES NOT EXIST — Fontsource mirrors
Google Fonts and Google has not onboarded the variable IBM Plex family. `@ibm/plex-mono@2.5.0`
ships static split faces only.

Current `exports` map has no `themes` and no `fonts` entry. `package.json` `files` is
`["dist", "README.md", "LICENSE"]`, so new CSS must land in `dist/`.
`scripts/postbuild.mjs` hard-codes `for (const css of ["tokens.css", "primitives.css",
"utilities.css"])` and throws on a copy failure by design — Phase 1 extends that loop.
`src/packaging.test.ts` already asserts every path in `exports` exists in dist, including
wildcard patterns, so the new entries get coverage for free.

Measured this phase: 8 `@font-face` rules from the split against the design system's current
73; the 73-rule state emits 128 font files totalling 2.36 MB and inflates `tokens.css` from
14,948 bytes on disk to 65,493 bytes bundled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the cascade, token contract and ownership-boundary sections</name>
  <read_first>
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Color in full — both token tables with every value and every three-surface ratio, Rules C-1 through C-6, the exhaustiveness invariant, and the token-ownership allowlist
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Typography — the family stacks, the four weights, the scale ruling and G-11
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §Measured Baseline Claim 3 and §"Charcoal Theme API Mechanics" → "Specificity arithmetic" and "The token-ownership boundary"
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-27, D-28, D-31, D-32, D-46, D-47
    - `.planning/phases/00-design-ideation/00-FINDINGS.md` — the measured evidence for AAA-1, G-11 and the cascade probe result
    - `.playground/src/styles/theme-charcoal.css` — the prototype as built, so the document and the running CSS agree
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/src/tokens.css` lines 278-292 — the exact scope definition charcoal must outrank
  </read_first>
  <files>.planning/phases/00-design-ideation/00-THEME-API.md</files>
  <action>
Create `00-THEME-API.md` with YAML frontmatter (`phase: 0`, `slug: design-ideation`,
`consumers: [phase-1, phase-06.1]`, `status: decided`) and, in this order, the H2 headings
this task owns: `## Cascade and scoping`, `## The exhaustiveness invariant`,
`## Token contract`, `## Ownership boundary`.

Under `## Cascade and scoping`, state D-27's decision — `:root[data-brand="charcoal"]` for
light and `:root[data-brand="charcoal"].dark` for dark — and give the specificity table that
justifies it, showing that (0,3,0) outranks `:root.dark`'s (0,2,0) by arithmetic rather than
by source order. Record the reasoning note honestly: the deciding factor was keeping a global
cascade migration out of a release that already carries the font split and the contrast
fixes, so a visual regression stays attributable — not consumer-breakage risk, since both
consumers are Akhil's. Record D-28: cascade layers happen, but as their own design-system
release after Phase 1, verified independently against the existing Playwright snapshots, and
carrying the `data-density` axis with them. Cite Phase 06.1 as the owner.

Under `## The exhaustiveness invariant`, state the rule as a testable sentence:
`:root[data-brand="charcoal"].dark` MUST declare every custom property that
`:root[data-brand="charcoal"]` declares — same count, same names, no exceptions, including
tokens whose value is identical in both modes and tokens that are pure aliases. Then give the
measurement that makes it load-bearing rather than stylistic: a token declared in the light
block and not restated in the dark block ties `:root.dark` at (0,2,0), so the winner is
decided by whichever stylesheet the bundler emitted last; measured across deliberately
constructed import orders, charcoal-after-tokens leaves the LIGHT value applied in dark mode
while charcoal-before-tokens lets `:root.dark` win and drops charcoal entirely. Explain why
light mode never breaks — (0,2,0) always beats `:root` (0,1,0) — which is exactly why this
class of bug ships unnoticed. State the consequence: with the invariant held the cascade is
order-independent BY CONSTRUCTION, with no dependence on `@layer` and none on the D-33
manifest's import order. Record that plan 07's probe confirmed this across four import
orders, two modes and both `inlineStylesheets` settings.

Under `## Token contract`, reproduce both token tables IN FULL — every token name, its light
value, its dark value, and its measured ratio against page, paper and panel in its own mode.
This is the section Phase 1 implements DS-01 through DS-03 from, so it must be complete
enough to work from without the playground. Include the six colour rules as numbered rules
with their evidence: ochre is a fill and `--ochre-d` is text (because `#B0722A` measures 4.20
on a raised card and 3.91 on an inset panel in dark, so the "dark mode is already clean"
claim does not survive contact with elevation); filled-accent ink flips between the two
ochres; `--wire` and `--rule` are different tokens with different jobs, with the rule stated
as "if the border is the only thing telling you where a control starts it is `--wire`";
muted text goes to AAA at `#4F4C42` / `#B1AEA8`, and PROJECT.md's proposed `~#6E6A5E` clears
neither bar (4.46 on an inset panel); dark elevation is surface plus hairline, never shadow,
with the three `--shadow-*` restatements; and the governing rule for the two accent text
tokens — `--ochre-d-strong` is for accent text at or below 22px that must clear 7:1,
`--ochre-d` is for everything else — plus its three prohibitions (never a fill, never the
focus ring, never display type).

State plainly, in this document, that PROJECT.md's contrast table is wrong in two places and
must not be trusted, and that the governing method rule going forward is that contrast is
measured against all three surfaces of a mode, never against the page alone — every ratio in
PROJECT.md was page-only, which is why both errors survived there.

Under `## Ownership boundary`, write D-31's boundary as an ALLOWLIST with two columns, MAY
redefine and MUST NOT redefine, taken from UI-SPEC's table. State the one non-obvious line
out loud because D-31 asks for precision: `--text-*` is sizing and therefore design-system
owned, while `--font-*` and `--weight-*` are identity and therefore theme-owned — sizes
express how big the system is, stacks and weights express who you are. Give the consequence:
a brand needing a step the scale lacks files it upstream as a new step available to every
brand, never as a theme-local override, which is why the 52px hole is G-11 and not a charcoal
token. Record D-32's corollary: because brand themes cannot own spacing, the admin's denser
layout must come from a design-system-level `data-density` axis and never from portfolio CSS
under an `.admin` selector, which would be exactly the workaround Core Value forbids.
  </action>
  <verify>
    <automated>test -f .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## Cascade and scoping' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## The exhaustiveness invariant' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## Token contract' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## Ownership boundary' .planning/phases/00-design-ideation/00-THEME-API.md</automated>
  </verify>
  <acceptance_criteria>
    - All four H2 headings present, spelled exactly
    - The specificity strings `(0,1,0)`, `(0,2,0)` and `(0,3,0)` all appear
    - Both selector strings appear verbatim: `:root[data-brand="charcoal"]` and `:root[data-brand="charcoal"].dark`
    - Every one of these token values appears: `#1A1815`, `#44403A`, `#4F4C42`, `#8D8779`, `#F4F1EA`, `#FBF9F4`, `#EDE9E0`, `#B0722A`, `#8C591F`, `#6B4417`, `#878173`, `#EAE7E0`, `#C9C5BC`, `#B1AEA8`, `#161616`, `#1E1E1D`, `#242423`, `#C6883A`, `#D4A66D`, `#727268`
    - Both failing dark ochre ratios appear: `4.20` and `3.91`
    - PROJECT.md's rejected muted value `#6E6A5E` appears together with the figure `4.46`
    - All six colour rules are present as numbered or labelled rules, and the three `--ochre-d-strong` prohibitions all appear
    - The ownership allowlist names `--space-`, `--text-`, `--lh-`, `--ls-`, `--ds-sidebar-w` on the MUST NOT side and `--font`, `--weight-`, `--radius-`, `--shadow-` on the MAY side
    - `grep -q 'G-11' .planning/phases/00-design-ideation/00-THEME-API.md` exits 0
  </acceptance_criteria>
  <done>The cascade decision, the exhaustiveness invariant with its measurement, the complete token contract with three-surface ratios, and the ownership allowlist are all written and self-contained.</done>
</task>

<task type="auto">
  <name>Task 2: Write the font delivery, packaging, no-flash and handoff sections</name>
  <read_first>
    - `.planning/phases/00-design-ideation/00-THEME-API.md` (created in task 1 — read current state before appending)
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §"Font delivery (D-29, D-30) — one correction and one real decision" and §"The `exports` map delta (D-35)"
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-29, D-30, D-33, D-34, D-35, D-36, D-37
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Typography — the `Variable`-suffix trap and the Option A resolution
    - `.planning/phases/00-design-ideation/00-FINDINGS.md` — the G-12 evidence, the manifest byte counts, and the 8-versus-73 font-rule baseline recorded by plans 04 and 07
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/scripts/postbuild.mjs` — the hard-coded CSS copy loop Phase 1 extends, and its throw-don't-warn comment
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/src/packaging.test.ts` — the existing exports-exist assertion
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/src/tokens.test.ts` lines 45-51 — the existing mirror assertion charcoal's invariant complements
    - `.playground/src/styles/fonts-charcoal.css` (as built in plan 04)
  </read_first>
  <files>.planning/phases/00-design-ideation/00-THEME-API.md</files>
  <action>
Append these H2 sections in order: `## Font delivery`, `## Packaging and exports`,
`## No-flash module`, `## Release and versioning`, `## CSS assembly`, `## Tests Phase 1
inherits`, `## Open items carried to Phase 1`.

Under `## Font delivery`, record D-29's split as decided: `theme-charcoal.css` carries tokens
only and `fonts-charcoal.css` carries the `@font-face` sets, so DS-04 becomes literally true
(`tokens.css` ends with zero `@font-face`) and the axis is fixed once for every present and
future theme. State the failure mode this buys: a missing font import fails LOUDLY at
integration rather than silently. Record why per-family subpaths were rejected — forgetting
one family is a silent single-face fallback, exactly the DS-05 failure mode being eliminated.

Correct D-30 in writing: `@fontsource-variable/ibm-plex-mono` does not exist and returns npm
404; Fontsource mirrors Google Fonts and the variable IBM Plex family has not been onboarded;
`@ibm/plex-mono@2.5.0` ships static split faces only. D-30's stated GOAL — collapse the
`@font-face` count — survives intact; its stated premise that all three families ship variable
does not. Two of three do.

Record the chosen shape as Option A with its reasoning: re-export the Fontsource CSS entry
points — `playfair-display/wght.css`, `dm-sans/wght.css`, `ibm-plex-mono/latin-400.css` and
`latin-500.css` — for 8 `@font-face` rules against the measured 73. Reword D-30's "latin
subset only" as "latin-only DOWNLOAD, guaranteed by `unicode-range`", since neither variable
package ships per-subset files and each non-latin rule carries a `unicode-range` the browser
never matches on this site's content. Name Option B (hand-authored rules, exactly 4, literally
latin-only) as the road not taken and state its cost: a Vite `url()` resolution question for
bare specifiers inside CSS, and a silent missing face whenever Fontsource renames a woff2 in a
minor bump. Also name Astro's own top-level `fonts` config as a second road not taken,
rejected because delivery must live in the design-system package so Cairn gets the same fix
and so the omission failure mode cannot recur per consumer.

State the `Variable`-suffix rule as a hard requirement: Fontsource variable packages register
the family as `Playfair Display Variable` and `DM Sans Variable`, so a token naming only
`Playfair Display` while the `@font-face` declares the Variable name renders Georgia and the
page looks almost right. Both names must appear in the stack in that order, Variable first,
and the agreement must be asserted by a test. Record that D-29's loud-failure design catches a
MISSING import but NOT a name mismatch, which is why the font-name check exists separately.
Record the measured cost of the current state: 73 rules, 128 font files, 2.36 MB, and
`tokens.css` inflating from 14,948 bytes on disk to 65,493 bundled, none of it Playfair, DM
Sans or IBM Plex.

Under `## Packaging and exports`, give the current `exports` map and the two proposed
additions spelled `"./themes/*.css"` and `"./fonts/*.css"`, and state that the `.css` in the
wildcard is deliberate: it makes `@akhil-saxena/design-system/themes/charcoal.css` — the exact
D-35 string — resolve, and avoids the double-extension trap the existing `./css/*` entry has.
Record that plan 07 proved this shape with a stub package in a real Astro build. Give the two
mechanical consequences Phase 1 must not skip: `files: ["dist", …]` means the new CSS must
land in `dist/`, and `scripts/postbuild.mjs` hard-codes its three-file copy loop and throws on
failure by design, so Phase 1 extends that loop; and `src/packaging.test.ts` already asserts
every `exports` path exists in dist including wildcards, so the new entries get coverage free.
Cite G-12 by ID for the extensionless per-component trap rather than restating it.

Under `## No-flash module`, record D-34: the design system ships a real, documented no-flash
entry point handling storage, system-preference fallback, `prefers-reduced-motion` on
transitions, and the brand attribute together. State the reasoning — the class-name contract
is the design system's, so leaving every consumer to reinvent the script is how consumers
drift out of sync with it.

Under `## Release and versioning`, record D-35 (theme ships as subpath exports in the SAME
package, so one version number covers components and theme and they cannot mismatch), D-36
(Phase 1 ships as v2.0.0, preserving the existing four families as `fonts/default.css`,
because removing `@font-face` from `tokens.css` is a definite break for Cairn, which gets its
fonts that way today — semver tells the truth and the migration becomes a one-line import
rather than a font hunt), and D-37 (charcoal gets full Playwright snapshot parity with the
default theme, every component, both modes, at the accepted cost of doubled snapshot count).

Under `## CSS assembly`, record D-33's hand-maintained manifest: one app-level CSS file
importing tokens, the charcoal theme and exactly the per-component sheets in use. State both
constraints it satisfies simultaneously — a single file preserves cascade order, and a
fraction of the whole sheet ships — and give the MEASURED numbers plans 04 and 07 produced
rather than D-33's estimate. State the accepted failure mode: a visibly unstyled component.
Note that automated manifest generation from component usage is deferred, with its scope owner
unsettled between the portfolio and the design system.

Under `## Tests Phase 1 inherits`, hand over both invariant assertions as `tokens.test.ts`
cases, stating that they catch DIFFERENT bugs and both must exist: the design system's
existing "declares a light value for every token the dark theme overrides" (which caught
`--rule-strong` shipping dark-only), and charcoal's new mirror, "restates in dark every token
declared in light". Add the tiered contrast assertions — 7:1 for `--ink-3`, `--ink-4` and
`--ochre-d-strong`; 4.5:1 for `--ink`, `--ink-2` and `--ochre-d`; 3:1 for `--wire` and
`--focus`; all against three surfaces per mode — and the font-family-to-`@font-face`
agreement assertion. State that the ratio helpers already exist in the design system's
`tokens.test.ts` and must be extended rather than reimplemented.

Under `## Open items carried to Phase 1`, list the findings register entries Phase 1 pulls —
those tagged `blocks-Phase-5` or `should-fix-in-Phase-1` — by ID only, pointing at
`00-FINDINGS.md` as the authority so the two documents cannot drift. State explicitly which
items are NOT Phase 1's: G-2 belongs to Phase 06.1, and G-1 and G-7 are `backlog` for Phase 1
while being `blocks-Phase-7`.
  </action>
  <verify>
    <automated>grep -qF '## Font delivery' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## Packaging and exports' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## No-flash module' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## Release and versioning' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## CSS assembly' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## Tests Phase 1 inherits' .planning/phases/00-design-ideation/00-THEME-API.md; grep -qF '## Open items carried to Phase 1' .planning/phases/00-design-ideation/00-THEME-API.md</automated>
  </verify>
  <acceptance_criteria>
    - All seven new H2 headings present, spelled exactly
    - The non-existent package `@fontsource-variable/ibm-plex-mono` is named together with `404`
    - The four Fontsource entry points appear: `playfair-display/wght.css`, `dm-sans/wght.css`, `ibm-plex-mono/latin-400.css`, `ibm-plex-mono/latin-500.css`
    - The figures `8`, `73`, `128`, `2.36` and `65,493` (or `65493`) all appear in the font-delivery section
    - Both proposed export strings appear verbatim: `"./themes/*.css"` and `"./fonts/*.css"`
    - `postbuild.mjs` and `packaging.test.ts` are both named as the files Phase 1 must touch
    - `v2.0.0` and `fonts/default.css` both appear in the release section
    - The three contrast tiers appear as `7:1`, `4.5:1` and `3:1`
    - `## Open items carried to Phase 1` cites `00-FINDINGS.md` as the authority and names G-2 as Phase 06.1's, and G-1 and G-7 as Phase 7's
    - `wc -l` of the whole file is at least 220
    - The document contains no path beginning `.playground/` presented as a place Phase 1 must read from — every number Phase 1 needs is inline (a mention of the playground as historical provenance is fine)
  </acceptance_criteria>
  <done>Font delivery, packaging, the no-flash module, release strategy, CSS assembly, the inherited tests and the Phase 1 scope boundary are all decided in writing and self-contained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this document → a published npm package | Every value written here becomes DS-01…DS-09's implementation target and ships to two consumers under one version number. A wrong value here is a wrong value in production for both the portfolio and Cairn. |
| this document → a breaking major release | The v2.0.0 decision removes `@font-face` from `tokens.css`, which is how Cairn gets its fonts today. Understating that break is how a consumer silently loses its typography. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-18 | Tampering | a wrong token value shipping to two consumers | mitigate | Every value and every ratio in the token contract was measured by `check-contrast.mjs` in plan 04 against all three surfaces of its mode, and the acceptance criteria assert each hex string is present so the document and the tested prototype cannot drift. The document also records that PROJECT.md's table is wrong in two places, so a future reader does not re-import the error. |
| T-00-12 | Tampering | typosquat exposure via a non-existent package name | mitigate | The document states in writing that `@fontsource-variable/ibm-plex-mono` returns npm 404 and does not exist. Without that correction a Phase 1 implementer following D-30 literally would attempt to install a name nobody currently owns — an open invitation to whoever registers it. |
| T-00-19 | Denial of Service (availability of a consumer's typography) | the v2.0.0 font break | mitigate | D-36 is recorded with its reason: removing `@font-face` from `tokens.css` is a definite break for Cairn, so the release is a major and the existing four families are preserved as `fonts/default.css`, making the consumer migration a one-line import rather than a font hunt. Semver telling the truth is the mitigation. |
| T-00-20 | Tampering | order-dependent theming reaching production | mitigate | The exhaustiveness invariant is written as a testable sentence and handed to Phase 1 as a `tokens.test.ts` case, alongside the design system's existing mirror assertion. Both are recorded as catching different bugs so neither is dropped as redundant. |
| — | Spoofing | — | not applicable | No authentication, session or identity surface is described or decided in this document. |
| — | Information Disclosure | — | not applicable | The document contains no secret, token, binding or credential. Every value in it is a colour, a font name, a byte count or a package path, all destined to be public in a published stylesheet. |
| — | Repudiation | — | not applicable | Single operator; no multi-actor action to attribute. |
| — | Elevation of Privilege | — | not applicable | No privilege boundary is crossed; this plan writes one Markdown file and installs nothing. |
| T-00-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no package. It records package decisions for Phase 1, which will re-run the legitimacy gate for anything it installs in `../design-system`. |
</threat_model>

<verification>
- All eleven H2 headings present in `00-THEME-API.md`
- Every charcoal token hex value from both modes appears in the token contract
- The npm-404 correction, the four Fontsource entry points and the two proposed export strings all appear
- `wc -l` at least 220, and no instruction sends a Phase 1 reader into the deleted playground for a number
</verification>

<success_criteria>
- DSGN-05 is satisfied in the terms it is written in: scope selector, composition with `:root.dark`, and font delivery are all decided IN WRITING, each with the arithmetic or measurement that decided it
- Phase 1 can implement DS-01 through DS-09 from this document alone, after `.playground/` no longer exists
- The brand-theme ownership boundary is an allowlist with the `--text-*` versus `--weight-*` line stated out loud, so it cannot erode one token at a time
- The Phase 1 scope boundary is expressed by citing findings IDs rather than restating them, so the two documents cannot drift apart
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-08-SUMMARY.md` when done.
Commit `00-THEME-API.md` and the SUMMARY.
</output>
