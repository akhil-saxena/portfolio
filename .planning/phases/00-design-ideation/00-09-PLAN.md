---
phase: 00-design-ideation
plan: 09
type: execute
wave: 4
depends_on: ["00-07"]
files_modified:
  - .playground/src/layouts/Public.astro
  - .playground/src/pages/index.astro
  - .playground/src/pages/work-recolour.astro
  - .playground/src/pages/work.astro
  - .playground/src/pages/photos.astro
  - .playground/src/pages/home-act2.astro
  - .playground/src/data
  - .playground/check-no-ivory.sh
  - .playground/dist
  - .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md
autonomous: true
requirements: [DSGN-03, DSGN-04]

must_haves:
  truths:
    - "Work and Photos exist as charcoal-dark sketches composed from real design-system components, carrying no ivory token value"
    - "The recolour and the restructure are separate artefacts, so a reviewer can attribute a dislike to one change or the other"
    - "The nine not-a-straight-swap resolutions are each visible in a sketch and recorded in a document that survives the playground"
    - "Every public sketch ships zero framework JavaScript"
    - "OQ-1 has an owner: Home's Act-2 grid holds five projects in a sketched layout rather than being improvised in Phase 5"
  artifacts:
    - path: ".playground/src/pages/work-recolour.astro"
      provides: "X-work-recolour — the handoff's ivory Work page, recoloured only"
    - path: ".playground/src/pages/work.astro"
      provides: "X-work — Work restructured into two bands with status badges and five projects"
    - path: ".playground/src/pages/photos.astro"
      provides: "X-photos — the 39-photo masonry on charcoal with anchor filters"
    - path: ".playground/src/pages/home-act2.astro"
      provides: "X-home-act2 — OQ-1 resolved, five projects in the Act-2 grid"
    - path: ".planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md"
      provides: "The nine ivory-to-charcoal resolutions and the OQ-1 decision, surviving deletion"
      contains: "## OQ-1"
  key_links:
    - from: ".playground/src/pages/photos.astro"
      to: "Chip / Link anchors"
      via: "category filter rendered as real anchors, never SegmentedControl"
      pattern: "href="
    - from: ".playground/src/pages/index.astro"
      to: "the X- artefacts"
      via: "contact-sheet index built from the first sketch, never retrofitted"
      pattern: "X-work|X-photos"
---

<objective>
Resolve Work and Photos onto the charcoal dark palette as running design-system sketches,
sketch the Home Act-2 grid so OQ-1 stops being ownerless, and start the contact sheet with
the first sketch rather than retrofitting it.

Purpose: DSGN-03's success criterion is that Work and Photos are resolved onto charcoal,
replacing the handoff's earlier ivory iteration. The handoff's two pages are 103 lines of
inline hardcoded hex with not one class name, so every substitution is mechanical AND
individually reviewable — but nine of them are not straight swaps, and mapping those
mechanically regresses the design while looking like progress. Separating the recolour from
the restructure is the same attributability argument that split cascade layers out of Phase 1.

Output: four public sketches, a contact sheet, an ivory guard, and the public design notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-RESEARCH.md
@.planning/phases/00-design-ideation/00-PATTERNS.md
@.planning/phases/00-design-ideation/00-COPY/one-liners.md

<structural_note>
Sketches live under `.playground/` and are gitignored and thrown away. The only committed
output of this plan is `00-PUBLIC-DESIGN-NOTES.md`. Screenshots are taken in plan 17, before
deletion.

The legacy React components referenced below are on the `legacy/nextjs-portfolio` branch and
are reachable ONLY via `git show legacy/nextjs-portfolio:<path>`. The handoff HTML files ARE
in the working tree at real paths under `design_handoff_portfolio/`.

Hydration budget (UI-SPEC): public sketches take ZERO `client:*` directives. Every hydrated
island currently costs about 177 KB gzip, and `check-no-js.sh` must stay green. Filters are
anchors, not `SegmentedControl` — which is a WAI-ARIA radiogroup with no anchor semantics AND
uses hooks, so it would force hydration.
</structural_note>

<interfaces>
Real data, verified 2026-08-17:
- `data/portfolio_images.json` — 39 photos; keys `id, title, category, tags, date, exif,
  urls, order, dimensions`; seven lowercase categories `abstract, architecture, nature,
  portraits, street, wildlife, product`. `urls.thumb` is a base64 LQIP; `dimensions` is
  present on every photo. `product-peppers` has NO exif; `architecture-redbuilding` has
  camera only.
- `data/resume.json` — five projects: `cairn`, `hued`, `momentum`, `timeshift`,
  `design-system`; `badges` is an array of objects, already carrying "Live" on Cairn.
  Experience: Brevo, PharmEasy, MAQ.
- `data/site_config.json` — `categoryColumns` keyed Title-case against the lowercase category
  values in the photo data. Do not "fix" this in a sketch; it is D-25's drift and Phase 3's
  migration.
- Photos load from `https://pub-2d90aedeebcf4142afe524930c3b6471.r2.dev/photos/...` — the
  playground's `public/` is empty, so the sketches use these live URLs.

Design-system components confirmed hook-free and therefore safe to render statically:
`Chip`, `Text`, `Heading`, `Card`, `Link`, `Divider`, `Eyebrow`, `Button`, `Badge`,
`Timeline`, `StatCard`, `AppBar`, `Footer`, `EmptyState`, `StatusPill`, `ProgressBar`.

Typography for public surfaces, from UI-SPEC: page header uses `--text-4xl` 44px pending
G-11 (do NOT hardcode 52px); Home name `--text-5xl` 60px at `--lh-tight`; one-liner and card
description are DM Sans at `--text-base` 13 and `--text-md` 15 respectively at
`--lh-relaxed`; eyebrows and metrics are IBM Plex Mono `--text-xs` 11 with `--ls-wide`;
badges and filter pills are `--text-2xs` 9.5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public layout, contact sheet, and X-work-recolour</name>
  <read_first>
    - `design_handoff_portfolio/Work.dc.html` — read in full; it is the verbatim ivory source, 103 lines, all inline styles with hardcoded hex and not one class name
    - `design_handoff_portfolio/README.md` — the handoff spec, including its stale "55 components" claim which must not be carried
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Ivory → Charcoal Resolution" — the straight-substitution table and the nine exceptions
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Review Convention — the contact sheet's four parts and the "what it proves, not what it shows" rule
    - `.planning/phases/00-design-ideation/00-RESEARCH.md` §Common Pitfalls, Pitfall 5 (the contact sheet is built from the first sketch, never retrofitted)
    - `.playground/src/styles/manifest.css` (from plan 07 — the public CSS entry point)
    - `git show legacy/nextjs-portfolio:src/app/layout.tsx` — the legacy shell, for nav and footer structure only
  </read_first>
  <files>.playground/src/layouts/Public.astro, .playground/src/data, .playground/src/pages/index.astro, .playground/src/pages/work-recolour.astro</files>
  <action>
Copy the four committed JSON files from `data/` into `.playground/src/data/` so the sketches
render against real content. Real data, never lorem — D-40's whole argument collapses if a
fixture is invented, and the known sharp edges in this data (the single `peekPosition`, the
missing EXIF, the category-case drift) are exactly what the sketches must expose.

Create `src/layouts/Public.astro`: sets `<html class="dark" data-brand="charcoal">`, imports
`src/styles/manifest.css` and nothing else, and composes the design system's `AppBar` for nav
and `Footer` for the footer — both hook-free, both static. No `client:*` directive anywhere.

Create `src/pages/index.astro` as the contact sheet, built NOW rather than retrofitted. This
plan delivers two of its four parts. Plan 12 makes Part 1 glob-driven so admin
screens self-register; plan 16 fills Parts 2 and 3 once all seven admin routes exist. Part 1 is the INDEX: every
artefact as a labelled link grouped by class, each with one line stating what it PROVES, not
what it shows. Part 4 is the MEASUREMENT READOUT: the latest numbers from the plan-01 and
plan-07 scripts — island chunk raw and gzip bytes, script-tag count, manifest CSS bytes, and
the exhaustiveness PASS or FAIL — printed inline so a reviewer never has to correlate two
artefacts. Leave clearly-labelled placeholders for Part 2 (coverage table) and Part 3 (phone
coverage), which plan 16 fills once every admin route has declared its STATES array.

Create `src/pages/work-recolour.astro` as artefact `X-work-recolour`: the handoff's Work page
with the STRUCTURE UNCHANGED and only the colours resolved. Keep the ivory layout, the
existing card grid, the "ALSO — ENGINEERING AT BREVO" tail and the four-project arrangement
exactly as the handoff has them. This artefact exists so that when the restructured version
lands in task 2, a reviewer who dislikes the result can tell which change caused it.

Apply the straight substitutions from UI-SPEC's table: `#F4F1EB` page becomes `--page-bg`;
`#26231E` ink becomes `--ink`; `#FFFEFB` card surface becomes `--cream-2`; `#8D8779` muted
becomes `--ink-3`; `#DDD6C8` control border becomes `--wire`; Newsreader becomes Playfair
Display, which the handoff's own type section already says to unify on.

Apply three of the nine exceptions here, because they live on this page:

- Exception 1, ochre as text: the Brevo metrics such as `+15% CONVERSION` at 11px mono and
  the italic serif cross-link at the page foot are small accent text and take
  `--ochre-d-strong`. The Work header's period, being display type, stays `--ochre-d`. Note
  that `#B0722A` measured 3.52:1 on ivory — it was already failing AA before the port, so
  mapping accent to accent regresses it twice over.
- Exception 2, the hued icon's ink: the handoff specifies a white italic `h` on `#B0722A` at
  3.97:1, which fails. On charcoal it takes `#161616` ink at 4.56:1. The other icon squares
  keep white, so the ink colour is PER-ICON, not global.
- Exception 3, card boundaries: on ivory the card is lighter than the page and the FILL draws
  the boundary; on charcoal dark the fill delta is about 1.08:1, so the BORDER becomes the
  sole boundary. Work's project cards therefore take `--wire`, not `--rule`. Mapping
  `#E6E0D2` to `--rule` mechanically produces cards that visually dissolve into the page.

Delete the `#C4BDAD` placeholder markers rather than recolouring them — the `[expand with
your details]` and `[your one-liner]` strings are replaced by the real copy from plan 02's
`00-COPY/one-liners.md`. Do not carry the handoff's "55 COMPONENTS"; the design system's
README says 80.

Use `--text-4xl` 44px for the page header and do NOT hardcode 52px. Render a second header at
52px beside it, labelled as the G-11 reference, so Phase 1 has evidence rather than an
assertion — and because Playfair has a larger x-height and heavier stems than Newsreader at
the same pixel size, so the swap is not size-neutral.
  </action>
  <verify>
    <automated>cd .playground; rm -rf node_modules/.vite dist; npx astro build; bash check-no-js.sh; test -f dist/work-recolour/index.html; test -f dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `cd .playground && npx astro build` exits 0 and `bash check-no-js.sh` exits 0
    - `test -f .playground/dist/work-recolour/index.html` and `test -f .playground/dist/index.html` both exit 0
    - `grep -c 'client:' .playground/src/pages/work-recolour.astro` prints `0` and the same for `Public.astro`
    - `grep -q 'X-work-recolour' .playground/src/pages/index.astro` exits 0, and its index line states what the artefact proves rather than what it shows
    - `.playground/src/data/` contains all four JSON files copied from `data/`
    - `grep -qiE '55 COMPONENTS|55 components' .playground/src/pages/work-recolour.astro` exits 1
    - `grep -qE '\[expand with your details\]|\[your one-liner\]' .playground/src/pages/work-recolour.astro` exits 1 — placeholder markers deleted, not recoloured
    - `grep -q '52' .playground/src/pages/work-recolour.astro` exits 0 only as the labelled G-11 reference header; the primary header uses `--text-4xl`
    - The contact sheet contains labelled placeholders naming Part 2 (coverage table) and Part 3 (phone coverage), each citing plan 16 as the plan that fills it
  </acceptance_criteria>
  <done>The public layout, the contact sheet's index and measurement readout, and the structure-unchanged recolour of Work all build with zero framework JavaScript.</done>
</task>

<task type="auto">
  <name>Task 2: X-work restructured, and X-home-act2 resolving OQ-1</name>
  <read_first>
    - `.playground/src/pages/work-recolour.astro` (created in task 1 — the baseline this diverges from)
    - `design_handoff_portfolio/Work.dc.html` lines 39-92 — the parts kept through the restructure
    - `design_handoff_portfolio/README.md` — the Act-2 specification: a 2x2 grid of project entries with gap 40px 56px, naming Design System, hued, Momentum and TimeShift, with Cairn absent
    - `design_handoff_portfolio/Akhil Saxena - Home.dc.html` — the approved Home treatment, for Act-2's surrounding context
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-38, D-43, D-44, D-45
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Ivory → Charcoal Resolution" exceptions 7, 8 and 9, and §"Flagged, not designed — OQ-1"
    - `.planning/phases/00-design-ideation/00-COPY/one-liners.md` — the five one-liners, five card descriptions and five badges
    - `git show legacy/nextjs-portfolio:src/components/ProjectCard.tsx` — the closest existing React rendering of this card, for prop shape only; the handoff is the visual authority
  </read_first>
  <files>.playground/src/pages/work.astro, .playground/src/pages/home-act2.astro, .playground/src/pages/index.astro</files>
  <action>
Create `src/pages/work.astro` as artefact `X-work`: the same page RESTRUCTURED. Three locked
decisions change the page independently of colour, and they must not be conflated with the
recolour:

- D-44 replaces the ivory 2x2 card grid plus "ALSO — ENGINEERING AT BREVO" tail with TWO
  BANDS: employment first, then projects. Brevo, PharmEasy and MAQ become a professional-
  experience band; the five own projects sit below. They are different kinds of evidence, read
  for different reasons.
- D-45 adds `Live` / `Maintained` / `Archived` status badges and removes date reliance, so a
  reader learns whether a project is real and current without the page ageing.
- D-38 makes it FIVE projects, not four, so the 2x2 grid is gone regardless.

Compose the employment band from the design system's `Timeline` or hairline `Divider` rows
plus a mono metric, and the projects band from a `Card` grid with `Badge` for D-45 status and
`Chip` for platform tags. Take the badge values and the card descriptions from plan 02's
`00-COPY/one-liners.md` — that file is canonical, and this sketch must not paraphrase it.

Apply exception 8: cap the Brevo band at 1080px to match Home while keeping the page at
1280px for the card grid. At 1280px a hairline row with a serif title left and a mono metric
right reads on a dark field as two unrelated things rather than one row. This is a DSGN-03
decision, not a token change, and belongs in the notes for review.

Create `src/pages/home-act2.astro` as artefact `X-home-act2`, resolving OQ-1. The handoff's
Act 2 is a 2x2 grid for four projects and omits Cairn entirely; D-38 locks five. OQ-1 is
flagged in UI-SPEC as having no Phase 0 owner despite Phase 0 being the design phase, and
UI-SPEC requires the planner to either scope one sketch or record an explicit Phase 5
deferral. THIS PLAN SCOPES THE SKETCH — leaving it implicit means Phase 5 improvises a layout
the handoff never specified.

Sketch the Act-2 grid holding all five projects, in charcoal dark, using the real one-liners
at their measured budget and the handoff's stated gap of 40px by 56px expressed as
`--space-10` and `--space-14`. Render exactly one layout resolution rather than a menu of
options, and state in the contact-sheet index line what it proves. Do not restyle the rest of
Home — Act 1, the name treatment and the peek gallery are PUB-01's in Phase 5 and are out of
DSGN-03's scope. This sketch resolves the five-into-four problem and nothing else.

Register `X-work` and `X-home-act2` in the contact sheet's Part 1 index, each with a one-line
statement of what it proves. `X-work-recolour` and `X-work` must sit adjacent in the index so
the attributability pairing is obvious to a reviewer.
  </action>
  <verify>
    <automated>cd .playground; rm -rf node_modules/.vite dist; npx astro build; bash check-no-js.sh; test -f dist/work/index.html; test -f dist/home-act2/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro build` exits 0 and `bash check-no-js.sh` exits 0
    - `test -f .playground/dist/work/index.html` and `test -f .playground/dist/home-act2/index.html` both exit 0
    - `grep -c 'client:' .playground/src/pages/work.astro` prints `0` and the same for `home-act2.astro`
    - All five project titles appear in `work.astro` and in `home-act2.astro`: Cairn, hued, Momentum, TimeShift, Design System
    - All three employers appear in `work.astro`: Brevo, PharmEasy, MAQ
    - `grep -qE 'Live|Maintained|Archived' .playground/src/pages/work.astro` exits 0 — D-45 badges present
    - `grep -q 'ALSO' .playground/src/pages/work.astro` exits 1 — the tail is gone; the band is first
    - `grep -q '1080' .playground/src/pages/work.astro` exits 0 — the Brevo band is capped per exception 8
    - `.playground/src/pages/index.astro` lists `X-work-recolour` and `X-work` on adjacent index lines, and lists `X-home-act2`
    - The card descriptions in `work.astro` match the `- card:` payloads in `00-COPY/one-liners.md` character for character
  </acceptance_criteria>
  <done>Work exists as two attributable artefacts — recoloured and restructured — and Home's Act-2 grid holds five projects in a sketched resolution rather than an open question.</done>
</task>

<task type="auto">
  <name>Task 3: X-photos, the ivory guard, and the public design notes</name>
  <read_first>
    - `design_handoff_portfolio/Photos.dc.html` — read in full; the visual authority for the masonry, the filter pills and the counter
    - `git show legacy/nextjs-portfolio:src/components/MasonryGrid.tsx` — the LQIP and responsive-source pattern, including the `sizes` string and the base64 background placeholder
    - `git show legacy/nextjs-portfolio:src/styles/photography.css` lines 88-110 — the column mechanics
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Ivory → Charcoal Resolution" exceptions 5 and 6, and the automated ivory guard command
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Component Mapping, the public surfaces table — including G-9 and G-10
    - `.planning/phases/00-design-ideation/00-FINDINGS.md` — the G-9, G-10 and G-14 rows, for the evidence cells
    - `data/portfolio_images.json` — the 39 photos, their categories, dimensions and LQIP thumbs
    - `.playground/src/pages/index.astro` (current state, from tasks 1 and 2)
  </read_first>
  <files>.playground/src/pages/photos.astro, .playground/check-no-ivory.sh, .playground/src/pages/index.astro, .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md</files>
  <action>
Create `src/pages/photos.astro` as artefact `X-photos`: all 39 photos in a three-column
masonry on charcoal dark, using the real R2 URLs, real `dimensions` for aspect ratio, and the
real base64 LQIP from `urls.thumb`.

Render the category filter as `Chip` or `Link` ANCHORS with real `href` attributes and
`aria-current` on the active one — never `SegmentedControl`. That component is a WAI-ARIA
radiogroup with state-driven selection and no navigable anchor semantics at all, and it uses
hooks, so it would force hydration. This is G-9, and the sketch is its evidence.

Apply exception 5: on a near-white field every photo has an implicit boundary, but on
`#161616` dark-toned photographs bleed into the page — and this gallery skews dark, with
architecture at night, `intothemist` and wildlife. Give photo tiles an inset one-pixel
`--rule` ring in DARK ONLY, none in light. Second consequence, and it is a Phase 5 build note
that originates here: the LQIP blur-up placeholder must sit on the charcoal page colour, not
white, because a white-backed placeholder flashes hard on `#161616`.

Apply exception 6: on ivory the active filter pill is filled DARK, but its semantic is not
"dark" — it is maximum contrast against the field. On charcoal dark that becomes filled
LIGHT: `--ink` background with `--page-bg` text. Mapping the literal colours produces an
active pill invisible against the page. Inactive pills keep an outline, and that outline is
`--wire`, not `--rule`, because a filter pill is an interactive control whose border is its
only boundary under WCAG 1.4.11. Pill labels are 9.5px accent text and take
`--ochre-d-strong` where they are accented at all. The accent-reserved list includes the
active pill in LIGHT MODE ONLY; in dark it is the ink ramp.

Keep the masonry as app layout CSS — `column-count: 3` with a 16px column gap, break-inside
avoid, 10px radius and the 0.6s scale hover, all of which port unchanged. That is G-10,
explicitly accepted, because QUAL-03 permits layout CSS.

Write `check-no-ivory.sh` implementing UI-SPEC's automated guard: grep the sketch sources for
the ivory token values `#F4F1EB`, `#FFFEFB`, `#E6E0D2`, `#8D8779`, `#DDD6C8`, `#C4BDAD` and
`#26231E`, and exit non-zero if any is found. It proves the ABSENCE of ivory, not the presence
of good — rules 1 through 9 are reviewed by eye in plan 11, and the script must say so in its
own output so nobody mistakes a pass for a design review.

Create `00-PUBLIC-DESIGN-NOTES.md` with YAML frontmatter and these exact H2 headings:
`## Straight substitutions`, `## The nine resolutions`, `## OQ-1`, `## Open for review`.
Record the substitution table; record each of the nine not-a-straight-swap resolutions with
the artefact that demonstrates it and the measured ratio that forced it; record the OQ-1
decision — that Phase 0 scoped one Act-2 sketch rather than deferring to Phase 5, what the
sketch resolved, and that the rest of Home remains PUB-01's in Phase 5; and record what is
open for human review in plan 11, specifically the 44px-versus-52px header judgement, the
1080px Brevo band cap, and the 22px cross-link's `--ochre-d-strong` treatment with its stated
fallback of raising the link to 24px and reverting to `--ochre-d`.

Append the G-9, G-10 and G-14 evidence to the contact sheet's index lines for `X-photos` so a
reviewer sees the finding beside the artefact that produced it.
  </action>
  <verify>
    <automated>cd .playground; rm -rf node_modules/.vite dist; npx astro build; bash check-no-js.sh; bash check-no-ivory.sh; test -f dist/photos/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro build` exits 0, `bash check-no-js.sh` exits 0, and `bash check-no-ivory.sh` exits 0
    - `test -f .playground/dist/photos/index.html` exits 0
    - `grep -c 'client:' .playground/src/pages/photos.astro` prints `0`
    - `grep -c 'SegmentedControl' .playground/src/pages/photos.astro` prints `0` and `grep -c 'href=' .playground/src/pages/photos.astro` prints at least `7` — one anchor per category
    - `grep -c 'aria-current' .playground/src/pages/photos.astro` prints at least `1`
    - The built `dist/photos/index.html` contains at least 39 `<img` occurrences
    - `grep -q 'column-count' .playground/src/pages/photos.astro` exits 0
    - Negative control: adding the literal `#8D8779` to any file under `.playground/src` makes `check-no-ivory.sh` exit non-zero and name the file; removing it restores exit 0
    - `00-PUBLIC-DESIGN-NOTES.md` contains all four H2 headings and nine numbered resolutions
    - `00-PUBLIC-DESIGN-NOTES.md` `## OQ-1` states the decision taken and names Phase 5 as the owner of the rest of Home
  </acceptance_criteria>
  <done>Photos renders 39 real photos on charcoal with anchor-based filters and zero JavaScript, the ivory guard is green and bites when tested, and the nine resolutions plus the OQ-1 decision are recorded in a committed document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pub-*.r2.dev` public origin → sketch pages | Remote images are loaded into locally-built pages. The origin is already the production photo host and the URLs are already public. |
| committed JSON content → sketch fixtures | Real site content is copied into a gitignored directory; nothing flows back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-21 | Information Disclosure | fixtures leaking non-public content into sketches | mitigate | Sketches use only `data/*.json`, which is already committed public site content, and the already-public `pub-*.r2.dev` photo URLs. No secret is needed by any Phase 0 artefact, so none can be embedded. The playground is gitignored, so even the copied fixtures never enter history. |
| T-00-22 | Tampering | an accessibility regression designed in | mitigate | Three measured failures from the handoff are corrected by name rather than carried: the hued icon's white ink at 3.97:1, ochre-as-small-text at 3.52:1 on ivory, and the ~1.4:1 single border colour used as an interactive control boundary against WCAG 1.4.11's 3:1. `check-no-ivory.sh` asserts the ivory values are gone; the eye review in plan 11 covers what a grep cannot. |
| T-00-23 | Denial of Service (performance, self-inflicted) | a hydrated public sketch | mitigate | Every hydrated island currently costs about 177 KB gzip because DS-09 tree-shaking fails. Public sketches take zero `client:*` directives, asserted per file, and `check-no-js.sh` gates the build. A sketch that hydrates would also make the DS-09 measurement unreadable. |
| T-00-04 | Information Disclosure | accidental commit of the playground | mitigate | `.playground/` is gitignored (asserted in plan 01). Only `00-PUBLIC-DESIGN-NOTES.md` is committed from this plan. |
| — | Spoofing | — | not applicable | These are public marketing surfaces with no authentication, no session and no credential. Nothing here reads or writes an identity. |
| — | Repudiation | — | not applicable | No multi-actor action; the sketches are static output. |
| — | Elevation of Privilege | — | not applicable | No privilege boundary exists on a public static page. No adapter, api route or binding is introduced. |
| — | Input Validation (ASVS V5) | — | not applicable | No user input is accepted. All content is build-time JSON authored in this repository. |
| T-00-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no package; it composes components already installed in plan 01. |
</threat_model>

<verification>
- `npx astro build` exits 0; `bash check-no-js.sh` and `bash check-no-ivory.sh` both exit 0
- Four public sketch routes emit HTML: `work-recolour`, `work`, `photos`, `home-act2`
- Zero `client:` directives across every public sketch and the public layout
- `00-PUBLIC-DESIGN-NOTES.md` records all nine resolutions and the OQ-1 decision
</verification>

<success_criteria>
- Work and Photos are resolved onto charcoal dark as running design-system sketches carrying no ivory token value
- Recolour and restructure are separate artefacts, so a review dislike is attributable to one change
- All nine not-a-straight-swap resolutions are demonstrated in a sketch and recorded in a document that survives the playground's deletion
- OQ-1 has an owner and a resolution rather than being left for Phase 5 to improvise
- Every public sketch ships zero framework JavaScript, keeping the DS-09 measurement readable and PUB-14 reachable
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-09-SUMMARY.md` when done.
Commit `00-PUBLIC-DESIGN-NOTES.md` and the SUMMARY. The playground stays uncommitted.
</output>
