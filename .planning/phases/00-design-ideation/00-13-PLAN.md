---
phase: 00-design-ideation
plan: 13
type: execute
wave: 8
depends_on: ["00-12"]
files_modified:
  - .playground/src/fixtures/photos.json
  - .playground/src/pages/admin/photos.astro
  - .playground/src/components/SortableReorder.tsx
  - .playground/dist
autonomous: true
requirements: [DSGN-01, DSGN-04]

must_haves:
  truths:
    - "The photos screen renders all 39 real photos at compact density, so row height is judged against real titles rather than against lorem"
    - "Two empty states exist and say different things — no photos at all, versus no photos in this category"
    - "Photo reorder is sketched as a running interaction, because whether the design system's keyboard fallback is adequate is the question being answered"
    - "The global-plus-per-category ordering model D-22 specifies is visible on screen, not just described"
  artifacts:
    - path: ".playground/src/pages/admin/photos.astro"
      provides: "S-photos, E-photos, E-category-filtered, T-loading-list"
      contains: "export const STATES"
    - path: ".playground/src/components/SortableReorder.tsx"
      provides: "The Sortable island — D-22 reorder, and G-13's keyboard-announcement evidence"
      contains: "onReorder"
    - path: ".playground/src/fixtures/photos.json"
      provides: "The 39 real photos plus the empty, filtered-empty, loading, error, dirty, conflict and success variants"
  key_links:
    - from: ".playground/src/pages/admin/photos.astro"
      to: ".playground/src/components/SortableReorder.tsx"
      via: "client:load — one of the three sanctioned islands"
      pattern: "client:load"
    - from: ".playground/src/pages/admin/photos.astro"
      to: ".playground/src/lib/artefacts.mjs"
      via: "STATES refs validated against CANONICAL_IDS"
      pattern: "artefacts"
---

<objective>
Sketch `/admin/photos` against all 39 real photos, with both of its genuinely distinct empty
states, and build the reorder island that answers D-22's open question about the design
system's keyboard fallback.

Purpose: photos is the densest admin screen and the one carrying the most unresolved
decisions — D-22's two-axis ordering, D-12's asynchronous publish, D-15's per-tile pipeline
state, D-25's category drift. It is also the screen where compact density either works or does
not: 39 rows at 32px is where a 2px error becomes visible. Two of the three permitted
hydrating islands are here and in plan 14 precisely because the *interaction* is what is under
review, and one of them exists to file a finding rather than to prove a success.

Output: the photos screen with four artefacts, the `Sortable` island, and the measured answer
to whether `Sortable`'s keyboard path announces anything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-PATTERNS.md
@.planning/phases/00-design-ideation/00-ADMIN-IA.md

<structural_note>
Sketch only. Everything under `.playground/` is gitignored and deleted in plan 17; this plan
commits its SUMMARY alone. Admin gap evidence is appended to `00-FINDINGS.md` in plan 16 in one
pass — record findings in the SUMMARY here and let plan 16 transcribe them, so `00-FINDINGS.md`
has exactly one writer among plans 12 to 16.

**Scope guard (D-02).** No upload endpoint, no R2 client, no `workflow_dispatch`, no D1. The
pipeline states are depicted from fixtures. `FileInput` is composed as a dropzone that accepts
a file and does nothing with it; wiring it to anything would be building Phase 7.

`?state=` is served by `astro dev`, not by `astro build` — see plan 12's structural note.
`astro build` emits the `populated` state, which is what the bundle and CSS gates measure.

Photo binaries are NOT in the playground. Use the live R2 URLs already in
`data/portfolio_images.json` under `https://pub-2d90aedeebcf4142afe524930c3b6471.r2.dev/`,
exactly as plan 09's public sketches do.
</structural_note>

<interfaces>
`DataGrid`'s relevant props (`../design-system/dist/index.d.ts:3178-3197`) — it already carries
the loading state D-03 needs, and its own docstring explains why:

```ts
interface DataGridProps extends React.HTMLAttributes<HTMLDivElement> {
  columns: DataGridColumn[];
  rows: DataGridRow[];
  page?: number; totalPages?: number; onPageChange?: (page: number) => void;
  onSelectionChange?: (ids: Array<string | number>) => void;
  /**
   * Show a loading row instead of the body while rows are being fetched. An
   * empty grid and a grid that has not loaded look identical otherwise, so the
   * user cannot tell "no matches" from "not yet".
   */
  loading?: boolean;
  loadingText?: string;   // @default "Loading…"
}
```

`Sortable` / `SortableDndContext` / `SortableItem` with `onReorder` are confirmed exports
(`dist/index.d.ts:3515-3550`). Measured limitation: the component wires dnd-kit's
`KeyboardSensor` so items *move* by keyboard, but passes no `announcements` and no
`screenReaderInstructions`, so nothing is announced. That is **G-13**, already registered, and
this island is its evidence — do not route around it by adding a local announcer.

Real data, verified 2026-08-17 in `data/portfolio_images.json`: 39 photos; keys `id, title,
category, tags, date, exif, urls, order, dimensions`; seven lowercase categories `abstract,
architecture, nature, portraits, street, wildlife, product`. `urls.thumb` is a base64 LQIP.
`dimensions` is present on every photo. `product-peppers` has **no** `exif` at all;
`architecture-redbuilding` has camera only. `data/site_config.json` keys `categoryColumns`
Title-case (`Architecture`) against the lowercase category values in the photo data — that is
D-25's drift and Phase 3's migration; surface it, do not fix it in a sketch.

The legacy field catalog for this screen is
`git show legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx` lines 335-418,
including the six-field EXIF sub-form at lines 382-409.

Compact density targets: 30px controls, 32px rows, 208px sidebar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The photos screen, both empty states, and the list loading treatment</name>
  <read_first>
    - `git show legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx` lines 335-418 — the verbatim field catalog for a photo, including the EXIF sub-form at 382-409 and the `c.charAt(0).toUpperCase()` render-time Title-casing that IS the D-25 drift
    - `git show legacy/nextjs-portfolio:src/components/admin/PhotoUploadZone.tsx` — the upload affordance being replaced by `FileInput variant="dropzone"`
    - `data/portfolio_images.json` — all 39 photos; note `product-peppers` has no `exif` and `architecture-redbuilding` has camera only
    - `data/site_config.json` — `categoryColumns` with Title-case keys
    - `.planning/phases/00-design-ideation/00-ADMIN-IA.md` §"Field catalog" — the photo row and the reconciled field names
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Component Mapping, the `/admin/photos` row — `DataGrid` · `Sortable` · `FileInput variant="dropzone"` · `Chip` · `Badge` · `EmptyState`
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Contract table" — the verbatim empty-photos and empty-filtered-category copy, and the pipeline-failure copy
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-03, D-12, D-13, D-15, D-22, D-25
    - `.playground/src/layouts/Admin.astro` and `.playground/src/lib/artefacts.mjs` (from plan 12)
    - `.playground/src/pages/admin/index.astro` (from plan 12 — copy its `SCREEN` / `STATES` shape exactly)
  </read_first>
  <files>.playground/src/fixtures/photos.json, .playground/src/pages/admin/photos.astro</files>
  <action>
Create `src/fixtures/photos.json` keyed by state. `populated` holds all 39 real photos copied
from `data/portfolio_images.json` — all 39, not a sample. Row height at compact density is the
thing being judged, and a ten-row sketch cannot show whether the longest real title wraps.
`empty` holds zero photos. `filtered-empty` holds the full 39 with an active category filter
that matches none. The remaining keys carry the loading, error, dirty, conflict and success
variants.

Create `src/pages/admin/photos.astro` as artefact `S-photos`, using the Admin layout and
exporting `SCREEN` and `STATES` in the same shape as the dashboard. Every `ref` must be a
member of `CANONICAL_IDS` from `src/lib/artefacts.mjs`.

Four artefacts come from this file:

- **`S-photos`** — populated. Compose `DataGrid` as the list view over all 39 photos, with
  `Chip` anchors for the seven category filters, `FileInput variant="dropzone"` for upload, and
  a `Badge` per row for D-15's pipeline state. State in the contact-sheet index line what this
  proves — that a 32px row stays legible with the longest real photo title — rather than what
  it shows.
- **`E-photos`** — empty. Copy verbatim from UI-SPEC's contract table: **No photos yet.** /
  *Upload a photo to start the gallery. Processing runs after you publish.* with an
  `Upload a photo` action. The second sentence is load-bearing: D-12 makes publish asynchronous
  for photos, and the empty state is the first place a reader learns that.
- **`E-category-filtered`** — filtered-empty. Copy verbatim: **No photos in Architecture.** /
  *Move a photo into this category, or delete the category on Site.* This is a genuinely
  different empty state from `E-photos` — one means "nothing exists", the other means "your
  filter excluded everything" — and conflating them is the exact failure `DataGrid`'s own
  `loading` docstring warns about one level up.
- **`T-loading-list`** — loading. Use `DataGrid`'s own `loading` prop rather than a bespoke
  skeleton, and put its docstring reasoning in a comment: an empty grid and a grid that has not
  loaded look identical otherwise, so the user cannot tell "no matches" from "not yet".

Render the per-photo detail form beside the grid using DS `Field` + `TextInput` + `Select`,
converted from the legacy bespoke `<label className="admin-field">` markup. Include the EXIF
sub-form, and make it show **omitted rows, never an em dash**: `product-peppers` has no EXIF at
all and `architecture-redbuilding` has camera only, so a sketch that renders `—` for every
missing field designs in a lie about the data. Put both photos in view.

Surface the D-25 drift rather than fixing it. The category `Select` reads lowercase values from
the photo data while `site_config.categoryColumns` is keyed Title-case, and the legacy code
bridged that with `c.charAt(0).toUpperCase()` at render time. Show the drift on screen — the
raw key beside the rendered label — and add a comment citing the legacy line as its origin.
D-25's canonical category records are edited on `/admin/site` in plan 15 and migrated in
Phase 3; this screen is where the cost of the drift is visible.

Add a comment recording that `Badge` carries the pipeline state because `StatusPill`'s `stage`
prop is a closed job-domain union that cannot express it — the same G-5 evidence the dashboard
records, now on a second surface, which is what makes the finding a pattern rather than an
anecdote.

Do not add a `client:` directive in this task. The island lands in task 2 and
`check-no-js.sh`'s allowlist tolerates `admin/photos` either way.
  </action>
  <verify>
    <automated>cd .playground && rm -rf node_modules/.vite dist && npx astro build && test -f dist/admin/photos/index.html && node check-states.mjs && [ "$(grep -o '<img' dist/admin/photos/index.html | wc -l | tr -d ' ')" -ge 39 ] && echo PHOTOS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd .playground && npx astro build` exits 0 and `test -f .playground/dist/admin/photos/index.html` exits 0
    - `node .playground/check-states.mjs` exits 0 and now covers `/admin/photos` as well as `/admin/`
    - The built `dist/admin/photos/index.html` contains at least 39 `<img` occurrences — all 39 real photos, not a sample
    - `photos.astro` exports `SCREEN` and `STATES`; `STATES` has 7 entries and every `ref` is a member of `CANONICAL_IDS`
    - `grep -q 'No photos yet.' .playground/src/pages/admin/photos.astro` exits 0 and the same file also contains `No photos in Architecture.` — the two empty states are distinct strings
    - `grep -q 'Processing runs after you publish' .playground/src/pages/admin/photos.astro` exits 0
    - `grep -q 'loading' .playground/src/pages/admin/photos.astro` exits 0 with `DataGrid`'s own `loading` prop used, and a comment quoting its "no matches versus not yet" reasoning is present
    - Both `product-peppers` and `architecture-redbuilding` appear in the rendered output, and `grep -c '—' dist/admin/photos/index.html` shows no em dash inside an EXIF row — missing fields are omitted rows
    - `grep -q 'charAt(0).toUpperCase' .playground/src/pages/admin/photos.astro` exits 0 inside a comment citing the legacy origin of the D-25 drift, and both `architecture` and `Architecture` appear in the file
    - `grep -q '<StatusPill' .playground/src/pages/admin/photos.astro` exits 1, and a comment naming G-5 is present
    - `grep -c 'client:' .playground/src/pages/admin/photos.astro` prints `0` at the end of this task
  </acceptance_criteria>
  <done>The photos screen renders all 39 real photos at compact density with two genuinely distinct empty states, the real EXIF holes visible as omissions, and the D-25 drift surfaced rather than papered over.</done>
</task>

<task type="auto">
  <name>Task 2: The Sortable reorder island and D-22's two ordering axes</name>
  <read_first>
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/dist/index.d.ts` lines 3515-3550 — the `Sortable` / `SortableDndContext` / `SortableItem` prop surface and the `onReorder` signature
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/src/interaction/Sortable/` — the source, specifically the dnd-kit sensor configuration and the absence of `announcements` / `screenReaderInstructions`
    - `git show legacy/nextjs-portfolio:src/components/admin/DraggableMasonry.tsx` — the legacy reorder grid, for the surrounding layout only; note it uses `@atlaskit/pragmatic-drag-and-drop` while the rest of the legacy admin used `@dnd-kit/*`, a duplication the port consolidates
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-22 — global ordering by default with a per-category override that wins when that filter is active, and the open question about the keyboard fallback
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Named gaps", the G-13 row
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Hydration budget" — this is one of exactly three sanctioned islands
    - `.playground/src/pages/admin/photos.astro` (from task 1)
    - `.playground/check-no-js.sh` (from plan 12 — confirm `admin/photos` is in the allowlist before adding the directive)
  </read_first>
  <files>.playground/src/components/SortableReorder.tsx, .playground/src/pages/admin/photos.astro</files>
  <action>
Create `src/components/SortableReorder.tsx` composing the design system's `Sortable`,
`SortableDndContext` and `SortableItem` with `onReorder`. It receives the photo list as a prop
and holds order in local state; there is no persistence and there must not be — D-10's draft
store is Phase 2, and a sketch that writes anywhere has left scope.

Sketch **both** of D-22's ordering axes so the model is visible rather than described. Global
order is the default. When a category filter is active, the per-category order wins for that
filter. Render a control that switches between the two and label plainly which order is being
edited, because the failure mode this design is guarding against is a user dragging a tile and
not knowing which of two orderings they just changed. Photos already carry an `order` field;
the second ordering field is new and is one of Phase 3's four migrations, so name it in a
comment rather than inventing a schema here.

Wire it into `photos.astro` as the grid view with `client:load`. This is one of exactly three
sanctioned hydrating islands, and the reason it is sanctioned is that the interaction is what
is under review. Add a comment on the directive stating that this island costs roughly 177 KB
gzip today because DS-09 tree-shaking fails, so its presence is a budgeted exception rather
than a default.

**Then answer the G-13 question by measurement, not by reading.** Drive the reorder with the
keyboard only — tab to a tile, activate, arrow, drop — and record in the SUMMARY exactly what
was and was not announced: whether any live region exists in the DOM, whether an item's new
position is spoken, and whether the drop is confirmed. `Sortable` wires dnd-kit's
`KeyboardSensor` so items move, but passes no `announcements` and no
`screenReaderInstructions`. If that holds, the finding is that the keyboard fallback moves
items silently, which is a different and more precise defect than "keyboard support is
missing". **Do not add a local announcer.** Routing around the gap destroys the evidence the
island exists to produce, and Core Value forbids the workaround.

Record the observation as G-13 evidence in the SUMMARY; plan 16 transcribes it into
`00-FINDINGS.md` with the rest of the admin evidence.
  </action>
  <verify>
    <automated>cd .playground && rm -rf node_modules/.vite dist && npx astro build && bash check-no-js.sh && node check-states.mjs && grep -q 'client:load' src/pages/admin/photos.astro && [ "$(grep -o '<script' dist/admin/photos/index.html | wc -l | tr -d ' ')" -ge 1 ] && echo ISLAND_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd .playground && npx astro build` exits 0, `bash check-no-js.sh` exits 0, `node check-states.mjs` exits 0
    - `dist/admin/photos/index.html` contains at least one `<script` tag — the sanctioned island is genuinely hydrating, which `check-no-js.sh`'s allowlist branch also asserts
    - `grep -c 'client:' .playground/src/pages/admin/photos.astro` prints exactly `1`, and it is `client:load` on `SortableReorder`
    - `grep -c 'client:' .playground/src/pages/admin/index.astro` still prints `0` — the budget was not spent twice
    - `SortableReorder.tsx` imports `Sortable`, `SortableDndContext` and `SortableItem` from `@akhil-saxena/design-system` and passes `onReorder`; `grep -qE 'aria-live|announcements|screenReaderInstructions' .playground/src/components/SortableReorder.tsx` exits 1 — no local announcer was added
    - `grep -qiE 'fetch\(|localStorage|sessionStorage' .playground/src/components/SortableReorder.tsx` exits 1 — no persistence, no endpoint
    - The component renders both ordering axes and labels which one is active; `grep -qi 'category' .playground/src/components/SortableReorder.tsx` exits 0
    - The SUMMARY records the keyboard-reorder observation as three explicit answers: whether a live region exists in the DOM, whether the new position is announced, and whether the drop is confirmed
    - `grep -q '177' .playground/src/pages/admin/photos.astro` exits 0 — the budgeted-exception comment cites the measured island cost
  </acceptance_criteria>
  <done>Photo reorder runs as one of the three sanctioned islands, both D-22 ordering axes are visible on screen, and G-13 has a measured observation rather than an inference — with the gap left intact as evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pub-*.r2.dev` public origin → sketch pages | 39 remote images are loaded into locally-built pages. The origin is already the production photo host and every URL is already public. |
| committed site content → gitignored fixtures | `data/portfolio_images.json` is copied into a throwaway directory. Nothing flows back. |
| the reorder sketch → Phase 7's implementation | A reorder interaction depicted as accessible when it is not would ship an inaccessible control three phases later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-31 | Tampering | an accessibility gap concealed by a local workaround | mitigate | `SortableReorder` is forbidden from adding `aria-live`, `announcements` or `screenReaderInstructions`, asserted by grep. The island exists to produce G-13 evidence; papering over the gap locally would both destroy the evidence and violate Core Value's rule that a gap is a finding, never a local component. |
| T-00-32 | Tampering | a sketched upload path implying an unvalidated ingest | mitigate | `FileInput` is composed as a dropzone that accepts a file and does nothing with it. No endpoint, no R2 client, no `workflow_dispatch`, no size or MIME handling is depicted, so Phase 7 cannot inherit a validation shape from this sketch. The legacy route's real gates (`MAX_BYTES`, magic-byte checks, 413/415) remain Phase 7's to build against `.planning/codebase/CONCERNS.md`. |
| T-00-29 | Information Disclosure | fixtures carrying non-public content | mitigate | Fixtures are built only from `data/portfolio_images.json` and `data/site_config.json`, both already committed public site content, plus already-public R2 URLs. No credential is needed by any artefact in this plan, so none can be embedded. The playground is gitignored. |
| T-00-23 | Denial of Service (performance) | an unbudgeted hydrated island | mitigate | Exactly one `client:` directive is permitted on this route and the count is asserted at exactly 1, with the dashboard asserted to remain at 0. `check-no-js.sh`'s allowlist fails both an unsanctioned island and a sanctioned one that stopped hydrating. |
| T-00-06 | Elevation of Privilege | the playground drifting into a Phase 7 foundation | mitigate | No persistence, no endpoint, no D1, no adapter, no auth. `fetch(`, `localStorage` and `sessionStorage` are all asserted absent from the island. Plan 17 deletes the directory. |
| T-00-04 | Information Disclosure | accidental commit of the playground | mitigate | `.playground/` is gitignored, asserted in plan 01. Only the SUMMARY is committed. |
| — | Spoofing | — | not applicable | No credential is accepted, stored or verified in this plan. |
| — | Repudiation | — | not applicable | Single-actor static sketches with no audit surface. |
| T-00-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no package; `Sortable` and dnd-kit arrive inside the design-system tarball already audited in plan 01. |
</threat_model>

<verification>
- `npx astro build` exits 0; `bash check-no-js.sh` exits 0; `node check-states.mjs` exits 0
- `dist/admin/photos/index.html` carries at least 39 images and at least one `<script`
- Exactly one `client:` directive on `/admin/photos` and still zero on `/admin`
- No local announcer and no persistence in the island, both asserted by grep
- The SUMMARY answers the G-13 keyboard question in three explicit parts

</verification>

<success_criteria>
- All 39 real photos render at compact density, so row height is judged against real content
- `E-photos` and `E-category-filtered` are distinct designed states with distinct copy, not one state reused
- The reorder island runs, shows both D-22 ordering axes, and leaves the G-13 gap intact as evidence
- The D-25 category drift and the real EXIF holes are visible on screen rather than smoothed away
- The hydration budget is spent exactly once here, with two sanctioned islands remaining for plan 14
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-13-SUMMARY.md` when done.
Commit the SUMMARY only. Record the G-13 keyboard observation and any new admin gap in the
SUMMARY; plan 16 transcribes all admin evidence into `00-FINDINGS.md` in one pass.
</output>
