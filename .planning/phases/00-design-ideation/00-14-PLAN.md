---
phase: 00-design-ideation
plan: 14
type: execute
wave: 9
depends_on: ["00-13"]
files_modified:
  - .playground/src/fixtures/home.json
  - .playground/src/fixtures/resume.json
  - .playground/src/pages/admin/home.astro
  - .playground/src/pages/admin/resume.astro
  - .playground/src/components/FocalPointSketch.tsx
  - .playground/src/components/RichTextBullets.tsx
  - .playground/dist
autonomous: true
requirements: [DSGN-01, DSGN-04]

must_haves:
  truths:
    - "The focal-point crop control runs against a real 3:2 frame with object-position following live, so D-23's argument against a preset grid is demonstrated rather than asserted"
    - "The crop picker's phone refusal is a designed screen with honest copy, not a broken control"
    - "The résumé bullet editor runs against the real RichText component, so G-3 and G-4 are shown failing rather than described as failing"
    - "The lenient-draft inline warning is legible and saving still works, per D-18's two severities"
    - "The PDF drift warning appears on the résumé screen as a soft warning that does not block"
  artifacts:
    - path: ".playground/src/pages/admin/home.astro"
      provides: "S-home and the crop control's host"
      contains: "export const STATES"
    - path: ".playground/src/components/FocalPointSketch.tsx"
      provides: "The focal-point island — D-23's control and G-1's evidence"
      contains: "objectPosition"
    - path: ".playground/src/pages/admin/resume.astro"
      provides: "S-resume, E-resume-section, T-error-inline, R-crop-picker's sibling refusal host"
      contains: "export const STATES"
    - path: ".playground/src/components/RichTextBullets.tsx"
      provides: "The RichText island — D-20/D-21's segment authoring and G-3/G-4's evidence"
      contains: "RichText"
  key_links:
    - from: ".playground/src/pages/admin/home.astro"
      to: ".playground/src/components/FocalPointSketch.tsx"
      via: "client:load — sanctioned island 2 of 3"
      pattern: "client:load"
    - from: ".playground/src/pages/admin/resume.astro"
      to: ".playground/src/components/RichTextBullets.tsx"
      via: "client:load — sanctioned island 3 of 3"
      pattern: "client:load"
---

<objective>
Sketch `/admin/home` and `/admin/resume` with the two remaining sanctioned islands: the
focal-point crop control that the design system does not have, and the bullet editor built on
the `RichText` component that cannot do what D-20 and D-21 require.

Purpose: these two screens carry the phase's two most consequential findings. G-1 says the DS
has no crop picker and D-23 says presets cannot substitute — the single existing
`peekPosition` value, `50% 25%`, is the entire argument, and a running control is what turns it
from a claim into a demonstration. G-3 and G-4 say `RichText` cannot restrict marks and cannot
emit segments, which means a naive bold-only serializer would silently drop italic, underline
and link content on save. That is data loss, and D-20 exists to design the stored-XSS class out
rather than filter it. Both findings must be *shown* failing in a sketch, because Phase 1 is
being asked to spend release scope on them.

Output: two screens, two islands, one designed refusal, and measured evidence for G-1, G-3 and
G-4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-PATTERNS.md
@.planning/phases/00-design-ideation/00-ADMIN-IA.md

<structural_note>
Sketch only, gitignored, deleted in plan 17. Committed output is the SUMMARY. Admin gap
evidence goes into the SUMMARY; plan 16 transcribes all of it into `00-FINDINGS.md` in one pass.

**Scope guard (D-02).** No persistence, no endpoint, no D1, no upload target. `FileInput` for
the PDF accepts a file and does nothing with it. The drift warning is computed from fixture
timestamps, never from a real upload record.

This plan spends the second and third of exactly three sanctioned `client:*` directives. After
it, the hydration budget is fully allocated and plans 15 and 16 take zero. `check-no-js.sh`'s
allowlist already names `admin/home` and `admin/resume`.

`?state=` is served by `astro dev`; `astro build` emits the `populated` state. See plan 12.
</structural_note>

<interfaces>
**`RichText`'s actual prop surface** (`../design-system/dist/index.d.ts:3200-3230`) — read this
before drawing a bullet editor, because two of D-20/D-21's requirements are impossible against
it:

```ts
interface RichTextProps {
  /** Controlled HTML string (default) or TipTap JSON Doc object when `outputFormat="json"`. */
  value: string | object;
  onChange: (value: string | object) => void;
  placeholder?: string;
  readOnly?: boolean;
  /** @default "html" */
  outputFormat?: "html" | "json";
  /** Replace the default toolbar with a custom ReactNode; pass `null` to suppress the toolbar entirely. */
  toolbar?: ReactNode;
  className?: string; ariaLabel?: string; style?: CSSProperties;
  inline?: boolean;
}
```

No `marks` / `extensions` prop — StarterKit plus `Link`, `Underline` and `CodeBlockLowlight`
are always on, and Cmd-I / Cmd-U / Cmd-K stay live even with `toolbar={null}`. That is **G-3**.
No `"segments"` in `outputFormat`. That is **G-4**.

**The existing focal-point control** (`legacy:PropertiesPanel.tsx:739-786`) — mouse-only,
inverted delta, `/2` damping, 0-100 clamp, live `objectPosition`:

```tsx
<div className="admin-pan-frame"
  onMouseDown={(e) => {
    const startX = e.clientX, startY = e.clientY;
    const pos = (currentPosition || "50% 50%").split(" ");
    const startPosX = parseFloat(pos[0]) || 50;
    const startPosY = parseFloat(pos[1]) || 50;
    const handleMove = (ev: MouseEvent) => {
      const newX = Math.max(0, Math.min(100, startPosX - ((ev.clientX - startX) / 2)));
      const newY = Math.max(0, Math.min(100, startPosY - ((ev.clientY - startY) / 2)));
      img.style.objectPosition = `${Math.round(newX)}% ${Math.round(newY)}%`;
    };
    document.addEventListener("mousemove", handleMove);
    document.addEventListener("mouseup", handleUp);
  }}>
```

**Three defects, each visible above, and each G-1 evidence:** `onMouseDown` only (no pointer or
touch events, which is also D-09's evidence for the desktop-only refusal), no `tabIndex` and no
`onKeyDown` so the keyboard cannot reach it at all, and `document`-level listeners with no
cleanup on unmount.

The 3:2 frame CSS already exists (`legacy:src/styles/admin.css:1777-1797`):
`aspect-ratio: 3 / 2`, `overflow: hidden`, `cursor: grab`, `object-fit: cover`,
`pointer-events: none` on the image.

**Real data.** `data/home_config.json` — `peekIds` has 6 entries; `peekPositions` has exactly
**one**, `50% 25%`. That single value is D-23's whole argument against a preset grid; put it in
the sketch. `data/resume.json` — 3 experience entries with 11, 3 and 4 bullets respectively,
3 skill groups, 1 education entry; bullets currently contain `<strong>` and nothing else, which
is why D-20's migration is narrow.

**Recovered legacy field labels, verbatim** (`00-RESEARCH.md:839-844`): Company, Role, Period,
Location, Icon, URL, Bullets, Skill Group/Skills, School, Degree, CGPA, Leadership,
Resume PDF / Upload New Resume, Replace with, Actions.

**The type drift to reconcile:** the legacy admin split dates into
`startMonth/startYear/endMonth/endYear/isPresent` while `data/resume.json` stores a single
`period` string. `00-ADMIN-IA.md` has already picked one — follow it; do not re-litigate.

`FileInput` (`dist/index.d.ts:2547-2600`): `variant: "dropzone" | "button"`, `accept`,
`maxSizeBytes`, `onError(reason)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /admin/home, the focal-point island, and the designed crop refusal</name>
  <read_first>
    - `git show legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx` lines 669-807 — the home field catalog and the working focal-point control at 739-786, whose three defects are the G-1 evidence
    - `git show legacy/nextjs-portfolio:src/styles/admin.css` lines 1777-1797 — the existing 3:2 frame CSS
    - `data/home_config.json` — title, subtitle, intro, 6 `peekIds`, exactly one `peekPositions` value `50% 25%`, 3 socials, 2 CTAs
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-09, D-23, and §"Specific Ideas" (the `50% 25%` argument)
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Component Mapping, the `/admin/home` row and the "Desktop-only refusal" cross-cutting row
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Contract table" — the verbatim `Refusal — crop picker` copy
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Viewport and mode contract" — `R-` artefacts are phone, 390x844, at COMFORTABLE density, not compact
    - `.planning/phases/00-design-ideation/00-ADMIN-IA.md` §"Field catalog", the home row
    - `.playground/src/pages/admin/photos.astro` (from plan 13 — copy its `SCREEN` / `STATES` shape and its island-comment convention)
  </read_first>
  <files>.playground/src/fixtures/home.json, .playground/src/pages/admin/home.astro, .playground/src/components/FocalPointSketch.tsx</files>
  <action>
Create `src/fixtures/home.json` from the real `data/home_config.json`, keyed by state, with the
six real `peekIds` and the single real `peekPositions` value `50% 25%` intact.

Create `src/components/FocalPointSketch.tsx` as sanctioned island 2 of 3. It renders one photo
inside a real 3:2 frame with a draggable focal marker and `object-position` following live, per
D-23. Build it from design-system primitives — this is the "custom control built from DS
primitives" D-23 names, and G-1 is already filed for the missing component, so composing it
here is evidence-gathering rather than a workaround.

Fix two of the legacy control's three defects and record what fixing them cost, because that
cost is the argument for the upstream component:
- Use **pointer events** rather than `onMouseDown`, so the control is not mouse-only.
- Add `tabIndex` and arrow-key handling so the focal point is keyboard-operable, which is
  exactly the shape G-1's proposed upstream fix specifies.
- Clean up listeners on unmount, which the legacy control never did.

Then state plainly in a comment what this demonstrates: that a keyboard-operable, pointer-aware
crop control is roughly N lines of bespoke code that every consumer would otherwise rewrite,
which is the case for `FocalPointPicker` existing upstream. Record the real line count in the
SUMMARY rather than guessing it.

Seed the frame with `50% 25%`. That one value is D-23's entire argument: no preset grid
containing centre, thirds or quadrants produces it, so a preset picker would have silently
changed the one crop the site already has. Put a visible readout of the current value beside
the frame so a reviewer can see it is a continuous value and not a snapped one.

Create `src/pages/admin/home.astro` as artefact `S-home`, exporting `SCREEN` and `STATES` in
the established shape with every `ref` a member of `CANONICAL_IDS`. Compose the home field
catalog with DS `Field` + `TextInput` + `Textarea` for title, subtitle and intro, `Sortable`
for the peek order, `Select` for CTA style, and the focal-point island with `client:load`.
Carry the same budgeted-exception comment convention plan 13 established: this island costs
about 177 KB gzip today because DS-09 tree-shaking fails.

Also render artefact **`R-crop-picker`**, D-09's designed refusal, at the phone viewport. Copy
verbatim from UI-SPEC's contract table: **Crop needs a bigger screen.** / *Setting a focal
point means dragging inside a 3:2 frame, which doesn't work on a phone. Everything else on this
screen does.* Compose it with `EmptyState` and an honest title. Two points matter and are easy
to get wrong: the last sentence is what makes it read as a deliberate refusal rather than as a
broken screen, and per UI-SPEC's viewport contract `R-` artefacts render at **comfortable**
density, not compact, because 30px controls sit under the 44px touch floor D-09 implies. Reach
the refusal by a distinct URL — a `?layout=phone` variant of this route is sufficient and
avoids a second file — and make `check-states.mjs`-style reachability visible by asserting the
refusal copy appears only under that variant.
  </action>
  <verify>
    <automated>cd .playground && rm -rf node_modules/.vite dist && npx astro build && test -f dist/admin/home/index.html && bash check-no-js.sh && node check-states.mjs && grep -q '50% 25%' src/fixtures/home.json && echo HOME_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd .playground && npx astro build` exits 0, `test -f .playground/dist/admin/home/index.html` exits 0, `bash check-no-js.sh` exits 0, `node check-states.mjs` exits 0
    - `dist/admin/home/index.html` contains at least one `<script` tag — sanctioned island 2 is hydrating
    - `grep -c 'client:' .playground/src/pages/admin/home.astro` prints exactly `1`
    - `grep -q '50% 25%' .playground/src/fixtures/home.json` exits 0 and the same literal appears in the rendered `dist/admin/home/index.html` — the real single crop value is in the sketch
    - `FocalPointSketch.tsx` contains `objectPosition`, uses pointer events (`grep -qE 'onPointerDown|PointerEvent'` exits 0), and `grep -q 'onMouseDown' .playground/src/components/FocalPointSketch.tsx` exits 1
    - `grep -qE 'tabIndex|onKeyDown' .playground/src/components/FocalPointSketch.tsx` exits 0 — keyboard-operable, per G-1's proposed upstream fix
    - `grep -qE 'removeEventListener|AbortController' .playground/src/components/FocalPointSketch.tsx` exits 0 — listeners are cleaned up, unlike the legacy control
    - `grep -qiE 'fetch\(|localStorage|sessionStorage' .playground/src/components/FocalPointSketch.tsx` exits 1 — no persistence
    - `grep -q 'aspect-ratio' .playground/src/components/FocalPointSketch.tsx` exits 0 — a real 3:2 frame, not a square proxy
    - The refusal copy `Crop needs a bigger screen.` is present, together with the `Everything else on this screen does.` sentence
    - The refusal renders at comfortable density: the phone variant does not carry `data-density="compact"`
    - The SUMMARY records the measured line count of `FocalPointSketch.tsx` as G-1 evidence
  </acceptance_criteria>
  <done>The focal-point control runs with pointer and keyboard input against a real 3:2 frame seeded with the site's one real crop value, and the phone refusal reads as a deliberate design rather than a broken control.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/resume, the RichText island, and the lenient-draft warning</name>
  <read_first>
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/dist/index.d.ts` lines 3200-3230 — `RichTextProps`, confirming there is no `marks` prop and no `"segments"` output format
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/src/interaction/RichText/` — the source, specifically which TipTap extensions are unconditionally registered
    - `/Users/akhilsaxena/Documents/Personal/Repositories/design-system/dist/index.d.ts` lines 2547-2600 — `FileInput`'s prop surface for the PDF field
    - `git show legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx` lines 421-668 and 893-937 — the role, skillGroup, education and résumé-PDF branches; this is the verbatim field catalog
    - `data/resume.json` — 3 experience entries with 11, 3 and 4 bullets, 3 skill groups, 1 education entry, bullets containing only `<strong>`
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-18, D-20, D-21, D-26
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Component Mapping, the `/admin/resume` row
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Contract table" — the verbatim inline-error and PDF-drift copy
    - `.planning/phases/00-design-ideation/00-ADMIN-IA.md` §"Field catalog" and §"Schema decisions this IA forces" — including which date shape wins
    - `.playground/src/pages/admin/home.astro` (from task 1)
  </read_first>
  <files>.playground/src/fixtures/resume.json, .playground/src/pages/admin/resume.astro, .playground/src/components/RichTextBullets.tsx</files>
  <action>
Create `src/fixtures/resume.json` from the real `data/resume.json`, keyed by state, preserving
the real bullet counts — 11, 3 and 4. The 11-bullet Brevo entry is the most demanding host for
both the bullet editor and the inline warning, and a three-bullet stand-in would hide the
scroll and density problems this screen exists to expose.

Create `src/components/RichTextBullets.tsx` as sanctioned island 3 of 3, composing the design
system's `RichText` per D-21 with bold as the intended only mark, and serializing to D-20's
`{text}` / `{text, emphasis: true}` segment array.

**The point of this island is to show the failure, not to route around it.** Do exactly this:

- Pass `toolbar={null}` to suppress the default toolbar, then verify by hand that Cmd-I, Cmd-U
  and Cmd-K still apply italic, underline and link. Record the result. There is no `marks` or
  `extensions` prop, so the extensions cannot be disabled — that is **G-3**.
- Write the segment serializer against the only available output formats, `"html"` and
  `"json"`. There is no `"segments"` — that is **G-4** — so the serializer must adapt one of
  the two, and D-20 requires that no HTML string exist anywhere, which puts `"html"` out of
  reach on principle. Use `"json"` and adapt, and say so in a comment.
- Then demonstrate the data loss concretely: type a bullet, apply italic with Cmd-I, serialize,
  and show the resulting segment array beside the editor. The italic run comes back as plain
  text. Render that before-and-after visibly on the page so a reviewer sees the dropped
  formatting rather than reading about it.

Do **not** add a local key handler to swallow Cmd-I, and do not fork `RichText`. Suppressing
the symptom locally would hide the finding and would be exactly the workaround Core Value
forbids; the fix belongs upstream as G-3's `marks?: Array<"bold" | "italic" | …>` prop.

Create `src/pages/admin/resume.astro` as artefact `S-resume`, exporting `SCREEN` and `STATES`
in the established shape. Compose the full recovered field catalog with DS `Field` sets —
Company, Role, Period, Location, Icon, URL, Bullets, Skill Group/Skills, School, Degree, CGPA,
Leadership, Resume PDF. Follow `00-ADMIN-IA.md`'s ruling on the date shape rather than
reopening it. Use `Sortable` for bullet order without a second `client:` directive — the budget
is now fully spent, so render it static and note that reordering itself is demonstrated on
`/admin/photos`.

Three further artefacts come from this file:

- **`E-resume-section`** — empty. Copy verbatim: **No education entries.** / *Add one, or leave
  this section out — the résumé page omits empty sections entirely.* The second clause matters:
  it tells the reader that an empty section is a supported outcome rather than an error.
- **`T-error-inline`** — error. D-18's lenient draft: an inline per-field warning at **warning**
  severity where saving still works. Copy verbatim: *Needs a title before this can be
  published.* Compose it with `Field` plus `FieldError` at warning severity, and make it
  unmistakable on screen that the save control remains enabled. One schema, two severities — the
  strict half is the publish block, which plan 16 sketches as `T-error-publish`. Sketch this
  here because the 11-bullet entry is the most demanding host for an inline warning.
- The **D-26 PDF drift warning** — `AlertBanner tone="warning"` reading verbatim: **The PDF is
  older than the résumé data.** / *`resume.json` changed 4 days after `resume.pdf` was uploaded.
  This won't block publishing.* Compute it from fixture timestamps. The last sentence is the
  design: D-26 is a soft warning, and rendering it as a blocking error would silently convert a
  decided-soft warning into a hard gate.

Compose the PDF field with `FileInput variant="button"`. It accepts a file and does nothing
with it — no upload target exists in this playground and none may be added.
  </action>
  <verify>
    <automated>cd .playground && rm -rf node_modules/.vite dist && npx astro build && test -f dist/admin/resume/index.html && bash check-no-js.sh && node check-states.mjs && [ "$(grep -c 'client:' src/pages/admin/resume.astro)" = "1" ] && echo RESUME_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd .playground && npx astro build` exits 0, `test -f .playground/dist/admin/resume/index.html` exits 0, `bash check-no-js.sh` exits 0, `node check-states.mjs` exits 0
    - `dist/admin/resume/index.html` contains at least one `<script` tag — sanctioned island 3 is hydrating
    - `grep -c 'client:' .playground/src/pages/admin/resume.astro` prints exactly `1`; the hydration budget across the whole playground is now exactly 4 pages (`probe/island`, `admin/photos`, `admin/home`, `admin/resume`)
    - `RichTextBullets.tsx` passes `toolbar={null}` and `outputFormat="json"`; `grep -q '"html"' .playground/src/components/RichTextBullets.tsx` exits 1 outside of comments — D-20 puts the HTML format out of reach
    - `grep -qE 'preventDefault.*(Key|key).*i|metaKey' .playground/src/components/RichTextBullets.tsx` exits 1 — no local handler swallows Cmd-I; the gap is left intact
    - The rendered page shows a serialized segment array beside the editor, and the SUMMARY records the measured before-and-after: what was typed with italic applied, and what the segment array contained
    - The SUMMARY answers three explicit questions for G-3: does Cmd-I still apply italic with `toolbar={null}`, does Cmd-U still apply underline, does Cmd-K still create a link
    - The résumé fixture preserves bullet counts of 11, 3 and 4, and the 11-bullet entry renders in full
    - `grep -q 'No education entries.' .playground/src/pages/admin/resume.astro` exits 0, including the "omits empty sections entirely" clause
    - `grep -q 'Needs a title before this can be published.' .playground/src/pages/admin/resume.astro` exits 0 and the save control is not disabled in that state
    - `grep -q 'The PDF is older than the résumé data.' .playground/src/pages/admin/resume.astro` exits 0 together with the literal `This won't block publishing.`
    - `grep -qiE 'fetch\(|localStorage|sessionStorage' .playground/src/components/RichTextBullets.tsx` exits 1
    - All thirteen recovered field labels appear in `resume.astro`
  </acceptance_criteria>
  <done>The bullet editor runs on the real RichText component and visibly drops an italic run on serialize, the lenient-draft warning saves rather than blocks, the PDF drift warning is soft, and G-3 and G-4 have demonstrated evidence rather than described evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored rich text → stored content | The bullet editor is the surface D-20 exists to redesign, because the legacy path stored an HTML string. Whatever shape is sketched here is what Phase 7 builds. |
| committed site content → gitignored fixtures | `data/home_config.json` and `data/resume.json` are copied into a throwaway directory. Nothing flows back. |
| sketched refusal → Phase 7's phone behaviour | A refusal depicted as a broken control rather than a designed state becomes a broken control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02 | Tampering (stored XSS class) | the résumé bullet editor | mitigate | D-20 designs the class out rather than filtering it: bullets are `{text}` / `{text, emphasis}` segments rendered as React elements, so no HTML string exists to inject through. The island is required to use `outputFormat="json"` and forbidden from using `"html"`, asserted by grep, and no paste-HTML affordance is depicted. The residual risk — that a naive serializer silently drops non-bold marks — is surfaced deliberately as G-3/G-4 rather than hidden. |
| T-00-33 | Tampering | a local workaround concealing G-3 | mitigate | Adding a key handler to swallow Cmd-I or forking `RichText` is forbidden and asserted absent by grep. Suppressing the symptom locally would remove the evidence Phase 1 is being asked to fund a fix with, and violates Core Value's rule that a gap is a finding, never a local component. |
| T-00-34 | Tampering | the D-26 drift warning depicted as a block | mitigate | D-26 decided a soft warning. The verbatim copy ends `This won't block publishing.` and is asserted present. Rendering it as a blocking error would silently convert a decided-soft warning into a hard gate three phases downstream. |
| T-00-35 | Denial of Service (usability) | a refusal that reads as a bug | mitigate | `R-crop-picker` carries the verbatim honest copy including the "Everything else on this screen does" sentence, and renders at comfortable density because 30px controls sit under D-09's implied 44px touch floor. Both are asserted. |
| T-00-29 | Information Disclosure | fixtures carrying non-public content | mitigate | Fixtures come only from `data/home_config.json` and `data/resume.json`, already committed public site content. No credential is needed by any artefact here, so none can be embedded. The playground is gitignored. |
| T-00-23 | Denial of Service (performance) | overspending the hydration budget | mitigate | Exactly one `client:` directive per route is asserted, bringing the playground total to the four allowlisted pages and no more. `check-no-js.sh` fails both an unsanctioned island and a sanctioned one that stopped hydrating. |
| T-00-06 | Elevation of Privilege | the playground drifting into a Phase 7 foundation | mitigate | No persistence, no endpoint, no upload target, no D1, no adapter, no auth. `fetch(`, `localStorage` and `sessionStorage` are asserted absent from both islands. `FileInput` accepts a file and does nothing with it. Plan 17 deletes the directory. |
| — | Spoofing | — | not applicable | No credential is accepted, stored or verified in this plan. |
| — | Repudiation | — | not applicable | Single-actor sketches with no audit surface. |
| T-00-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no package; `RichText`, TipTap and dnd-kit arrive inside the design-system tarball already audited in plan 01. |
</threat_model>

<verification>
- `npx astro build` exits 0; `bash check-no-js.sh` exits 0; `node check-states.mjs` exits 0
- Exactly four pages in `dist/` emit a `<script` tag, matching the allowlist exactly
- The focal control uses pointer events and keyboard input, cleans up its listeners, and carries the real `50% 25%` value
- The bullet editor uses `outputFormat="json"`, adds no local mark suppression, and visibly drops an italic run on serialize
- The SUMMARY answers G-1's line count and G-3's three Cmd-key questions explicitly

</verification>

<success_criteria>
- D-23's crop control runs against a real 3:2 frame with a continuous value, demonstrating why a preset grid could not produce the site's one existing crop
- The phone refusal is a designed state with honest copy at the correct density, not a broken control
- G-1, G-3 and G-4 each carry measured evidence produced by a running sketch rather than by reading a type definition
- D-18's lenient half and D-26's soft warning are both visibly non-blocking
- The hydration budget is exactly and fully spent, leaving zero for plans 15 and 16
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-14-SUMMARY.md` when done.
Commit the SUMMARY only. Record in it: `FocalPointSketch.tsx`'s measured line count (G-1
evidence), the three Cmd-key answers (G-3 evidence), and the before-and-after segment array
showing the dropped italic run (G-4 evidence). Plan 16 transcribes all admin evidence into
`00-FINDINGS.md` in one pass.
</output>
