---
phase: 00-design-ideation
plan: 15
type: execute
wave: 10
depends_on: ["00-14"]
files_modified:
  - .playground/src/fixtures/projects.json
  - .playground/src/fixtures/site.json
  - .playground/src/fixtures/conflict.json
  - .playground/src/pages/admin/projects.astro
  - .playground/src/pages/admin/projects/[id].astro
  - .playground/src/pages/admin/site.astro
  - .playground/src/pages/admin/conflict.astro
  - .playground/dist
autonomous: true
requirements: [DSGN-01, DSGN-04]

must_haves:
  truths:
    - "A project and its case study are edited together on one screen, matching the visitor path Work to project to case study"
    - "The 401 re-authentication screen denies and re-authenticates while keeping on-screen state, and depicts no bypass"
    - "Deleting a category names how many photos use it and requires choosing where they go"
    - "The per-file conflict screen lets one file be resolved without abandoning another, which is the whole point of D-16"
    - "The conflict diff has no design-system component behind it, and the sketch is the evidence for that"
  artifacts:
    - path: ".playground/src/pages/admin/projects.astro"
      provides: "S-projects and E-projects"
      contains: "export const STATES"
    - path: ".playground/src/pages/admin/projects/[id].astro"
      provides: "S-project-detail, R-case-study-authoring, O-reauth-401"
      contains: "getStaticPaths"
    - path: ".playground/src/pages/admin/site.astro"
      provides: "S-site and O-category-reassign"
      contains: "export const STATES"
    - path: ".playground/src/pages/admin/conflict.astro"
      provides: "O-conflict-diff — the largest admin surface with zero DS coverage, G-7's evidence"
      contains: "Overwrite"
  key_links:
    - from: ".playground/src/pages/admin/projects/[id].astro"
      to: ".playground/src/pages/admin/conflict.astro"
      via: "conflict state declared as inherits, pointing at the dedicated diff artefact"
      pattern: "O-conflict-diff"
    - from: ".playground/src/pages/admin/site.astro"
      to: "data/site_config.json"
      via: "canonical category records replacing the lowercase/Title-case split"
      pattern: "categoryColumns"
---

<objective>
Sketch the three remaining admin screens — projects, project detail and site — plus the two
overlays that belong to them, and then the per-file conflict diff, which UI-SPEC calls the most
substantial single screen in the admin and which has zero design-system coverage.

Purpose: D-24 pairs a project with its case study, which is a route shape the legacy admin had
no home for at all. D-25 turns categories into canonical records, which is what kills the
lowercase-versus-Title-case drift, and its rename and delete paths need a designed reassignment
rather than a warning. D-19's expiring session needs a screen that denies and re-authenticates
without losing on-screen state, and getting that wrong in a sketch means getting it wrong in
Phase 2's auth boundary. And D-16's per-file conflict resolution is the phase's single largest
design gap: there is no DS component, so if it is not sketched now Phase 7 invents it under
deadline.

Output: three screens, three overlays, and G-7's evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-design-ideation/00-UI-SPEC.md
@.planning/phases/00-design-ideation/00-CONTEXT.md
@.planning/phases/00-design-ideation/00-PATTERNS.md
@.planning/phases/00-design-ideation/00-ADMIN-IA.md

<structural_note>
Sketch only, gitignored, deleted in plan 17. Committed output is the SUMMARY. Admin gap
evidence goes into the SUMMARY; plan 16 transcribes all of it into `00-FINDINGS.md`.

**The hydration budget is fully spent.** Plans 13 and 14 used all three sanctioned islands.
This plan takes **zero** `client:*` directives. Every dialog, sheet, error summary and the
conflict diff itself is sketched **open and static** via `?state=`, which is exactly the
reduction UI-SPEC's hydration table prescribes — 32 of about 35 sketches then cost 0 KB and the
DS-09 measurement stays uncontaminated.

**Scope guard (D-02).** No auth, no D1, no endpoint, no GitHub API client, no `projects.json`
written into `data/`. D-24 implies extracting `projects` out of `resume.json`, and that
extraction is **Phase 3's migration** — this sketch reads a fixture shaped the way the migration
will produce, and says so, rather than performing it.

`?state=` is served by `astro dev`; `astro build` emits the `populated` state. See plan 12.

`[id].astro` needs `getStaticPaths` returning the five real project ids so the route builds
statically. That is an Astro routing requirement, not a scope expansion.
</structural_note>

<interfaces>
Real data, verified 2026-08-17:
- `data/resume.json` — five projects: `cairn`, `hued`, `momentum`, `timeshift`,
  `design-system`. `badges` is an array of objects already carrying "Live" on Cairn. The
  design-system entry's id was corrected from the auto-generated `project-1777750009929` and
  its description now reads "80-component".
- `data/site_config.json` — 176 bytes, one key, `categoryColumns`, Title-case (`Architecture`)
  against the lowercase category values in `data/portfolio_images.json` (`architecture`). This
  is D-25's drift.
- `data/portfolio_images.json` — 39 photos across the seven lowercase categories; the real
  per-category counts are what the reassignment dialog must quote.

DS composition per UI-SPEC §Component Mapping:
- `/admin/projects` — `DataGrid` or `Card` list · `Badge` (Live/Maintained/Archived, D-45) ·
  `EmptyState`
- `/admin/projects/[id]` — `Field` set · `RichText` (case-study body) · `FileInput` (hero and
  screenshots, D-42) · `Tabs` (card versus case study)
- `/admin/site` — `Table` or `DataGrid` · `InlineEdit` (label) · `NumberStepper` (column count)
  · `ConfirmDialog` + `Select` (delete with reassignment)
- 401 re-auth — `Modal role="alertdialog"` + `Button`, **deny and re-authenticate, never a
  bypass**
- Per-file conflict diff — **nothing**. That is G-7: the most substantial single screen in the
  admin has no DS component behind it.

The legacy analog for the project card fields is
`git show legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx` lines 499-577. The
case-study authoring half has **no analog at all**, because D-24 invents the pairing.
`admin/site.astro` has only a `NumberStepper` precedent (`PropertiesPanel.tsx:261-334`); the
canonical category records and the reassignment path are new.

`RichText` cannot restrict marks (G-3) and cannot emit segments (G-4) — established in plan 14.
Do not re-derive it here; reuse the finding and keep this screen static.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /admin/projects, /admin/projects/[id], the case-study refusal and the 401 re-auth</name>
  <read_first>
    - `git show legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx` lines 499-577 — the project card field catalog; note the case-study half has no analog
    - `data/resume.json` — the five projects, their `badges` arrays and their descriptions
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-19, D-24, D-38, D-40, D-41, D-42, D-45
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Component Mapping — the `/admin/projects` and `/admin/projects/[id]` rows, and the "401 re-auth" and "Desktop-only refusal" cross-cutting rows
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Contract table" — the verbatim empty-projects copy, the network/401 copy, and the case-study-authoring refusal copy
    - `.planning/phases/00-design-ideation/00-ADMIN-IA.md` §Routes and §"Schema decisions this IA forces" — the `projects.json` extraction D-24 forces
    - `.planning/phases/00-design-ideation/00-COPY/case-design-system.md` and `00-COPY/case-cairn.md` (from plan 05) — real drafted case-study prose, so the authoring surface is sized against real text
    - `.playground/src/pages/admin/resume.astro` (from plan 14 — copy its `SCREEN` / `STATES` shape and its static-overlay convention)
  </read_first>
  <files>.playground/src/fixtures/projects.json, .playground/src/pages/admin/projects.astro, .playground/src/pages/admin/projects/[id].astro</files>
  <action>
Create `src/fixtures/projects.json` shaped the way D-24's `projects.json` extraction will
produce — the five real projects with their card fields, `badges`, and a case-study body drawn
from the real drafts in `00-COPY/`. Add a header comment stating plainly that this fixture
anticipates a Phase 3 migration and that nothing here writes to `data/`.

Create `src/pages/admin/projects.astro` as artefact `S-projects` with `SCREEN` and `STATES`
exports in the established shape, every `ref` a member of `CANONICAL_IDS`. Compose the five
projects as a `Card` list or `DataGrid` with a `Badge` per project carrying D-45's status —
Live, Maintained or Archived. Take the status values from `00-COPY/one-liners.md`, which is
canonical; do not invent a status. D-45's point is that a reader learns whether a project is
real and current without the page ageing, so no date may appear in the list.

Render **`E-projects`** — empty. Copy verbatim: **No projects yet.** / *A project owns its case
study, so adding one here adds both.* → `Add a project`. That second sentence is D-24's whole
model stated in one line, and it is the first place a reader meets it.

Create `src/pages/admin/projects/[id].astro` as artefact `S-project-detail`, with
`getStaticPaths` returning the five real ids. This is D-24's screen: opening a project edits its
card fields **and** its case study together, matching the visitor path Work → project → case
study. Use `Tabs` to switch between the card half and the case-study half. Compose the card
half from the legacy field catalog and the case-study half against the real drafted prose, so
the authoring surface is sized against real text lengths rather than against lorem — which is
D-40's entire argument. Compose `FileInput` for the hero and the inline screenshots per D-41,
and record D-42's path in a comment: screenshots go straight to R2 under `assets/` on the same
custom domain as photos, dimensions captured at upload for CLS, and they do **not** pass through
the photo pipeline, which composites a watermark and extracts EXIF — neither of which belongs on
a screenshot.

Keep the case-study body **static**. `RichText`'s limitations are already established as G-3 and
G-4 by plan 14's island, and the hydration budget is spent. Add a comment pointing at
`/admin/resume` as where that interaction is demonstrated.

Two further artefacts come from this file:

- **`R-case-study-authoring`** — D-09's second designed refusal, at the phone viewport via a
  `?layout=phone` variant, at **comfortable** density per UI-SPEC's viewport contract. Copy
  verbatim: **Case studies are desktop-only.** / *Long-form editing on a phone loses more than
  it gains. You can still edit this project's card fields below.* The final sentence is what
  makes it a refusal rather than a dead end — the card fields remain editable, so the phone
  screen must actually render them below the refusal.
- **`O-reauth-401`** — D-19's expiring session, sketched open and static. Compose
  `Modal role="alertdialog"` with a `Button`. Copy verbatim: **Your session expired.** / *Sign
  in again — nothing on this screen has been lost.* → `Sign in`. Three things about this screen
  are security decisions, not styling, and each must be visibly true in the sketch: the
  on-screen edits stay rendered behind the modal, because D-19 requires keeping state and
  retrying the save in place; the only action is to re-authenticate, with **no dismiss, no
  continue-offline and no skip affordance of any kind**; and this is the most demanding host
  precisely because a long unsaved case-study draft is what makes losing state expensive. The
  admin's auth fails closed — a sketch that depicts a bypass is where Phase 2 and Phase 7 would
  learn the wrong boundary.
  </action>
  <verify>
    <automated>cd .playground && rm -rf node_modules/.vite dist && npx astro build && test -f dist/admin/projects/index.html && test -f dist/admin/projects/cairn/index.html && bash check-no-js.sh && node check-states.mjs && [ "$(grep -c 'client:' src/pages/admin/projects.astro)" = "0" ] && [ "$(grep -c 'client:' 'src/pages/admin/projects/[id].astro')" = "0" ] && echo PROJECTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd .playground && npx astro build` exits 0; `test -f .playground/dist/admin/projects/index.html` and `test -f .playground/dist/admin/projects/cairn/index.html` both exit 0
    - `bash check-no-js.sh` exits 0 and `node check-states.mjs` exits 0
    - `grep -c 'client:'` prints `0` for both `projects.astro` and `[id].astro` — the hydration budget stays fully spent at four allowlisted pages
    - `getStaticPaths` returns exactly five ids and all five build: `cairn`, `hued`, `momentum`, `timeshift`, `design-system`
    - All five projects carry a D-45 `Badge` whose value is one of `Live`, `Maintained`, `Archived`, and those values match `00-COPY/one-liners.md`
    - `grep -qE '20[0-9]{2}' .playground/src/pages/admin/projects.astro` exits 1 — no year appears in the project list, per D-45
    - `grep -q 'No projects yet.' .playground/src/pages/admin/projects.astro` exits 0 together with `A project owns its case study, so adding one here adds both.`
    - `[id].astro` renders both halves behind `Tabs`, and the case-study half contains prose drawn from `00-COPY/case-*.md` rather than placeholder text
    - `grep -q 'Case studies are desktop-only.' '.playground/src/pages/admin/projects/[id].astro'` exits 0, together with `You can still edit this project's card fields below.`, and the phone variant renders the card fields below the refusal
    - `grep -q 'Your session expired.' '.playground/src/pages/admin/projects/[id].astro'` exits 0 together with `nothing on this screen has been lost`
    - `grep -qiE 'dismiss|continue offline|skip|not now|maybe later|bypass' '.playground/src/pages/admin/projects/[id].astro'` exits 1 — the 401 modal offers re-authentication and nothing else
    - `grep -qiE 'watermark|EXIF' '.playground/src/pages/admin/projects/[id].astro'` exits 0 inside the D-42 comment recording why screenshots skip the photo pipeline
    - The projects fixture header states that it anticipates a Phase 3 migration; `grep -r 'projects.json' data/` finds nothing — no migration was performed
  </acceptance_criteria>
  <done>A project and its case study are edited together against real drafted prose, both D-09 refusals now exist as designed states, and the 401 screen denies and re-authenticates with no bypass affordance anywhere on it.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/site and the category reassignment dialog</name>
  <read_first>
    - `data/site_config.json` — all 176 bytes of it; the single `categoryColumns` key with Title-case keys
    - `data/portfolio_images.json` — the seven lowercase category values and the real per-category photo counts the dialog must quote
    - `git show legacy/nextjs-portfolio:src/components/admin/PropertiesPanel.tsx` lines 261-334 — the only precedent, the column-count field
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-25
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Component Mapping, the `/admin/site` row and the "Category reassignment" cross-cutting row
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Contract table" — the verbatim delete-category copy
    - `.planning/phases/00-design-ideation/00-ADMIN-IA.md` §"Schema decisions this IA forces" — the canonical category record shape
    - `.playground/src/pages/admin/photos.astro` (from plan 13 — where the D-25 drift was surfaced; this screen is where it is fixed)
  </read_first>
  <files>.playground/src/fixtures/site.json, .playground/src/pages/admin/site.astro</files>
  <action>
Create `src/fixtures/site.json` holding D-25's canonical category records — for each of the
seven real categories, an `id` (lowercase, the key), a display `label` (the human string), and
a column count taken from the real `site_config.categoryColumns`. Include the real per-category
photo counts, computed from `data/portfolio_images.json`, because the reassignment dialog quotes
them and a made-up number would defeat the point.

Create `src/pages/admin/site.astro` as artefact `S-site` with `SCREEN` and `STATES` exports in
the established shape. Compose the category table with `Table` or `DataGrid`, `InlineEdit` for
the label and `NumberStepper` for the column count — static, no `client:` directive.

Make the D-25 fix legible on screen. The whole point is that `id` and `label` are **different
fields**: `architecture` is the key that `portfolio_images.json` stores, `Architecture` is the
string a visitor reads, and the legacy code bridged them with a render-time
`c.charAt(0).toUpperCase()` that silently broke for any category whose display form is not a
simple capitalisation. Show both columns side by side and label them, so a reviewer can see that
renaming the label no longer touches the key.

Render **`O-category-reassign`** open and static. Compose `ConfirmDialog tone="danger"` plus a
`Select`, with copy verbatim from UI-SPEC's contract table: **Delete the Architecture category?**
/ *12 photos use it. Choose where they go:* — with the count read from the real data rather than
transcribed from the example, and the `Select` listing the other six real categories as
destinations. D-25 says rename and delete need a *designed reassignment path*, so the dialog
must make it impossible to delete without choosing a destination: no bare confirm, and no
"delete anyway" escape.

Also state, in the `## States` note or a comment, why this screen legitimately has no `loading`
cell: `/admin/site` reads one 176-byte committed JSON file with no async load. That is exactly
the `n/a: <reason>` shape the coverage table expects in plan 16, and writing the reason here
means plan 16 transcribes it rather than inventing it.

Declare `conflict` in `STATES` as `coverage: "inherits"` pointing at `O-conflict-diff`, which
task 3 sketches.
  </action>
  <verify>
    <automated>cd .playground && rm -rf node_modules/.vite dist && npx astro build && test -f dist/admin/site/index.html && bash check-no-js.sh && node check-states.mjs && [ "$(grep -c 'client:' src/pages/admin/site.astro)" = "0" ] && echo SITE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd .playground && npx astro build` exits 0, `test -f .playground/dist/admin/site/index.html` exits 0, `bash check-no-js.sh` exits 0, `node check-states.mjs` exits 0
    - `grep -c 'client:' .playground/src/pages/admin/site.astro` prints `0`
    - The site fixture holds all seven real category ids, each with a distinct `id` and `label` field, and a photo count that matches the real count in `data/portfolio_images.json`
    - Both `architecture` and `Architecture` render on the page in separately labelled columns
    - `grep -q 'charAt(0).toUpperCase' .playground/src/pages/admin/site.astro` exits 0 inside a comment citing the legacy bridge being replaced
    - `grep -q 'Delete the' .playground/src/pages/admin/site.astro` exits 0 and the dialog quotes a photo count that matches the real per-category count for that category
    - `grep -q 'Choose where they go' .playground/src/pages/admin/site.astro` exits 0, and the `Select` lists the other six real categories
    - `grep -qiE 'delete anyway|delete without|just delete' .playground/src/pages/admin/site.astro` exits 1 — there is no escape from choosing a destination
    - `STATES` declares `loading` as `n/a` with a one-clause reason naming the absence of an async load, and declares `conflict` as `inherits` with `ref: "O-conflict-diff"`
    - `grep -qiE 'fetch\(|localStorage' .playground/src/pages/admin/site.astro` exits 1
  </acceptance_criteria>
  <done>Categories are canonical records with id and label as separate fields, the drift surfaced on /admin/photos is visibly fixed here, and deleting a category is impossible without choosing where its real photos go.</done>
</task>

<task type="auto">
  <name>Task 3: O-conflict-diff — the per-file conflict screen with zero DS coverage</name>
  <read_first>
    - `.planning/phases/00-design-ideation/00-CONTEXT.md` D-10, D-11, D-16, D-17 — the draft baseline SHA, the layered-not-forked rule, per-file reload or overwrite, and discard
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §Component Mapping — the "Per-file conflict diff" row, whose DS composition column reads **nothing**, and the G-7 row in §"Named gaps"
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"Contract table" — the verbatim `Destructive — overwrite (D-16)` copy
    - `.planning/phases/00-design-ideation/00-UI-SPEC.md` §"The six review passes", pass 5 — the question a reviewer will ask of this screen
    - `git show legacy/nextjs-portfolio:src/app/api/deploy/route.ts` — the 409 the legacy route already returns, and the `baseSha` check `DeployButton` bypassed
    - `.planning/codebase/CONCERNS.md` — the bypassed concurrency guard, one of the four reinforcing defects this rebuild fixes
    - `.planning/phases/00-design-ideation/00-ADMIN-IA.md` §"States and where each actually lives" — the ruling that `conflict` is one dedicated screen rather than a per-screen state
    - `.playground/src/pages/admin/site.astro` (from task 2)
  </read_first>
  <files>.playground/src/fixtures/conflict.json, .playground/src/pages/admin/conflict.astro</files>
  <action>
Create `src/fixtures/conflict.json` describing a realistic multi-file 409: `portfolio_images.json`
changed remotely by the photo pipeline while a résumé edit and a home edit were pending locally.
That specific collision is the scenario D-16 was decided for — a pipeline run touching the photo
manifest must not force abandoning an unrelated résumé edit — so the fixture must contain at
least three files with at least one conflicted and at least one clean.

Create `src/pages/admin/conflict.astro` as artefact `O-conflict-diff`, sketched **open and
static** with no `client:` directive. This is the largest single admin surface and it has **zero
design-system coverage** — there is no diff or side-by-side component. Compose it from
primitives and record every place a primitive had to be stretched, because those are the
specific requirements G-7's proposed `DiffView` must satisfy. Do not treat the absence as a
blocker; treat it as the measurement.

The screen must make three things visibly true, and each is a decision rather than a layout
preference:

- **Per file, not per publish.** Each conflicted file gets its own row showing the remote change
  beside the pending change, with its own `Reload remote` and `Overwrite` actions. A reviewer's
  question in pass 5 is precisely whether one file can be resolved without abandoning another,
  so a single global "theirs or mine" toggle fails this artefact outright.
- **Resolution state is per file and legible.** After a row is resolved it must look resolved,
  and unresolved rows must remain obviously outstanding, otherwise a partially resolved conflict
  reads as finished. Sketch at least one resolved and one unresolved row simultaneously.
- **Overwrite states its cost.** Copy verbatim from UI-SPEC's contract table: **Overwrite the
  published `portfolio_images.json`?** / *The remote version changed after your draft started.
  Overwriting drops the remote change shown on the left.* → `Overwrite` / `Reload remote`. Naming
  what is lost is the difference between a decision and a click.

Add a comment recording where the baseline comes from and why this screen can exist at all: D-10
stores the blob SHA the draft was based on **at save time** in the D1 draft row, so the 409
comparison has a real server-side baseline. The legacy `DeployButton` posted `baseSha: "latest"`,
which bypassed `/api/deploy`'s own conflict check entirely — that bypass is why this screen never
existed before, and D-10 is its root-cause fix. Cite it; do not reproduce it.

Also record, in the SUMMARY, the concrete G-7 requirements this sketch surfaced: which
primitives were stretched, what a `DiffView` would need to accept as props, and how per-file
resolution state would be expressed. Phase 7 pulls G-7, and the difference between a useful
finding and a vague one is exactly this list.

Finally, confirm the coverage bookkeeping: every screen that declared `conflict` as
`coverage: "inherits"` with `ref: "O-conflict-diff"` now points at an artefact that exists. Plan
16's coverage gate will fail the build otherwise, and finding that out here is cheaper.
  </action>
  <verify>
    <automated>cd .playground && rm -rf node_modules/.vite dist && npx astro build && test -f dist/admin/conflict/index.html && bash check-no-js.sh && node check-states.mjs && [ "$(grep -c 'client:' src/pages/admin/conflict.astro)" = "0" ] && grep -q 'Overwrite' dist/admin/conflict/index.html && echo CONFLICT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd .playground && npx astro build` exits 0, `test -f .playground/dist/admin/conflict/index.html` exits 0, `bash check-no-js.sh` exits 0, `node check-states.mjs` exits 0
    - `grep -c 'client:' .playground/src/pages/admin/conflict.astro` prints `0`
    - The conflict fixture holds at least three files, at least one conflicted and at least one clean
    - The rendered page contains at least two independent `Reload remote` occurrences and at least two independent `Overwrite` occurrences — actions are per file, not global
    - `grep -qiE 'accept all|take theirs|take mine|resolve all' .playground/src/pages/admin/conflict.astro` exits 1 — no single global toggle stands in for per-file resolution
    - The page renders at least one resolved row and at least one unresolved row simultaneously, and they are visually distinguishable
    - `grep -q 'Overwriting drops the remote change shown on the left.' .playground/src/pages/admin/conflict.astro` exits 0
    - `grep -q 'baseSha' .playground/src/pages/admin/conflict.astro` exits 0 inside a comment citing the legacy `"latest"` bypass and D-10's fix
    - Every `ref: "O-conflict-diff"` declared across all seven admin routes resolves to this artefact; a grep for that ref returns at least two other screens
    - The SUMMARY lists at least three concrete G-7 requirements: primitives stretched, props a `DiffView` would need, and how per-file resolution state is expressed
  </acceptance_criteria>
  <done>The per-file conflict screen exists, one file can be resolved without abandoning another, overwrite names what it drops, and G-7 has a concrete requirements list instead of a one-line absence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expired session → admin surface | D-19's 401 path is where the admin decides what happens when auth lapses mid-edit. The sketch sets the pattern Phase 2's fail-closed boundary and Phase 7's autosave both inherit. |
| remote published state → pending draft | The conflict screen is where two writers meet. What it permits is what Phase 7 will permit. |
| committed site content → gitignored fixtures | `data/resume.json`, `site_config.json` and `portfolio_images.json` are copied into a throwaway directory. Nothing flows back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Spoofing / Elevation of Privilege | the 401 re-authentication screen | mitigate | `O-reauth-401` depicts deny-and-re-authenticate and nothing else. `dismiss`, `continue offline`, `skip`, `not now`, `maybe later` and `bypass` are all asserted absent by grep. On-screen state stays rendered behind the modal because D-19 requires retrying the save in place, not because the session partially survives. CLAUDE.md's fail-closed rule means a missing configuration denies rather than degrades, and a sketch depicting a soft path is where Phase 2 would learn the wrong boundary. |
| T-00-36 | Tampering | silent overwrite of remote changes | mitigate | Overwrite is per file, states what it drops in verbatim copy, and offers `Reload remote` beside it. A global "accept all" or "take mine" toggle is asserted absent, because that is precisely the shape that silently discards an unrelated writer's change — the legacy `baseSha: "latest"` failure mode, one of the four reinforcing defects in `.planning/codebase/CONCERNS.md`. |
| T-00-37 | Repudiation | a partially resolved conflict reading as finished | mitigate | Resolution state is per file and legible, with at least one resolved and one unresolved row rendered simultaneously and visually distinguishable. A screen where partial resolution looks complete would let a publish proceed on an unreviewed file. |
| T-00-38 | Tampering | destructive delete without reassignment | mitigate | `O-category-reassign` requires choosing a destination; `delete anyway` and equivalents are asserted absent. The dialog quotes the real photo count from `data/portfolio_images.json`, so the cost stated is the actual cost. |
| T-00-29 | Information Disclosure | fixtures carrying non-public content | mitigate | Fixtures derive only from `data/*.json` — already committed public site content — and from the drafted case-study copy in `00-COPY/`, which is authored for publication. No credential, D1 connection string, GitHub PAT or Cloudflare Access value appears anywhere; none is needed, because nothing here authenticates or persists. The playground is gitignored. |
| T-00-23 | Denial of Service (performance) | overspending the hydration budget | mitigate | This plan takes zero `client:` directives, asserted per file. All four overlays are sketched open and static, which is what keeps 32 of about 35 sketches at 0 KB and the DS-09 measurement uncontaminated. |
| T-00-06 | Elevation of Privilege | the playground drifting into a Phase 2/7 foundation | mitigate | No auth, no D1, no GitHub API client, no endpoint, no adapter. D-24's `projects.json` extraction is anticipated by a fixture and explicitly not performed — `data/` is asserted unchanged. Plan 17 deletes the directory. |
| T-00-04 | Information Disclosure | accidental commit of the playground | mitigate | `.playground/` is gitignored, asserted in plan 01. Only the SUMMARY is committed. |
| T-00-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs no package; every component comes from the design-system tarball audited in plan 01. |
</threat_model>

<verification>
- `npx astro build` exits 0; `bash check-no-js.sh` exits 0; `node check-states.mjs` exits 0
- Zero `client:` directives across all four new routes — the playground still has exactly four hydrating pages
- The 401 modal offers re-authentication only, asserted by a negative grep over six bypass phrasings
- Conflict actions are per file, with no global toggle, and overwrite names what it drops
- Category delete cannot proceed without a destination, and quotes a real count
- All seven admin routes now exist, so plan 16's coverage gate has a complete input

</verification>

<success_criteria>
- All seven admin routes from D-05 plus D-24's implied seventh now exist as running sketches
- D-16's per-file conflict resolution is demonstrated, not described, and G-7 has a concrete requirements list
- The 401 screen sets a fail-closed pattern with no bypass affordance anywhere on it
- D-25's canonical category records make id and label visibly separate fields, fixing the drift surfaced on /admin/photos
- The hydration budget is still exactly four pages, so the DS-09 measurement remains readable
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-15-SUMMARY.md` when done.
Commit the SUMMARY only. Record in it the concrete G-7 requirements list — primitives stretched,
props a `DiffView` would need, and how per-file resolution state is expressed — plus any new
admin gap. Plan 16 transcribes all admin evidence into `00-FINDINGS.md` in one pass.
</output>
