---
phase: 00-design-ideation
plan: 23
type: execute
wave: 5
depends_on: ["00-19", "00-22"]
gap_closure: true
files_modified:
  - .playground/src/components/PhotoLayoutBoard.tsx
  - .playground/src/components/SortableReorder.tsx
  - .playground/src/pages/admin/photos/[...state].astro
  - .playground/src/fixtures/photos.json
  - .planning/phases/00-design-ideation/00-ADMIN-IA.md
autonomous: true
requirements: [DSGN-01, DSGN-04]

must_haves:
  truths:
    - "Photos are reordered in the masonry the public gallery actually uses, so the operator sees where a photo will sit rather than where an abstract tile sits"
    - "Every one of the 39 photos has an editable focal point, and the tile shows the crop live as it changes"
    - "A drag says in words which ordering field it writes, so an operator cannot conclude a reorder was lost"
    - "No local announcer was added to work around G-13, so the gap stays an upstream finding rather than becoming a silent local fix"
  artifacts:
    - path: ".playground/src/components/PhotoLayoutBoard.tsx"
      provides: "The real-layout reorder board: public masonry, drag order, live focal point, both order numbers per tile"
      contains: "activeCategory"
    - path: ".playground/src/fixtures/photos.json"
      provides: "39 records carrying focalPoint in the same shape as home_config.peekPositions"
      contains: "focalPoint"
    - path: ".planning/phases/00-design-ideation/00-ADMIN-IA.md"
      provides: "The recorded escalation of G-1 and G-13 from six peek slots to the whole gallery"
      contains: "G-1"
  key_links:
    - from: ".playground/src/pages/admin/photos/[...state].astro"
      to: ".playground/src/components/PhotoLayoutBoard.tsx"
      via: "the screen's single hydration slot, one directive line on the whole route"
      pattern: "client:load"
    - from: ".playground/src/components/PhotoLayoutBoard.tsx"
      to: ".playground/src/components/FocalPointSketch.tsx"
      via: "the focal control is reused, not rebuilt — that reuse is G-1's measured cost"
      pattern: "FocalPointSketch"
---

<objective>
Rebuild `/admin/photos`'s order view as the real public masonry, with a per-photo focal point
across all 39.

Purpose: the user asked to *"move and position photos as I want, and see them in the actual
view where i can move and see the position on actual final website"*. Confirmed as
**drag-to-reorder in the real public layout** plus a **focal point per photo, all 39** — and
explicitly **not** resize or span, so the public gallery's layout model stays intact.

This reopens `S-photos`, which plan 00-13 completed. Today the screen has a `DataGrid` list
view for metadata and an **abstract** `Sortable` grid for order. Neither shows the real
masonry, so neither answers *"where will this actually sit"* — which is the entire question the
direction asks. It is **consistent with D-07**, which already chose a preview that toggles to
full width rather than a split pane: this makes that preview editable rather than read-only.

Two existing findings escalate here and the escalation is part of the output, not a side
effect. **G-1** (no `FocalPointPicker` in the design system) was scoped to Home's six peek
slots and now covers all 39 photos, with plan 14's hand-built cost already measured at **269
non-comment lines**. **G-13** (`Sortable` has no accessibility passthrough) becomes the
**primary interaction** of the screen.

Output: one board that is the public layout, editable, with the findings written up rather than
worked around.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/00-design-ideation/00-13-SUMMARY.md
@.planning/phases/00-design-ideation/00-ADMIN-IA.md
@.planning/phases/00-design-ideation/00-19-SUMMARY.md
@.planning/phases/00-design-ideation/00-RESPONSIVE-CONTRACT.md
@.playground/src/components/SortableReorder.tsx
@.playground/src/components/FocalPointSketch.tsx
@.playground/src/pages/admin/photos/[...state].astro

<execution_environment>
**RUN THIS PLAN ON THE MAIN WORKING TREE. NOT IN A GIT WORKTREE.**

`.playground/` is gitignored (the D-02 fence) and does not propagate into a worktree.
**Tasks 1 and 2 produce NO COMMIT** — every file they touch is inside `.playground/`. Five
prior plans recorded the same thing. The durable output is task 3's committed amendment to
`00-ADMIN-IA.md` plus the screenshots plan 00-17 takes before deletion.

Only one playground plan runs at a time. Do not parallelise with another `.playground/` plan.

The legacy precedent is not checked out. Read it without leaving `main`:
  git show legacy/nextjs-portfolio:src/components/MasonryGrid.tsx
  git show legacy/nextjs-portfolio:src/components/admin/DraggableMasonry.tsx
</execution_environment>

<interfaces>
<!-- Read out of the playground this session. Use directly; do not go exploring. -->

`.playground/src/components/SortableReorder.tsx` — the island being replaced.
  export interface ReorderPhoto { id, title, category, order, src, width, height,
                                  pipeline: "processed"|"processing"|"failed" }
  export interface ReorderCategory { id, label, count }
  Props: { photos, staged?, categories, activeCategory? }
  Derives a per-axis order at mount from the existing global `order`, restricted per category,
  held in component state. Phase 3 names the real field.

`.playground/src/components/FocalPointSketch.tsx` — REUSE THIS, DO NOT REBUILD IT.
  export interface FocalSlot { id, title, category, src, width, height,
                               orientation: "wide"|"tall"|"exact",
                               position: string | null }
  Props: { slot?: FocalSlot | null, frameRatioW?: number, frameRatioH?: number }
  The frame ratio is ALREADY parameterised, so a non-3:2 frame needs no component change.
  Pointer events with capture; `tabIndex={0}` with arrow keys; Shift for coarse steps; `Home`
  resets to `50% 50%`; a `role="status" aria-live="polite"` readout; listeners aborted through
  an AbortController so an unmount mid-drag cannot leak them.
  No network request, no storage, no endpoint — the value lives in component state and is
  thrown away on navigation, and that is asserted by grep rather than promised in a comment.

`.playground/src/pages/admin/photos/[...state].astro`
  exports SCREEN, STATES (7 entries) and `getStaticPaths`; reads `Astro.params.state ??
  "populated"` and THROWS on a missing fixture key. Never `Astro.url.searchParams`.
  Renders: eight `Chip` category anchors (rendered label + raw stored key in mono, D-25),
  a `FileInput variant="dropzone"` that accepts a file and does nothing (D-02), a `DataGrid`
  over all 39 as the list view, the order grid over the 36 in the gallery with the 3 still
  processing in a separate STAGED strip, and two detail panels pinned to
  `architecture-redbuilding` (camera only) and `product-peppers` (no EXIF at all).
  Line ~468 carries the ONLY `client:load` in the file.
  Claims `T-loading-list` and `T-error-inline`; declares `inherits` for `T-dirty-badge`,
  `O-conflict-diff` and `T-success-published`.
  Extra route with no coverage cell: `E-category-filtered` at `/admin/photos/filtered-empty/`.

HYDRATION BUDGET, MECHANICAL — from plan 13's idiom, all four still binding:
  - EXACTLY ONE LINE of the .astro file may contain the directive string. The acceptance greps
    count LINES, not occurrences, so do not mention it in a comment.
  - RENDER THE ISLAND UNCONDITIONALLY. `check-no-js.sh` FAILS an allowlisted route that ships
    zero script tags, and every state route of an allowlisted screen is allowlisted. Pass empty
    data where the island has nothing to do and return null from the component.
  - Island props must be JSON-serializable. No functions.
  - A page carrying an island needs `<style is:global>`, not `<style>` — Astro's scoped styles
    stamp `data-astro-cid-*` onto elements in the Astro template, and a framework component
    renders its own DOM and never receives the attribute.

NEVER PUT A CONTROL INSIDE A SORTABLE TILE. `SortableItem` spreads dnd-kit's listeners onto
the item wrapper, so the tile IS the drag handle and carries `role="button"` and `tabindex=0`.
A nested button is invalid ARIA and unreachable by keyboard. This is a component constraint,
not taste.

D-22, as plan 13 rendered it, in words above the grid:
  no category active — "Dragging now writes Global order (order). Per-category orders are
                       untouched."
  category active   — "Dragging now writes Architecture order (the per-category order for
                       architecture). The global order is untouched, and so is every other
                       category."
Every tile carries both numbers (`global #5 · architecture #1`), so independence is visible on
the tiles rather than asserted in a caption.

Density: `density-compact.css` owns control heights and is scoped to compact AND
`pointer: fine`. Any control you author needs its own
`@media (pointer: coarse) { min-height: 44px }` on the HIT AREA, not the paint. Never gate on
width. Never use `any-pointer`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The real-layout board — public masonry, drag order, live focal point</name>
  <files>.playground/src/components/PhotoLayoutBoard.tsx, .playground/src/components/SortableReorder.tsx</files>
  <action>
Create `PhotoLayoutBoard.tsx` and **delete `SortableReorder.tsx`**. Replace rather than add: a
component named for the abstract grid it no longer renders is a name that lies, and the
hydration budget allows exactly one island on this screen. Carry `ReorderPhoto` and
`ReorderCategory` across unchanged where they still fit, plus a `focalPoint: string | null`
field per photo.

**The board renders the public gallery's masonry, not an abstract grid.** Read the legacy
precedent for the column model before writing anything —
`git show legacy/nextjs-portfolio:src/components/MasonryGrid.tsx` for the public layout and
`git show legacy/nextjs-portfolio:src/components/admin/DraggableMasonry.tsx` for the admin
variant. Match the public layout's column count and gap so the operator is looking at the real
composition. If the public sketch at `/photos` and this board disagree on columns at any device
class, that disagreement is the bug this whole plan exists to remove; make them agree and say
in the SUMMARY which one moved.

**Each tile applies its own focal point live** as `object-position`, so changing a crop changes
the tile in the masonry immediately. This is the literal request — *"see the position on actual
final website"*.

**Focal editing does not live on the tile.** `SortableItem` spreads dnd-kit's listeners onto
the item wrapper, so the tile is the drag handle and already carries `role="button"` and
`tabindex=0`; a control nested inside it is invalid ARIA and unreachable by keyboard. Selecting
a tile reveals a focal panel **beside the board**, rendered by this same island, holding
`FocalPointSketch` for the selected photo. The tile updates live from that panel. Write the
constraint and the resolution as a comment where the tile is defined, because the obvious
implementation is the forbidden one.

**Reuse `FocalPointSketch` unmodified.** Its frame ratio is already a prop, so pass the
photo's own aspect rather than 3:2. Do not fork it, do not reimplement it, do not copy its 86
lines of frame CSS. The reuse is the measurement: G-1's cost is 269 non-comment lines that
every consumer wanting a focal point has to write, and reusing them here is what shows the
absence is now load-bearing for the main gallery admin rather than for six peek slots.

**Carry D-22's sentence and both per-tile numbers across verbatim in substance.** A live
reorder that does not say which ordering field a drag writes leaves an operator to conclude a
reorder was lost. Per-category order stays modal on the active filter and stays derived at
mount from the existing global `order`; Phase 3 names the field.

**Do NOT add a local announcer.** `grep -qE 'aria-live|announcements|screenReaderInstructions'`
must still exit 1 against the board component. dnd-kit's `DndContext` already supplies
`defaultAnnouncements`, so a live region exists and speech happens — it just speaks raw record
slugs and never a position, and `Sortable`'s prop surface
(`{items, onReorder, renderItem, id, className, style}`) gives a consumer no way to replace it.
The fix is to **expose** an announcer upstream, not to bolt one on here. Adding a third
`aria-live` region to a page that already has one per `DndContext` produces duplicate speech
and converts an upstream finding into a silent local workaround, which PROJECT.md's Core Value
forbids. `FocalPointSketch`'s own `role="status"` readout is not affected by this rule — it is
the component's own, it was there before, and it is not a reorder announcement.

**Return `null` on states where the board has nothing to do**, so the route can render the
island unconditionally and still satisfy `check-no-js.sh`. Props stay JSON-serializable: no
functions.

Every control the board authors gets `@media (pointer: coarse) { min-height: 44px }` on the hit
area, never on the paint. Never gate on width. Never use `any-pointer`.
  </action>
  <verify>
    <automated>cd .playground && test -f src/components/PhotoLayoutBoard.tsx && test ! -f src/components/SortableReorder.tsx && grep -q 'FocalPointSketch' src/components/PhotoLayoutBoard.tsx && ! grep -qE 'aria-live|announcements|screenReaderInstructions' src/components/PhotoLayoutBoard.tsx && test "$(grep -v '^[[:space:]]*[/*]' src/components/PhotoLayoutBoard.tsx | grep -c 'any-pointer')" = "0" && grep -q 'pointer: coarse' src/components/PhotoLayoutBoard.tsx && npx tsc --noEmit -p tsconfig.json && echo BOARD_OK</automated>
  </verify>
  <done>`PhotoLayoutBoard.tsx` exists and `SortableReorder.tsx` is gone; the board reuses `FocalPointSketch` with a per-photo frame ratio; no local announcer was added; the coarse-pointer floor is present and `any-pointer` is not; the project typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the board into the route and give all 39 a focal point</name>
  <files>.playground/src/pages/admin/photos/[...state].astro, .playground/src/fixtures/photos.json</files>
  <action>
**Extend the fixture.** Add `focalPoint` to every one of the 39 records in
`src/fixtures/photos.json`, in the same `"50% 25%"` shape `home_config.peekPositions` uses —
per plan 00-19 schema decision 6, which cites that precedent so the two do not diverge into two
shapes for one concept. Default `50% 50%`; give a handful of records a non-default value so the
board demonstrably renders different crops rather than 39 identical ones. Generate the edit with
a throwaway script from the existing fixture, **count what you changed and print the count**,
and keep every existing value byte-identical.

The real `data/portfolio_images.json` is **not** touched. Phase 0 writes no production code and
Phase 3's schema module owns the migration; 39 empty fields in the committed manifest would
read as a completed migration that has not happened.

**Swap the island.** Replace the `SortableReorder` usage with `PhotoLayoutBoard` at the same
site. Keep exactly one line carrying the directive string in the whole file, and keep the
island rendered unconditionally with empty data on states where it has nothing to do — an
allowlisted route that ships zero script tags FAILS `check-no-js.sh`, and every state route of
an allowlisted screen is allowlisted.

**Everything else on the screen survives.** All seven `STATES`, the extra
`E-category-filtered` path with no coverage cell, the eight `Chip` category anchors with their
raw stored keys in mono, the `FileInput variant="dropzone"` that does nothing, the `DataGrid`
list view over all 39, the staged strip holding the 3 still processing, the two pinned detail
panels, and the two claimed treatments `T-loading-list` and `T-error-inline`. The list view
stays: `DataGrid` is for comparing records and finding one record, the board is for order and
for anything visual or spatial, and the screen legitimately has both. Keep them stacked with an
`Eyebrow` naming each and the existing comment that production switches them with a
`SegmentedControl`.

Update the `GRID VIEW / ORDER` section head copy so it describes the board rather than the
abstract grid — it should say the operator is looking at the public layout.

The route already needs `<style is:global>` because it carries an island; confirm any new rule
that must reach inside the board is in a global block, not a scoped one.
  </action>
  <verify>
    <automated>cd .playground && node -e "const fx=require('./src/fixtures/photos.json');const seen=new Map();(function walk(v){if(Array.isArray(v))return v.forEach(walk);if(v&&typeof v==='object'){if(typeof v.id==='string'&&typeof v.title==='string')seen.set(v.id,v);Object.values(v).forEach(walk)}})(fx);const all=[...seen.values()];const miss=all.filter(p=>!('focalPoint' in p));if(miss.length){console.error('NO focalPoint on',miss.length,'of',all.length,miss.slice(0,5).map(p=>p.id).join(','));process.exit(1)}if(all.length<39){console.error('EXPECTED >=39 RECORDS, GOT',all.length);process.exit(1)}console.log('FOCAL_ON_ALL',all.length)" && test "$(grep -c 'client:' 'src/pages/admin/photos/[...state].astro')" = "1" && grep -q 'PhotoLayoutBoard' 'src/pages/admin/photos/[...state].astro' && test "$(grep -c 'SortableReorder' 'src/pages/admin/photos/[...state].astro')" = "0" && grep -q 'style is:global' 'src/pages/admin/photos/[...state].astro' && rm -rf dist && npx astro build && bash check-no-js.sh && node check-states.mjs && node check-coverage.mjs && git -C .. diff --quiet -- data/portfolio_images.json && echo WIRED_OK</automated>
  </verify>
  <done>Every fixture photo carries `focalPoint`; the route renders `PhotoLayoutBoard` with exactly one directive line and no reference to the deleted component; all seven states plus `filtered-empty` still build; `check-no-js.sh`, `check-states.mjs` and `check-coverage.mjs` pass; the real manifest is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Escalate G-1 and G-13 in writing, audit in a browser, prove the controls</name>
  <files>.planning/phases/00-design-ideation/00-ADMIN-IA.md</files>
  <action>
**Amend `00-ADMIN-IA.md`** with a section recording the reopening of `S-photos` and the two
escalations. Do **not** add rows to `00-FINDINGS.md`: that file states its own scope rule —
findings outside the sixteen go in SUMMARYs, not as new rows, because the tiers bound Phase 1's
and Phase 7's scope, and plan 16 refused permission to add rows for exactly this reason.
Escalating an existing row's *scope* is not a new row; record it here and in the SUMMARY and
leave the register's denominator alone.

Record:

- **Why `S-photos` reopened**, in the user's terms, and why it is **consistent with D-07**
  rather than a contradiction: D-07 already chose a preview that toggles to full width rather
  than a split pane, and this makes that preview editable rather than read-only.
- **G-1's escalation.** Was: no focal-point picker, scoped to Home's six peek slots. Now:
  load-bearing for the main gallery admin across all 39 photos. Cite plan 14's measured cost —
  269 non-comment lines of `FocalPointSketch.tsx`, of which 86 are frame CSS ported from
  `legacy:src/styles/admin.css` — and state that this plan **reused** those lines rather than
  writing a second copy, which is what makes the absence measurable rather than theoretical.
  Note that the tier is unchanged; only the blast radius is.
- **G-13's escalation.** Was: an accessibility gap on a secondary reorder control. Now: the
  **primary interaction** of the screen. Restate it correctly, as plan 13 corrected it —
  dnd-kit's `DndContext` **does** supply `defaultAnnouncements`, so a live region exists and
  speech happens; it speaks raw record slugs, never a title and never a position, and
  `Sortable`'s props give a consumer no way to replace it. The fix is **expose an announcer**,
  not "pass one". State the consequence plainly: **Phase 7's photo positioning depends on G-13
  landing in Phase 1**, and record that no local announcer was added here.
- **The D-22 axis statement as shipped**, so Phase 7 does not have to rediscover that a live
  reorder must name the field it writes.
- **The board's column model** and whether `/photos` or the board moved to make them agree.

**Then audit in a browser, not by grep:**

    node audit15.mjs /admin/photos/ /admin/photos/empty/ /admin/photos/loading/ \
                     /admin/photos/error/ /admin/photos/filtered-empty/ /photos/

Two of this phase's recorded 44px measurements were wrong because a 1px visually-hidden input
masks the label that is the real hit area. Record the offender count per route per class and
fix everything the board owns with a coarse-pointer block on the hit area. Report the R-6
content count at 344 against 1440 — categories, tiles, staged rows — and assert nothing dropped.

**Then drive the keyboard path**, as plan 13 did, in Chromium against the built `dist/`: focus
a tile, `Space`, `ArrowDown`, `Space`, and record what is announced verbatim. This is how G-13's
escalation stays a measurement rather than a restatement. Also drive `FocalPointSketch` by
keyboard on a selected photo and record the before/after `object-position` values, confirming
the tile in the masonry moved with it.

**Two negative controls, each restored to a byte-identical SHA-256.** Capture the hash before,
compare after. Assert on behaviour, not on `grep -c` of the edited string — `grep -c` counts
LINES not matches and this phase already had a control nearly report a false result that way
(plan 16, control 4).

- **Control A** — remove the `focalPoint` key from one fixture record. Expect the board to fall
  back to `50% 50%` **without throwing**, since the field is optional by schema decision 6, and
  expect the acceptance check in task 2 to fail. A control that proves the check, not the app.
- **Control B** — add a second `client:` directive line to the route. Expect `check-no-js.sh` or
  the one-line grep to fail. This guards the hydration budget, which is the constraint most
  likely to erode as the screen grows.
  </action>
  <verify>
    <automated>grep -q "G-13" .planning/phases/00-design-ideation/00-ADMIN-IA.md && grep -q "expose" .planning/phases/00-design-ideation/00-ADMIN-IA.md && grep -q "269" .planning/phases/00-design-ideation/00-ADMIN-IA.md && grep -q "D-07" .planning/phases/00-design-ideation/00-ADMIN-IA.md && test "$(grep -c '^| \*\*G-' .planning/phases/00-design-ideation/00-FINDINGS.md)" = "16" && cd .playground && npx astro build && node audit15.mjs /admin/photos/ /photos/ && echo ESCALATION_RECORDED</automated>
  </verify>
  <done>`00-ADMIN-IA.md` records the reopening, both escalations with their measured costs, the D-07 consistency argument and the Phase 7 dependency on G-13; `00-FINDINGS.md` still has exactly sixteen rows; the browser audit and the keyboard drive are recorded with verbatim announcements; both controls bite and both restores are SHA-256-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an editable preview → the published gallery | The board's whole claim is that what the operator sees is where the photo lands. A board that disagrees with `/photos` is worse than an abstract grid, because it looks authoritative. |
| a design-system gap → a local fix | G-1 and G-13 are both one convenient patch away from becoming invisible. |
| `00-FINDINGS.md`'s sixteen rows → Phase 1 and Phase 7 scope | The tiers bound two other phases' commitments. A new row changes them without a decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-67 | Spoofing | an "actual view" board whose column model differs from `/photos` | mitigate | The board's column count and gap are matched to the public sketch, the disagreement is resolved in one direction, and the SUMMARY records which one moved. A board that lies about placement is the failure this plan exists to remove. |
| T-00-68 | Elevation of privilege | a local announcer added to work around G-13 | mitigate | `grep -qE 'aria-live\|announcements\|screenReaderInstructions'` must exit 1 on the board, matching plan 13's precedent. The gap is escalated in writing with the Phase 7 dependency stated, so the pressure to patch it locally is answered by a scheduled upstream fix. |
| T-00-69 | Tampering | `00-FINDINGS.md` gaining rows for escalations that are scope changes, not new gaps | mitigate | The register's row count is an acceptance gate at exactly sixteen. Escalations are recorded in `00-ADMIN-IA.md` and the SUMMARY, per the file's own fixed-denominator rule. |
| T-00-70 | Denial of service | a control nested inside a sortable tile, unreachable by keyboard | mitigate | Focal editing lives in a panel beside the board, not on the tile, because `SortableItem` makes the tile the drag handle with `role="button"` and `tabindex=0`. The constraint and its resolution are commented at the tile definition, where the forbidden implementation is the obvious one. |
| T-00-71 | Tampering | the hydration budget eroding as the screen grows | mitigate | Exactly one line may carry the directive string, asserted by a line count; control B adds a second and proves the check fails. |
</threat_model>

<verification>
- `PhotoLayoutBoard.tsx` exists, `SortableReorder.tsx` is gone, the route references only the former, and exactly one line carries a hydration directive
- All 39 fixture records carry `focalPoint`; `data/portfolio_images.json` is untouched in git
- `astro build`, `check-no-js.sh`, `check-states.mjs`, `check-coverage.mjs`, `check-no-ivory.sh`, `check-theme-exhaustive.mjs`, `check-font-names.mjs`, `check-contrast.mjs` and `check-css-size.mjs` all exit 0; `check-bundle.mjs` still exits 1 as finding G-15
- `audit15.mjs` reports zero undersized targets owned by this plan across the five photos routes at all six classes
- The keyboard drive is recorded verbatim, and `FocalPointSketch`'s arrow-key edit is shown moving the tile's `object-position` in the masonry
- `00-FINDINGS.md` still carries exactly sixteen `G-` rows
- No `aria-live`, `announcements` or `screenReaderInstructions` in the board component

## Reachability

The board is reachable at `/admin/photos/` and at all six of the screen's other emitted paths;
the focal panel is reachable by selecting any tile, and every tile is keyboard-focusable
because `SortableItem` gives it `tabindex=0`. The `focalPoint` field is reachable because it is
present on every fixture record and rendered as `object-position` on every tile, so an
unwritten value would be visible rather than silent.
</verification>

<success_criteria>
- Reordering happens in the public masonry, and the board and `/photos` agree on the column model
- All 39 photos have an editable focal point, reusing `FocalPointSketch` rather than a second implementation
- A drag states which ordering field it writes, and each tile carries both of its numbers
- G-1 and G-13 escalated in writing with measured evidence, no rows added to the sixteen, no local announcer
- One island, one directive line, every state still building
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-23-SUMMARY.md` when done. Tasks 1 and 2 produce
no commit — say so and cite the D-02 fence. Quote the keyboard announcements verbatim, as plan
13 did; a paraphrase is what let G-13's original wording be wrong for four plans.
</output>
