---
phase: 00-design-ideation
plan: 25
type: execute
wave: 7
depends_on: ["00-24"]
gap_closure: true
files_modified:
  - .planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md
  - .planning/phases/00-design-ideation/00-17-PLAN.md
  - .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04]

must_haves:
  truths:
    - "The screenshot record has a written contract covering six device classes, so plan 00-17 captures a responsive design rather than a 1440 design photographed twice"
    - "Plan 00-17's three stale automated assertions match the six-class contract, so the phase-exit gate can actually pass"
    - "Plan 00-17 is still OPEN with its three tasks, its checkpoint and its deletion fence untouched"
    - "The human reviewer at plan 00-11 knows which artefacts changed since that plan was written, so the review is against what exists rather than what existed"
  artifacts:
    - path: ".planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md"
      provides: "The six-class capture policy, filename grammar, mode rules and derived file count"
      contains: "344|390|768|841|1024|1440"
    - path: ".planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md"
      provides: "The re-review brief for plan 00-11: what changed under the rework and what still needs an eye"
      contains: "## Re-review brief"
  key_links:
    - from: ".planning/phases/00-design-ideation/00-17-PLAN.md"
      to: ".planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md"
      via: "the amended assertions implement the contract's grammar and floor"
      pattern: "1024|1440"
    - from: ".planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md"
      to: ".playground/src/lib/artefacts.mjs"
      via: "the capture list is derived from CANONICAL_IDS and each route's STATES, never hand-listed"
      pattern: "CANONICAL_IDS"
---

<objective>
Write the six-class screenshot contract, bring plan 00-17's three stale assertions onto it, and
brief the human reviewer on what the rework changed.

Purpose: `00-RESPONSIVE-CONTRACT.md` §9 multiplies the screenshot record from two viewports to
six device classes, and states plainly that **three of plan 00-17's automated assertions break**
— the `(1440|390)` filename regex, the `SHOTS -ge 35` floor, and the check that every `X-` id
has both a `-dark-1440.png` and a `-dark-390.png`. Left as they are, plan 00-17 cannot pass:
its regex assertion fails the moment a `-dark-344.png` exists, which under the six-class policy
is immediately. That would fail the phase-exit gate for a reason that has nothing to do with
the design.

**Plan 00-17 is not replanned by this plan.** Its three tasks, its one human checkpoint, its
`autonomous: false`, its six review passes, its full-page-capture assertion, its
admin-never-dark assertion and its deletion fence are untouched. What changes is three viewport
literals and one floor — an assertion amendment, isolated into its own task so it can be
declined on its own.

Output: a contract plan 00-17's task 1 can implement, an amended gate it can pass, and a brief
plan 00-11's reviewer can use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/00-design-ideation/00-RESPONSIVE-CONTRACT.md
@.planning/phases/00-design-ideation/00-17-PLAN.md
@.planning/phases/00-design-ideation/00-11-PLAN.md
@.planning/phases/00-design-ideation/00-20-SUMMARY.md
@.planning/phases/00-design-ideation/00-21-SUMMARY.md
@.planning/phases/00-design-ideation/00-22-SUMMARY.md
@.planning/phases/00-design-ideation/00-23-SUMMARY.md
@.planning/phases/00-design-ideation/00-24-SUMMARY.md

<interfaces>
<!-- Read out of the committed plans and the contract. Use directly. -->

`00-17-PLAN.md` — OPEN, `autonomous: false`, 3 tasks, wave 12, `depends_on: ["00-16"]`.
  Task 1  "Capture the screenshot record"        type auto
  Task 2  "The six review passes, and sign-off to destroy the playground"  CHECKPOINT
  Task 3  "Delete the playground and assert the fence holds"               type auto
  Frontmatter must_haves artifacts: screenshots dir with `min_files: 35`
  Key link: capture list derived from `CANONICAL_IDS` and each route's `STATES`, so a new
            artefact cannot be missed.

  THE FOUR STALE LITERALS, all of them viewport or count, none of them behavioural:
    task 1 [automated]   test "$SHOTS" -ge 35
    task 1 [automated]   grep -vE '^00-[SETOPRX]-[a-z0-9-]+-[a-z0-9-]+-(light|dark)-(1440|390)\.png$'
    task 1 [done]/criteria prose  "Every filename matches ^...-(1440|390)\.png$"
    task 3 [automated]   [ "$SHOTS" -ge 35 ]
    frontmatter           min_files: 35

  UNAFFECTED AND NOT TO BE TOUCHED: the full-page-capture assertion; the admin-never-dark
  assertion (`ls … | grep -E '^00-[SETOPR]-' | grep -c dark` prints 0, which §9 says survives
  verbatim); the `.playground` deletion and every clause of the fence check
  (`@astrojs/cloudflare` absent, no `package.json`, no `src/pages/api`, no `wrangler.toml`);
  the four-document existence check; the ONE checkpoint task's type (task 2 only — tasks 1 and 3
  are `type="auto"`, and the plan is `autonomous: false` because of task 2 alone).

`00-RESPONSIVE-CONTRACT.md` §9 — canonical capture sizes and the per-class policy:
  344 x 882 · 390 x 844 · 768 x 1024 · 841 x 768 · 1024 x 768 · 1440 x 900

  class of artefact                              captured at        why not all six
  S- E- T- O-  (admin desktop, 29 artefacts)     1440 only          the admin has one user, at
                                                                    a laptop; density is decided
                                                                    by pointer and pointer has
                                                                    TWO values, not six
  P- R-        (admin phone, 6 artefacts)        390 + 344          344 is the class that
                                                                    actually breaks a phone
                                                                    layout
  X- public work-recolour, work, photos          all six            responsive is the hard
                                                                    requirement and the audience
                                                                    is on unknown hardware
  X-home state A and state B                     all six, both      the transition IS the
                                                                    artefact; one state is not
                                                                    evidence of two
  X-case  longest and shortest study             all six            two studies bracket the
                                                                    measure; five x six would be
                                                                    thirty files photographing
                                                                    one template
  X-contact-sheet                                1440 only          review chrome, not a design

  New regex:
  ^00-[SETOPRX]-[a-z0-9-]+-[a-z0-9-]+-(light|dark)-(344|390|768|841|1024|1440)\.png$

  Mode is unchanged and non-negotiable: every admin file `-light-`, every public file `-dark-`.
  Floor: >= 80.

WHAT THE REWORK CHANGED IN THE `X-` SET, which §9 was written before:
  removed  X-case-long · X-case-short              (plan 00-20)
  added    X-case-design-system · X-case-cairn · X-case-hued · X-case-momentum ·
           X-case-timeshift                        (plan 00-20)
  added    X-home  — the two-state landing         (plan 00-22)
  kept     X-home-act2 — plan 09's OQ-1 resolution, now also rendered inside X-home's state B
           through one shared component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the six-class screenshot contract</name>
  <files>.planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md</files>
  <action>
Create `00-SCREENSHOT-CONTRACT.md` as the executable specification plan 00-17's task 1
implements. It must be complete enough that the capture script can be written from it without
re-reading `00-RESPONSIVE-CONTRACT.md` §9.

Carry across, verbatim in substance:

- **The six canonical capture sizes**, with the width and the height, and the note that 841 x 768
  is the single most demanding capture in the matrix because class 3 is the only class whose
  aspect band contains 1.0.
- **The per-class capture policy table**, with the *reason* for each asymmetry. The reasons are
  the load-bearing part: without them the next person captures 116 extra admin files to measure
  a distinction that does not exist, because pointer has two values and not six.
- **The filename grammar** and the new regex.
- **The mode rules.** Every admin file `-light-`, every public file `-dark-`. Non-negotiable.
- **The floor**, >= 80.

Then add what §9 could not know, because the rework happened after it was written:

- **The amended `X-` inventory**, listing every current public artefact id and its capture
  policy. Resolve the four ids §9 does not cover, each with a stated reason rather than a
  default:
  - the **five** `X-case-*` ids that replaced the two tier ids — two studies at all six classes
    and three at 1440 only, choosing the longest and the shortest **after compression**, which
    is a different pair than before plan 00-18 ran, so name the pair by measurement rather than
    by memory;
  - `X-home`, both states, all six;
  - `X-home-act2`, 1440 only, because it is the same composition `X-home` already carries at
    six classes through one shared component, so six captures would photograph one design twice.
- **The derived total**, computed from the policy rather than asserted, with the arithmetic
  shown so the next person can check it. Note that it lands above 80 and that 80 is a floor, not
  a target — a contract whose count is asserted rather than derived goes stale the first time an
  artefact is added.
- **The derivation rule, and it is the one that matters most:** the capture list comes from
  `CANONICAL_IDS` and each route's own `STATES` export, **never from a hand-listed URL set.**
  That is already plan 00-17's own stated key link, and it is what makes a new artefact
  impossible to miss. State the failure it prevents: a hand-listed set silently omits whatever
  was added last, which is always the least-reviewed thing.
- **How `X-home`'s two states are captured**, since both live at one URL: state A at scroll 0,
  state B after scrolling exactly one viewport height. Name the state slot values used in the
  filename so they are stable rather than invented per run.

Write this as a contract, not as a description. Every line should be something the capture
script can be checked against.
  </action>
  <verify>
    <automated>test -f .planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md && grep -q '344|390|768|841|1024|1440' .planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md && grep -q 'CANONICAL_IDS' .planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md && grep -q 'X-home-act2' .planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md && grep -q 'X-case-cairn' .planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md && grep -qE '(^|[^0-9])80([^0-9]|$)' .planning/phases/00-design-ideation/00-SCREENSHOT-CONTRACT.md && echo CONTRACT_WRITTEN</automated>
  </verify>
  <done>The contract carries the six sizes, the per-class policy with its reasons, the new regex, the mode rules, the >= 80 floor, the amended `X-` inventory including the five case ids and both home artefacts, a shown-arithmetic total, the derive-from-`CANONICAL_IDS` rule and the two-state capture method.</done>
</task>

<task type="auto">
  <name>Task 2: Amend plan 00-17's four stale literals, and nothing else</name>
  <files>.planning/phases/00-design-ideation/00-17-PLAN.md</files>
  <action>
**This task edits an OPEN plan. It is deliberately isolated so it can be declined on its own
without losing task 1 or task 3.** If the developer would rather amend `00-17-PLAN.md` by hand
at execution time, skip this task and say so in the SUMMARY — task 1's contract is what makes
that hand-amendment a two-minute job.

Change **exactly four literals**, all of them viewport or count, none of them behavioural:

1. Task 1's `[automated]` — the filename regex `(1440|390)` becomes
   `(344|390|768|841|1024|1440)`.
2. Task 1's `[automated]` — `test "$SHOTS" -ge 35` becomes `-ge 80`.
3. Task 3's `[automated]` — `[ "$SHOTS" -ge 35 ]` becomes `-ge 80`.
4. Frontmatter `must_haves.artifacts` — `min_files: 35` becomes `min_files: 80`.

Plus the one prose line in task 1's acceptance criteria that quotes the old regex, so the
criteria and the command do not disagree — a plan whose prose and command state different
contracts is one an executor resolves by guessing.

Also update task 1's `[done]`/criteria clause that reads *"every `X-` id has both a
`-dark-1440.png` and a `-dark-390.png`"* to the six-class form, deferring to
`00-SCREENSHOT-CONTRACT.md` for the per-artefact policy rather than restating the whole table
inside a plan file. Add an `@` reference to the contract in 00-17's `<context>` so its executor
reads the policy rather than inferring it.

**Everything else stays byte-identical.** Specifically, do not touch:

- the checkpoint task's `type`, or `autonomous: false`
- the admin-never-dark assertion — §9 says it survives verbatim
- the full-page-capture assertion
- any clause of the deletion fence: the `git status --porcelain -- .playground` check, the
  `rm -rf .playground`, `test ! -d .playground`, the `@astrojs/cloudflare` grep, `test ! -f
  package.json`, `test ! -d src/pages/api`, `test ! -f wrangler.toml`
- the four-document existence check
- the six review passes in task 2
- the plan's `wave`, `depends_on` or `phase`

**Do not create `00-17-SUMMARY.md`, do not mark 00-17 complete, and do not tick it in
`ROADMAP.md`.** It stays OPEN. Assert all three.

Verify the edit with a diff rather than by reading the file back: capture `git diff --stat` and
confirm the changed-line count is small and confined to this file, and assert structurally that
the task count is still 3, that one task is still a checkpoint, and that `autonomous: false`
is unchanged. Do not verify with `grep -c` on the changed strings — `grep -c` counts LINES not
matches, and this phase already had a control nearly report a false result that way (plan 16,
control 4).
  </action>
  <verify>
    <automated>P=.planning/phases/00-design-ideation/00-17-PLAN.md && test "$(grep -c '344|390|768|841|1024|1440' $P)" -ge 2 && test "$(grep -c '(1440|390)' $P)" = "0" && test "$(grep -c 'ge 35' $P)" = "0" && test "$(grep -c 'min_files: 80' $P)" = "1" && test "$(grep -c '<name>Task' $P)" = "3" && test "$(grep -c 'type="checkpoint' $P)" = "1" && grep -q '^autonomous: false' $P && grep -q 'rm -rf .playground' $P && grep -q 'wrangler.toml' $P && grep -q "grep -E '\^00-\[SETOPR\]-'" $P && test ! -f .planning/phases/00-design-ideation/00-17-SUMMARY.md && grep -q '00-17-PLAN.md' .planning/ROADMAP.md && test "$(grep -c '\[x\] 00-17-PLAN.md' .planning/ROADMAP.md)" = "0" && echo AMENDED_AND_STILL_OPEN</automated>
  </verify>
  <done>The four literals and the two prose clauses are updated, the contract is referenced from 00-17's context, the task count is still 3 with 1 checkpoint and `autonomous: false`, every fence clause and the admin-never-dark assertion are intact, no SUMMARY exists and the roadmap still shows 00-17 unticked.</done>
</task>

<task type="auto">
  <name>Task 3: Brief the human reviewers on what the rework changed</name>
  <files>.planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md</files>
  <action>
Plans 00-11 and 00-17 are the phase's two human checkpoints and both are still OPEN. Plan 00-11
was written to review artefacts that have since changed underneath it —
`00-RESPONSIVE-CONTRACT.md` §11 says so explicitly: *"No verdict on plan 00-11's three by-eye
judgements. They are unanswered, they require a human eye, and the artefacts they judge are
about to change. Plan 00-11 remains OPEN and must re-run against the reworked sketches."*
A reviewer opening 00-11 today would look for `/case/long` and `/case/short`, which no longer
exist.

Append a `## Re-review brief` section to `00-PUBLIC-DESIGN-NOTES.md`. **This is a brief for
00-11, not a replacement of it** — do not answer its three by-eye judgements, do not edit
`00-11-PLAN.md`, and do not create a summary for it.

Record:

- **Route map, before and after.** `/case/long` and `/case/short` are gone; `/work/{id}` x 5
  exist; `/home` is new; `/work`, `/photos`, `/home-act2` and `/` survive. A reviewer needs the
  URL list before anything else.
- **What plan 00-11's task 2 now reviews.** It was written as "the two case-study templates
  against real drafted copy". It is now one template against five compressed studies at
  500-700 words. The judgement it asks for — does this read as deliberate rather than as lorem
  — is unchanged; the artefact is not.
- **What is newly in front of the eye**, and it is the substance of this phase's rework: the
  Home two-state landing at six classes, the compressed case studies, the real-layout photo
  board with per-photo focal points, and the responsive shell. Each named with the plan that
  built it and where to look.
- **What is deliberately still broken, so a reviewer does not report it as a finding.**
  D-16-1 stays open: `AppBar`'s brand link and `Footer`'s link list are under the 44px floor on
  every public route, they paint their own geometry inside the design system, and their owners
  are already assigned — Phase 1 for the geometry with G-2. G-13's announcer speaks record
  slugs and no position on the photo board, and Phase 1 owns the passthrough. `check-bundle.mjs`
  still exits 1, which is finding G-15. Every one of these is deliberate, and a reviewer who
  does not know that spends the review re-finding them.
- **The three `00-RESPONSIVE-CONTRACT.md` §10 items that are still confirm-or-override**, with
  their stated positions, so silence at the review is a decision and not an omission. R-1
  (500-700 words), R-2 (state B continues below the fold) and R-3 (`/work/{id}`) were all acted
  on by this rework; note that acting on them does not confirm them, and that overriding R-2 in
  particular means Home's content has to shrink, which is a content decision no plan here is
  authorised to take.
- **The `00-SCREENSHOT-CONTRACT.md` pointer**, so the reviewer at plan 00-17 knows the record
  they will be walking is six classes rather than two, and roughly how many files that is.
  </action>
  <verify>
    <automated>grep -q "## Re-review brief" .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md && grep -q "work/cairn\|/work/{id}" .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md && grep -q "D-16-1" .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md && grep -q "G-15" .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md && grep -q "R-2" .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md && grep -q "00-SCREENSHOT-CONTRACT" .planning/phases/00-design-ideation/00-PUBLIC-DESIGN-NOTES.md && test ! -f .planning/phases/00-design-ideation/00-11-SUMMARY.md && git diff --quiet -- .planning/phases/00-design-ideation/00-11-PLAN.md && echo BRIEF_OK</automated>
  </verify>
  <done>The design notes carry a re-review brief with the before/after route map, what each of 00-11's tasks now reviews, what is deliberately still broken, the three open confirm-or-override items and a pointer to the screenshot contract; `00-11-PLAN.md` is unmodified and has no summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the screenshot record → every phase after this one | The playground is deleted at phase exit. The PNGs are the only surviving evidence of the design. |
| an amendment to an open plan → that plan's checkpoints | An edit that widens beyond the stale literals silently changes what a human is being asked to approve. |
| a human reviewer's time → known-open findings | A reviewer who does not know what is deliberately broken spends the review re-discovering it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-78 | Denial of service | plan 00-17 failing its own gate on a stale viewport literal | mitigate | Four literals are amended to the six-class contract, and the amendment is verified by structure (task count, checkpoint count, `autonomous: false`, every fence clause present) rather than by reading the file back. |
| T-00-79 | Tampering | an assertion amendment widening into a replan of a human checkpoint | mitigate | The amendment is isolated in its own task with an explicit decline path; the unaffected assertions are enumerated as must-not-touch; `00-17-SUMMARY.md` must not exist and the roadmap entry must stay unticked, both asserted. |
| T-00-80 | Repudiation | a capture list hand-written instead of derived, silently omitting the newest artefact | mitigate | The contract states the derive-from-`CANONICAL_IDS`-and-`STATES` rule and names the failure it prevents. It is already plan 00-17's own key link, so the two agree. |
| T-00-81 | Information disclosure | a reviewer reporting D-16-1, G-13 and G-15 as new findings | mitigate | The re-review brief lists what is deliberately still broken, with owners, so review time goes to what has not been looked at. |
| T-00-82 | Tampering | the derived screenshot total asserted as a constant and going stale | mitigate | The contract shows the arithmetic and states that 80 is a floor, not a target; the count is recomputed from the policy rather than transcribed. |
</threat_model>

<verification>
- `00-SCREENSHOT-CONTRACT.md` exists and covers six sizes, the per-class policy with reasons, the regex, mode rules, the >= 80 floor, the amended `X-` inventory and the two-state capture method
- `00-17-PLAN.md` contains no `(1440|390)` and no `-ge 35`; it still has 3 tasks, 1 checkpoint, `autonomous: false`, the admin-never-dark assertion and every clause of the deletion fence
- No `00-17-SUMMARY.md` and no `00-11-SUMMARY.md` exist; `00-11-PLAN.md` is unmodified in git; neither is ticked in `ROADMAP.md`
- `00-PUBLIC-DESIGN-NOTES.md` carries the re-review brief with the route map, the deliberate-breakage list and the three open confirm-or-override items

## Reachability

The contract is reachable from `00-17-PLAN.md`'s `<context>` via an `@` reference added in task
2, so its executor reads it rather than inferring the policy. The re-review brief is reachable
from `00-PUBLIC-DESIGN-NOTES.md`, which `00-11-PLAN.md` already lists in `files_modified` and
already writes its outcome into, so the reviewer opens that file anyway.
</verification>

<success_criteria>
- A screenshot contract complete enough to write the capture script from, with every asymmetry justified
- Plan 00-17's four stale literals amended and nothing else, verified structurally
- Plans 00-11 and 00-17 both still OPEN, unticked, with no summaries and no checkpoint changes
- A re-review brief that tells the human what changed, what is deliberately broken and what still needs a decision
</success_criteria>

<output>
Create `.planning/phases/00-design-ideation/00-25-SUMMARY.md` when done. State explicitly
whether task 2 was executed or declined, and quote the `git diff --stat` for `00-17-PLAN.md` so
the size of the amendment is on the record.
</output>
