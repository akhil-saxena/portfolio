---
phase: 02-astro-foundation-fail-closed-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md
autonomous: false
requirements: [FND-07]

must_haves:
  truths:
    - "akhilsaxena.com is confirmed to be on Cloudflare-managed nameservers, so a Worker Custom Domain is possible at all"
    - "An R2 custom domain is provisioned in front of the portfolio-photos bucket"
    - "A photo fetched twice through that custom domain returns cf-cache-status: HIT on the second request"
    - "A non-apex Worker hostname is decided and recorded, so 02-03 can declare it as a custom_domain route and Access can be applied to it"
    - "The uncached behaviour of the pub-*.r2.dev origin is measured, not assumed, so the migration has a before/after record"
  artifacts:
    - path: ".planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md"
      provides: "Recorded DNS/nameserver evidence, the provisioned R2 custom domain hostname, and the cache-status before/after measurement"
      contains: "cf-cache-status"
      min_lines: 50
  key_links:
    - from: ".planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md"
      to: "the R2 custom domain hostname Phase 3 CONT-04 rewrites 39 manifest URLs to"
      via: "a single explicitly named canonical image host line"
      pattern: "R2_PUBLIC_URL"
    - from: ".planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md"
      to: "wrangler.jsonc routes[].pattern with custom_domain true, written by plan 02-03"
      via: "a single canonical WORKER_CUSTOM_DOMAIN line naming a non-apex subdomain"
      pattern: "WORKER_CUSTOM_DOMAIN"
---

<objective>
Resolve the two longest-lead items in the entire project before any code exists: confirm
`akhilsaxena.com` is on Cloudflare-managed nameservers, and provision an R2 custom domain in
front of the `portfolio-photos` bucket that demonstrably caches.

Purpose: The roadmap puts FND-07 in Phase 2 **for lead time, not because it feels like cutover
work**. Two facts make this urgent rather than tidy:

1. Cloudflare's own migration guide states, verbatim, *"Unlike Pages, Workers does not support
   any domain whose nameservers are not managed by Cloudflare."* If `akhilsaxena.com` is not on
   Cloudflare nameservers, **the chosen platform cannot serve the apex domain at all** — and
   that invalidates the stack decision, not just a config file. Discovering it at cutover means
   redoing DNS, secrets and CI simultaneously under outage pressure.
2. Every one of the 39 photos in `data/portfolio_images.json` currently points at
   `pub-2d90aedeebcf4142afe524930c3b6471.r2.dev`, which Cloudflare documents as rate-limited,
   uncached and "for development purposes only" — no WAF, no caching, no Bot Management. That
   directly blocks the Lighthouse 95+ goal and makes Lighthouse scores non-reproducible under
   rate-limiting.

This plan provisions the custom domain and **measures** that it caches. It deliberately does
**not** rewrite the 39 manifest URLs — that is Phase 3's CONT-04 ("No `pub-*.r2.dev` URL remains
anywhere in the repository"), a data migration with its own verification. What this plan owes
Phase 3 is one thing: the canonical hostname, written down.

Output: `02-DNS-R2-PREREQS.md` — nameserver evidence, the provisioned hostname, and a
before/after `cf-cache-status` measurement.

This plan touches no application code and shares no files with any other plan in the phase. It
runs in wave 1 in parallel with the supply-chain gate (02-01) so the developer's dashboard work
starts at minute one instead of after hours of scaffolding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

Platform facts, including the verbatim nameserver constraint and the cutover sequence:
@.planning/research/STACK.md

Read §"Deployment Target: Workers, not Pages" — specifically the block beginning "The
akhilsaxena.com custom domain — the one real migration risk".

The r2.dev pitfall and the deterministic-key/stale-cache interaction that follows from fixing
it:
@.planning/research/PITFALLS.md

The live manifest this plan measures against (do not modify it):
@data/portfolio_images.json

A concrete object that exists in the bucket today, taken from the first entry's `urls.small`:
`photos/abstract/intothemist-sm.webp`, currently served as
`https://pub-2d90aedeebcf4142afe524930c3b6471.r2.dev/photos/abstract/intothemist-sm.webp`.
The R2 bucket is `portfolio-photos` (confirmed from the legacy `wrangler.toml`:
`git show legacy/nextjs-portfolio:wrangler.toml`).

**Why this plan uses the dashboard rather than a CLI.** `wrangler r2 bucket domain add` exists,
but `wrangler` is not installed until plan 02-03, and running it through `npx` here would
execute a package that the parallel supply-chain gate (02-01) has not yet cleared. The
provisioning step is therefore a dashboard action, and every assertion about the result is
automated with `curl`/`dig`.
</context>

<verify_idiom>
**Every `<automated>` block below uses one idiom, and it is deliberately not the default.** The
Bash tool does not set `-e`, so a `;`-joined chain ending in `echo OK` reports success no matter
what failed inside it — the exit status is the `echo`'s. `set -e` is not the fix either: under it
a negative control written `grep BAD && { echo …; exit 1; }` aborts the shell in the *passing*
case, because the `&&` list returns non-zero when `grep` finds nothing.

The idiom used throughout, with no `set -e`:
- **Positive assertion** — `CMD || { echo "<what failed and why it matters>"; exit 1; }`
- **Negative assertion** — `if CMD; then echo "<what was found and why it is wrong>"; exit 1; fi`

No command appears bare. Every step either exits non-zero with a diagnostic or falls through with
status 0, so the trailing `echo` is a marker rather than the thing being reported. If you add a
step, write it in one of those two forms.
</verify_idiom>

<tasks>

<task type="auto">
  <name>Task 1: Measure the DNS and image-origin baseline</name>
  <files>.planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md</files>
  <action>
Gather the before-state as hard evidence, and write it down before anything is changed.

Nameservers: run `dig NS akhilsaxena.com +short` and record the exact output. Cloudflare-managed
zones return two hostnames ending in `.ns.cloudflare.com`. Record an explicit verdict line —
`NAMESERVERS: CLOUDFLARE-MANAGED` or `NAMESERVERS: NOT CLOUDFLARE-MANAGED` — because the second
verdict is a platform-level blocker for the whole phase, not a task failure. Also run
`dig +short akhilsaxena.com` and `curl -sS -o /dev/null -D - https://akhilsaxena.com` to record
what the apex currently resolves to and serves; the site is expected to be down or serving the
legacy Pages project, and either is fine to record.

Image origin baseline: fetch
`https://pub-2d90aedeebcf4142afe524930c3b6471.r2.dev/photos/abstract/intothemist-sm.webp` twice
in a row with `curl -sS -o /dev/null -D -` and record the full response headers of both,
particularly `cf-cache-status`, `age` and `content-length`. Do not assume the result — the point
is to have a measured before-state to contrast against the after-state in Task 3. If the header
is absent entirely, record "absent", which is itself the finding.

Write `02-DNS-R2-PREREQS.md` with sections `## Nameserver evidence`, `## Apex baseline`,
`## r2.dev origin baseline (before)`, and stub headings `## R2 custom domain (provisioned)` and
`## Cache evidence (after)` for Tasks 2 and 3 to fill. Quote raw command output verbatim in
fenced blocks; do not paraphrase headers.

If the nameserver verdict is NOT Cloudflare-managed, say so loudly at the top of the file and in
your task output — Task 2 then has a second, larger job.
  </action>
  <verify>
    <automated>F=.planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md; test -f "$F" || { echo "the prereqs file was not created"; exit 1; }; grep -qE 'NAMESERVERS: *(CLOUDFLARE-MANAGED|NOT CLOUDFLARE-MANAGED)' "$F" || { echo "no explicit NAMESERVERS verdict line, or it is not one of the two allowed values"; exit 1; }; grep -qi 'cf-cache-status\|absent' "$F" || { echo "no cf-cache-status observation recorded for the r2.dev baseline"; exit 1; }; grep -Fq '## Apex baseline' "$F" || { echo "missing section: ## Apex baseline"; exit 1; }; grep -Fq '## r2.dev origin baseline (before)' "$F" || { echo "missing section: ## r2.dev origin baseline (before)"; exit 1; }; echo BASELINE_RECORDED</automated>
  </verify>
  <done>
The file records a verbatim `dig NS` result with an explicit CLOUDFLARE-MANAGED / NOT
CLOUDFLARE-MANAGED verdict, the apex resolution and response headers, and two full header dumps
from consecutive r2.dev fetches of a real object.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Cloudflare account work — nameservers and the R2 custom domain</name>
  <action>Pause. Present the recorded nameserver verdict and the provisioning steps below, and hold until the developer reports the hostname they provisioned. Attempt no CLI substitute: `wrangler` is not installed until plan 02-03 and invoking it through `npx` here would execute a package the parallel supply-chain gate has not cleared.</action>
  <what-built>
Task 1 measured and recorded the current nameservers for `akhilsaxena.com`, what the apex serves
today, and the caching behaviour of the `pub-*.r2.dev` origin that all 39 photos currently use.
Everything that could be automated has been. The remaining steps require your Cloudflare account
and have no CLI path that does not bypass the still-open supply-chain gate in 02-01.
  </what-built>
  <how-to-verify>
Read the `NAMESERVERS:` verdict line at the top of
`.planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md` first — it decides
how much work this checkpoint is.

**If the verdict is NOT CLOUDFLARE-MANAGED** — this is the blocker the roadmap put this plan in
wave 1 to find. Move `akhilsaxena.com` onto Cloudflare nameservers at your registrar now, before
anything else, and note that propagation can take hours. Do not skip it and hope: Workers cannot
serve the apex domain otherwise, and the entire platform decision depends on it. Report back once
the registrar change is submitted; re-verification is automated in Task 3.

**Then, in the Cloudflare dashboard, provision the R2 custom domain:**

1. Go to R2 → the `portfolio-photos` bucket → Settings → Public access → Custom domains.
2. Connect a subdomain of `akhilsaxena.com`. Use `images.akhilsaxena.com` unless you prefer
   another — whatever you choose becomes the canonical image host for the rest of the project's
   life, and Phase 3 rewrites all 39 manifest URLs to it.
3. Confirm the DNS record Cloudflare offers to create is **proxied** (orange cloud). An
   unproxied record serves the bucket without Cloudflare's cache and would reproduce exactly the
   r2.dev problem this step exists to fix.
4. Wait for the domain status to read Active.
5. **Also decide the Worker's own hostname now** — a second, different non-apex subdomain of
   `akhilsaxena.com` (`preview.akhilsaxena.com` unless you prefer another). You do not need to
   create anything for it: plan 02-03 writes it into `wrangler.jsonc` as a `custom_domain` route
   and `wrangler deploy` attaches it on the first deploy in plan 02-09. It must be a subdomain,
   never the apex — `akhilsaxena.com` still points at the legacy Pages project and moving it is
   cutover work with a propagation window. It must also not already have a conflicting DNS record,
   or `wrangler deploy` will fail loudly; check that now while you are in the DNS tab.
   The reason a subdomain is required at all: Cloudflare Access applications are scoped to hostnames
   in a zone you control, so a Worker living only at `*.workers.dev` cannot have Access applied and
   the production auth verification in plans 02-09 and 02-10 would be impossible.
6. Tell me both hostnames: the R2 image host you provisioned, and the Worker subdomain you chose.

**Do not** disable or delete the `pub-*.r2.dev` public URL yet. All 39 manifest entries still
point at it and Phase 3 owns the rewrite; removing it now breaks the only image source the
project has.

**Also note for later, do not act on it now:** once images are served through a cached custom
domain, re-uploading a photo under the same deterministic R2 key will serve permanently stale
bytes from cache. Phase 4 owns the fix (content-hashed keys, or purge-on-write). This is recorded
here only so the interaction is not rediscovered as a bug.
  </how-to-verify>
  <resume-signal>Reply with both hostnames — the provisioned R2 image host (e.g. "images.akhilsaxena.com") and the chosen Worker subdomain (e.g. "preview.akhilsaxena.com") — or describe what blocked you.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Prove the custom domain caches, and name the canonical host</name>
  <files>.planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md</files>
  <action>
Using the hostname the developer provided, verify the provisioning actually worked and actually
caches — a provisioned-but-unproxied domain looks correct in the dashboard and fails the only
requirement that matters.

Re-run `dig NS akhilsaxena.com +short` and confirm the CLOUDFLARE-MANAGED verdict now holds
(it may have changed if the developer moved nameservers in Task 2). Run
`dig +short <hostname>` and confirm it resolves.

Then fetch the same object used for the baseline —
`https://<hostname>/photos/abstract/intothemist-sm.webp` — and assert:
- the first request returns HTTP 200 with a `content-type` of `image/webp`;
- a second, immediately following request returns 200 **and** `cf-cache-status: HIT`.

If the second request reports MISS, DYNAMIC or BYPASS, do not paper over it. Retry once
(the first request may have raced the cache write), and if it still does not HIT, stop and report
that the DNS record is probably unproxied or the bucket is being served without the cache — that
is a real finding and Criterion 5 is not met.

Cross-check byte-for-byte that the custom domain and the r2.dev origin are serving the same
object: compare `content-length` between the two responses, and compare the SHA-256 of the two
downloaded bodies using `shasum -a 256`. A mismatch means the custom domain is pointed at the
wrong bucket.

Append to `02-DNS-R2-PREREQS.md`: fill `## R2 custom domain (provisioned)` with the hostname,
the DNS record type, and the proxied status as observed; fill `## Cache evidence (after)` with
both verbatim header dumps and the SHA-256 comparison. Add a final section
`## Handoff to later phases` containing exactly **two** canonical lines, each on its own line:

`R2_PUBLIC_URL = https://<image hostname>` — the single value Phase 3 rewrites 39 manifest URLs
to as part of CONT-04, and the value `wrangler.jsonc` will carry in `vars` from Phase 3 onward.
Note explicitly that the manifest rewrite and the removal of the `pub-*.r2.dev` public URL are
Phase 3's work, not this phase's.

`WORKER_CUSTOM_DOMAIN = <worker hostname>` — the non-apex subdomain the developer chose in Task 2
for the Worker itself. **Plan 02-03 reads this line** and writes it into `wrangler.jsonc` as a
`routes` entry with `custom_domain: true`, which is what makes `wrangler deploy` attach the
hostname automatically. This matters more than it looks: Cloudflare Access applications can only be
scoped to hostnames in a zone you control, so a Worker reachable only at `*.workers.dev` cannot
have Access applied to it at all — and plans 02-09 and 02-10 would then have no way to verify the
production auth path. Record the apex `akhilsaxena.com` as deliberately **not** attached; cutover
owns it.
  </action>
  <verify>
    <automated>F=.planning/phases/02-astro-foundation-fail-closed-auth/02-DNS-R2-PREREQS.md; HOST=$(grep -oE 'R2_PUBLIC_URL = https://[A-Za-z0-9.-]+' "$F" | head -1 | sed 's|.*https://||'); test -n "$HOST" || { echo "no canonical R2_PUBLIC_URL line recorded"; exit 1; }; WHOST=$(grep -oE 'WORKER_CUSTOM_DOMAIN = [A-Za-z0-9.-]+' "$F" | head -1 | sed 's|.* = ||'); test -n "$WHOST" || { echo "no WORKER_CUSTOM_DOMAIN line recorded — plan 02-03 needs it for the wrangler.jsonc custom_domain route, and without it the Worker is only reachable on workers.dev where Cloudflare Access cannot be applied"; exit 1; }; case "$WHOST" in akhilsaxena.com) echo "WORKER_CUSTOM_DOMAIN must be a NON-apex subdomain — the apex belongs to cutover"; exit 1;; *.akhilsaxena.com) : ;; *) echo "WORKER_CUSTOM_DOMAIN must be a subdomain of akhilsaxena.com, got: $WHOST"; exit 1;; esac; dig +short "$HOST" | grep -qE '.' || { echo "the R2 custom domain does not resolve: $HOST"; exit 1; }; curl -sS -o /dev/null -D /tmp/h1.txt "https://$HOST/photos/abstract/intothemist-sm.webp" || { echo "first fetch through the R2 custom domain failed"; exit 1; }; curl -sS -o /dev/null -D /tmp/h2.txt "https://$HOST/photos/abstract/intothemist-sm.webp" || { echo "second fetch through the R2 custom domain failed"; exit 1; }; grep -qiE '^HTTP/[0-9.]+ 200' /tmp/h2.txt || { echo "the second request was not 200:"; cat /tmp/h2.txt; exit 1; }; grep -qi '^cf-cache-status: *HIT' /tmp/h2.txt || { echo "NOT CACHED — the DNS record is probably unproxied. cf-cache-status was:"; grep -i cf-cache-status /tmp/h2.txt; exit 1; }; dig NS akhilsaxena.com +short | grep -q 'ns.cloudflare.com' || { echo "nameservers are not Cloudflare-managed — a Worker Custom Domain is impossible and the platform choice is blocked"; exit 1; }; echo FND07_VERIFIED</automated>
  </verify>
  <done>
`dig NS akhilsaxena.com +short` returns `.ns.cloudflare.com` hostnames; the provisioned R2
custom domain resolves, returns 200 `image/webp` for a real object, and returns
`cf-cache-status: HIT` on a second consecutive request; the served bytes match the r2.dev origin
by SHA-256; and `02-DNS-R2-PREREQS.md` records exactly one canonical
`R2_PUBLIC_URL = https://<hostname>` line for Phase 3 to consume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → R2 custom domain | Anonymous readers fetch image objects; the bucket is intentionally public-read for photos, so the boundary controls *what* is exposed, not *who* reads it |
| registrar → Cloudflare zone | Whoever controls the nameservers controls where `akhilsaxena.com` points, including the apex the Worker will serve |
| Cloudflare dashboard session → account resources | This plan's provisioning step is performed with full account authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | R2 bucket exposed via a new public custom domain | mitigate | The custom domain is attached to `portfolio-photos`, whose entire contents are already public via `pub-*.r2.dev`; Task 3 asserts the object served matches the existing origin byte-for-byte by SHA-256, proving no additional bucket was exposed. Later phases that write non-public objects (the `temp/` staging prefix) must not use this bucket's public prefix — recorded as a Phase 4 constraint in the handoff section. |
| T-02-06 | Denial of Service | uncached `pub-*.r2.dev` origin | mitigate | This plan's whole purpose: r2.dev is rate-limited and has no WAF or Bot Management. Provisioning a proxied custom domain puts Cloudflare's cache and WAF in front of the 39 photos. Task 3 fails the plan unless `cf-cache-status: HIT` is observed, so an unproxied record cannot pass as done. |
| T-02-07 | Spoofing | apex domain served by a Worker on non-Cloudflare nameservers | mitigate | `dig NS` evidence is recorded and re-asserted in Task 3's automated verify. Workers refuses domains not on Cloudflare nameservers, so a passing verify is also proof the platform constraint is satisfiable. |
| T-02-08 | Tampering | stale cached bytes after a same-key re-upload | accept (this phase) | Deterministic R2 keys plus a newly-cached domain means re-uploading a photo under the same key serves stale bytes indefinitely. No photo is re-uploaded in Phase 2, so the risk is not live yet. Recorded in the checkpoint text and the handoff section as a Phase 4 obligation (content-hashed keys or purge-on-write). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing and runs no package manager. It deliberately avoids `npx wrangler` precisely so it cannot bypass the still-open supply-chain gate in 02-01. |
</threat_model>

<verification>
- `dig NS akhilsaxena.com +short` returns Cloudflare nameservers.
- The provisioned R2 custom domain resolves and serves a real object with HTTP 200.
- Two consecutive fetches produce `cf-cache-status: HIT` on the second.
- The custom-domain body SHA-256 equals the r2.dev body SHA-256 for the same key.
- `02-DNS-R2-PREREQS.md` contains exactly one `R2_PUBLIC_URL = https://<hostname>` line.
- `data/portfolio_images.json` is unmodified (`git diff --quiet data/portfolio_images.json`).
- No `package.json` was created and no package manager ran.
</verification>

<success_criteria>
Roadmap Criterion 5's first two clauses are met and evidenced: `akhilsaxena.com` is confirmed on
Cloudflare-managed nameservers, and an R2 custom domain is provisioned and measurably serving
cached images. The canonical image host is written down once, for Phase 3's CONT-04 rewrite and
for `wrangler.jsonc`'s `vars`. The 39 manifest URLs are untouched by design.
</success_criteria>

<output>
Create `.planning/phases/02-astro-foundation-fail-closed-auth/02-02-SUMMARY.md` when done.
</output>
