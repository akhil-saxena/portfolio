---
phase: 02-astro-foundation-fail-closed-auth
plan: 05
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - package.json
  - vitest.config.ts
  - vitest.workers.config.ts
  - vitest.integration.config.ts
  - test/setup/preview-server.ts
  - test/harness/runtime.workerd.test.ts
  - test/harness/preview-reachable.node.test.ts
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "A test can execute inside real workerd, with the R2 binding present, and prove it is not running in Node"
    - "A test can issue an HTTP request against the built site running under real workerd and read the status code"
    - "The workerd claim is backed by a negative control: the same assertion fails when run outside workerd"
    - "npm test runs both projects and is safe to put in CI"
  artifacts:
    - path: "vitest.workers.config.ts"
      provides: "The @cloudflare/vitest-pool-workers project — tests execute inside workerd with real bindings"
      contains: "defineWorkersConfig"
      min_lines: 10
    - path: "test/setup/preview-server.ts"
      provides: "Global setup that builds the site and spawns astro preview on real workerd, exposing its base URL"
      min_lines: 25
    - path: "test/harness/runtime.workerd.test.ts"
      provides: "Proof the workers project runs in workerd and not Node, plus the R2 binding smoke check"
      min_lines: 15
  key_links:
    - from: "vitest.workers.config.ts"
      to: "wrangler.jsonc"
      via: "poolOptions.workers.wrangler.configPath so the pool gets the real bindings"
      pattern: "wrangler\\.jsonc"
    - from: "test/setup/preview-server.ts"
      to: "astro preview"
      via: "child process spawn with its base URL exported to the integration project"
      pattern: "preview"
---

<objective>
Build the test harness that Criterion 2 requires: one project that executes tests **inside real
workerd** with real bindings, and one that issues real HTTP requests against the built site served
by real workerd. Then prove the harness is genuinely what it claims by making the workerd
assertion fail when run outside workerd.

Purpose: Criterion 2 does not merely ask that unauthenticated requests be rejected — it asks that
this be *"proven by a test running against real workerd rather than a mock."* That sentence is
load-bearing because the failure mode it guards against is specific: a jsdom or Node test of
`requireAccess()` proves nothing about the runtime that ships, and a green mock-based suite is
exactly the kind of evidence that lets a fail-open auth path reach production. Phase 0 learned the
same lesson from the other direction — four negative controls passed for the wrong reason before
the discipline of proving a control *bites* caught them.

This plan builds only the harness. It asserts nothing about auth, because auth does not exist
yet — plan 02-07 writes the auth tests into this harness in the next wave. Splitting them means
that when 02-07's tests are red, the redness is unambiguously about auth and not about a
half-configured pool.

This plan runs in parallel with 02-04 and owns `package.json` for this wave. 02-04 explicitly does
not touch the manifest, so the two cannot collide.

Output: `npm test` runs two projects — `workers` (inside workerd) and `integration` (HTTP against
`astro preview`) — both green, both proven to be real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

Testing stack, versions and the reason each layer exists:
@.planning/research/STACK.md

Read §"Testing" in full. Three facts from it are constraints, not suggestions:
- `@cloudflare/vitest-pool-workers` runs tests *inside `workerd`* with real bindings and
  Miniflare-simulated R2. It is the only way to actually test that `requireAccess()` fails closed.
- The workers pool **needs its own Vitest project entry — it cannot share a config** with another
  project. Attempting one merged config is the most common way this setup fails.
- `astro preview` runs the built output under real `workerd` (via `@cloudflare/vite-plugin`), so
  it — not `astro dev`, and not a mocked server — is the right target for HTTP-level assertions.

Peer ranges to respect (confirmed against the registry): `@cloudflare/vitest-pool-workers` peers
`vitest ^4.1.0`, `@vitest/runner ^4.1.0` and `@vitest/snapshot ^4.1.0`. Astro 7 ships Vite 8 and
`vitest@4.1.x` peers `vite ^6 || ^7 || ^8`, so the matrix is clean — but if `npm ls` reports a
duplicated `vitest` or a peer warning, fix the versions rather than suppressing the warning.

The manifest, scripts and local-env bootstrap this builds on:
@.planning/phases/02-astro-foundation-fail-closed-auth/02-03-PLAN.md

Note that `npm run bootstrap:local` must be run before any build or preview, because
`astro:env` has `validateSecrets: true` and both Access secrets are non-optional. The committed
examples supply placeholders on the `.invalid` TLD, which never resolve — that is deliberate and
is what makes it impossible for a test to accidentally reach a real Cloudflare Access endpoint.
Read 02-03's summary for the empirically determined answer to *which* env file feeds build-time
secret validation; the global setup must honour it.

<interfaces>
Two module specifiers matter and they are not interchangeable:

- `cloudflare:workers` — what **application** code imports (`src/lib/r2.ts` uses it). Available
  inside workerd, therefore available inside the pool.
- `cloudflare:test` — what **pool test** code imports for `env`, `SELF` and `fetchMock`. This is
  the pool's own module and exists only under `@cloudflare/vitest-pool-workers`.

Verify both against the installed package's own types before relying on them; if the pool's API
has moved, follow the installed version rather than this note.

**Test-file naming is a contract, not a style choice.** The two projects must have mutually
exclusive include globs, because a workers-pool test executed by the Node project (or the reverse)
fails in a way that looks like an application bug rather than a configuration one. The convention
for the whole repository, established here:

- `*.workerd.test.ts` — runs inside `workerd` in the `workers` project. May import `cloudflare:test`.
- `*.node.test.ts` — runs in Node in the `integration` project. Talks HTTP; never imports `cloudflare:test`.
- `__control-*` — a deliberately-broken negative-control fixture, always temporary, always deleted
  before the plan finishes. The prefix exists so a verify can distinguish "a control fixture was
  left behind" from "the executor legitimately added new tests", which are otherwise identical to
  a glob: at verify time both are untracked, because execute-plan verifies before it commits.

A test file matching neither suffix belongs to no project and would silently never run. The verify
asserts there are none.

Globals that exist in `workerd` and do **not** exist in Node — these are the basis of the
negative control: `WebSocketPair`, `caches.default`, and `navigator.userAgent` equal to
`"Cloudflare-Workers"`. Any one of them distinguishes the runtimes; assert on at least two so a
single API change cannot silently turn the control into a no-op.
</interfaces>
</context>

<verify_idiom>
**Every `<automated>` block below uses one idiom, and it is deliberately not the default.** The
Bash tool does not set `-e`, so a `;`-joined chain ending in `echo OK` reports success no matter
what failed inside it — the exit status is the `echo`'s. `set -e` is not the fix either: under it
a negative control written `grep BAD && { echo …; exit 1; }` aborts the shell in the *passing*
case, because the `&&` list returns non-zero when `grep` finds nothing.

The idiom used throughout, with no `set -e`:
- **Positive assertion** — `CMD || { echo "<what failed and why it matters>"; exit 1; }`
- **Negative assertion** — `if CMD; then echo "<what was found and why it is wrong>"; exit 1; fi`

No command appears bare. Every step either exits non-zero with a diagnostic or falls through with
status 0, so the trailing `echo` is a marker rather than the thing being reported. If you add a
step, write it in one of those two forms.
</verify_idiom>

<tasks>

<task type="auto">
  <name>Task 1: Two Vitest projects — one inside workerd, one over HTTP</name>
  <files>package.json, vitest.config.ts, vitest.workers.config.ts, vitest.integration.config.ts</files>
  <action>
Create `vitest.workers.config.ts` using `defineWorkersConfig` from
`@cloudflare/vitest-pool-workers/config`. Name the project `workers`. Point
`poolOptions.workers.wrangler.configPath` at `./wrangler.jsonc` so the pool inherits the real
`PORTFOLIO_BUCKET` R2 binding and the real compatibility date and flags — a pool configured with
ad-hoc Miniflare options instead would be testing a runtime that is not the one that ships.
Restrict its `include` glob to `test/**/*.workerd.test.ts` and nothing else. The integration project
takes `test/**/*.node.test.ts`. The two globs must not overlap — a workers-pool test executed by the
Node project fails in a way that reads as an application bug rather than a configuration one, and
the verify asserts both the exclusivity and that no test file matches neither glob.

Create `vitest.integration.config.ts` as a separate project named `integration`, running in the
default Node environment (it is an HTTP client, not a runtime under test), with
`globalSetup` pointing at `test/setup/preview-server.ts`.

Create `vitest.config.ts` as the root config that composes both projects. The workers pool cannot
share a config with another project, so composition must be by project reference, not by merging
options. If Vitest 4's project composition API differs from what you expect, follow the installed
version's own types and document what you used.

Add exactly three scripts to `package.json` — `test` running `vitest run`, `test:workers` running
the workers project only, and `test:integration` running the integration project only. Do not add
`gate:*` or `deploy`: plan 02-06 owns those in the next wave, and adding them here would break
that plan's wave isolation. Do not modify any script 02-03 already declared.

Confirm the dependency graph is sane before moving on: `npm ls vitest` must show a single resolved
version satisfying the pool's `^4.1.0` peer, with no duplicate copy and no unmet peer warning.
  </action>
  <verify>
    <automated>for f in vitest.config.ts vitest.workers.config.ts vitest.integration.config.ts; do test -f "$f" || { echo "$f was not created"; exit 1; }; done; grep -q 'defineWorkersConfig' vitest.workers.config.ts || { echo "the workers project does not use defineWorkersConfig"; exit 1; }; grep -q 'wrangler.jsonc' vitest.workers.config.ts || { echo "the pool does not inherit bindings from wrangler.jsonc — it would be testing a runtime that is not the one that ships"; exit 1; }; grep -q 'workerd.test.ts' vitest.workers.config.ts || { echo "the workers project must include only *.workerd.test.ts"; exit 1; }; grep -q 'node.test.ts' vitest.integration.config.ts || { echo "the integration project must include only *.node.test.ts"; exit 1; }; if grep -q 'node.test.ts' vitest.workers.config.ts; then echo "the workers glob overlaps the integration glob"; exit 1; fi; if grep -q 'workerd.test.ts' vitest.integration.config.ts; then echo "the integration glob overlaps the workers glob"; exit 1; fi; grep -q 'globalSetup' vitest.integration.config.ts || { echo "no globalSetup on the integration project"; exit 1; }; grep -q 'preview-server' vitest.integration.config.ts || { echo "globalSetup does not point at the preview server module"; exit 1; }; BAD=$(find test -name '*.test.ts' ! -name '*.workerd.test.ts' ! -name '*.node.test.ts' 2>/dev/null); test -z "$BAD" || { echo "these test files match neither project glob and would silently never run: $BAD"; exit 1; }; node -e 'const p=require("./package.json"); for(const s of ["test","test:workers","test:integration"]) if(!p.scripts?.[s]) throw new Error("missing script "+s); for(const s of ["gate:routes","gate:deps","deploy"]) if(p.scripts?.[s]) throw new Error("script belongs to 02-06, not here: "+s); for(const s of ["dev","build","preview","check","format","typecheck","bootstrap:local"]) if(!p.scripts?.[s]) throw new Error("a 02-03 script was removed: "+s);' || { echo "package.json scripts are wrong"; exit 1; }; if npm ls vitest 2>&1 | grep -qi 'unmet\|invalid'; then echo "the vitest peer graph is broken:"; npm ls vitest; exit 1; fi; echo PROJECTS_CONFIGURED</automated>
  </verify>
  <done>
Three config files exist. The `workers` project uses `defineWorkersConfig` and inherits bindings
from `wrangler.jsonc`. The `integration` project runs in Node with a `globalSetup` pointing at the
preview-server module. `package.json` gained exactly `test`, `test:workers` and `test:integration`,
kept all of 02-03's scripts, and declared none of 02-06's. `npm ls vitest` reports one clean
version.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the harness is real — workerd globals, R2 binding, live preview, and a negative control</name>
  <files>test/setup/preview-server.ts, test/harness/runtime.workerd.test.ts, test/harness/preview-reachable.node.test.ts</files>
  <action>
Write `test/setup/preview-server.ts` as the integration project's global setup. It must: run the
local env bootstrap so `astro:env`'s non-optional secrets are satisfied; run a production build;
spawn `astro preview` as a child process; wait until it is actually accepting connections rather
than sleeping a fixed interval; expose the base URL to tests (a `provide`/context value or an env
var, whichever the installed Vitest supports); and tear the child process down in the teardown
callback so no orphan is left listening.

Allocate the preview port dynamically — parse it from the preview process's own startup output
rather than hardcoding one. Plan 02-04 runs a dev server on port 4331 in this same wave, and plan
02-07 will spawn a preview server in the next; a fixed port turns a parallel wave into a flaky
one. If dynamic allocation is not workable, use 4332 and say so, but prefer dynamic.

Because the port is dynamic, the teardown check cannot be port-based either. Kill the child by its
process handle in the teardown callback, and let the verify confirm the outcome with a
process-name check rather than a hardcoded `lsof` on a port the harness may never have used.

Invoke the build and preview through the raw `astro` binary rather than `npm run build` /
`npm run preview`. In the next wave plan 02-06 appends a gate to the `build` script, and the
harness must keep working across that change — its job is to serve the built output, not to run
the project's gates.

Write `test/harness/runtime.workerd.test.ts` for the **workers** project. It must assert at least
two workerd-only globals — `typeof WebSocketPair`, `caches.default`, and/or `navigator.userAgent`
being `"Cloudflare-Workers"` — so that a single upstream API change cannot quietly turn the check
into a no-op. In the same file, import `env` from `cloudflare:test` and assert
`env.PORTFOLIO_BUCKET` is present and that a real `list({ limit: 1 })` against it resolves. That
second assertion is what makes this a harness for FND-03 and AUTH-04 rather than a runtime trivia
test.

Write `test/harness/preview-reachable.node.test.ts` for the **integration** project: fetch the base URL
the global setup provided, assert HTTP 200, and assert the response body contains the
`stack-proof-ok` marker that 02-03's index page renders. Do not assert anything about `/admin`,
`/api/health` or `/_actions/ping` here — those routes are created by plan 02-04 in a parallel
worktree and are not present in this one. Plan 02-07 adds those assertions once both are merged.

Then run the negative control, and record it in the plan summary as a control, not as a passing
test. The claim being controlled is *"the workers project genuinely executes inside workerd."* Copy
the workerd-globals assertion into a temporary file named
`test/harness/__control-runtime.node.test.ts` — the `__control-` prefix is required, because at
verify time your three legitimate new test files are also untracked and a check that simply looked
for untracked `*.test.ts` would flag them and tell you to delete real work. Run only the integration
project and confirm it **fails** — Node has no `WebSocketPair` and no `caches.default`. Record the
exact failure output. Then delete the `__control-` file and confirm `npm test` is green. If the copied assertion *passes* under
the Node project, stop: it means the assertion is not actually runtime-discriminating and the
whole "real workerd" claim is unproven — find an assertion that discriminates before proceeding.

Finish by running `npm test` and confirming both projects execute and pass.
  </action>
  <verify>
    <automated>W=test/harness/runtime.workerd.test.ts; N=test/harness/preview-reachable.node.test.ts; npm run bootstrap:local || { echo "local env bootstrap failed"; exit 1; }; npm test || { echo "npm test failed — both projects must be green"; exit 1; }; test -f "$W" || { echo "$W was not created"; exit 1; }; test -f "$N" || { echo "$N was not created"; exit 1; }; C=$(grep -oE 'WebSocketPair|caches\.default|Cloudflare-Workers' "$W" | sort -u | wc -l | tr -d ' '); test "$C" -ge 2 || { echo "need at least two DISTINCT workerd-only global assertions so one upstream API change cannot turn the control into a no-op; found $C"; exit 1; }; grep -q "cloudflare:test" "$W" || { echo "the workers test does not import env from cloudflare:test"; exit 1; }; grep -q 'PORTFOLIO_BUCKET' "$W" || { echo "no R2 binding assertion in the workers test"; exit 1; }; grep -q 'stack-proof-ok' "$N" || { echo "the integration test does not assert the static marker"; exit 1; }; if grep -nE '/admin|/api/health|_actions' "$N"; then echo "the harness must not assert on routes owned by the parallel plan 02-04 — they do not exist in this worktree"; exit 1; fi; grep -q 'preview' test/setup/preview-server.ts || { echo "the global setup does not spawn astro preview"; exit 1; }; if grep -nE 'npm run (build|preview)' test/setup/preview-server.ts; then echo "the global setup must call the astro binary directly, not the npm scripts — 02-06 appends a gate to the build script and the harness must survive that"; exit 1; fi; LEFT=$(find test -name '__control-*' 2>/dev/null); test -z "$LEFT" || { echo "a negative-control fixture was left behind: $LEFT"; exit 1; }; if pgrep -f 'astro preview' >/dev/null 2>&1; then echo "an orphan astro preview process is still running"; exit 1; fi; echo HARNESS_VERIFIED</automated>
  </verify>
  <done>
`npm test` runs both projects green. The workers project asserts at least two workerd-only globals
and performs a real `list()` against `env.PORTFOLIO_BUCKET` from `cloudflare:test`. The
integration project fetches the live preview server over HTTP and finds the `stack-proof-ok`
marker. The negative control was run: the same workerd-globals assertion **failed** under the Node
project, its output is recorded in the summary, the temporary file is deleted and the tree is
clean. No orphan preview process remains.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → the claim "auth fails closed" | The test harness is the evidence-producing boundary; a harness that silently runs somewhere other than workerd produces confident, false evidence |
| test process → local network | The global setup spawns a real HTTP server on the developer's machine and in CI |
| test process → external network | JWKS verification in later plans could reach out to a real Access endpoint if configuration leaked into tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | Spoofing | a "real workerd" suite that is silently running in Node | mitigate | The workers project asserts at least two workerd-only globals, and Task 2 runs an explicit negative control proving those assertions *fail* under the Node project. A control that passes in both runtimes is treated as a stop condition, not a pass. |
| T-02-20 | Spoofing | a pool configured with ad-hoc Miniflare options rather than the shipped config | mitigate | `poolOptions.workers.wrangler.configPath` points at `./wrangler.jsonc`, so the pool inherits the real R2 binding, compatibility date and flags. The R2 `list()` assertion fails if the binding did not come through. |
| T-02-21 | Information Disclosure | test configuration reaching a real Cloudflare Access endpoint | mitigate | The committed env examples use the non-resolving `.invalid` TLD, so no JWKS fetch from a test can reach a real team domain. Plan 02-07 additionally intercepts the JWKS URL rather than allowing egress. |
| T-02-22 | Denial of Service | orphaned preview processes and port collisions across parallel plans | mitigate | The global setup allocates the preview port dynamically and tears the child down in teardown; the verify asserts nothing is left listening. Plan 02-04 uses 4331 in this same wave, which is why a fixed port is explicitly the fallback rather than the default. |
| T-02-23 | Tampering | a leftover control fixture silently weakening the suite | mitigate | The temporary negative-control test is deleted and the verify fails if any stray `*.test.ts` remains uncommitted in the working tree. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `vitest` and `@cloudflare/vitest-pool-workers` were installed by 02-03 from the set approved in `02-PACKAGE-AUDIT.md`; the verify only inspects the existing graph with `npm ls`. |
</threat_model>

<verification>
- `npm test` runs two named projects, `workers` and `integration`, both green.
- The workers project asserts two or more workerd-only globals and a real R2 `list()`.
- The integration project fetches the built site over HTTP from `astro preview` and finds
  `stack-proof-ok`.
- The negative control failed under Node, and its output is recorded in the summary.
- No `__control-*` fixture remains under `test/`.
- Every file under `test/` matching `*.test.ts` also matches exactly one project glob.
- No orphan preview process is listening.
- `package.json` gained only `test`, `test:workers` and `test:integration`, and lost nothing.
- `test/harness/preview-reachable.node.test.ts` makes no assertion about `/admin`, `/api/*` or
  `/_actions/*` — those belong to the parallel plan 02-04 and are asserted in 02-07.
</verification>

<success_criteria>
A test harness exists that can execute assertions inside real `workerd` with real bindings and can
issue real HTTP requests against the built site under real `workerd` — and the "real workerd"
claim is backed by a control proving the same assertion fails outside it. Plan 02-07 can now write
auth tests whose redness means only one thing.
</success_criteria>

<output>
Create `.planning/phases/02-astro-foundation-fail-closed-auth/02-05-SUMMARY.md` when done.
Record the negative-control failure output verbatim, and the exact Vitest project-composition API
you used — plan 02-07 extends both projects.
</output>
