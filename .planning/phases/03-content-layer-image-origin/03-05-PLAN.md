---
phase: 03-content-layer-image-origin
plan: 05
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - data/resume.json
  - data/projects.json
  - src/lib/period.ts
  - scripts/migrate-resume-structure.mjs
  - test/content/resume-structure.unit.test.ts
autonomous: false
requirements: [CONT-01]
owns_criteria: []
owns_open_decisions: [OD-4, OD-6]
user_setup: []

must_haves:
  truths:
    - "The five project records live in their own file, moved verbatim, and resume.json no longer has a projects key"
    - "The design-system component figure is not a literal in committed content, and putting one back fails a gate"
    - "If the date shape migrates, the four period strings on disk today are reproduced byte-for-byte by a single formatter — en dash included"
    - "Every decision in this plan that changes content Akhil reviewed was taken by Akhil, not by the executor"
  artifacts:
    - path: "data/projects.json"
      provides: "the five project records, split out per D-24"
    - path: "src/lib/period.ts"
      provides: "the one formatter deriving a display period from structured dates (only if OD-4 = migrate)"
    - path: "test/content/resume-structure.unit.test.ts"
      provides: "verbatim-move proof and exact period reproduction"
  key_links:
    - from: "data/projects.json"
      to: "data/resume.json"
      via: "a clean split — no record appears in both files"
      pattern: "\"id\""
---

<objective>
Split `projects` out of `resume.json` into `projects.json` (D-24), settle the résumé date-shape
drift, and stop the design-system component figure from going stale a fourth time.

Purpose: three changes to one file, all of which the source artefacts resolved *in principle* and
none of which anyone has applied. Each changes content Akhil has reviewed, which is why the plan
opens with a checkpoint rather than a script.

**D-24 — the split.** 00-ADMIN-IA §1: *"A project owns its case study, and a case-study body has no
business living inside the résumé document."* ADR-002 then removed case-study *authoring* from the
admin entirely, which strengthens the split rather than weakening it: `projects.json` becomes the
card data that `/admin/projects` edits inline, and the essay lives elsewhere. Nothing about the
migration is speculative — five records move verbatim, and 00-ADMIN-IA already checked that
`badges` is *"already an array of objects"* so D-45's status set is an extension of an existing
field rather than a new column.

**The component figure is wrong right now, for the third time.** `data/resume.json`'s
`design-system` project description reads **"79-component React library with semantic tokens, dark
mode, and live Storybook docs."** Measured against the sibling repository this session:
`README.md:5` says **81**, and `find src -mindepth 2 -maxdepth 2 -type d` returns **83**. The
sequence so far is 80 → 79 → 81, and commit `db65b12 fix(data): repair a dead CTA route and the
stale component count` already fixed it by hand once. The 00-11 review's verdict — *"caught the
stale component count, now derived at build time"* — is not implemented, and cannot be implemented
here, because `@akhil-saxena/design-system` **is not a dependency of this repository yet**. See
**OD-6**.

**The date drift.** 00-ADMIN-IA §5 resolves the shape (structured fields win; `period` is derived
and never stored) with a reason that generalises: *"a free-text string is a lossy encoding… and
leaves 'is this role current?' un-modelled."* What it does not resolve is *when*, and the answer
determines whether 03-06 declares `period: string` and Phase 7 changes it later — which is the
drift criterion 1 exists to prevent, arriving in slow motion. See **OD-4**.

Output: `projects.json`, a `resume.json` without `projects`, a component figure that cannot go
stale silently, and whatever date shape Akhil chooses — implemented with a formatter whose
acceptance test is exact reproduction, not approximate agreement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-content-layer-image-origin/03-CONTEXT.md

- `.planning/phases/00-design-ideation/00-ADMIN-IA.md` §"Schema decisions this IA forces" §1 (D-24)
  and §5 (the date drift, the chosen shape, and the formatter's acceptance test stated as *"exact
  reproduction of the four strings on disk today"*).
- `.planning/ADR-002-admin-scope-revised.md` — `/admin/projects` edits the list **inline**, with no
  detail route. That is why `projects.json` needs no case-study body field in this phase.
- `.planning/phases/00-design-ideation/00-COPY/one-liners.md` lines 24–28 — the copy that quotes the
  figure, and the recorded ruling that `README.md` is *"retired as an authority for this figure"*
  while the shipped catalog (`src/OverviewPage.tsx`) is the authority.
</context>

<interfaces>
<!-- Read out of both working trees at planning time. -->

**`data/resume.json` top-level keys:** `experience` (3), `projects` (5), `skills` (3),
`education` (1). After 03-02 the bullets are bold-only markdown.

**The five project records** — every one carries the same eight keys, so the split is uniform:

```
id, title, label{text,icon}, description, tech[], icon, href, badges[{label,href,icon}]
ids: cairn, hued, momentum, timeshift, design-system
```

`icon` is `null` on `cairn` and `design-system`, and a `/assets/*.png` path on the other three.
`badges` is 1 entry on `cairn` and `design-system`, 2 on the rest.

**The four period strings on disk**, which are the formatter's acceptance test if OD-4 selects
migration. Copy them from the file, not from here:

```
experience[0] "Jul 2023 – Present"     Brevo
experience[1] "Nov 2022 – Jun 2023"    PharmEasy
experience[2] "Dec 2021 – Nov 2022"    MAQ Software
education[0]  "Jul 2018 – Jun 2022"    VIT
```

The separator is **U+2013 EN DASH with a space on each side**, not a hyphen and not an em dash.
Months are three-letter, capitalised. A formatter that emits `-` or `—` reproduces none of the four
and will look correct in a terminal.

**The component count, measured in `../design-system` this session:**

| Source | Figure | How |
|---|---:|---|
| `data/resume.json` (this repo) | **79** | the literal in the `design-system` project description |
| `../design-system/README.md:5` | **81** | `"81 components across 10 categories."` |
| `../design-system/src` directories | **83** | `find src -mindepth 2 -maxdepth 2 -type d \| wc -l` |
| `../design-system/src/OverviewPage.tsx` | computed | the file derives `TOTAL` from its own `categories` array — this is the authority per 01-12's ruling |

`package.json` in *this* repo has **no** `@akhil-saxena/design-system` entry, and Phase 1's v2.0.0
is unpublished (`01-21` is still an open human gate). So nothing here can resolve the figure today.
</interfaces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Two calls on `resume.json` — the date shape and the component figure (OD-4, OD-6)</name>
  <decision>Two calls on `resume.json`, asked together because they land in one file (OD-4 and OD-6)</decision>
  <context>
Both change content you have already reviewed, and both are in the same file, so they are asked in
one pause rather than two.

---

**A · The date shape (OD-4).** 00-ADMIN-IA §5 chose it: experience and education entries carry
`startMonth, startYear, endMonth, endYear, isPresent`, and `period` stops being stored — derived at
render time by one formatter. The reasoning is that `period` is a lossy encoding of a date range
that also leaves *"is this role current?"* unmodelled, and that a free-text string invites format
drift across entries.

The open part is **when**. Doing it now changes four reviewed records for the benefit of a screen
that does not exist until Phase 7. Deferring means 03-06 ships a schema declaring `period: string`
that Phase 7 must then change — a schema migration and an admin change landing together, later,
which is the drift criterion 1 exists to prevent.

If you migrate: the formatter's acceptance test is **exact reproduction of the four strings on
disk**, en dash and all, so the public page does not visibly change.

- `migrate-both` — experience **and** education take the structured shape.
- `migrate-experience-only` — experience migrates; education keeps `period` (it is one record and
  the "is this current?" question does not arise for a completed degree).
- `defer` — nothing changes; 03-06 declares `period: string`; Phase 7 owns the migration.

---

**B · The component figure (OD-6).** `resume.json` says **79-component**. `../design-system`
says 81 in its README and has 83 component directories. It has been wrong three times. The
00-11 review said "derived at build time" but the design system is not yet a dependency of this
repository, so nothing here can resolve it.

- `placeholder` — store a token (`{{ds.componentCount}}`) instead of a number; 03-06's schema
  **rejects** any project description containing a literal `\d+-component`; Phase 5, which installs
  the tarball, supplies the resolver reading the shipped catalog. The gate is provable today: put
  `79-component` back and it fails.
- `hardcode-81` — write 81 and accept a fourth staleness at the next release.
- `reword` — take the number out of the sentence entirely. `00-COPY/one-liners.md` already carries
  a version that makes the argument without a figure: *"a React component library where one token
  change lands across every screen at once."* The problem stops existing rather than being managed.

`reword` is worth ten seconds even though `placeholder` is the recommended answer, because the
figure is the weakest part of that sentence and the token machinery only earns its keep if the
number is genuinely load-bearing copy.
  </context>
  <options>
    <option id="see-context">
      <name>Answer A and B from the lists above</name>
      <pros>One interruption, one file, both answers on the record before anything is written.</pros>
      <cons>Two unrelated questions in one pause.</cons>
    </option>
  </options>
  <action>
Present both questions verbatim in one pause — they are unrelated, but they land in the same file
Akhil reviewed, and two interruptions for one file is worse than one interruption for two questions.
Include the measured figures: 13 bullets not 18, and 79 in `resume.json` against 81 in the sibling
README and 83 directories on disk. **Do not choose either.** Record both verdicts and hand them to
Tasks 2 and 3.
  </action>
  <resume-signal>Reply with both: `A=migrate-both|migrate-experience-only|defer` and `B=placeholder|hardcode-81|reword`.</resume-signal>
  <done>The user has replied with both `A=` and `B=` verdicts, recorded verbatim for Tasks 2 and 3.</done>
</task>

<task type="auto">
  <name>Task 2: Split the five projects out, verbatim, and apply the OD-6 verdict</name>
  <files>scripts/migrate-resume-structure.mjs, data/resume.json, data/projects.json, test/content/resume-structure.unit.test.ts</files>
  <action>
**Re-derive first.** Confirm `resume.json` has exactly the four top-level keys, that `projects` has
5 records, and that all five carry the same eight keys. If not, stop and report.

**Write the split** into `scripts/migrate-resume-structure.mjs`:

- `data/projects.json` is a **top-level array** of the five records, in their current order, with
  every key and value **byte-identical** — including `icon: null` on two of them and the nested
  `label` and `badges` objects. Do not reorder keys, do not drop nulls, do not "normalise" anything.
  A top-level array matching `data/portfolio_images.json`'s shape is deliberate: 03-06 can then use
  Astro's `file()` loader for both, which its documentation describes as taking *"a single file
  that contains an array of objects with a unique `id` field."*
- `resume.json` loses the `projects` key and keeps the other three in their current order.
- Idempotent; `JSON.stringify(data, null, 2)` plus a trailing newline; `data/` is Biome-excluded.

**Apply the OD-6 verdict** to the `design-system` record's `description`. If `placeholder`: replace
the figure with the agreed token and nothing else — the rest of the sentence is unchanged. If
`reword`: use the wording Akhil supplies at the checkpoint, verbatim. If `hardcode-81`: write 81
and record in the summary that a fourth staleness is expected and accepted.

**Write `test/content/resume-structure.unit.test.ts`.** The verbatim-move proof compares
`data/projects.json` against the previous committed revision of `resume.json`
(`git show HEAD~1:data/resume.json`, falling back to `HEAD`):

- the five records are deep-equal, **key order included** — compare `JSON.stringify` of each record
  with the same key ordering, because a key reshuffle is invisible to a deep-equality check and is
  exactly the kind of churn that makes a future diff unreadable;
- **except** the one field OD-6 changed, which is asserted separately and explicitly by id and field
  name, so "one field differs" is a named expectation rather than a tolerance;
- `resume.json` has no `projects` key, and no project `id` appears in both files;
- the count is 5, asserted, so a test iterating an empty array cannot report success.

If OD-6 = `placeholder` or `reword`, also assert that **no** project description matches
`/\b\d+[- ]component/i`. This is the assertion 03-06 will lift into the schema; putting it here too
means the data is guarded from the moment it is written rather than from the moment the schema
lands.

Commit: `content(resume): split projects into projects.json (D-24)`.
  </action>
  <verify>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && node -e '
const fs=require("fs");
const r=JSON.parse(fs.readFileSync("data/resume.json","utf8"));
const p=JSON.parse(fs.readFileSync("data/projects.json","utf8"));
if("projects" in r){console.error("FAIL: resume.json still has a projects key");process.exit(1)}
const keys=Object.keys(r).join(",");
if(keys!=="experience,skills,education"){console.error("FAIL: resume.json top-level keys are ["+keys+"], expected [experience,skills,education]");process.exit(1)}
if(!Array.isArray(p)||p.length!==5){console.error("FAIL: projects.json is not an array of 5");process.exit(1)}
const ids=p.map(x=>x.id);
if(new Set(ids).size!==5){console.error("FAIL: duplicate project ids");process.exit(1)}
const want=["id","title","label","description","tech","icon","href","badges"].sort().join(",");
for(const x of p){const k=Object.keys(x).sort().join(",");if(k!==want){console.error("FAIL: "+x.id+" keys ["+k+"]");process.exit(1)}}
console.log("OK 5 project records: "+ids.join(", "))'</automated>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && npx vitest run --project unit test/content/resume-structure.unit.test.ts --reporter=verbose 2>&1 | tail -40; test ${PIPESTATUS[0]} -eq 0 || { echo "FAIL: verbatim-move proof red"; exit 1; }</automated>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && cp data/projects.json /tmp/pj.bak && node -e '
// NEGATIVE CONTROL: drop one badge from a record. Every structural assertion above stays green —
// the record still has all eight keys and the right id. Only the verbatim comparison sees it.
const fs=require("fs");const p=JSON.parse(fs.readFileSync("data/projects.json","utf8"));
const h=p.find(x=>x.id==="hued"); h.badges=h.badges.slice(0,1);
fs.writeFileSync("data/projects.json",JSON.stringify(p,null,2)+"\n")' && ( npx vitest run --project unit test/content/resume-structure.unit.test.ts >/tmp/pj.txt 2>&1 && R=0 || R=1 ); cp /tmp/pj.bak data/projects.json; test "$R" = "1" && echo "CONTROL OK: a dropped badge fails the verbatim proof" || { echo "FAIL: a record can lose a badge in the move with the suite green"; tail -20 /tmp/pj.txt; exit 1; }</automated>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && cp data/projects.json /tmp/pj.bak && node -e '
// NEGATIVE CONTROL (skip if OD-6 = hardcode-81): put the stale literal back.
const fs=require("fs");const p=JSON.parse(fs.readFileSync("data/projects.json","utf8"));
const d=p.find(x=>x.id==="design-system"); d.description="79-component React library with semantic tokens, dark mode, and live Storybook docs.";
fs.writeFileSync("data/projects.json",JSON.stringify(p,null,2)+"\n")' && ( npx vitest run --project unit test/content/resume-structure.unit.test.ts >/tmp/pj2.txt 2>&1 && R=0 || R=1 ); cp /tmp/pj.bak data/projects.json; test "$R" = "1" && echo "CONTROL OK: a hardcoded component figure fails" || { echo "FAIL (expected only if OD-6=hardcode-81): a literal component count is accepted"; tail -20 /tmp/pj2.txt; exit 1; }</automated>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && node scripts/migrate-resume-structure.mjs >/dev/null 2>&1 && git diff --quiet data/resume.json data/projects.json && echo "OK re-run is a no-op" || { echo "FAIL: not idempotent"; git diff --stat data/resume.json data/projects.json; exit 1; }</automated>
  </verify>
  <done>`data/projects.json` holds the five records byte-identical to their previous home including key order, with the single OD-6 field change asserted by name; `resume.json` has exactly `experience, skills, education`; a dropped badge and a reinstated literal figure each make the suite red; the migration re-runs as a no-op.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the OD-4 verdict, with exact reproduction as the acceptance test</name>
  <files>scripts/migrate-resume-structure.mjs, data/resume.json, src/lib/period.ts, test/content/resume-structure.unit.test.ts</files>
  <action>
**If OD-4 = `defer`:** do not migrate. Instead, write the deferral into the summary and add one
assertion to `test/content/resume-structure.unit.test.ts`: every experience and education entry has
a non-empty `period` string matching `/^[A-Z][a-z]{2} \d{4} – (Present|[A-Z][a-z]{2} \d{4})$/` with
a literal U+2013. That is not busywork — it pins the format that Phase 7's migration will have to
reproduce, and it is the only thing standing between "deferred" and "unspecified". Then skip the
rest of this task.

**If OD-4 = `migrate-both` or `migrate-experience-only`:**

Add to `scripts/migrate-resume-structure.mjs` a conversion of each in-scope entry's `period` into
`startMonth, startYear, endMonth, endYear, isPresent`, and **delete** the `period` key. Month values
should be integers 1–12 rather than the three-letter strings — the string is a rendering concern and
storing it would recreate the very coupling this decision removes. Record the choice either way.

`isPresent: true` implies `endMonth` and `endYear` are absent (not `null`, not `0`). One entry,
Brevo, is in this state today.

**Write `src/lib/period.ts`.** One exported function, `formatPeriod(entry): string`. Constraints:

- **No Node-only imports** — it will be imported by a zod schema running in `workerd` during
  prerender, and by a React component in Phase 5.
- **No `Intl.DateTimeFormat`, and no `toLocaleString`.** Both are locale- and ICU-build-dependent;
  `workerd`'s ICU data is not the developer's Node's, so the same code can emit `Jul` locally and
  `July` or `juil.` in production. Use an explicit twelve-element month table. This is the same
  class of failure as a value that renders correctly in jsdom and wrongly in a browser.
- The separator is a literal U+2013 with a space either side. Write it as `'–'` in the source
  with a comment, not as a pasted glyph, so a future editor cannot silently normalise it to a
  hyphen and leave a byte-identical-looking diff.

**The acceptance test is exact reproduction of the four strings.** 00-ADMIN-IA §5 states it that
way, and it is the right shape: read the four `period` values from the **previous committed
revision** of `resume.json` and assert `formatPeriod(migratedEntry) === oldPeriodString`, byte for
byte, for each. Do not hardcode the four strings in the test — read them from git, so the test
proves the migration reproduced *what was on disk* rather than what the plan believed was on disk.

Add a `isPresent` round-trip case and a case with a single-digit month, neither of which exists in
the corpus and both of which the Phase 7 editor will produce.

Commit: `content(resume): store structured dates and derive the period string`.
  </action>
  <verify>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && npx vitest run --project unit test/content/resume-structure.unit.test.ts --reporter=verbose 2>&1 | tail -40; test ${PIPESTATUS[0]} -eq 0 || { echo "FAIL: period reproduction red"; exit 1; }</automated>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && if [ -f src/lib/period.ts ]; then node -e '
const s=require("fs").readFileSync("src/lib/period.ts","utf8");
if(/Intl\.DateTimeFormat|toLocaleString|toLocaleDateString/.test(s)){console.error("FAIL: locale-dependent month formatting — workerd ICU is not the local Node ICU");process.exit(1)}
if(!/\\u2013/.test(s)){console.error("FAIL: the en dash is not written as an escape, so it can be normalised silently");process.exit(1)}
const bad=[...s.matchAll(/^\s*import[^\n]*from\s+"(node:[^"]+|fs|path|url|crypto)"/gm)];
if(bad.length){console.error("FAIL: Node-only import");process.exit(1)}
console.log("OK period formatter is locale-free, escape-written and workerd-safe")'; else echo "SKIP: OD-4 = defer, no formatter"; fi</automated>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && node -e '
const r=JSON.parse(require("fs").readFileSync("data/resume.json","utf8"));
const hasPeriod=[...r.experience,...r.education].filter(e=>"period" in e).map(e=>e.id);
const hasStruct=[...r.experience,...r.education].filter(e=>"startYear" in e).map(e=>e.id);
console.log("entries with period:", JSON.stringify(hasPeriod));
console.log("entries with structured dates:", JSON.stringify(hasStruct));
const both=[...r.experience,...r.education].filter(e=>"period" in e && "startYear" in e).map(e=>e.id);
if(both.length){console.error("FAIL: "+both.join(", ")+" carry BOTH period and structured dates — that is the legacy defect 00-ADMIN-IA §5 names, reproduced exactly");process.exit(1)}
console.log("OK no entry carries two representations of the same fact")'</automated>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && if [ -f src/lib/period.ts ]; then cp src/lib/period.ts /tmp/p.bak && node -e '
// NEGATIVE CONTROL: swap the en dash for a plain hyphen. Every string still "looks right" in a
// terminal; only byte-exact reproduction catches it.
const fs=require("fs");let s=fs.readFileSync("src/lib/period.ts","utf8");
const before=s; s=s.replace(/\\u2013/g,"-");
if(s===before){console.error("FAIL: control anchor stale");process.exit(1)}
fs.writeFileSync("src/lib/period.ts",s)' && ( npx vitest run --project unit test/content/resume-structure.unit.test.ts >/tmp/pd.txt 2>&1 && R=0 || R=1 ); cp /tmp/p.bak src/lib/period.ts; test "$R" = "1" && echo "CONTROL OK: a hyphen instead of an en dash fails" || { echo "FAIL: the separator can change and the test stays green"; tail -20 /tmp/pd.txt; exit 1; }; else echo "SKIP: OD-4 = defer"; fi</automated>
    <automated>cd /Users/akhilsaxena/Documents/Personal/Repositories/portfolio && npm run check && npm run typecheck && npx vitest run --project unit && echo "OK lint, typecheck and the whole unit project clean"</automated>
  </verify>
  <done>Either `period` is untouched and its format is pinned by an assertion, or every in-scope entry carries structured dates only, `period` is gone from disk, and `formatPeriod` reproduces the four strings read from git byte for byte — with a hyphen-for-en-dash swap proven to fail. No entry carries both representations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `resume.json` → `projects.json` | Five reviewed records crossing files. A move is the cheapest place to lose a nested value nobody looks at twice. |
| committed copy → a public page | A project description is a factual claim rendered on a page hiring managers read. A stale number is a wrong claim, not a formatting issue. |
| stored dates → the displayed period | Two representations of one fact is the legacy defect `src/types.ts` documented in a header comment. Reintroducing it here would be doing so knowingly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | the five project records | mitigate | Verbatim comparison against the previous git revision including key order, with a dropped-badge negative control that every structural assertion is blind to. |
| T-03-05-02 | Repudiation | the component figure | mitigate | The figure stops being a literal (OD-6 = placeholder or reword) and a gate rejects `\d+-component` in any project description, proven by reinstating `79-component`. Under `hardcode-81` the residual staleness is recorded as accepted rather than unnoticed. |
| T-03-05-03 | Tampering | the four period strings | mitigate | Exact byte reproduction against strings read from git, not from the plan, with an en-dash-to-hyphen negative control. |
| T-03-05-04 | Spoofing | month names | mitigate | An explicit month table, with `Intl.DateTimeFormat` and `toLocaleString` banned by a check. `workerd`'s ICU data is not the developer's Node's, so a locale-derived `Jul` can become `July` in production with no code change. |
| T-03-05-05 | Elevation of privilege | executor judgement | mitigate | Both decisions that alter reviewed content are blocking human checkpoints. The executor implements a verdict; it does not choose one. |
| T-03-05-06 | Tampering | duplicate representations | mitigate | An assertion that no entry carries both `period` and `startYear` — the exact defect 00-ADMIN-IA §5 quotes from the legacy `src/types.ts` header. |
| T-03-05-SC | Tampering | npm/pip/cargo installs | mitigate | No install. `@akhil-saxena/design-system` is deliberately **not** added here; that is Phase 5's tarball step and `gate:deps` governs it. |
</threat_model>

<verification>
- `projects.json`: 5 records, uniform keys, byte-identical to their previous home except the one
  named OD-6 field; a dropped badge fails.
- `resume.json`: exactly `experience, skills, education`; no id in both files.
- No project description contains a literal component figure (unless OD-6 = `hardcode-81`).
- Dates: either `period` pinned by format assertion, or structured-only with byte-exact
  reproduction of four strings read from git; never both representations on one entry.
- `npm run check`, `npm run typecheck`, and the full `unit` project green.

**What these checks cannot see.** Under OD-6 = `placeholder`, nothing proves the token ever
*resolves* — no page renders a project card until Phase 5, and the design system is not installed,
so `{{ds.componentCount}}` is currently a string that no code reads. The gate proves only that a
stale literal cannot come back. **Phase 5 must fail its build on an unresolved token**, or this
phase will have replaced a wrong number with a visible placeholder, which is worse. Record that
obligation in the summary and in ROADMAP's Phase 5 entry.
</verification>

<success_criteria>
- D-24's split is done and provably lossless, so `/admin/projects` in Phase 7 opens a file that
  already has the right shape.
- The component figure's staleness is closed at the layer where it recurs — the committed copy —
  rather than fixed by hand a fourth time.
- The date-shape question is answered on the record by the person whose résumé it is, and whichever
  answer he gives, 03-06's schema encodes exactly one representation of a date range.
</success_criteria>

<output>
Create `.planning/phases/03-content-layer-image-origin/03-05-SUMMARY.md` when done.
Include: both checkpoint verdicts verbatim; the full `data/projects.json` as shipped; the OD-6
wording actually used; whether months are stored as integers or strings and why; all negative-
control transcripts; and — if OD-6 = `placeholder` — the Phase 5 obligation to fail the build on an
unresolved token, stated as a sentence Phase 5's planner can lift.
</output>
