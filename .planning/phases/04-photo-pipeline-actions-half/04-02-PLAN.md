---
phase: 04-photo-pipeline-actions-half
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/photo-pipeline.ts
  - src/schemas/photo.ts
  - src/lib/image-origin.ts
  - scripts/assert-no-r2dev-urls.mjs
  - test/pipeline/photo-pipeline-contract.unit.test.ts
autonomous: false
requirements: [CONT-05, PIPE-02, PIPE-04]
owns_criteria: [5]
owns_open_decisions: [OD-1, OD-2, OD-3, OD-6, OD-7, OD-11]
user_setup: []

must_haves:
  truths:
    - "There is exactly one module in the repository that says what an R2 key for a published photo looks like, what the staging prefix is, and what the workflow's dispatch inputs are named"
    - "Two different sets of image bytes produce two different published keys; identical bytes produce the same key"
    - "The pipeline structurally cannot emit a non-canonical image origin, because it composes every URL from IMAGE_ORIGIN and never reads R2_PUBLIC_URL"
    - "`gate:origin` scans .github/** and fails on a legacy origin literal there"
    - "`src/schemas/photo.ts` states, in writing, what `dimensions` describes"
  artifacts:
    - path: "src/lib/photo-pipeline.ts"
      provides: "The one definition of the staging prefix, the published key scheme, the variant table, the object cache policy and the dispatch input names"
      exports: ["STAGING_PREFIX", "STAGING_KEY_RE", "assertStagingKey", "PUBLISHED_PREFIX", "VARIANTS", "THUMB", "CONTENT_HASH_BYTES", "contentHash", "publishedKey", "publishedUrl", "OBJECT_CACHE_CONTROL", "DISPATCH_INPUTS", "PUBLISH_BRANCH", "PUBLISH_RETRY_LIMIT", "STAGING_BUCKET", "STAGING_EXPIRE_DAYS"]
      min_lines: 120
    - path: "test/pipeline/photo-pipeline-contract.unit.test.ts"
      provides: "Independent assertions on the contract — re-implemented, never imported from the producer"
      min_lines: 80
    - path: "scripts/assert-no-r2dev-urls.mjs"
      provides: ".github/** reclassified from SKIP to SCAN with its reason written in place"
      contains: "scan: true"
  key_links:
    - from: "src/lib/photo-pipeline.ts"
      to: "src/lib/image-origin.ts"
      via: "publishedUrl composes from IMAGE_ORIGIN — no hostname literal anywhere in the pipeline"
      pattern: "import .*IMAGE_ORIGIN.*image-origin"
    - from: "src/lib/photo-pipeline.ts VARIANTS"
      to: "src/lib/image-origin.ts REMOTE_URL_KEYS"
      via: "VARIANTS.map(v => v.urlKey) must deep-equal REMOTE_URL_KEYS — asserted, not assumed"
      pattern: "REMOTE_URL_KEYS"
---

<objective>
Write down, once and in executable form, the four contracts every later plan in this phase
depends on: **what a published photo's URL looks like**, **where staged uploads live**, **what
the workflow's dispatch interface is**, and **what `dimensions` means**.

This is the interface-first plan. 04-05, 04-06, 04-07, 04-08 and 04-09 all import from
`src/lib/photo-pipeline.ts`; none of them re-derives any of it. Six of the phase's twelve open
decisions land here because six of them change this module's contents.

Purpose: CONT-05 is settled by the key scheme, not by the CDN — the measurement in
`04-RESEARCH.md` §4 is that a GET returns `cache-control: max-age=14400` injected by the zone,
so a re-upload at the same key serves stale bytes to a returning browser for up to four hours
and **no purge can reach that**. The only fix that works is a URL that changes when the bytes do.
Output: one contract module, one independent contract test, one gate reclassification, and one
sentence in `src/schemas/photo.ts` that turns an accident into a written contract before Phase 5
builds `srcset` on top of it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-photo-pipeline-actions-half/04-RESEARCH.md
@.planning/phases/04-photo-pipeline-actions-half/04-VALIDATION.md
@.planning/phases/03-content-layer-image-origin/03-CONTEXT.md

Read before editing:
@src/lib/image-origin.ts
@src/schemas/photo.ts
@scripts/assert-no-r2dev-urls.mjs

<interfaces>
From `src/lib/image-origin.ts` — import these, never restate them:
  export const IMAGE_ORIGIN = 'https://images.akhilsaxena.com';  // no trailing slash
  export const REMOTE_URL_KEYS = ['original', 'large', 'medium', 'small'] as const;

The legacy variant table, read from `git show legacy/nextjs-portfolio:scripts/process-images.js`
and confirmed against served bytes (a 400px `-sm.webp` decodes to 400x267):
  ''   -> maxWidth 2000, quality 85 -> urls.original
  '-lg'-> maxWidth 1200, quality 85 -> urls.large
  '-md'-> maxWidth  800, quality 85 -> urls.medium
  '-sm'-> maxWidth  400, quality 80 -> urls.small
  thumb: width 40, quality 60, emitted as `data:image/webp;base64,<...>`, no watermark
Resize is `sharp(buf).resize({ width: Math.min(maxWidth, sourceWidth), withoutEnlargement: true })`.

The measured id invariant on all 39 committed records:
  id === category + '-' + basename(urls.original, '.webp')
It is asserted nowhere. OD-1 option A breaks it, which is why its replacement must be written down.

`scripts/assert-no-r2dev-urls.mjs` RULES is an EXHAUSTIVE first-match-wins classification; each
entry is `{ scan: boolean, label, test: (repoRelativePosixPath) => boolean, why }`. A tracked path
matching no rule is a failure, not a silent pass.
</interfaces>

**Shell discipline.** Interactive shell is **zsh**; Actions is **bash**. No `${PIPESTATUS[0]}`,
no `R=$?`, no `( cmd && R=0 || R=1 )`. Capture as `if cmd; then R=0; else R=1; fi`.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Six open decisions — the phase's shape depends on all six</name>
  <decision>OD-1, OD-2, OD-3, OD-6, OD-7 and OD-11, batched. They are asked together because they
interact: OD-2 determines OD-7, OD-6 determines OD-5 (asked in 04-04), and OD-1 and OD-3 both
decide what a produced URL looks like.</decision>
  <context>
There is no `04-CONTEXT.md` — discuss-phase was skipped deliberately, and `04-RESEARCH.md`'s
`## Open decisions` section carries what it would have held. Every recommendation below is the
researcher's, with the measurement behind it. **A plan may not pass one of these on an executor's
judgement.**
  </context>
  <options>
    <option id="OD-1">
      <name>OD-1 · The cache-versioning scheme for CONT-05</name>
      <pros>Measured: a GET of an existing photo returns `cf-cache-status: HIT` with
`cache-control: max-age=14400` — a four-hour BROWSER cache, injected by the zone, not by the
object (the R2 object carries no `Cache-Control` of its own). A fresh `?v=` produced a genuine
MISS then HIT, so the query string IS part of the cache key. **Option C (purge API) is ruled out
on evidence: no server-side purge reaches a browser cache that already holds max-age=14400.**

**A (recommended)** — content-hashed keys, `photos/&lt;cat&gt;/&lt;slug&gt;-&lt;hash8&gt;&lt;suffix&gt;.webp`, with
`Cache-Control: public, max-age=31536000, immutable` set at PutObject. Bytes at a URL never
change, which independently helps PIPE-04: a re-run cannot half-overwrite an object a live page
is reading. The roadmap already presumes this — but a presumption is not a recorded decision,
which is why it is asked.

**B** — keep the path, add `?v=&lt;hash8&gt;`. Measurably works at the edge, smallest diff, and the
`id === category + '-' + basename` invariant survives. But the object stays mutable, and R2
documents 1 concurrent write per second to the same key, so a mid-write reader can get either
version.</pros>
      <cons>A breaks the measured `id === category + '-' + basename(urls.original)` invariant
(true on 39/39 today) and leaves the superseded objects in the bucket on a re-upload. If A is
chosen, this plan writes the REPLACEMENT invariant down and asserts it.

**A1, and it does not block A.** Whether the R2 custom domain re-emits an object's
`Cache-Control` in place of the zone's `max-age=14400` is `[ASSUMED]` — not stated on any
first-party page. It is measured in 04-10 with one `wrangler r2 object put --cache-control`
followed by a **GET** (never HEAD — HEAD returns `DYNAMIC` with no `cache-control` at all).
Option A is correct either way, because the URL itself changes; only the `immutable` claim is
provisional until that measurement lands. Say so in the module comment rather than asserting it.</cons>
    </option>
    <option id="OD-2">
      <name>OD-2 · Where `alt` comes from for a new photograph</name>
      <pros>**A (recommended)** — `alt` is a **required `workflow_dispatch` input**, validated
against the same four rules before any R2 read, so a bad value costs nothing. It is the only
option under which criterion 1 is satisfiable by one command, and `gh workflow run -F
alt=@alt.txt` reads it from a file so length is not a constraint (65,535-char payload, 25 inputs).

**C** — the pipeline opens a PR; the human fills `alt` and merges. Safest, and it gives `alt` a
review surface. But it makes criterion 1 ("committed to `main`") false as written, and criterion 1
would then have to be amended in writing rather than quietly reinterpreted.</pros>
      <cons>**B (placeholder, human edits later) is measurably bad and is not offered as viable:**
`alt: "TODO"` passes `min(1)` and all four `superRefine` rules and would ship a photograph
announced to a screen reader as "TODO"; `alt: "[AKHIL-ALT] …"` fails the marker rule and reds
`main`, blocking the deploy of everything else. **D** (VLM draft + human approval) still needs C's
machinery and is out of scope.

`alt` cannot be machine-generated. All 39 existing values were written from viewing the
photographs and reviewed on 2026-08-23; the enrichment test calls `alt` "the entire non-visual
experience" of the gallery. **This is the decision most likely to reshape the phase.**</cons>
    </option>
    <option id="OD-3">
      <name>OD-3 · `R2_PUBLIC_URL` — reuse, repoint, or refuse to read it</name>
      <pros>**A (recommended)** — the pipeline **never reads `R2_PUBLIC_URL`**. It imports
`IMAGE_ORIGIN` from `src/lib/image-origin.ts`, whose header says it is the only place the
hostname is written, so the pipeline structurally cannot emit a non-canonical origin. The secret
is then deleted so nothing can drift back to it.</pros>
      <cons>**B (repoint the secret and read it) is the dangerous option.** The secret exists,
dated 2026-03-28 — five months before `images.akhilsaxena.com` was provisioned, so it very
likely still holds the `r2.dev` value. And `gate:origin` skips `.github/**`; even with that
fixed, a workflow contains `${{ secrets.R2_PUBLIC_URL }}` — a *reference*, not a literal.
**No gate in this repository can see a wrong value inside that secret.**

**Regardless of the choice, `.github/**` moves from SKIP to SCAN.** 03-01 flagged that rule in
its own summary as "the rule most likely to be wrong once Phase 4 lands", and the rule's own
`why` field says "REVISIT IN PHASE 4". Phase 4 has landed. Whether `R2_PUBLIC_URL` is also
deleted from the repository secrets is a `user_setup` item recorded in 04-10, not something this
plan does.</cons>
    </option>
    <option id="OD-6">
      <name>OD-6 · The staging prefix, and who creates the lifecycle rule</name>
      <pros>**A (recommended)** — prefix `temp/`, matching the legacy `/api/dispatch` contract and
its `/^temp\/[a-zA-Z0-9._\/-]+$/` validator; a lifecycle rule `--expire-days 7` created **once**
by Akhil, and asserted thereafter against the shared constant this module exports.
`wrangler r2 bucket lifecycle add &lt;bucket&gt; &lt;name&gt; &lt;prefix&gt; --expire-days N` is verified present
on the installed wrangler 4.123.0.</pros>
      <cons>**B** (the pipeline creates the rule idempotently on every run) needs a bucket-admin
token in every job for a one-time act.

**Granularity is days and removal lags up to 24h, so expiry cannot be observed inside a session.**
The only honest gate is that the rule's prefix is **byte-equal** to the constant the pipeline
writes with. "A lifecycle rule exists" would pass against a rule scoped to the wrong prefix —
this project's tenth unfailable gate. Decide OD-6 **before** OD-5 (04-04): if a Cloudflare API
token with R2 Edit is needed for lifecycle anyway, `wrangler r2 object` becomes the tidier answer
for object I/O too and the five `R2_*` secrets can be retired.</cons>
    </option>
    <option id="OD-7">
      <name>OD-7 · Whether the pipeline commits to `main` at all</name>
      <pros>**A (recommended)** — commit directly to `main` with a bounded **re-derive-and-retry**
loop (never rebase, never force). Matches criterion 1 verbatim. `git push` on a non-fast-forward
is refused by the remote; recovery is `git fetch`, re-read the fetched manifest, re-run the
upsert against the NEW maxima, re-validate, commit, push — bounded at 3 attempts, then fail
loudly.</pros>
      <cons>**B** (open a PR) is safer against clobbering and follows automatically if OD-2
resolves to C — in which case criterion 1 must be amended in writing.

A textual `git rebase` of an appended JSON array element is a conflict waiting to happen and
resolves `order` incorrectly even when it succeeds. That is why the recovery is re-derive, not
rebase.</cons>
    </option>
    <option id="OD-11">
      <name>OD-11 · What `dimensions` describes</name>
      <pros>**A (recommended)** — declare it: "`dimensions` is the intrinsic size of the SOURCE
photograph; consumers use it for aspect ratio, never for pixel dimensions." Write it into
`src/schemas/photo.ts` as a comment and keep producing source dimensions. Zero data change; the
ratio is preserved (worst measured drift 0.03%).</pros>
      <cons>**B** redefines it as the dimensions of `urls.original` and backfills three records —
a change to reviewed data for a benefit CSS `aspect-ratio` already provides.

Measured: `nature-fairwayreflections` is `4608x3072` in the manifest while `urls.original` serves
`2000x1333`. Two other records differ too. **This gates Phase 5's PUB-05** (CLS reservation), and
the contract has never been written down — Phase 4 is the producer, so it writes it.</cons>
    </option>
  </options>
  <resume-signal>**ASK OD-2 FIRST, ON ITS OWN, AND DO NOT OFFER A PRE-FILLED ANSWER.**

(**W4 repair.** This block previously read `Reply with one line per decision, e.g. OD-1: A · OD-2: A
· OD-3: A · OD-6: A · OD-7: A · OD-11: A`. Five of those six have no live counter-case, so replying
with the template was the path of least resistance — and it carried OD-2, the phase's only genuine
product fork, along with it. `04-RESEARCH.md:1165` says OD-2 "is the decision most likely to reshape
the phase, so it should be asked first"; this plan quoted that sentence and then buried it fourth of
six behind a filled-in reply. A default is not a decision.)

**Step 1 — OD-2 alone.** Present its options plus this consequence, which was previously unstated:

> Under **option A** (a human supplies `alt` at dispatch), `alt: "TODO"` is **accepted by all four
> content rules** — Task 3 of 04-08 asserts exactly this. So a hurried or mistyped dispatch ships a
> photograph announced to a screen reader as "TODO", and **no control anywhere in this phase
> prevents it.** If you choose A, say whether you also want a dispatch-time refusal for
> placeholder-shaped values (`TODO`, `TBD`, `alt`, `photo`, the filename, the title). That is cheap
> to add now and awkward to retrofit once the workflow interface exists.

**Step 2 — then the remaining five** (OD-1, OD-3, OD-6, OD-7, OD-11) as one batch, since each has a
clear recommendation and no live counter-case. One line per decision.

If OD-2 resolves to C, say so explicitly — OD-7 then becomes B and criterion 1 needs amending, which
is a roadmap edit, not an executor's reinterpretation.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write src/lib/photo-pipeline.ts — the one contract</name>
  <!-- Two review repairs land in this task because it already edits both files.

       W6 — EXPORT `THUMB_PREFIX` from src/schemas/photo.ts (line 109, currently `const
       THUMB_PREFIX = '...'` with no export). This plan asks elsewhere for an assertion that the
       pipeline's thumb prefix "is the same string PhotoUrlsSchema enforces; import it if photo.ts
       exports it, otherwise add an assertion that the two agree". It does NOT export it, so that
       assertion degrades to comparing THUMB.dataUriPrefix against a literal re-typed in the test —
       which agrees with itself and proves nothing. A bare string constant is not a rival schema
       shape, so `gate:schema` is unaffected by exporting it. Verify that: run `npm run gate:schema`
       after the export and confirm exit 0.

       W7 — RETIRE the false obligation in src/lib/image-origin.ts:27. That comment says a future
       phase must add `R2_PUBLIC_URL` to the astro:env schema and a wrangler `vars` block.
       04-RESEARCH.md measured this wrong: the stated trigger is a RUNTIME consumer inside the
       Worker, and this pipeline runs on a Node runner and never reads the value. No plan in this
       phase obeys it — correct — but if it is left standing, a Phase 5 or Phase 7 reader finds a
       file asserting an unmet Phase 4 obligation. Replace it with one sentence recording that
       Phase 4 checked the condition and it did not hold, naming why. Do not delete the reasoning;
       amend it, per the 01-23 precedent that a document recording what was true on a date is
       falsified rather than improved by a blanket replace. -->
  <files>src/lib/photo-pipeline.ts, src/schemas/photo.ts, test/pipeline/photo-pipeline-contract.unit.test.ts</files>
  <behavior>
    - Two different byte buffers produce two different published keys.
    - Identical byte buffers produce the identical published key — the hash is content-addressed,
      not random, so a re-run is reproducible.
    - `publishedUrl(key)` parses as a URL whose `.origin` is exactly `IMAGE_ORIGIN` (origin
      equality, the same comparison `PhotoSchema`'s `remoteUrl` refinement makes — a `startsWith`
      is defeated by `https://HOST.evil.test/` and `https://HOST@evil.test/`).
    - `VARIANTS.map(v => v.urlKey)` deep-equals `REMOTE_URL_KEYS` — the variant table and the
      manifest's remote key list cannot drift.
    - `assertStagingKey('temp/2026-08-26-riverbend.jpg')` returns; `assertStagingKey` THROWS,
      naming the prefix, for each of: `photos/x.webp` (wrong prefix), `temp/../secrets` (traversal),
      `/temp/x.jpg` (absolute), `temp/` (empty remainder), `../temp/x.jpg`, `Temp/x.jpg` (case),
      and the empty string.
    - Every published key round-trips: `slugFromPublishedKey(publishedKey({category, slug, hash,
      suffix}))` returns the original slug, for every suffix in `VARIANTS`, including slugs that
      themselves contain hyphens and digits.
    - `photoIdFor({category, slug})` equals `` `${category}-${slug}` `` and matches the schema's
      `/^[a-z0-9-]+$/`.
  </behavior>
  <action>
Create `src/lib/photo-pipeline.ts`. Model the header on `src/lib/image-origin.ts`: state what the
module is the only definition of, and state each decision it encodes with its OD number and the
measurement behind it, in the file. A constant whose rationale lives in a plan file is one nobody
can evaluate later.

Export, implementing the decisions taken in Task 1 (values below are the recommended options —
substitute whatever Akhil actually chose, and record the choice in the header):

  - `STAGING_BUCKET` — the R2 bucket name, whose single existing source is `wrangler.jsonc`
    `"bucket_name": "portfolio-photos"`. **(W3.)** 04-10 needs it and previously re-typed the literal
    four times; a plan in wave 5 adding an export to a wave-1 file is a backwards dependency.
  - `STAGING_EXPIRE_DAYS` — the staging TTL in days, so 04-10's lifecycle assertion compares against a
    constant rather than an unsourced `7`.
  - `STAGING_PREFIX` — `'temp/'` (OD-6). The single string the workflow, the input validator, the
    delete step and the lifecycle assertion all read. Nothing else may spell it.
  - `STAGING_KEY_RE` and `assertStagingKey(key)` — anchored at both ends, rejecting `..` in any
    segment, a leading `/`, backslashes, and an empty remainder after the prefix. Throw with a
    message naming `STAGING_PREFIX` and the offending key. This is the V5 input-validation control:
    `temp_key` is attacker-influenced input that becomes an R2 object path.
  - `PUBLISHED_PREFIX` — `'photos/'`.
  - `VARIANTS` — a readonly tuple of `{ urlKey, suffix, maxWidth, quality }` for
    original/large/medium/small, exactly as measured from the legacy source. `urlKey` values must
    be the members of `REMOTE_URL_KEYS`, and the module asserts that at type level.
  - `THUMB` — `{ width: 40, quality: 60, dataUriPrefix: 'data:image/webp;base64,' }`. The prefix is
    the same string `PhotoUrlsSchema` enforces. **Import it — and add the one-word `export` HERE, in this task, not in Task 3.**

    (**B2 repair.** This previously read "Task 3 exports `THUMB_PREFIX` for this purpose", but Task 3
    runs *after* Task 2. `src/schemas/photo.ts:109` is `const THUMB_PREFIX = '...'` with no export, and
    an ESM named import of a non-exported binding is a **load-time** error — so Task 2's own test file
    would never load and its verify would exit 1 on correct work. `npm run check` cannot catch it: it
    is `biome check . && prettier --check` only, with no type resolution. Adding `export` is a
    visibility change, not a rule change, so Task 3's "do not change any zod rule" still holds.
    Confirm `npm run gate:schema` exits 0 — a bare string constant is not a rival schema shape.) (The clause that used to sit here allowed a fallback
    of "state in a comment why it is duplicated and assert the two agree", which degrades to
    comparing a value against a literal re-typed in the test — an assertion that agrees with itself.)
    Formerly this read: state
    in a comment why it is duplicated and add an assertion in the test that the two agree.
  - `CONTENT_HASH_BYTES` / `contentHash(buffer)` — `crypto.createHash('sha256')` from `node:crypto`,
    hex, sliced to 8 characters. Never hand-rolled (ASVS V6). Document the collision reasoning: 8
    hex characters is 32 bits, scoped to one slug in one category, so a collision requires two
    different byte sequences for the SAME photograph — and the failure mode of a collision is a
    re-upload serving the old bytes, not a wrong photograph.
  - `publishedKey({ category, slug, hash, suffix })` → `photos/<category>/<slug>-<hash><suffix>.webp`
    (OD-1 A) and `slugFromPublishedKey(key)` as its inverse.
  - `publishedUrl(key)` → composed from `IMAGE_ORIGIN`. **No hostname literal in this file** (OD-3).
  - `OBJECT_CACHE_CONTROL` — `'public, max-age=31536000, immutable'`, with the A1 caveat written
    beside it: whether the custom domain re-emits it is measured in 04-10, and option A does not
    depend on it.
  - `PHOTO_ID_SEPARATOR` / `photoIdFor({category, slug})`, plus a comment recording the OLD
    invariant (`id === category + '-' + basename(urls.original)`, true on 39/39 pre-hash records)
    and the NEW one (`id === category + '-' + slug`, where slug is recoverable via
    `slugFromPublishedKey`). Two eras, both written down, so nobody re-derives the wrong one.
  - `DISPATCH_INPUTS` — a readonly ordered array of `{ name, required, description }` for the
    `workflow_dispatch` interface (OD-2). Under OD-2 A that is `temp_key`, `category`, `title`,
    `alt` (all required) and `place` (optional). 04-08 generates the workflow's `inputs:` block
    against this array and 04-09's entrypoint reads argv against it, so neither can drift.
  - `PUBLISH_BRANCH` (`'main'` under OD-7 A) and `PUBLISH_RETRY_LIMIT` (`3`).

Then write `test/pipeline/photo-pipeline-contract.unit.test.ts` asserting the behaviours above.
Follow the suite's stated convention and **re-implement** the expectations rather than importing
the module's own helpers to check itself — `photo-enrichment.unit.test.ts` says why: "Importing
the merge's own parser would make this file assert that the merge agrees with itself." Compose the
expected key from string literals in the test; compare to what `publishedKey` returns.
  </action>
  <verify>
    <automated>npx vitest run --project unit test/pipeline/photo-pipeline-contract.unit.test.ts</automated>
    <automated>npm run gate:schema</automated>
    <automated>npm run check</automated>
  </verify>
  <done>The contract test is green. `gate:schema` still exits 0 — the new module declares no rival
content shape. `test -f src/lib/photo-pipeline.ts && ! grep -q 'images\.akhilsaxena\.com' src/lib/photo-pipeline.ts` succeeds: the
    (**B5 sibling repair:** `grep -c` prints `0` but **exits 1** when the count is zero — verified —
    so "returns 0" inverts the moment it is used as a shell condition. `! grep -q` is the form that
    means what this sentence intends.) The point is that the
hostname appears nowhere in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Reclassify .github/** to SCAN, write the dimensions contract, and prove both can fail</name>
  <files>scripts/assert-no-r2dev-urls.mjs, src/schemas/photo.ts, src/lib/image-origin.ts</files>
  <!-- Two review repairs land HERE, in the task that owns both files. They were previously delivered
       as an XML comment inside Task 2 — which owns neither file — so they were inert. A repair
       described is not a repair applied; that is the same defect this project keeps producing one
       level up. Both are real <action> parts below (Parts 3 and 4). -->
  <action>
**Part 1 — the gate reclassification (OD-3).** In `scripts/assert-no-r2dev-urls.mjs`, move the
`.github/**` rule from the SKIP block to the SCAN block. Rewrite its `why` to record what changed:
the Actions photo pipeline now exists and is the only writer of new photo URLs in the project, so
a workflow genuinely can carry an origin; 03-01 flagged this rule in its own summary as the one
most likely to be wrong once Phase 4 landed, and the rule's previous text said "REVISIT IN PHASE 4".
Keep the classification exhaustive — `.github/**` must still match exactly one rule.

**Measure before you commit it.** `.github/workflows/ci.yml:97` contains the string `r2.dev` inside
a prose comment. The gate's pattern is `pub-[0-9a-f]+\.r2\.dev|\.r2\.dev` — both alternatives
require a leading `pub-<hex>.` or a bare leading dot, and the comment has neither, so it should NOT
match. Confirm that by running the gate, and if it does go red, **fix the comment, do not add an
exclusion** — an exemption for "the file that mentions the hostname in passing" is a hole big
enough to hide a real URL in, which is the reasoning already written into this file's header about
its own source.

**Part 2 — the dimensions contract (OD-11).** In `src/schemas/photo.ts`, above
`PhotoDimensionsSchema`, write the contract as a comment: what `dimensions` describes, the measured
evidence (`nature-fairwayreflections` is 4608x3072 in the manifest while `urls.original` serves
2000x1333; two other records also differ), what a consumer may use it for (aspect ratio — PUB-05's
CLS reservation) and what it must not be used for (the pixel dimensions of any served variant).
Name Phase 4 as the producer that guarantees it. Do not change any zod rule; this is a contract
being written down, not a validation change.

**Part 5 — prove both gates can fail. Four steps each, run in a `git clone --no-hardlinks`
sandbox, never a `cp -r` (a worktree without `.git` fabricates four failures).**

For the `.github/**` SCAN flip:
  1. **PLANT THE DEFECT.** Add a line to `.github/workflows/ci.yml` in the sandbox containing a
     `pub-<hex>.r2.dev` URL. `npm run gate:origin` must exit 1 and name that file and that line.
  2. **NOTHING TO CHECK.** Delete `.github/` entirely in the sandbox and re-run. The gate must
     still behave correctly — either it passes with `.github` absent (there is nothing tracked to
     scan) or it reports the absence; what it must NOT do is report a SCAN result over a directory
     it never opened. Record which it does, in the SUMMARY, as a known property.
  3. **PASS ON CORRECT CODE.** With `.github/` unmodified, `npm run gate:origin` exits 0 and its
     report lists `.github/**` under SCAN with a non-zero file count. **A SCAN rule that matched
     zero files would be indistinguishable from a SKIP.** Assert the count.
  4. **WALK-THROUGH ATTEMPT.** Put the legacy hostname in `.github/workflows/deploy.yml` inside a
     `${{ secrets.X }}`-shaped string and confirm the gate still catches the literal. Then put it
     ONLY inside a secret reference (`${{ secrets.R2_PUBLIC_URL }}` with no literal) and confirm
     the gate does NOT catch it — that is the blind spot OD-3 names, and it must be recorded in
     the rule's `why` so nobody later believes the gate covers it.

For the dimensions contract: it is a comment, so the proof is different and must be stated as
such — assert that no assertion changed by running the full content suite and confirming the same
test count passes, and record in the SUMMARY that Part 2 is documentation whose enforcement is
04-07's derivation test (source metadata, not emitted variant size), not a gate here.

Capture exit codes as `if cmd; then R=0; else R=1; fi`. Never `R=$?`, never `${PIPESTATUS[0]}`.
  
**Part 3 (W6) — VERIFY the `THUMB_PREFIX` export that Task 2 added.** (Moved to Task 2 by the B2 repair, since Task 2 imports it and runs first. If it is somehow absent, add it here and record the deviation.) Formerly this task added it: It is declared at line 109 as
`const THUMB_PREFIX = 'data:image/webp;base64,'` with **no export**, so no test can import it. This
plan asks elsewhere for an assertion that the pipeline's thumb prefix is the same string
`PhotoUrlsSchema` enforces; without the export that assertion can only compare against a literal
re-typed in the test, which agrees with itself and proves nothing. Add `export`. A bare string
constant is not a rival schema shape, so `gate:schema` is unaffected — **verify that** by running
`npm run gate:schema` after the change and confirming exit 0. This is the one exception to Part 2's
"do not change any zod rule": nothing about the rule changes, only its visibility.

**Part 4 (W7) — retire the false obligation in `src/lib/image-origin.ts:27-32`.** That comment
records a "PHASE 4 OBLIGATION" to add `R2_PUBLIC_URL` to the `astro:env` schema and a `wrangler`
`vars` block. `04-RESEARCH.md` measured the premise wrong: the stated trigger is a **runtime consumer
inside the Worker**, and this pipeline runs on a Node runner and never reads the value. No plan in
this phase obeys it — correct — but left standing, a Phase 5 or Phase 7 reader finds a file asserting
an unmet Phase 4 obligation and may act on it.

**Amend, do not delete.** Per the 01-23 precedent, a document recording what was true on a date is
falsified rather than improved by a blanket replace. Replace the obligation with one sentence stating
that Phase 4 **checked** the condition, that it did not hold, and why — naming the Node-runner
reason. Keep the surrounding rationale about why the origin lives in a plain module.

</action>
  <verify>
    <automated>npm run gate:origin</automated>
    <automated>npm run gate:content</automated>
    <automated>npx vitest run --project unit test/content/schemas.unit.test.ts</automated>
  </verify>
  <done>`gate:origin` exits 0 on the clean tree with `.github/**` listed under SCAN and a non-zero
scanned-file count; it exits 1 with a planted `pub-<hex>.r2.dev` in a workflow, naming the file.
All four proof steps are recorded verbatim in the SUMMARY, including step 4's negative result
(a value hidden inside a secret reference is invisible to this gate). `src/schemas/photo.ts`
carries the `dimensions` contract and its zod rules are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `workflow_dispatch` input → R2 object path | `temp_key` is caller-supplied text that becomes a key in a bucket the pipeline has write credentials for. |
| `workflow_dispatch` input → committed manifest record | `category`, `title`, `alt`, `place` cross from a caller into a file on `main` and, in Phase 5, into rendered HTML. |
| repository secret → produced URL | `R2_PUBLIC_URL` could silently point every new photo at a retired origin, and no gate can see inside a secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `assertStagingKey` in `src/lib/photo-pipeline.ts` | mitigate | Anchored regex scoped to `STAGING_PREFIX`, rejecting `..`, absolute forms, backslashes and an empty remainder; seven negative cases asserted in the contract test. Dropping this lets a caller point processing at an arbitrary bucket key. |
| T-04-05 | Information disclosure | `publishedUrl` / origin composition | mitigate | OD-3 A: compose from `IMAGE_ORIGIN`; the module contains no hostname literal (asserted by grep in Task 2's `done`). The pipeline structurally cannot emit a non-canonical origin. |
| T-04-06 | Spoofing | `PhotoSchema` remote-URL comparison | mitigate | Origin **equality**, not `startsWith` — `https://HOST.evil.test/` and `https://HOST@evil.test/` both defeat a prefix comparison. Already implemented in `photo.ts`; the contract test asserts `publishedUrl` output satisfies it. |
| T-04-07 | Tampering | `.github/**` scanned for a legacy origin | mitigate | SKIP → SCAN, with the residual blind spot (a wrong value inside `${{ secrets.* }}`) measured in Task 3 step 4 and written into the rule's `why`. |
| T-04-08 | Elevation of privilege | `contentHash` | mitigate | `node:crypto` `createHash('sha256')`; never hand-rolled. Collision reasoning documented; the failure mode is a stale re-upload, not a wrong photograph. |
| T-04-09 | Information disclosure | `private/*-clean.webp` — 39/39 unwatermarked masters publicly downloadable today | accept (deferred) | **Pre-existing and DEFERRED BY AKHIL 2026-08-26 to Phase 8**, recorded in `STATE.md` Blockers and on Phase 8's roadmap line. This plan does not fix it and **must not make it worse**: `src/lib/photo-pipeline.ts` defines no `private/` key helper, so nothing downstream can compose one from this contract. What a new run uploads is OD-9, decided in 04-07. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing (`npm ci` only). The Package Legitimacy Gate and its blocking checkpoint live in 04-04, which is the only plan that touches `package.json` dependencies. |
</threat_model>

<verification>
- `npx vitest run --project unit` exits 0, including the new contract test.
- `npm run gate:content` exits 0 with `.github/**` under SCAN and a non-zero scanned count.
- `npm run check` and `npm run typecheck` exit 0.
- `test -f src/lib/photo-pipeline.ts && ! grep -q 'images\.akhilsaxena\.com' src/lib/photo-pipeline.ts` succeeds (**B5**: `grep -c` exits 1 on a zero count, so "returns 0" inverts as a condition).
- `node -e "import('./src/lib/photo-pipeline.ts')"`-equivalent through the test run resolves the
  module without a bundler — the scripts in `scripts/` import from `src/lib/` the same way
  `assert-no-r2dev-urls.mjs` already does.
</verification>

<success_criteria>
- Six open decisions recorded, each implemented as written, each traceable to its OD number in the
  module's own header.
- One module is the sole definition of the staging prefix, the published key scheme, the variant
  table, the object cache policy and the dispatch input names. 04-05/06/07/08/09 import it.
- CONT-05's mechanism exists in code: two different byte sequences cannot share a URL.
- `gate:origin` covers `.github/**`, and its one remaining blind spot is written into the rule.
- `dimensions` has a written contract before Phase 5 builds on it.
</success_criteria>

<output>
Create `.planning/phases/04-photo-pipeline-actions-half/04-02-SUMMARY.md` when done. It must record
the six decisions as taken, the exported surface of `src/lib/photo-pipeline.ts`, and the verbatim
output of all four proof steps for the `.github/**` reclassification — including step 4's negative
result.
</output>
