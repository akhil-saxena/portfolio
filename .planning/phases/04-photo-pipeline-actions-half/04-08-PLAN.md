---
phase: 04-photo-pipeline-actions-half
plan: 08
type: execute
wave: 2   # W-g: depends only on 04-02 (wave 1); 3 was over-serialised
depends_on: ["04-02"]
files_modified:
  - .github/workflows/process-photos.yml
  - scripts/lib/dispatch-input.mjs
  - test/pipeline/dispatch-input.unit.test.ts
  - test/pipeline/workflow-contract.unit.test.ts
autonomous: false
requirements: [PIPE-02, PIPE-05]
owns_criteria: [4]
owns_open_decisions: [OD-8]
user_setup:
  - service: github
    why: "A pipeline commit pushed with GITHUB_TOKEN triggers no workflow, so the photo never deploys. Criterion 5 is unreachable without a token that triggers events normally."
    env_vars:
      - name: PHOTO_PIPELINE_APP_ID
        source: "GitHub → Settings → Developer settings → GitHub Apps → the app's App ID (OD-8 option A)"
      - name: PHOTO_PIPELINE_APP_PRIVATE_KEY
        source: "The same app → Generate a private key → paste the whole PEM (OD-8 option A)"
      - name: PHOTO_PIPELINE_TOKEN
        source: "GitHub → Settings → Developer settings → Fine-grained tokens → single repository akhil-saxena/portfolio, Contents: Read and write, with an expiry (OD-8 option B — only if B is chosen instead of A)"
    dashboard_config:
      - task: "Install the GitHub App on akhil-saxena/portfolio with Contents: Read and write, and nothing else"
        location: "GitHub App settings → Install App (OD-8 option A only)"
      - task: "Confirm whether the existing CLOUDFLARE_API_TOKEN (created 2026-08-19 for wrangler deploy) also carries R2 Storage → Edit. It was created for Workers Scripts Edit; R2 Edit is a separate permission and the scope is not readable from outside the dashboard."
        location: "Cloudflare dashboard → My Profile → API Tokens"

must_haves:
  truths:
    - "`gh workflow run process-photos.yml -f temp_key=… -f category=… -f title=… -F alt=@alt.txt` is a valid invocation and the job validates its inputs before touching anything"
    - "A temp_key outside the staging prefix, or containing traversal, is rejected before any R2 read"
    - "A category that does not exist in site_config is rejected before any R2 read"
    - "An `alt` violating any of PhotoSchema's four rules is rejected before any R2 read"
    - "The workflow's declared inputs are exactly DISPATCH_INPUTS — neither can drift from the other"
    - "Pipeline runs queue against each other rather than cancelling, and checkout depth is set deliberately"
  artifacts:
    - path: ".github/workflows/process-photos.yml"
      provides: "The workflow_dispatch trigger, inputs, permissions, token, concurrency, checkout depth and the validation step"
      min_lines: 90
    - path: "scripts/lib/dispatch-input.mjs"
      provides: "Validation of every dispatch input, run before any side effect"
      exports: ["validateDispatchInputs", "DispatchInputError"]
      min_lines: 90
    - path: "test/pipeline/workflow-contract.unit.test.ts"
      provides: "Parses the YAML and asserts the trigger, inputs, permissions, pinning and concurrency contract"
      min_lines: 90
  key_links:
    - from: ".github/workflows/process-photos.yml"
      to: "src/lib/photo-pipeline.ts DISPATCH_INPUTS"
      via: "the workflow-contract test asserts the YAML inputs equal DISPATCH_INPUTS, name for name"
      pattern: "DISPATCH_INPUTS"
    - from: "scripts/lib/dispatch-input.mjs"
      to: "src/lib/photo-pipeline.ts assertStagingKey"
      via: "temp_key validation uses the one anchored pattern; the workflow contains no prefix literal"
      pattern: "assertStagingKey"
    - from: "scripts/lib/dispatch-input.mjs"
      to: "data/site_config.json"
      via: "category is checked against the real id set, with no case transform — the RI-1 comparison"
      pattern: "site_config"
---

<objective>
Build the interface: a `workflow_dispatch` job that takes a staged object key and the human-authored
metadata, **validates every input before any side effect**, and is drivable from one command line
with no admin UI in existence.

This settles the `/api/upload` vs `/api/dispatch` question with evidence rather than preference.
Measured: `git grep -n "api/dispatch" legacy/nextjs-portfolio` returns **zero call sites in `src/`**
— three prose documents and nothing else — while `/api/upload` has exactly one, at
`src/app/admin/page.tsx:249`. So the wired path base64'd the whole file through a Worker isolate and
`PUT` it to `new-photos/`, putting a 25 MB blob in git history permanently. `PROJECT.md:248` already
records the decision: implement the R2 staging design, "the dead `/api/dispatch` code was the better
design, just abandoned."

**Consequence to plan for, and it is a hard one:** `new-photos/` must not be created. It is not a
tracked directory today, and a new top-level path **fails `gate:origin`** with `unclassified path`
until someone adds a rule with a written reason. Every file this plan creates lives under
`.github/`, `scripts/` or `test/`, all of which are already classified.

Purpose: PIPE-02 (drivable from the command line) and PIPE-05's serialisation half.
Output: the workflow's trigger, inputs, permissions, token, concurrency and checkout depth; an
input validator; and a contract test that parses the YAML so the two cannot drift.

**Scope boundary, stated so nobody fills it in silently:** this plan writes the workflow **up to
and including input validation**. The job's processing steps — the R2 read, the derivation, the
upload, the liveness check, the commit and the staged-object delete — are 04-09's, which lands in
the next wave. Until it does, the job validates and exits 0 with a step whose name says so. Do not
dispatch it against a real staged object before 04-09 is on `main`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-photo-pipeline-actions-half/04-RESEARCH.md
@.planning/phases/04-photo-pipeline-actions-half/04-VALIDATION.md
@.planning/phases/04-photo-pipeline-actions-half/04-02-SUMMARY.md

Read before writing — every convention below is already established in these two files and must be
matched, not reinvented:
@.github/workflows/ci.yml
@.github/workflows/deploy.yml

<interfaces>
From `src/lib/photo-pipeline.ts` (04-02):
  STAGING_PREFIX, STAGING_KEY_RE, assertStagingKey
  DISPATCH_INPUTS   // ordered [{ name, required, description }]
  PUBLISH_BRANCH, PUBLISH_RETRY_LIMIT

From `src/schemas/photo.ts` — the four `alt` rules the validator must apply BEFORE any R2 read,
re-implemented independently per the suite's stated convention:
  non-empty after trim; not equal to `title` compared case- and whitespace-insensitively;
  must not open with `image of` / `photo of` / `picture of`; must not contain `[AKHIL-`.

`data/site_config.json` → `categories[].id`, seven values today:
  abstract, architecture, nature, portraits, product, street, wildlife
RI-1 compares with **no case transform on either side**, so `Nature` is a different value and is
refused rather than coerced. The validator must match that exactly.

Measured `gh` surface (gh 2.93.0): `gh workflow run <file> --ref main -f k=v` (raw field) and
`-F k=@file` (respects `@` syntax, so long alt text comes from a file). `workflow_dispatch` allows
**25** top-level inputs and a 65,535-character payload — note this contradicts the widely repeated
"10 inputs" figure, which the current docs do not state.

Existing workflow conventions in this repository, all of which apply here:
  - third-party actions pinned to a **full commit SHA**, with the version in a trailing comment:
    actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
    actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
  - `node-version-file: .nvmrc`, `cache: npm`, then `npm ci`
  - a `permissions:` block, least privilege, stated at the top
  - credentials scoped to the **single step** that needs them, never the job or the workflow
  - `fetch-depth: 0` in both existing workflows, added after depth-1 broke Deploy at run
    `32941901693` on 2026-08-26 07:15 — four migration proofs walk `git log -- <file>` and THROW
    rather than pass vacuously under a shallow clone
  - `concurrency`: `ci.yml` cancels superseded runs; `deploy.yml` queues them
    (`cancel-in-progress: false`) with the reason written beside it
</interfaces>

**Shell discipline.** Interactive shell is **zsh**; **Actions steps run bash**. Any snippet in the
workflow is bash and may use bash constructs; any snippet you run locally to verify is zsh and may
not use `${PIPESTATUS[0]}`. State which shell each control ran under in the SUMMARY. Capture exit
codes as `if cmd; then R=0; else R=1; fi` — never `R=$?`, never a subshell assignment.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: OD-8 — how a pipeline commit reaches the live site</name>
  <decision>Which credential the pipeline pushes with, and therefore whether criterion 5 is
reachable at all. This one has a security dimension in both directions.</decision>
  <context>
**Verified, and it changes the design.** GitHub documents that events triggered by the repository's
`GITHUB_TOKEN` do not create a new workflow run: "if a workflow run pushes code using the
repository's `GITHUB_TOKEN`, a new workflow will not run even when the repository contains a
workflow configured to run when `push` events occur." The exceptions are `workflow_dispatch` and
`repository_dispatch`, and **a GitHub App installation token or a personal access token**, which
trigger events normally.

This repository's deploy chain is `push → CI → workflow_run → Deploy`, and `deploy.yml`
deliberately has **no push trigger and no manual-dispatch trigger**, with its own comment saying
why: "either would be a way to reach the publish without a green CI run." Confirmed live — every
`Deploy` run has event `workflow_run`, each preceded by a green `CI` run on `push`.

**Therefore: a pipeline commit pushed with `GITHUB_TOKEN` lands on `main`, runs no CI, and deploys
nothing.** The photo appears in the manifest and not on the site until the next human push.
**Criterion 5 ("re-uploading a photo serves the new bytes from the CDN") is unreachable as the
repository stands**, and this decision is the only thing that changes that.
  </context>
  <options>
    <option id="OD-8-A">
      <name>A (recommended) — a GitHub App installation token</name>
      <pros>`actions/create-github-app-token` mints a short-lived, repo-scoped token that triggers
`push` events normally, so the existing `push → CI → Deploy` chain runs unchanged and remains the
**only** deploy path. Short-lived and revocable by uninstalling the app.</pros>
      <cons>Needs an app created and installed, and two secrets (`APP_ID`, `PRIVATE_KEY`). More
setup than B.</cons>
    </option>
    <option id="OD-8-B">
      <name>B — a fine-grained PAT, single repository, `contents: write`, with an expiry</name>
      <pros>Simpler to provision and triggers events normally too. Phase 7's admin will need a
repo-write credential in the Worker regardless, so this is not a new class of secret for the
project.</pros>
      <cons>A long-lived credential sitting in repository secrets. Rotation is a diary entry rather
than a mechanism.</cons>
    </option>
    <option id="OD-8-C">
      <name>C — add the photo workflow to `deploy.yml`'s `workflow_run.workflows` list</name>
      <pros>No new credential.</pros>
      <cons>**Do not do this** without also making the photo workflow run the full gate chain. It
creates a **second deploy path**, and only one of the two would be gated — the exact hazard
`deploy.yml`'s header exists to prevent, and the same "two deploy paths, one gated" reasoning that
already rules out connecting Cloudflare Workers Builds to this repository.</cons>
    </option>
    <option id="OD-8-D">
      <name>D — accept it</name>
      <pros>Honest and costs nothing. The photo is committed and appears at the next human push.</pros>
      <cons>Makes criterion 5 **false as written**. If this is the answer, the roadmap's criterion 5
must be amended in writing, not quietly reinterpreted — and 04-10's CONT-05 verification becomes a
manual "push something, then check" rather than an end-to-end proof.</cons>
    </option>
  </options>
  <resume-signal>Reply `OD-8: A`, `B`, `C` or `D`. If A or B, the secrets listed in this plan's
`user_setup` must exist before 04-09 can be dispatched — provision them and say so. If D, confirm
that criterion 5 is being amended rather than claimed.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The dispatch input validator — everything checked before any side effect</name>
  <files>scripts/lib/dispatch-input.mjs, test/pipeline/dispatch-input.unit.test.ts</files>
  <behavior>
    RED first. Every case below is a rejection with a message naming the input and the rule.

    `temp_key` — the input that becomes a bucket path, so it gets the most cases:
    - accepted: `temp/2026-08-26-riverbend.jpg`, `temp/a/b/c.jpg`
    - rejected: `photos/x.webp` (outside the staging prefix), `temp/../secrets`,
      `temp/a/../../x`, `/temp/x.jpg` (absolute), `temp/` (empty remainder), `Temp/x.jpg` (case),
      `temp/x\y.jpg` (backslash), the empty string, and a key longer than R2's 1,024-byte limit.
    - the rejection message names `STAGING_PREFIX`. It must not echo the whole offending key
      unbounded — truncate, so a hostile key cannot flood a log.

    `category`:
    - accepted: each of the seven ids read from `data/site_config.json` at validation time, not
      from a list in the validator.
    - rejected: `Nature` (case variant — RI-1 applies no case transform), `archtecture` (typo),
      the empty string, and a value with a leading or trailing space.
    - the rejection message lists the legal ids, because that is the one thing the caller needs.

    `title`: rejected when empty or whitespace-only.

    `alt` — all four `PhotoSchema` rules, applied here so a bad value costs nothing:
    - rejected: empty/whitespace; equal to `title` after case- and whitespace-insensitive
      normalisation; opening with `Image of` / `photo of` / `Picture of` (assert all three, and
      assert case-insensitivity); containing `[AKHIL-`.
    - accepted: a real sentence.
    - **`"TODO"` is accepted by these four rules and that is the measured, intended behaviour** —
      assert it, with a comment naming OD-2. A test that expected `TODO` to be rejected would be
      asserting a rule the schema does not have, and the honest control is that a human supplies
      `alt`, not that a validator can recognise a bad one.

    `place` (optional): absent is fine; present-and-empty is rejected — `PhotoSchema` declares
    `z.string().min(1).optional()`, so an empty string is a failure and an absent key is not.

    Anti-vacuity:
    - `validateDispatchInputs({})` rejects, naming every required input.
    - The validator's required-input list is derived from `DISPATCH_INPUTS`, and the test asserts
      that removing an input from that array changes what the validator demands. A validator whose
      required set is hardcoded would silently stop checking a new input.
  </behavior>
  <action>
Write `scripts/lib/dispatch-input.mjs`. It imports `assertStagingKey` and `DISPATCH_INPUTS` from
`src/lib/photo-pipeline.ts` and reads the legal category ids from `data/site_config.json` at
validation time. **No prefix literal, no category list, no origin literal in this file.**

`validateDispatchInputs(raw, { siteConfigPath })` returns a normalised, typed object or throws
`DispatchInputError` carrying **every** finding, not just the first — the same accumulate-then-fail
convention `src/schemas/content-set.ts` and both existing gate scripts use. A caller fixing four
mistakes one dispatch at a time is a bad interface.

**Do not normalise inputs into validity.** Do not lowercase `category`, do not trim `alt` into
shape, do not rewrite `temp_key`. Reject and say why. `PhotoSchema`'s `category` error already
states the reasoning: the comparison applies no case transform "so `Abstract` is a different value
from `abstract` and is refused here rather than silently coerced."

Give the module a small CLI entry so the workflow can run it as a step without the rest of the
pipeline existing: reading the inputs from environment variables the workflow sets, printing the
findings and exiting 1 on any. Keep the CLI behind a guard so importing the module never runs it.

RED → GREEN → REFACTOR, three atomic commits.
  </action>
  <verify>
    <automated>npx vitest run --project unit test/pipeline/dispatch-input.unit.test.ts</automated>
    <automated>npx vitest run --project unit</automated>
    <automated>npm run check</automated>
  </verify>
  <done>Every listed case is covered and named. `grep -nE "'temp/'|\"temp/\"|abstract|architecture|images\.akhilsaxena\.com" scripts/lib/dispatch-input.mjs` returns nothing outside comments — the prefix and the category set each have one definition elsewhere.</done>
</task>

<task type="auto">
  <name>Task 3: The workflow, and a contract test that parses it</name>
  <files>.github/workflows/process-photos.yml, test/pipeline/workflow-contract.unit.test.ts</files>
  <action>
**Write `.github/workflows/process-photos.yml`**, matching every convention in `ci.yml` and
`deploy.yml` — read both first; their header comments explain each choice and this file gets the
same treatment.

  - `name: Process Photos`
  - `on: workflow_dispatch:` with `inputs:` generated from `DISPATCH_INPUTS` — same names, same
    `required` flags, same descriptions. **No push trigger.** `new-photos/**` does not exist and
    must not be created; say so in the header with the `gate:origin` reason.
  - `permissions:` — least privilege, stated at the top with its reasoning. Under OD-8 A or B the
    push uses the app/PAT token, so `GITHUB_TOKEN` itself needs no write; say that explicitly,
    because a `contents: write` left in "just in case" is a standing grant nobody re-reads.
  - `concurrency: { group: photo-pipeline, cancel-in-progress: false }` — **queue, do not cancel**,
    with the reason written beside it and a pointer to `deploy.yml`'s identical setting: a
    cancelled run mid-publish is how the bucket and the manifest end up disagreeing. Note that this
    serialises pipeline runs against **each other**; contention with a human push is handled by
    04-06's retry loop, not by this block.
  - `actions/checkout` pinned to the same full SHA `ci.yml` uses, with **`fetch-depth` set
    deliberately and commented**. Both existing workflows set `0` after depth-1 broke Deploy at run
    `32941901693`; do the same and state the reason here — the retry loop fetches and resets, and
    if this job ever runs `npm test` the four migration proofs throw rather than skip under a
    shallow clone. **Do not inherit the default silently.**
  - `actions/setup-node` pinned to the same full SHA, `node-version-file: .nvmrc`, `cache: npm`,
    then `npm ci`.
  - Under OD-8 A: `actions/create-github-app-token` **pinned to a full commit SHA**, minting the
    token in its own step, with the app secrets scoped to that step only.
  - A **validate inputs** step running the CLI from Task 2 with the dispatch values passed through
    `env:` — never interpolated into a `run:` shell string. `${{ inputs.alt }}` inside a `run:`
    line is a shell-injection surface: `alt` is caller-supplied text and a `run:` block is bash.
    Pass every input as an environment variable and let the Node process read it.
  - A final step named to make the boundary obvious, e.g.
    `Pipeline steps land in plan 04-09 — this run validated inputs only`, echoing what was
    validated and exiting 0.
  - **No secret at workflow or job level.** Each credential appears in the `env:` of the one step
    that needs it, exactly as `deploy.yml` does and for the reason its comment gives.
  - **No `R2_PUBLIC_URL` anywhere** (OD-3): the pipeline composes URLs from `IMAGE_ORIGIN` and this
    workflow reads no origin at all. Note in the header that `gate:origin` now SCANs `.github/**`
    (04-02) and that its remaining blind spot is a wrong value inside a `${{ secrets.* }}`
    reference, which is precisely why the origin is not read from a secret.

**Write `test/pipeline/workflow-contract.unit.test.ts`** — parse the YAML and assert:
  1. The only trigger is `workflow_dispatch`. Assert the key set, not just the presence of
     `workflow_dispatch`; a `push:` added later must fail this test.
  2. `Object.keys(on.workflow_dispatch.inputs)` equals `DISPATCH_INPUTS.map(i => i.name)` **in
     order**, and each input's `required` matches. This is the anti-drift link between the workflow
     and the contract module.
  3. Input count is at most 25 (the documented `workflow_dispatch` limit).
  4. `concurrency.cancel-in-progress` is `false` and `concurrency.group` is a non-empty string.
  5. Every `uses:` value ends in `@<40 hex characters>` — no tags, no branches. Assert the count of
     `uses:` entries is greater than zero first, so the loop cannot pass over an empty list.
  6. `fetch-depth` is present on the checkout step and is `0`. Its **absence** must fail, which is
     what "decide it deliberately" means in executable form.
  7. `permissions:` exists at workflow level.
  8. No `secrets.` reference appears at workflow or job level — only inside a step's `env:`.
  9. No `${{ inputs.` interpolation appears inside any `run:` block. This is the injection
     assertion, and it must be written against the parsed YAML rather than a raw grep so a
     multi-line block scalar cannot hide one.
 10. The file contains no legacy-origin literal — belt and braces alongside `gate:origin`, which now
     scans this tree.

**Prove the contract test can fail. Four steps, each recorded verbatim, run in a
`git clone --no-hardlinks` sandbox — never a `cp -r` of the worktree.**
  1. **PLANT THE DEFECT**, once per assertion class, and confirm each fails **naming the rule**:
     add a `push:` trigger (assertion 1); rename one input (assertion 2); set
     `cancel-in-progress: true` (4); change one `uses:` to a tag (5); delete `fetch-depth` (6);
     move a secret to job level (8); put `${{ inputs.alt }}` inside a `run:` line (9). Seven
     plants. **A single "the workflow is valid" test that fails for all seven for the same reason
     is not seven assertions.**
  2. **NOTHING TO CHECK.** Point the test at an empty YAML file and at a missing file. It must fail
     naming the cause, never pass over an absent workflow.
  3. **PASS ON CORRECT CODE.** The committed workflow satisfies all ten assertions, and the test
     reports how many `uses:` entries and how many inputs it checked — a count, not a bare PASS.
  4. **WALK-THROUGH ATTEMPT.** Try to satisfy assertion 9 while still being injectable: pass an
     input through `env:` and then reference `$ALT` unquoted inside a `run:` block. Record whether
     the assertion catches it. If it does not — and a raw `$VAR` in bash is word-split, not
     evaluated, so it probably is safe but must be checked rather than assumed — write the residual
     boundary into the test's header rather than leaving a false sense of coverage.

Also confirm, and record: `npm run gate:origin` still exits 0 with the new workflow present, and
its report lists `.github/**` under SCAN with a file count that has increased by one.
  </action>
  <verify>
    <automated>npx vitest run --project unit test/pipeline/workflow-contract.unit.test.ts</automated>
    <automated>npm run gate:content</automated>
    <automated>npm test</automated>
  </verify>
  <done>All ten assertions green against the committed workflow, with the counts reported. All
seven plants fail, each naming its own rule. `gate:origin` exits 0 and shows `.github/**` under
SCAN. `gh workflow list` shows `Process Photos` after the commit reaches `main` — record the
output, and record that it has NOT been dispatched, because 04-09 has not landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher → workflow inputs | Anyone who can write to the repository can dispatch this workflow. Their `temp_key` becomes a bucket path and their `alt`/`title` become committed content. |
| workflow inputs → bash `run:` blocks | A `run:` block is bash; `${{ inputs.x }}` is textually substituted before bash sees it. |
| runner → `main` | Under OD-8 A/B the job holds a token that can push to the default branch and, by design, trigger CI. |
| third-party actions → the runner | Every action runs in the same job as that token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-34 | Tampering | `temp_key` → R2 object path | mitigate | `assertStagingKey` anchored to `STAGING_PREFIX`, rejecting traversal, absolute forms, backslashes, case variants and over-long keys — ten negative cases asserted. The legacy `/api/dispatch` did exactly this with `/^temp\/[a-zA-Z0-9._/-]+$/`; dropping it lets a caller point processing at an arbitrary key. |
| T-04-35 | Elevation of privilege | `${{ inputs.* }}` inside a `run:` block | mitigate | Every input reaches Node through `env:`, never through string interpolation into bash. Assertion 9 checks this against the **parsed** YAML so a block scalar cannot hide one, and Task 3 step 4 probes the residual boundary rather than assuming it. |
| T-04-36 | Elevation of privilege | A long-lived push credential | mitigate | OD-8 A prefers a short-lived GitHub App installation token, repo-scoped, `contents: write` only, minted in its own step with the app secrets scoped to that step. Option B (fine-grained PAT, single repo, with an expiry) is accepted as a slower-rotating equivalent. |
| T-04-37 | Tampering | A second, ungated deploy path | mitigate | OD-8 C is called out as the thing not to do: adding this workflow to `deploy.yml`'s `workflow_run.workflows` would create a deploy path that no gate chain protects, which is the hazard `deploy.yml`'s header exists to prevent and the same reasoning that keeps Cloudflare Workers Builds disconnected. |
| T-04-38 | Supply chain | Third-party actions | mitigate | Every `uses:` pinned to a full 40-character commit SHA with the version in a trailing comment — the convention both existing workflows already state, now enforced by assertion 5 rather than by review. |
| T-04-39 | Information disclosure | Credentials in step scope | mitigate | No secret at workflow or job level; each appears in the `env:` of the one step that needs it, asserted by assertion 8. Same rule and same reason as `deploy.yml`. |
| T-04-40 | Information disclosure | Logging a hostile input | mitigate | Rejection messages truncate the offending value, so a 1,024-byte crafted key cannot flood a public workflow log. |
| T-04-41 | Denial of service | Concurrent dispatches | mitigate | `concurrency: { group: photo-pipeline, cancel-in-progress: false }` — queue, do not cancel. Contention with a human push is 04-06's retry loop, not this block. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing; the workflow runs `npm ci` from the committed lockfile only. |
</threat_model>

<verification>
- `npx vitest run --project unit` exits 0 and lists both new files by name.
- `npm test` exits 0.
- `npm run gate:content` exits 0 with `.github/**` under SCAN and an increased file count.
- `npm run check` and `npm run typecheck` exit 0.
- `gh workflow list` shows `Process Photos` once the commit is on `main`.
- The workflow has **not** been dispatched — 04-09 has not landed.
</verification>

<success_criteria>
- OD-8 recorded, and its `user_setup` items provisioned or explicitly deferred with criterion 5
  amended in writing.
- Every dispatch input is validated before any side effect, with rejections that name the input and
  the rule and list every finding at once.
- The workflow's declared inputs cannot drift from `DISPATCH_INPUTS` — a rename fails a test.
- Seven distinct defects planted in the workflow each fail their own assertion by name.
- No new top-level path was created, so `gate:origin` reports no `unclassified path`.
</success_criteria>

<output>
Create `.planning/phases/04-photo-pipeline-actions-half/04-08-SUMMARY.md` when done, recording OD-8
as taken, which `user_setup` items now exist, the verbatim result of all seven plants, and step 4's
finding on the residual injection boundary.
</output>
