---
phase: 05-public-site
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]   # B4b: both add a REQUIRED schema field and both run `npm run build`; the content gate validates all five files on every build, so concurrently they red each other
files_modified:
  - src/schemas/resume.ts
  - data/resume.json
  - scripts/migrate-experience-metric.mjs
  - scripts/assert-no-unresolved-placeholders.mjs
  - test/content/experience-metric.unit.test.ts
autonomous: false
requirements: [PUB-02]
owns_criteria: []
owns_open_decisions: [OQ-1b]
user_setup:
  - service: Akhil (content author)
    why: "The three employment metrics are the only content in this phase nobody else can supply. They exist only in a Phase 0 sketch; ExperienceEntry has no metric field and deriving one from the bullets was measured wrong on one of three (05-UI-SPEC.md §14.5 OQ-1b)."
    dashboard_config:
      - task: "Confirm or replace the three sketch metrics: +15% CONVERSION (Brevo), 4K+ FRANCHISES (PharmEasy), 6× FASTER PIPELINES (MAQ Software)"
        location: "The checkpoint in task 1 of this plan"
must_haves:
  truths:
    - "Each of the three employment entries carries a metric the Work band can right-align"
    - "A {{…}} placeholder that survives into a rendered public page fails the build"
    - "The placeholder refusal has been proven able to fail on a planted token, on nothing to check, and to pass on correct output"
  artifacts:
    - path: "src/schemas/resume.ts"
      provides: "ExperienceEntry.metric as a required { value, label }"
      contains: "metric"
    - path: "scripts/assert-no-unresolved-placeholders.mjs"
      provides: "The OQ-1b build refusal over rendered HTML"
    - path: "data/resume.json"
      provides: "Three metric records"
  key_links:
    - from: "scripts/assert-no-unresolved-placeholders.mjs"
      to: "dist/client/**/*.html"
      via: "scan of built public routes for an unresolved token"
      pattern: "\\{\\{"
---

<objective>

**RESOLVED — do NOT stop at this checkpoint. Take the three sketch values.**

Akhil asked for placeholders and said he would supply real metrics later. **Measured, that option is
not "decide later" — it is a hard stop at wave 3.** `gate:placeholders` is chained into
`gate:content` by 05-14, but 05-09 Task 1 already runs
`assert-no-unresolved-placeholders.mjs dist` under `set -e`, so a surviving `{{…}}` token fails that
task; and 05-13 → 05-14 → 05-15 all depend on 05-09. Deferring blocks **waves 4, 5, 6 and 7**.

So the values below are landed now. **They are not invented** — each traces to a bullet in
`data/resume.json` that Akhil wrote and reviewed, quoted here so the provenance travels with them:

| Company | Metric | Supporting bullet |
|---|---|---|
| Brevo | `+15%` / `CONVERSION` | *"Improved **conversion by 15%** by transforming a one-page checkout for 2.5M+ users into a 3-step flow"* |
| PharmEasy | `4K+` / `FRANCHISES` | *"…enhancing productivity for **4K+ franchises** across **4 countries**"* |
| MAQ Software | `6×` / `FASTER PIPELINES` | *"Improved pipeline execution time by **6×** by replacing Power Automate workflows with Azure Data Factory"* |

**MAQ's is bullet FOUR, not bullet one.** That is why this plan's option 2 — deriving each metric
from its first bullet — was rejected: it yields `7+ data sources` for MAQ, which is wrong. A
derivation right two times in three encodes a false relationship into a renderer.

These are ordinary editable content in `data/projects.json` once landed. Akhil changes them whenever
he likes; the difference is that the phase is not blocked while he decides, and the build stays green.

**The placeholder mechanism still ships.** `assert-no-unresolved-placeholders.mjs` and its wiring into
`gate:content` are unchanged and still required — so if a `{{…}}` token ever reaches a rendered public
route, the build fails. The guard is built; it simply is not tripped today.

Close OQ-1b: give the employment band the field it renders, and make it impossible for a
placeholder to go live.

Purpose: §0.3 measured that `ExperienceEntrySchema` has no `metric`. §14.5 resolves that Akhil
supplies the values and that **the placeholder must fail the build rather than sit quietly.** This
project has paid for that lesson twice — Phase 3 stored `{{ds.componentCount}}` and made the schema
refuse the literal form so a stale figure could not return; Phase 4 measured that `alt: "TODO"`
passes all four content rules, which is exactly why Akhil then asked for a placeholder refusal on
`alt`. An unguarded placeholder ships, and the employment band is the first thing a hiring manager
reads on `/work`.

Output: a required `metric` field, three records, and one refusal that guards every `{{…}}` token
on the site — the metric tokens if they land, and `{{ds.componentCount}}` / `{{ds.categoryCount}}`
whether they land or not.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-site/05-UI-SPEC.md — §0.3 (the metric field does not exist), §10
(the Work employment band: company Playfair 22/700, role · period DM Sans 13 `--ink-3`, metric
right-aligned mono 11px in `--ochre-d-strong`), §14.5 OQ-1b (the resolution and the two
precedents), §15 OQ-1 (why option 2 — deriving the metric from the first bold span — was rejected:
it yields `7+ data sources` for MAQ, whose real metric is in bullet 4).

@src/schemas/resume.ts — read the header. It records what is deliberately absent and why, and it
asks `src/lib/period.ts` for the date invariant rather than restating it. The new field follows the
same discipline.

@scripts/assert-no-raw-html-sinks.mjs — the gate shape: parameterised scan root, refusal on an
empty root, per-rule canary self-check before scanning.
</context>

<interfaces>
<!-- The three sketch metrics and their provenance. MEASURED by 05-UI-SPEC §14.5 against the
     reviewed bullets in data/resume.json — this was traced, not assumed. -->

| Company      | Sketch metric         | Supporting bullet                                            |
|--------------|-----------------------|--------------------------------------------------------------|
| Brevo        | `+15% CONVERSION`     | "Improved **conversion by 15%** by transforming a one-page checkout…" |
| PharmEasy    | `4K+ FRANCHISES`      | "enhancing productivity for **4K+ franchises** across **4 countries**" |
| MAQ Software | `6× FASTER PIPELINES` | "Improved pipeline execution time by **6×**…" — **bullet 4**, not bullet 1 |

The field shape:  metric: { value: string, label: string }
  Brevo        → { value: "+15%", label: "CONVERSION" }
  PharmEasy    → { value: "4K+",  label: "FRANCHISES" }
  MAQ Software → { value: "6×",   label: "FASTER PIPELINES" }

Split into two because §10 renders them as one mono line but the value carries `--ochre-d-strong`
while the label is the same size in the ink ramp — and because a single string forces the renderer
to parse it back apart.

The placeholder forms, if the checkpoint defers:
  { value: "{{metric.value}}", label: "{{metric.label}}" }
</interfaces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: The three employment metrics</name>
  <decision>What goes in the metric column of the three employment rows on /work.</decision>
  <context>
    The reviewed Work design puts a right-aligned mono figure at the end of each employment row.
    That figure exists nowhere in the data. It exists in a Phase 0 sketch, and §14.5 traced all
    three sketch values back to specific reviewed bullets in `data/resume.json` — so they are
    supportable, not invented.

    Deriving them automatically was measured and rejected: taking each entry's first bold span
    produces `conversion by 15%`, `4K+ franchises` and `7+ data sources`. Two of three are close;
    the third is wrong, because MAQ's real figure is in its fourth bullet. A derivation that is
    right two times in three encodes a false relationship into a renderer.

    Whatever is chosen, the refusal in task 3 ships either way — it also guards
    `{{ds.componentCount}}` and `{{ds.categoryCount}}`, which are live tokens today.
  </context>
  <options>
    <option id="approve-sketch">
      <name>Approve the three sketch values as final</name>
      <pros>Phase 5 ships complete. Every value traces to a reviewed bullet quoted above. No token ever lands, so the refusal in task 3 is a standing control rather than a live blocker.</pros>
      <cons>The wording is a Phase 0 sketch's, not Akhil's — "6× FASTER PIPELINES" compresses "improved pipeline execution time by 6×".</cons>
    </option>
    <option id="author-new">
      <name>Author three different value/label pairs now</name>
      <pros>The words are Akhil's. Same shipping outcome as above.</pros>
      <cons>Costs three sentences of his attention at this checkpoint.</cons>
    </option>
    <option id="defer">
      <name>Land `{{metric.value}}` / `{{metric.label}}` placeholders and supply the real values later</name>
      <pros>Unblocks wave 1 immediately.</pros>
      <cons>**This makes the build red the moment /work renders (plan 05-09) and keeps it red until the values arrive.** That is the requested behaviour, not a defect — but it means Phase 5 cannot be declared done and Phase 8 cannot cut over. State that outcome plainly rather than discovering it at 05-09.</cons>
    </option>
  </options>
  <resume-signal>Select: approve-sketch, author-new (and give the three value/label pairs), or defer.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: The metric field and the migration, landed in one commit</name>
  <files>src/schemas/resume.ts, data/resume.json, scripts/migrate-experience-metric.mjs</files>
  <action>
    As in 05-02, the schema change and the data change land in ONE commit. Wave 1 runs four other
    plans in this working tree, `astro:config:done` fires on `astro check` and `astro sync` as well
    as `astro build`, and a required field with no data makes every wave-mate's build red.

    **`src/schemas/resume.ts`** — add to `ExperienceEntrySchema`, inside the existing
    `z.strictObject`, before the `.superRefine(checkPeriod)`:

      metric: z.strictObject({
        value: z.string().min(1),
        label: z.string().min(1),
      })

    Required, not optional: all three entries carry one and the design has no row without it. Do
    NOT add it to `EducationEntrySchema` — the education record is not in the employment band.

    Do NOT add a placeholder-shaped refusal to the schema. It is tempting and it is wrong here:
    under option `defer` the placeholder is the intended stored value, and a schema that refused it
    would refuse the data the decision just chose. The refusal belongs on the RENDERED output,
    which is what §14.5 asks for and what task 3 builds.

    **`scripts/migrate-experience-metric.mjs`** — plain node:
      - Reads `data/resume.json`, adds `metric` to each experience entry from a table declared at
        the top of the script, keyed by entry `id`. THROW if the table and the file disagree on
        the id set in either direction, naming the difference. A migration that silently skipped an
        entry leaves a required field absent, which then reads as a schema bug.
      - Idempotent, measured in-process by re-running the transform over its own output and
        comparing strings — not by `git diff --quiet`, which measures convergence rather than work
        (03-04).
      - Preserves key order and formatting.

    Populate the table from the checkpoint's answer. Under `defer`, the table holds the two token
    strings and the script prints, with `process.stdout.write`, a one-line warning naming plan
    05-09 as the point at which the build will go red.

    Run it, then `npm run build`.
  </action>
  <verify>
    <automated>bash -c 'set -e; node scripts/migrate-experience-metric.mjs; A=$(sha256sum data/resume.json | cut -d" " -f1); node scripts/migrate-experience-metric.mjs; B=$(sha256sum data/resume.json | cut -d" " -f1); if [ "$A" != "$B" ]; then echo "FAIL: not idempotent"; exit 1; fi; echo "OK idempotent $A"; npm run build'</automated>
  </verify>
  <done>
    All three experience entries carry `metric.value` and `metric.label`; the education entry does
    not. `npm run build` exits 0. Second run byte-identical (sha256 quoted).
    A planted defect is recorded: delete `metric` from one entry, confirm the build exits 1 naming
    the entry and the field, revert, quote the message.
  </done>
</task>

<task type="auto">
  <name>Task 3: The placeholder refusal, and the proof that it bites</name>
  <files>scripts/assert-no-unresolved-placeholders.mjs, test/content/experience-metric.unit.test.ts</files>
  <action>
    **`scripts/assert-no-unresolved-placeholders.mjs`** — the OQ-1b build refusal.

      - Scan root is a parameter, defaulting to `dist`. Scan every `*.html` under it. The default
        matters: this gate reads the ARTEFACT, not the source, because that is where "survived into
        a rendered public route" is a fact rather than an inference.
      - Fail on any occurrence of `{{` followed by anything other than `{`, reporting the file, the
        1-based line, the ~60 characters of surrounding text, and the token verbatim. Enumerate
        nothing: any `{{…}}` in shipped HTML is a failure, because this site emits no template
        syntax to the browser. A deny-list of known token names would miss the next one, and a
        token deny-list is not a control — Phase 4 defeated one three ways with the guard silent.
      - REFUSE TO PASS ON NOTHING. If the scan root does not exist, is not a directory, or matches
        zero `.html` files, exit 1 with "scan root matched no HTML — a gate that read nothing is
        indistinguishable from a gate that found nothing". A bare `! grep` passes on a missing file
        and that shape has appeared three times in this project.
      - Self-check before scanning: run the detection function against a built-in canary string
        holding `{{x}}` and an anti-canary holding `{ x }` and `{{{raw}}}`-free prose, and exit 1
        if the canary does not fire or the anti-canary does. A rule that does not match its own
        canary forgives everything.
      - Report with `process.stdout.write`. `console.log` prints nothing under this repo's vitest
        setup and would make a finding indistinguishable from silence.
      - Do NOT wire it into `package.json` here. Plan 05-14 owns the `gate:*` wiring and
        `package.json` is single-owner per wave; this plan's proofs invoke it by path.

    **The unit suite** asserts the three metrics are present, that their id set equals the
    experience id set derived from the file (never a literal `3`), and — under `approve-sketch` or
    `author-new` — that no stored metric string contains `{{`.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=$(mktemp -d); mkdir -p "$F/good" "$F/bad" "$F/empty"; printf "<html><body><p>+15%% CONVERSION</p></body></html>" > "$F/good/i.html"; printf "<html><body><p>{{metric.value}}</p></body></html>" > "$F/bad/i.html"; if node scripts/assert-no-unresolved-placeholders.mjs "$F/bad"; then echo "FAIL: token not caught"; exit 1; fi; echo "OK: token caught"; if node scripts/assert-no-unresolved-placeholders.mjs "$F/empty" 2>/dev/null; then echo "FAIL: passed on nothing"; exit 1; fi; echo "OK: refused nothing"; node scripts/assert-no-unresolved-placeholders.mjs "$F/good"; echo "OK: passed on correct output"; npx vitest run test/content/experience-metric.unit.test.ts'</automated>
  </verify>
  <done>
    The gate is proven able to fail, in bash, and every run is quoted in the SUMMARY with its exit
    code and the shell it ran in:
      1. PLANTED DEFECT — an HTML file holding `{{metric.value}}`: exit 1, message names the file,
         the line and the token.
      2. NOTHING TO CHECK — an empty directory: exit 1 with the "matched no HTML" refusal.
      3. CORRECT OUTPUT — HTML holding the resolved metric: exit 0.
      4. WALK-THROUGH ATTEMPTED — try to slip a token past it three ways and record each outcome:
         `&#123;&#123;metric.value&#125;&#125;` (HTML-entity encoded), a token split across a line
         break inside the HTML source, and a token inside an HTML comment. Record which get through
         as named residuals with the reason, or the fix that closed them. Do not claim completeness.
    The gate is ALSO run against the real `dist/client/` produced by task 2's build and exits 0 — which at
    this point proves only that no token reaches the four routes that exist today; 05-09 and 05-14
    are where it starts guarding `/work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed content → rendered public HTML | A stored string is printed to every visitor, including a hiring manager |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Repudiation / integrity of published claim | `/work` employment band | mitigate | `assert-no-unresolved-placeholders.mjs` refuses any `{{…}}` in built HTML; the schema requires the field so it cannot be silently absent |
| T-05-03-02 | Information disclosure | metric copy | accept | Three public résumé figures already present in the reviewed bullets; no new disclosure |
</threat_model>

<verification>
- `npm run build` exits 0.
- `npx vitest run test/content/experience-metric.unit.test.ts` passes.
- `node scripts/assert-no-unresolved-placeholders.mjs dist` exits 0.
- `git diff --stat` shows no change to `package.json` — the gate wiring is 05-14's.
- No verify step runs `git add`.
</verification>

<success_criteria>
- OQ-1b is closed as decided: the metric exists as a field, and a placeholder cannot reach a
  rendered public route without failing the build.
- The refusal has been proven able to fail three ways and a walk-through was attempted and recorded.
- If `defer` was chosen, the SUMMARY states in its first paragraph that `main` will go red at
  plan 05-09 and stay red until Akhil supplies the values.
</success_criteria>

<output>
Create `.planning/phases/05-public-site/05-03-SUMMARY.md`. Carry: the checkpoint decision and the
three chosen pairs verbatim; both idempotence sha256s; the four gate-control runs with exit codes
and shell; the walk-through results including any residual.
</output>
