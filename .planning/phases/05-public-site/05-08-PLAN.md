---
phase: 05-public-site
plan: 08
type: execute
wave: 4
depends_on: ["05-04", "05-05", "05-06"]
files_modified:
  - src/pages/photos/[category]/[slug].astro
  - src/components/public/ExifList.astro
  - src/styles/photo-detail.css
  - test/public/photo-detail.node.test.ts
autonomous: true
requirements: [PUB-07, PUB-08, PUB-09, SEO-01]
owns_criteria:
  - "Criterion 3 — EXIF with absent fields omitted entirely rather than placeheld, cameras and lenses as human names, and each photograph with its own prerendered page and social card"
owns_open_decisions: []
user_setup: []
must_haves:
  truths:
    - "Every photograph has its own prerendered page, reachable with JavaScript off"
    - "A photograph whose EXIF is entirely null shows no Details block at all — no heading, no rule, no empty panel"
    - "No page shows a camera or lens model code"
    - "Sharing any photo page produces a card with the right image and the photograph's own alt text"
    - "Previous and next are real anchors that wrap inside the category"
  artifacts:
    - path: "src/pages/photos/[category]/[slug].astro"
      provides: "One prerendered page per photograph, zero framework JavaScript"
      contains: "getStaticPaths"
    - path: "src/components/public/ExifList.astro"
      provides: "The omit-null EXIF definition list, driven by exifRows"
    - path: "src/styles/photo-detail.css"
      provides: "The frame and the EXIF panel"
  key_links:
    - from: "src/components/public/ExifList.astro"
      to: "src/lib/exif-display.ts"
      via: "exifRows is the only row builder"
      pattern: "exifRows"
    - from: "src/pages/photos/[category]/[slug].astro"
      to: "src/components/public/Seo.astro"
      via: "og:image = urls.large, og:image:alt = photo.alt, og:type = article"
      pattern: "og:image"
---

<objective>
Give every photograph its own page: the frame, the EXIF that omits rather than placeholds, human
equipment names, real previous/next anchors, and a social card.

Purpose: PUB-09 is the no-JS destination of every gallery tile, so it carries the crawlability and
Back-button guarantee that PUB-04 and PUB-06 both lean on — one decision, four requirements (§9.2).
PUB-07's rule is the strict one: §9.3 measured two degenerate records and both are real —
`architecture-redbuilding` has a camera and nothing else, and `product-peppers` has all six fields
null. **A "Details" heading over nothing is the placeholder PUB-07 forbids, wearing a different
hat.**

Output: one route pattern producing one page per photograph, one EXIF component, one stylesheet,
one HTTP suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-site/05-UI-SPEC.md — §3.1 (the type roles for title, meta and EXIF),
§4.4 rule 2, §7.2 and §7.4 (reservation and srcset), §9.3 (EXIF omitted entirely, with the two
degenerate records and the null counts), §9.4 (`date` is not rendered and not sorted by),
§9.5 (camera and lens as human names), §9.6 (the detail page in full, including the social card),
§13.2 (the copy table — the two back links).

@src/lib/exif-display.ts (05-04) — `exifRows` is the only row builder; do not restate the omit-null
rule here.
@src/lib/photo-srcset.ts (05-05).
@src/layouts/PublicLayout.astro and @src/components/public/Seo.astro (05-06).
@src/schemas/photo.ts — read the header. `alt` already has three enforced rules including
"not equal to the title", so the §9.6 alt-vs-title contract is a schema property, not a new check.
</context>

<interfaces>
MEASURED 2026-08-28 against the committed manifest. **40 records, not 39** (Phase 4's
`wildlife-gentlegiants`). Every number below is for confirmation; **none may be written into code.**

Null counts across the corpus: camera 1 · lens 11 · aperture 2 · shutter 2 · iso 2 · focalLength 2.
`place` is present on 17 of 40 and deliberately empty on the rest — "an empty description renders
nothing at all: no em dash, no gap."

The two degenerate records, both real:
  `architecture-redbuilding` — camera present, every other field null → exactly one EXIF row.
  `product-peppers`          — all six fields null → **no EXIF block at all**.

Already display-ready strings, rendered verbatim: `aperture` (`f/1.8` … `f/13`), `shutter`
(`1/25` … `1/3200`), `focalLength` (`4.3mm` … `300mm`, including `wildlife-starfish`'s `4.745mm`).
`iso` is a number and renders as `ISO 200`.

`urls.large` is the 1200w variant — the Open Graph recommended width — and is therefore the
per-photo `og:image` (§9.6). `og:type` is `article`, `twitter:card` is `summary_large_image`,
canonical is the page's own absolute URL, and `og:image:alt` is `photo.alt`.

Route shape: `src/pages/photos/[category]/[slug].astro`. It coexists with
`src/pages/photos/[category]/index.astro` (plan 05-07) — a directory plus an `index.astro` is the
form that does NOT collide; do not use a bare `[category].astro` alongside the directory.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The route and the frame</name>
  <files>src/pages/photos/[category]/[slug].astro, src/styles/photo-detail.css</files>
  <action>
    `getStaticPaths()` over `getCollection('photos')`, producing one path per record with
    `{ category, slug }` params. **Import `photoHref` and `photoSlug` from `src/lib/photo-srcset.ts` (05-05, wave 2) — the SINGLE definition.** Do NOT derive a slug here:
    05-07's gallery tile imports the same helper, both plans are wave 4 and cannot read each other, and two independent derivations disagree silently — every tile 404s against a page that exists under a different slug, with a green build, a green suite and a green gate.
    builds its href — the two must agree or every tile 404s, and there is no runtime to catch it.
    State in the SUMMARY how the slug is derived and confirm it round-trips for all records,
    including `wildlife-gentlegiants`, whose published key carries a content hash
    (`gentlegiants-ff17a846.webp`) while its id does not.

    **Zero framework JavaScript on this route.** No `client:*` directive, no island. It is the
    destination of every gallery tile and the whole no-JS path depends on it staying static.

    The frame, per §9.6:
      - `<img src={urls.large}>` with the full `srcsetFor(photo)`, `sizes="min(100vw, 1080px)"`,
        `loading="eager"`, `fetchpriority="high"`, and the same `aspect-ratio` plus LQIP background
        as the tile. Emit no `width`/`height` attributes, for the same reason §7.2 gives.
      - `<h1>` is the `title` (Playfair 22px per §3.1). `alt` is the photograph's own `alt`. They
        are different strings by rule and by gate — `PhotoSchema` already enforces
        `alt !== title`, so this is a property to cite, not to re-check.
      - `place` below the title, only if present. No em dash, no gap, no fallback.
      - `.pd-frame` in `src/styles/photo-detail.css`, dark-only inset hairline per §4.4 rule 2.

    **Do not reference `photo.date` anywhere.** §9.4: three distinct dates across 40 records is an
    ingest window, not a capture history, and `REQUIREMENTS.md` Out of Scope settled it. Plan 05-12
    adds `scripts/assert-photo-date-unrendered.mjs` over exactly this file's directory.
  </action>
  <verify>
    <automated>bash -c 'set -e; npm run build; node -e "const fs=require(\"fs\");const p=require(\"./data/portfolio_images.json\");let miss=0;for(const r of p){const f=\`dist/client/photos/\${r.category}/\${r.id.replace(r.category+\"-\",\"\")}/index.html\`;if(!fs.existsSync(f)){process.stdout.write(\`MISSING \${f}\n\`);miss++}}process.stdout.write(\`records \${p.length}, missing \${miss}\n\`);if(p.length===0){process.stdout.write(\"FAIL: empty manifest\n\");process.exit(1)}process.exit(miss?1:0)"'</automated>
  </verify>
  <done>
    One page exists per manifest record — the count compared at check time against
    `portfolio_images.json`, with a refusal if the manifest is empty. **Adjust the path expression
    in the verify command to `photoHref` imported from `src/lib/photo-srcset.ts` — **never to whatever the route happens to do**, which would make this gate agree with its own route by construction and unable to ever observe a disagreement with 05-07**, and record both the
    derivation and the final command in the SUMMARY; the expression above assumes the id is
    `<category>-<slug>` and that assumption must be proven, not carried.
    `grep -rn "\.date" src/pages/photos` returns nothing; quote the command and the empty output.
    No `<script type="module">` in any `dist/client/photos/*/*/index.html`.
  </done>
</task>

<task type="auto">
  <name>Task 2: The EXIF list that omits rather than placeholds</name>
  <files>src/components/public/ExifList.astro, src/styles/photo-detail.css</files>
  <action>
    A `<dl>` driven entirely by `exifRows(photo.exif)` from `src/lib/exif-display.ts`. The component
    contains **no null checks of its own** — if `exifRows` returns an empty array the component
    renders nothing at all: no `<dl>`, no heading, no rule, no wrapper. PUB-07's rule has one
    implementation and it is in the library module, so a second copy cannot grow an em dash.

    Layout, per §9.3: label in IBM Plex Mono `--text-xs` 11px `--ls-wide` at `--ink-3`, value in
    DM Sans `--text-sm` 12.5px at `--ink-2`.

    Never render `—`, never "Unknown", never an empty `<dd>`. Never render `--amber-vivid` as text
    anywhere on this page: §4.2 measured it at 3.11 / 3.19 / 2.96 in light, so it fails AA as text
    on every surface, and it is the same literal in both modes. It is a marker step, not a text
    colour.

    The camera and lens values come from `displayCamera` / `displayLens`, which throw on an
    unrecognised string. That throw happens in Astro frontmatter, so it fails the build — which is
    §9.5's ruling: rendering the raw code ships `SM-N970F` to a reader, rendering a guess ships a
    lie, and refusing the build is the only option that does neither.
  </action>
  <verify>
    <automated>bash -c 'set -e; npm run build; node -e "const fs=require(\"fs\");const g=fs.readFileSync(\"dist/client/photos/product/peppers/index.html\",\"utf8\");if(/<dl/.test(g)){process.stdout.write(\"FAIL: product-peppers rendered an EXIF list\n\");process.exit(1)}process.stdout.write(\"OK: no EXIF block on the all-null record\n\");const r=fs.readFileSync(\"dist/client/photos/architecture/redbuilding/index.html\",\"utf8\");const n=(r.match(/<dt/g)||[]).length;process.stdout.write(\`redbuilding rows: \${n}\n\`);if(n!==1){process.stdout.write(\"FAIL: expected exactly one row\n\");process.exit(1)}"'</automated>
  </verify>
  <done>
    `product-peppers` renders no `<dl>`, no "Details" heading and no empty panel — asserted against
    the built HTML, not against a component test.
    `architecture-redbuilding` renders exactly one row and it is the camera.
    A repo-wide check over every built photo page finds zero occurrences of the strings `—`,
    `Unknown`, `N/A` and `<dd></dd>` inside an EXIF list. Quote the command; filter out any match
    inside prose so the check is not self-invalidating.
    No built photo page contains any of the five raw camera strings or the four raw lens strings.
    Quote that command and its empty output — this is PUB-08's real assertion, and it is stronger
    than testing the mapper in isolation.
    A planted defect is recorded: add a sixth EXIF field with a null value to a COPY of the manifest,
    build against it, confirm no empty row appears; and separately set one camera to an unknown
    string and confirm the BUILD fails with the value named. Never mutate `data/` in a shared-index
    wave.
  </done>
</task>

<task type="auto">
  <name>Task 3: Previous/next, the back links, and the social card</name>
  <files>src/pages/photos/[category]/[slug].astro, test/public/photo-detail.node.test.ts</files>
  <action>
    **Previous / next as real anchors**, ordered by `categoryOrder` WITHIN the category, wrapping at
    both ends. `categoryOrder` is dense 1..n in all seven categories (measured), so the neighbour is
    an index arithmetic — but derive the sequence from the data and assert density rather than
    assuming it, because a Phase 7 reorder is one drag away. If a category has exactly one
    photograph, previous and next both point at the page itself; decide deliberately whether to
    render them at all in that case and record the decision. (Measured today: the smallest
    categories hold 2, so the one-photograph case is unreachable — which is exactly why it must be
    decided now rather than discovered later.)

    Plus the two back links from §13.2, verbatim: `← All photographs` and `← {Category}`.

    **The social card** via `<Seo>`:
      - `og:image` = `urls.large` (1200w, measured, the OG recommended width)
      - `og:image:alt` = `photo.alt`
      - `og:type` = `article`
      - `twitter:card` = `summary_large_image`
      - canonical = the page's own absolute URL

    **`test/public/photo-detail.node.test.ts`** — an HTTP-level suite against the built artefact:
      - For every record, assert the page exists, carries exactly one `<h1>`, and that the `<h1>`
        text and the `alt` attribute are different strings.
      - Assert `og:image` is absolute and ends in the `-lg` suffix from `VARIANTS`, read from the
        module — not from a literal `-lg` typed into the test.
      - Assert previous/next hrefs resolve to pages that exist, and that following `next` n times
        from any starting point returns to the start, where n is that category's derived size.
        That single assertion covers wrapping, ordering and density at once.
      - Derive every count from the data. Do not write 40, 7, or any per-category number.
  </action>
  <verify>
    <automated>bash -c 'set -e; npx vitest run test/public/photo-detail.node.test.ts'</automated>
  </verify>
  <done>
    The suite passes and prints, with `process.stdout.write`, the number of pages it checked and the
    per-category cycle lengths it walked.
    A planted defect is recorded: break the wrap by clamping instead of wrapping at the last index,
    confirm the cycle assertion goes red naming the category, then revert. Without that control the
    cycle test could pass by never reaching an end.
    The `og:image` suffix assertion reads `VARIANTS` from `src/lib/photo-variants.ts`; proven
    non-vacuous by temporarily changing the large suffix in a copy and watching it go red.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed manifest → rendered HTML and meta tags | `alt`, `title`, `place` and URLs are interpolated into markup a scraper reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering (injection) | meta tag values | mitigate | Astro escapes attribute interpolation; no `set:html`; `gate:sinks` re-run in verification |
| T-05-08-02 | Spoofing | `og:url` / canonical | mitigate | Built from `Astro.site` by `<Seo>`, which throws on anything it cannot make absolute |
| T-05-08-03 | Integrity of a published claim | camera and lens names | mitigate | `displayCamera`/`displayLens` throw on an unknown string, failing the build rather than guessing; plan 05-04's coverage gate catches it earlier |
</threat_model>

<verification>
- `npm run build` exits 0; `npm test` passes; `npm run check` and `npm run typecheck` pass.
- `node scripts/assert-exif-display-coverage.mjs` exits 0.
- `node scripts/assert-no-raw-html-sinks.mjs` and `node scripts/assert-ds-import-contract.mjs` exit 0.
- No `dist/client/photos/*/*/index.html` contains `<script type="module">`.
- No verify step runs `git add`; every control writes into `mktemp -d`.
</verification>

<success_criteria>
- PUB-09: one prerendered page per photograph, with a social card, reachable with JavaScript off.
- PUB-07: absent fields produce no row and an all-null record produces no block — asserted against
  built HTML for both degenerate records by name.
- PUB-08: no built page contains any raw camera or lens string, asserted repo-wide.
- SEO-01: canonical, Open Graph and Twitter card present on every photo page and pointing at the
  1200w variant.
</success_criteria>

<output>
Create `.planning/phases/05-public-site/05-08-SUMMARY.md`. Carry: the slug derivation and the proof
it round-trips for every record including the content-hashed one; the two degenerate-record
assertions with their built-HTML evidence; the repo-wide raw-string grep and its empty output; the
one-photograph-category decision; and the planted wrap control.
</output>
