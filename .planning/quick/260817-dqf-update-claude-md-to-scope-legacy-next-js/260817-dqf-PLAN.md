---
phase: quick
plan: 260817-dqf
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [DOC-ORIENT-01]

must_haves:
  truths:
    - "An agent reading CLAUDE.md top-down learns main is pre-code (no src/, no package.json) before reaching any legacy content"
    - "An agent can copy a literal `git show legacy/nextjs-portfolio:<path>` invocation out of CLAUDE.md to read legacy source"
    - "An agent landing directly on the Technology Stack, Conventions, or Architecture heading sees it is legacy without scrolling up"
    - "All legacy porting detail survives the edit intact"
    - "The Project / Constraints / GSD Workflow Enforcement / Project Skills / Developer Profile sections are untouched"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Orientation note + legacy-scoped section headings"
      contains: "git show legacy/nextjs-portfolio:"
  key_links:
    - from: "CLAUDE.md orientation note"
      to: "legacy/nextjs-portfolio branch"
      via: "literal git show / git ls-tree invocations"
      pattern: "git show legacy/nextjs-portfolio:"
---

<objective>
Scope CLAUDE.md's legacy Next.js documentation to the `legacy/nextjs-portfolio` branch and
state plainly that `main` is a pre-code from-scratch rebuild.

Purpose: `## Technology Stack`, `## Conventions`, and `## Architecture` describe the legacy
Next.js 15 / `@cloudflare/next-on-pages` app in the present tense, as though it were checked
out. It is not — the working tree has no `src/` and no `package.json`. This actively misleads
every agent that reads the file; a Phase 0 pattern-mapper run already had to be corrected
because plan steps said "modify `src/...`".

Output: CLAUDE.md gains a top orientation note and three legacy-scoped section headings. No
legacy detail is deleted — it is the porting reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<verified_facts>
Confirmed by `ls -A` and `git branch -a` at plan time — do not re-derive:

- Repo root contains exactly: `.git`, `.gitignore`, `.planning`, `CLAUDE.md`, `README.md`,
  `data`, `design_handoff_portfolio`, `public`, `tsconfig.tsbuildinfo`.
  There is NO `src/` and NO `package.json`.
- The branch `legacy/nextjs-portfolio` exists locally and on `origin`.
- No project skills directory exists (`.claude/skills/`, `.agents/skills/`, etc. all absent).
</verified_facts>

<marker_structure>
CLAUDE.md is a GSD-managed file. Sections are wrapped in HTML comment markers, and content
BETWEEN a `-end` marker and the next `-start` marker is outside GSD-managed regions
(confirmed against `$HOME/.claude/get-shit-done/templates/claude-md.md`). Current markers
and their line numbers in the pre-edit file.

All 14 marker lines in the pre-edit file (verified with
`grep -n '<!-- GSD:' CLAUDE.md`):

- L1   `<!-- GSD:project-start source:PROJECT.md -->`
- L45  `<!-- GSD:project-end -->`
- L47  `<!-- GSD:stack-start source:codebase/STACK.md -->`
- L104 `<!-- GSD:stack-end -->`
- L106 `<!-- GSD:conventions-start source:CONVENTIONS.md -->`
- L159 `<!-- GSD:conventions-end -->`
- L161 `<!-- GSD:architecture-start source:ARCHITECTURE.md -->`
- L256 `<!-- GSD:architecture-end -->`
- L258 `<!-- GSD:skills-start source:skills/ -->`
- L262 `<!-- GSD:skills-end -->`
- L264 `<!-- GSD:workflow-start source:GSD defaults -->`
- L275 `<!-- GSD:workflow-end -->`
- L279 `<!-- GSD:profile-start -->`
- L284 `<!-- GSD:profile-end -->`

Consequence for placement: the orientation note goes in the unmanaged gap between
`<!-- GSD:project-end -->` and `<!-- GSD:stack-start ... -->`, so a future
`generate-claude-md` run that refreshes the Project or Stack section does not clobber it.

Consequence for the heading edits: the three headings ARE inside GSD-managed regions and
WILL be lost if those sections are regenerated from `.planning/codebase/*.md`. That is an
accepted tradeoff for this task (scope is one file), and Task 1 records it as a maintenance
note so whoever regenerates knows to re-apply.
</marker_structure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the pre-code orientation note above the legacy sections</name>

  <read_first>
    - CLAUDE.md (full file — 284 lines; read once, extract everything needed in that pass)
  </read_first>

  <files>CLAUDE.md</files>

  <action>
Insert a new orientation section into the unmanaged gap between `<!-- GSD:project-end -->`
(currently L45) and `<!-- GSD:stack-start source:codebase/STACK.md -->` (currently L47).
Do not move, reword, or reindent either marker, and do not touch anything above
`<!-- GSD:project-end -->`.

Wrap the inserted block in its own non-GSD markers so it is visibly hand-maintained:
open with `<!-- ORIENTATION:start — hand-maintained, NOT generated by generate-claude-md -->`
and close with `<!-- ORIENTATION:end -->`.

Inside those markers write an `## Repository Orientation` heading followed by four short
labelled paragraphs covering, in this order:

1. **Pre-code state.** `main` is a from-scratch rebuild and is currently pre-code: there is
   no `src/` and no `package.json` yet. State that any instruction phrased as "modify
   `src/...`" or "run `npm run ...`" is wrong until the rebuild creates those files, and
   tell the reader to confirm with `ls -A` at the repo root before assuming a path exists.
   Do NOT enumerate the full current directory listing — it goes stale as the rebuild lands.

2. **Where the legacy code is.** The legacy Next.js app is not checked out; it lives on the
   `legacy/nextjs-portfolio` branch. Give three copyable invocations, each on its own line
   inside a fenced `bash` block, with a trailing comment explaining each:
   `git show legacy/nextjs-portfolio:src/lib/access.ts` (read one file),
   `git ls-tree -r --name-only legacy/nextjs-portfolio` (list every legacy file), and
   `git checkout legacy/nextjs-portfolio` (check the branch out).
   The literal substring `git show legacy/nextjs-portfolio:` must appear verbatim.

3. **Scope of the sections below.** State that Technology Stack, Conventions, and
   Architecture describe the legacy Next.js 15 / `@cloudflare/next-on-pages` app in the
   present tense, that they are a porting reference — the inventory of what the rebuild must
   replace — and explicitly NOT a description of the current working tree. In the same
   paragraph, draw the contrast: the `## Project` section and its `### Constraints` above are
   the opposite — they describe the Astro + React islands target and ARE current, so nothing
   in them is legacy.

   CRITICAL formatting constraint: when naming those three sections in this paragraph, write
   them in backticks WITHOUT a leading `##` (e.g. `` `Technology Stack` ``), and never begin
   a line with `##` followed by one of those three names. A line-start `## Technology Stack`
   here would collide with the heading assertions in Task 2's acceptance criteria.

4. **Maintenance note.** The three legacy sections are GSD-managed regions sourced from
   `.planning/codebase/*.md`; if `generate-claude-md` regenerates them, the `(LEGACY ...)`
   heading suffixes and lead-in lines added in Task 2 will be lost and must be re-applied.

Keep the whole block tight — roughly 20-30 lines. It is orientation, not a second PROJECT.md.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=CLAUDE.md && S=$(grep -n '<!-- ORIENTATION:start' $F|head -1|cut -d: -f1) && E=$(grep -n '<!-- ORIENTATION:end' $F|head -1|cut -d: -f1) && P=$(grep -n '<!-- GSD:project-end -->' $F|head -1|cut -d: -f1) && K=$(grep -n '<!-- GSD:stack-start' $F|head -1|cut -d: -f1) && echo "$S $E $P $K" | grep -qE '^[0-9]+ [0-9]+ [0-9]+ [0-9]+$' && [ "$S" -gt "$P" ] && [ "$E" -lt "$K" ] && grep -q 'git show legacy/nextjs-portfolio:' $F && grep -q 'git ls-tree -r --name-only legacy/nextjs-portfolio' $F && grep -q 'git checkout legacy/nextjs-portfolio' $F && grep -q 'generate-claude-md' $F && test "$(grep -cE '^## (Technology Stack|Conventions|Architecture)' $F)" -eq 3 && echo PASS</automated>
    <note>The `echo "$S $E $P $K" | grep -qE '^[0-9]+ ...'` step is load-bearing: without it an absent ORIENTATION marker yields an empty variable and the gate can pass vacuously. Verified at plan time that this exact command exits non-zero against the pre-edit CLAUDE.md.</note>
  </verify>

  <acceptance_criteria>
    - `CLAUDE.md` contains the literal string `git show legacy/nextjs-portfolio:`
    - `CLAUDE.md` contains the literal string `git ls-tree -r --name-only legacy/nextjs-portfolio`
    - `CLAUDE.md` contains the literal string `git checkout legacy/nextjs-portfolio`
    - `CLAUDE.md` contains the literal string `<!-- ORIENTATION:start`
    - `CLAUDE.md` contains the literal string `<!-- ORIENTATION:end -->`
    - The line number of `<!-- ORIENTATION:start` is greater than the line number of
      `<!-- GSD:project-end -->` and the line number of `<!-- ORIENTATION:end -->` is less
      than the line number of `<!-- GSD:stack-start`
    - `CLAUDE.md` contains the literal string `generate-claude-md` (the maintenance note)
    - `grep -cE '^## (Technology Stack|Conventions|Architecture)' CLAUDE.md` returns exactly `3`
      (the inserted note added no new line-start heading colliding with those three names)
    - `git diff --name-only` lists exactly `CLAUDE.md` and nothing else
  </acceptance_criteria>

  <done>
    The orientation note sits in the unmanaged gap between the project-end and stack-start
    markers, carries copyable `git show` / `git ls-tree` / `git checkout` invocations for the
    `legacy/nextjs-portfolio` branch, states the no-`src/` no-`package.json` pre-code fact,
    scopes the three sections below as a legacy porting reference while exempting
    `## Project` / `### Constraints`, and records the regeneration hazard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scope the three legacy section headings in place</name>

  <read_first>
    - CLAUDE.md (already in context from Task 1 — do NOT re-read the same ranges)
  </read_first>

  <files>CLAUDE.md</files>

  <action>
Make the three legacy section headings self-identifying, so a reader landing mid-document
knows the content is legacy without having scrolled past the orientation note.

Rewrite each of these three headings in place, preserving their exact position immediately
after their opening GSD marker:

- `## Technology Stack` becomes `## Technology Stack (LEGACY — `legacy/nextjs-portfolio` branch)`
- `## Conventions` becomes `## Conventions (LEGACY — `legacy/nextjs-portfolio` branch)`
- `## Architecture` becomes `## Architecture (LEGACY — `legacy/nextjs-portfolio` branch)`

Use an em dash, and backtick the branch name inside the parenthetical exactly as shown.

Directly beneath each rewritten heading, insert one blockquote lead-in line (a line starting
with `> `). Each lead-in must state that the section describes the Next.js app on the
`legacy/nextjs-portfolio` branch rather than the current working tree (which has no `src/`),
must include the literal substring `git show legacy/nextjs-portfolio:` as the way to read the
files, and must frame the content as the inventory the Astro rebuild replaces. Vary the
phrasing per section so each reads naturally for its own subject matter — do not paste three
identical lines.

Then leave everything else in those three sections byte-for-byte alone. Do NOT delete,
summarise, compress, reorder, or "clean up" any legacy bullet, table row, or sub-heading —
that detail is the porting reference and every line of it must survive. Do NOT alter the GSD
marker comments themselves. Do NOT touch `## Project`, `### Constraints`, `## Project Skills`,
`## GSD Workflow Enforcement`, or `## Developer Profile`.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -cE '^## (Technology Stack|Conventions|Architecture) \(LEGACY' CLAUDE.md)" -eq 3 && test "$(grep -cE '^## (Technology Stack|Conventions|Architecture)$' CLAUDE.md)" -eq 0 && test "$(grep -c '^> .*git show legacy/nextjs-portfolio:' CLAUDE.md)" -eq 3 && for h in '^## Project$' '^### Constraints$' '^## GSD Workflow Enforcement$' '^## Project Skills$' '^## Developer Profile$'; do grep -qE "$h" CLAUDE.md || { echo "MISSING: $h"; exit 1; }; done && for s in 'next-on-pages' 'DraggableMasonry' 'PropertiesPanel' 'requireAccess' 'PORTFOLIO_BUCKET' 'action-process-dispatch.js' 'Diff-by-JSON-stringify'; do grep -q "$s" CLAUDE.md || { echo "LOST LEGACY DETAIL: $s"; exit 1; }; done && test "$(wc -l < CLAUDE.md)" -ge 284 && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '^## (Technology Stack|Conventions|Architecture) \(LEGACY' CLAUDE.md` returns exactly `3`
    - `grep -cE '^## (Technology Stack|Conventions|Architecture)$' CLAUDE.md` returns exactly `0`
      (no bare, unscoped version of any of the three headings remains)
    - `grep -c '^> .*git show legacy/nextjs-portfolio:' CLAUDE.md` returns exactly `3`
      (one blockquote lead-in per legacy section, each carrying the copyable invocation)
    - All five preserved headings are still present: `^## Project$`, `^### Constraints$`,
      `^## GSD Workflow Enforcement$`, `^## Project Skills$`, `^## Developer Profile$`
    - Legacy detail survived intact — `CLAUDE.md` still contains every one of these literals:
      `next-on-pages`, `DraggableMasonry`, `PropertiesPanel`, `requireAccess`,
      `PORTFOLIO_BUCKET`, `action-process-dispatch.js`, `Diff-by-JSON-stringify`
    - `wc -l < CLAUDE.md` is at least `284` (the pre-edit line count — the file only grew)
    - All 14 `<!-- GSD:*-start` / `<!-- GSD:*-end -->` marker lines are still present
      (`grep -c '<!-- GSD:' CLAUDE.md` returns `14`) and unmodified
      (`git diff CLAUDE.md | grep -E '^[-+].*<!-- GSD:'` produces no output)
    - `git diff --name-only` lists exactly `CLAUDE.md` and nothing else
  </acceptance_criteria>

  <done>
    Each of the three legacy headings carries a `(LEGACY — `legacy/nextjs-portfolio` branch)`
    suffix and a blockquote lead-in with a copyable `git show` invocation; no bare heading
    remains; all legacy body content and all GSD markers are unchanged; the five preserved
    sections are untouched.
  </done>
</task>

</tasks>

<threat_model>
No trust boundaries are crossed. This is a documentation-only edit to a single tracked
Markdown file, with no code, no dependency installs, no package-manager invocation, no
network calls, and no runtime surface. No STRIDE category applies; no threats to register.

The one real risk is a correctness risk, not a security one: an over-eager executor
"tidying" the legacy sections and deleting the porting reference. Task 2's acceptance
criteria mitigate it with explicit literal-survival assertions and a line-count floor.
</threat_model>

<verification>
Run from the repo root after both tasks:

1. `git diff --name-only` → exactly `CLAUDE.md`
2. `git diff CLAUDE.md | grep -E '^[-+].*<!-- GSD:'` → no output (markers untouched)
3. `git diff --numstat CLAUDE.md` → deletions column is small (only the three rewritten
   heading lines), additions dominate
4. Read the first 80 lines of the resulting CLAUDE.md and confirm an agent reaches the
   pre-code fact and the `git show` invocation before any legacy content
</verification>

<success_criteria>
- CLAUDE.md no longer presents legacy Next.js documentation as current working-tree state
- An agent can locate and read legacy source from the instructions in CLAUDE.md alone
- Every legacy porting detail is preserved
- The five protected sections are byte-identical to their pre-edit state
- Exactly one file changed
</success_criteria>

<output>
Create `.planning/quick/260817-dqf-update-claude-md-to-scope-legacy-next-js/260817-dqf-SUMMARY.md` when done
</output>
